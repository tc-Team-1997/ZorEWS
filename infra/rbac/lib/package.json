{
  "name": "@apex-ews/rbac",
  "version": "0.1.0",
  "private": true,
  "description": "Tiny TS helper for the canonical RBAC matrix at infra/rbac/matrix.json. Provides loadMatrix() + can(role, operation) for service-side guards (auth-svc, regulatory-svc, bff, collection-adapter). T3.9.",
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "jest --runInBand"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.14.10",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.5.3"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": ["**/__tests__/**/*.test.ts"]
  }
}
