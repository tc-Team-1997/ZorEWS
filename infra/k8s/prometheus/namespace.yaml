apiVersion: v1
kind: Namespace
metadata:
  name: monitoring
  labels:
    pod-security.kubernetes.io/enforce: baseline
  annotations:
    description: "Prometheus + Grafana + Loki observability stack"
    owned-by: "SRE"
    operationalization-phase: "T5-P1"
