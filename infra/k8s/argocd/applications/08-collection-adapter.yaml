# Wave 7 — collection-adapter. Bridges case.events → bank Collection module via mTLS.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: collection-adapter
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "7"
  finalizers: [resources-finalizer.argocd.argoproj.io]
spec:
  project: default
  source:
    repoURL: https://github.com/tc-Team-1997/ZorEWS.git
    targetRevision: main
    path: infra/k8s
    directory: {include: "deployments.yaml"}
  destination: {server: https://kubernetes.default.svc, namespace: apex-ews}
  syncPolicy:
    automated: {prune: true, selfHeal: true}
    syncOptions: [CreateNamespace=true, ServerSideApply=true]
    retry: {limit: 5, backoff: {duration: 30s, factor: 2}}
