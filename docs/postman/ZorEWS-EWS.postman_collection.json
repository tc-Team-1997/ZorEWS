{
  "info": {
    "_postman_id": "ae7651b5-05d1-4ba7-8bb9-cc74deb99bff",
    "name": "ZorEWS — EWS APIs",
    "description": "EWS routes from the ZorEWS BFF + auth-svc surface. 80 operations. Auto-generated from `docs/api/swagger.json` by `scripts/gen-postman.js`. Re-import after running `npm run openapi:generate` to refresh.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "auth": {
    "type": "bearer",
    "bearer": [
      {
        "key": "token",
        "value": "{{access_token}}",
        "type": "string"
      }
    ]
  },
  "event": [
    {
      "listen": "prerequest",
      "script": {
        "type": "text/javascript",
        "exec": [
          "// Collection-level pre-request — log the active environment + tenant for visibility",
          "if (!pm.environment.get('access_token') && pm.request.url.toString().indexOf('/auth/login') === -1 && pm.request.url.toString().indexOf('/oauth/token') === -1 && pm.request.url.toString().indexOf('/healthz') === -1 && pm.request.url.toString().indexOf('/.well-known/') === -1) {",
          "  console.warn('access_token is empty — run the Smoke > Login request first to capture a token.');",
          "}"
        ]
      }
    }
  ],
  "variable": [],
  "item": [
    {
      "name": "00 — Smoke tests",
      "description": "Happy-path + negative-test sanity checks. Run this folder first to verify the local stack + auth flow.",
      "item": [
        {
          "name": "✅ Happy path — GET /v1/indicators/catalog-stats",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/indicators/catalog-stats",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "indicators",
                "catalog-stats"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('2xx or 401 (no auth in env)', () => {",
                  "  pm.expect([200, 201, 401, 403]).to.include(pm.response.code);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "❌ Negative — missing X-Tenant-ID → 400",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{bff_url}}/v1/indicators/catalog-stats",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "indicators",
                "catalog-stats"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('400 when tenant header missing', () => pm.expect(pm.response.code).to.equal(400));",
                  "pm.test('error envelope', () => {",
                  "  const j = pm.response.json();",
                  "  pm.expect(j).to.have.property('error');",
                  "  pm.expect(j.error).to.have.property('code');",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "❌ Negative — unknown role → 403",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "unknown_role",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/indicators/catalog-stats",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "indicators",
                "catalog-stats"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('403 with unknown role', () => pm.expect(pm.response.code).to.equal(403));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "/api/rules",
      "description": "1 route(s) under /api/rules",
      "item": [
        {
          "name": "GET /api/rules",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{bff_url}}/api/rules",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "api",
                "rules"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /api/rules",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "/api/rules — list of rules in the SPA's RuleSummary shape (sourced from BFF rules seed)."
        }
      ]
    },
    {
      "name": "/v1/ews/evaluate",
      "description": "1 route(s) under /v1/ews/evaluate",
      "item": [
        {
          "name": "POST /v1/ews/evaluate",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ews/evaluate",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ews",
                "evaluate"
              ]
            },
            "description": "{ header: { status: 'SUCCESS', code, message, requestId, timestamp }, body: ScoreResponse }\nResponse envelope (error, §11 shape):\n{ header: { status: 'FAILURE', requestId, timestamp }, error: { code, message, severity } }\nTenant context (X-Tenant-ID + X-Channel) is enforced via the\nrequireTenant middleware before this handler runs.\nLegacy callers that POST a bare body (no envelope) are still served:\nthe handler peels `body.body` if present, otherwise treats the whole\npayload as the inner body — preserves T3.7 compatibility while\npartners migrate.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"customer_id\": \"{{customer_id}}\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/ews/evaluate",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Response envelope (success):"
        }
      ]
    },
    {
      "name": "/v1/ews/rules",
      "description": "19 route(s) under /v1/ews/rules",
      "item": [
        {
          "name": "GET /v1/ews/rules",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ews/rules",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ews",
                "rules"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ews/rules",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/ews/rules — auto-discovered (server.ts:30678)"
        },
        {
          "name": "POST /v1/ews/rules",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ews/rules",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ews",
                "rules"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/ews/rules",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/ews/rules — auto-discovered (server.ts:30743)"
        },
        {
          "name": "DELETE /v1/ews/rules/{rule_id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ews/rules/:rule_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ews",
                "rules",
                ":rule_id"
              ],
              "variable": [
                {
                  "key": "rule_id",
                  "value": "{{rule_id}}",
                  "description": "Path parameter: rule_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/ews/rules/{rule_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DELETE /v1/ews/rules/:rule_id — auto-discovered (server.ts:30890)"
        },
        {
          "name": "GET /v1/ews/rules/{rule_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ews/rules/:rule_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ews",
                "rules",
                ":rule_id"
              ],
              "variable": [
                {
                  "key": "rule_id",
                  "value": "{{rule_id}}",
                  "description": "Path parameter: rule_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ews/rules/{rule_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/ews/rules/:rule_id — auto-discovered (server.ts:30799)"
        },
        {
          "name": "PUT /v1/ews/rules/{rule_id}",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ews/rules/:rule_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ews",
                "rules",
                ":rule_id"
              ],
              "variable": [
                {
                  "key": "rule_id",
                  "value": "{{rule_id}}",
                  "description": "Path parameter: rule_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PUT /v1/ews/rules/{rule_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PUT /v1/ews/rules/:rule_id — auto-discovered (server.ts:30820)"
        },
        {
          "name": "POST /v1/ews/rules/{rule_id}/activate",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ews/rules/:rule_id/activate",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ews",
                "rules",
                ":rule_id",
                "activate"
              ],
              "variable": [
                {
                  "key": "rule_id",
                  "value": "{{rule_id}}",
                  "description": "Path parameter: rule_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/ews/rules/{rule_id}/activate",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/ews/rules/:rule_id/activate — auto-discovered (server.ts:30981)"
        },
        {
          "name": "GET /v1/ews/rules/{rule_id}/approvals",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ews/rules/:rule_id/approvals",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ews",
                "rules",
                ":rule_id",
                "approvals"
              ],
              "variable": [
                {
                  "key": "rule_id",
                  "value": "{{rule_id}}",
                  "description": "Path parameter: rule_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ews/rules/{rule_id}/approvals",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/ews/rules/:rule_id/approvals — auto-discovered (server.ts:31592)"
        },
        {
          "name": "POST /v1/ews/rules/{rule_id}/approve",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ews/rules/:rule_id/approve",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ews",
                "rules",
                ":rule_id",
                "approve"
              ],
              "variable": [
                {
                  "key": "rule_id",
                  "value": "{{rule_id}}",
                  "description": "Path parameter: rule_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/ews/rules/{rule_id}/approve",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "4-eyes activate. Refuses if approver === maker. */"
        },
        {
          "name": "POST /v1/ews/rules/{rule_id}/clone",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ews/rules/:rule_id/clone",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ews",
                "rules",
                ":rule_id",
                "clone"
              ],
              "variable": [
                {
                  "key": "rule_id",
                  "value": "{{rule_id}}",
                  "description": "Path parameter: rule_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/ews/rules/{rule_id}/clone",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "body { new_rule_id, new_name? } → 201 fresh DRAFT v0.1.0. */"
        },
        {
          "name": "GET /v1/ews/rules/{rule_id}/hits",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ews/rules/:rule_id/hits",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ews",
                "rules",
                ":rule_id",
                "hits"
              ],
              "variable": [
                {
                  "key": "rule_id",
                  "value": "{{rule_id}}",
                  "description": "Path parameter: rule_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ews/rules/{rule_id}/hits",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/ews/rules/:rule_id/hits — auto-discovered (server.ts:31034)"
        },
        {
          "name": "POST /v1/ews/rules/{rule_id}/reject",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ews/rules/:rule_id/reject",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ews",
                "rules",
                ":rule_id",
                "reject"
              ],
              "variable": [
                {
                  "key": "rule_id",
                  "value": "{{rule_id}}",
                  "description": "Path parameter: rule_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/ews/rules/{rule_id}/reject",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "4-eyes reject. body { reason } required. */"
        },
        {
          "name": "POST /v1/ews/rules/{rule_id}/submit",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ews/rules/:rule_id/submit",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ews",
                "rules",
                ":rule_id",
                "submit"
              ],
              "variable": [
                {
                  "key": "rule_id",
                  "value": "{{rule_id}}",
                  "description": "Path parameter: rule_id"
                }
              ]
            },
            "description": "maker so /approve can refuse self-approval. */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/ews/rules/{rule_id}/submit",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "body { reason? } → 200 with rule (PENDING_REVIEW). Records the"
        },
        {
          "name": "POST /v1/ews/rules/{rule_id}/test",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ews/rules/:rule_id/test",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ews",
                "rules",
                ":rule_id",
                "test"
              ],
              "variable": [
                {
                  "key": "rule_id",
                  "value": "{{rule_id}}",
                  "description": "Path parameter: rule_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/ews/rules/{rule_id}/test",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "sample values (does NOT record telemetry). */"
        },
        {
          "name": "GET /v1/ews/rules/{rule_id}/versions",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ews/rules/:rule_id/versions",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ews",
                "rules",
                ":rule_id",
                "versions"
              ],
              "variable": [
                {
                  "key": "rule_id",
                  "value": "{{rule_id}}",
                  "description": "Path parameter: rule_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ews/rules/{rule_id}/versions",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/ews/rules/:rule_id/versions — auto-discovered (server.ts:31324)"
        },
        {
          "name": "GET /v1/ews/rules/{rule_id}/versions/{semver}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ews/rules/:rule_id/versions/:semver",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ews",
                "rules",
                ":rule_id",
                "versions",
                ":semver"
              ],
              "variable": [
                {
                  "key": "rule_id",
                  "value": "{{rule_id}}",
                  "description": "Path parameter: rule_id"
                },
                {
                  "key": "semver",
                  "value": "<semver>",
                  "description": "Path parameter: semver"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ews/rules/{rule_id}/versions/{semver}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/ews/rules/:rule_id/versions/:semver — auto-discovered (server.ts:31349)"
        },
        {
          "name": "POST /v1/ews/rules/{rule_id}/versions/{semver}/revert",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ews/rules/:rule_id/versions/:semver/revert",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ews",
                "rules",
                ":rule_id",
                "versions",
                ":semver",
                "revert"
              ],
              "variable": [
                {
                  "key": "rule_id",
                  "value": "{{rule_id}}",
                  "description": "Path parameter: rule_id"
                },
                {
                  "key": "semver",
                  "value": "<semver>",
                  "description": "Path parameter: semver"
                }
              ]
            },
            "description": "named version. Bumps the patch number off the latest. Audit row\nwritten to admin_audit_log via the report-audit pool. Refuses if\nthe rule has a pending approval (4-eyes invariant). */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/ews/rules/{rule_id}/versions/{semver}/revert",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "body { reason? } → creates a new version whose snapshot equals the"
        },
        {
          "name": "POST /v1/ews/rules/{rule_id}/versions/diff",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ews/rules/:rule_id/versions/diff",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ews",
                "rules",
                ":rule_id",
                "versions",
                "diff"
              ],
              "variable": [
                {
                  "key": "rule_id",
                  "value": "{{rule_id}}",
                  "description": "Path parameter: rule_id"
                }
              ]
            },
            "description": "- format='fields'    (default) → returns field-by-field RuleDiffEntry[]\n- format='snapshots' (T-diff)  → returns the same diff PLUS the\nfull from + to snapshots so the SPA can render side-by-side\nJSON without a second round-trip. */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/ews/rules/{rule_id}/versions/diff",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "body { from, to, format? }"
        },
        {
          "name": "POST /v1/ews/rules/evaluate",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ews/rules/evaluate",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ews",
                "rules",
                "evaluate"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/ews/rules/evaluate",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "all active rules. Body { entity_type, entity_id, values }. */"
        },
        {
          "name": "GET /v1/ews/rules/indicators",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ews/rules/indicators",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ews",
                "rules",
                "indicators"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ews/rules/indicators",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/ews/rules/indicators — auto-discovered (server.ts:30560)"
        }
      ]
    },
    {
      "name": "/v1/feature-store/catalog",
      "description": "1 route(s) under /v1/feature-store/catalog",
      "item": [
        {
          "name": "GET /v1/feature-store/catalog",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/feature-store/catalog",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "feature-store",
                "catalog"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/feature-store/catalog",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "features matching `ml/data/load_from_mart.py`). Platform-static. */"
        }
      ]
    },
    {
      "name": "/v1/feature-store/coverage",
      "description": "1 route(s) under /v1/feature-store/coverage",
      "item": [
        {
          "name": "GET /v1/feature-store/coverage",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/feature-store/coverage",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "feature-store",
                "coverage"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/feature-store/coverage",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "size + earliest/latest observed_at window + 24-month cap. */"
        }
      ]
    },
    {
      "name": "/v1/feature-store/customers",
      "description": "2 route(s) under /v1/feature-store/customers",
      "item": [
        {
          "name": "GET /v1/feature-store/customers/{customer_id}/history",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/feature-store/customers/:customer_id/history",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "feature-store",
                "customers",
                ":customer_id",
                "history"
              ],
              "variable": [
                {
                  "key": "customer_id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: customer_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/feature-store/customers/{customer_id}/history",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/feature-store/customers/:customer_id/history — auto-discovered (server.ts:3851)"
        },
        {
          "name": "GET /v1/feature-store/customers/{customer_id}/snapshot",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/feature-store/customers/:customer_id/snapshot",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "feature-store",
                "customers",
                ":customer_id",
                "snapshot"
              ],
              "variable": [
                {
                  "key": "customer_id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: customer_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/feature-store/customers/{customer_id}/snapshot",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "point-in-time row of every catalog feature for this customer. */"
        }
      ]
    },
    {
      "name": "/v1/indicators/backtest",
      "description": "2 route(s) under /v1/indicators/backtest",
      "item": [
        {
          "name": "POST /v1/indicators/backtest",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/indicators/backtest",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "indicators",
                "backtest"
              ]
            },
            "description": "Simulation surface — \"what would this indicator have fired on\nover the last N days?\". Required pre-launch evidence per BIL §10.\nUses M6.2's IndicatorWeightLookup so the catalog stays shared.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/indicators/backtest",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "BIL indicator backtest (T6 M4.2)"
        },
        {
          "name": "POST /v1/indicators/backtest/compare",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/indicators/backtest/compare",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "indicators",
                "backtest",
                "compare"
              ]
            },
            "description": "via /v1/indicators/backtest and passes the resolved results in\nbody `{a, b}`. Returns `{diff: BacktestCompareResult}` with\nfires/precision/recall/F1/mean_value deltas, per-cell confusion\ndelta, per-day fires delta on the overlapping window, and\nsame_indicator / same_segment warning bools. Pure-function; no\nper-tenant state involved. */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/indicators/backtest/compare",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "between two BacktestResult objects. Caller runs both backtests"
        }
      ]
    },
    {
      "name": "/v1/indicators/catalog-stats",
      "description": "1 route(s) under /v1/indicators/catalog-stats",
      "item": [
        {
          "name": "GET /v1/indicators/catalog-stats",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/indicators/catalog-stats",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "indicators",
                "catalog-stats"
              ]
            },
            "description": "insurance) count + by_family Record + distinct_families +\nweight stats (min/mean/max) + top_weighted top-3. Envelope:\ntotal_indicators, total_distinct_families, most_populated_vertical\n(canonical tie-break: banking wins), heaviest_indicator (canonical\nid asc tie-break). Same response per tenant (platform-static).\nMounted BEFORE /v1/indicators/thresholds so the literal segment\nisn't captured by any future wildcard. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/indicators/catalog-stats",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "rollup over the M6.2 STUB_CATALOG. Per-vertical (banking +"
        }
      ]
    },
    {
      "name": "/v1/indicators/overrides",
      "description": "1 route(s) under /v1/indicators/overrides",
      "item": [
        {
          "name": "GET /v1/indicators/overrides/vertical-family-matrix",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/indicators/overrides/vertical-family-matrix",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "indicators",
                "overrides",
                "vertical-family-matrix"
              ]
            },
            "description": "{vertical, total_overrides, by_family (9 keys at 0 — stable grid),\nfamilies_without[] canonical, indicator_ids[] sorted asc,\ndistinct_families}. Per-col {family, total_overrides, by_vertical\n(2 keys at 0), verticals_without[] canonical, indicator_ids[]\nsorted asc, distinct_verticals}. Envelope: peak_cell (canonical\niteration tie-break: ALL_INDICATOR_VERTICALS × ALL_INDICATOR_FAMILIES;\nnull on empty), most_overridden_family (most distinct verticals;\ncanonical family-order tie-break; null on empty), most_active_vertical\n(most distinct families; canonical vertical-order tie-break; null\non empty), empty_cells[] canonical row-major, unknown_families[]\n(indicator_ids with family NOT in ALL_INDICATOR_FAMILIES — drift\ndetector). Mirror of M4.16 for the override surface. Drives BIL\nops \"which (vertical, family) combinations do operators most often\noverride?\" governance view. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/indicators/overrides/vertical-family-matrix",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "the OVERRIDE surface (vs M4.16's platform CATALOG). Per-row"
        }
      ]
    },
    {
      "name": "/v1/indicators/scan-customer",
      "description": "1 route(s) under /v1/indicators/scan-customer",
      "item": [
        {
          "name": "POST /v1/indicators/scan-customer",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/indicators/scan-customer",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "indicators",
                "scan-customer"
              ]
            },
            "description": "effective thresholds (M4.4), return ranked breaches + summary.\nCloses the M4.1 catalog → M4.3 thresholds → M4.4 overrides chain. */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/indicators/scan-customer",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "applicable indicator values for a customer, run each through"
        }
      ]
    },
    {
      "name": "/v1/indicators/scan-customers",
      "description": "1 route(s) under /v1/indicators/scan-customers",
      "item": [
        {
          "name": "POST /v1/indicators/scan-customers",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/indicators/scan-customers",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "indicators",
                "scan-customers"
              ]
            },
            "description": "per-customer summary + portfolio aggregate. */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/indicators/scan-customers",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "M4.5: scan up to 50 customers in one shot, return ranked"
        }
      ]
    },
    {
      "name": "/v1/indicators/thresholds",
      "description": "11 route(s) under /v1/indicators/thresholds",
      "item": [
        {
          "name": "GET /v1/indicators/thresholds",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/indicators/thresholds",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "indicators",
                "thresholds"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/indicators/thresholds",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/indicators/thresholds — auto-discovered (server.ts:13996)"
        },
        {
          "name": "DELETE /v1/indicators/thresholds/{indicator_id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/indicators/thresholds/:indicator_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "indicators",
                "thresholds",
                ":indicator_id"
              ],
              "variable": [
                {
                  "key": "indicator_id",
                  "value": "<indicator_id>",
                  "description": "Path parameter: indicator_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/indicators/thresholds/{indicator_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "→ revert to platform default. 204 / 404. */"
        },
        {
          "name": "GET /v1/indicators/thresholds/{indicator_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/indicators/thresholds/:indicator_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "indicators",
                "thresholds",
                ":indicator_id"
              ],
              "variable": [
                {
                  "key": "indicator_id",
                  "value": "<indicator_id>",
                  "description": "Path parameter: indicator_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/indicators/thresholds/{indicator_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "resolved through tenant overrides (M4.4 wires getEffectiveThreshold). */"
        },
        {
          "name": "PUT /v1/indicators/thresholds/{indicator_id}",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/indicators/thresholds/:indicator_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "indicators",
                "thresholds",
                ":indicator_id"
              ],
              "variable": [
                {
                  "key": "indicator_id",
                  "value": "<indicator_id>",
                  "description": "Path parameter: indicator_id"
                }
              ]
            },
            "description": "monotonic + [0,1] enforced. */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PUT /v1/indicators/thresholds/{indicator_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "per-tenant override. Body { yellow_at, orange_at, red_at } —"
        },
        {
          "name": "POST /v1/indicators/thresholds/{indicator_id}/suggest",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/indicators/thresholds/:indicator_id/suggest",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "indicators",
                "thresholds",
                ":indicator_id",
                "suggest"
              ],
              "variable": [
                {
                  "key": "indicator_id",
                  "value": "<indicator_id>",
                  "description": "Path parameter: indicator_id"
                }
              ]
            },
            "description": "red} thresholds from historical observed values via percentile.\nDefault polarity='higher_is_worse' (red=p95, orange=p75, yellow=p50).\nReturns 200 with `suggested=null + insufficient_reason` when\nfewer than 5 finite samples — lets ops bootstrap thresholds\nfrom real data without forcing every endpoint into 400-territory\nwhen the cohort is small. 404 unknown_indicator. Mounted BEFORE\n`/:indicator_id` GET so the literal `/suggest` segment wins. */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/indicators/thresholds/{indicator_id}/suggest",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "body { values[], polarity? } — derives suggested {yellow, orange,"
        },
        {
          "name": "GET /v1/indicators/thresholds/band-gap",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/indicators/thresholds/band-gap",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "indicators",
                "thresholds",
                "band-gap"
              ]
            },
            "description": "name, yellow_at/orange_at/red_at, yellow_orange_gap (= orange_at\n− yellow_at), orange_red_gap (= red_at − orange_at), total_spread\n(= red_at − yellow_at), monotonic flag, and 5 boolean quality\nflags: has_tight_yellow_orange_gap (gap < 0.10),\nhas_tight_orange_red_gap, has_high_yellow_floor (yellow_at >\n0.65), has_low_red_ceiling (red_at < 0.65), has_inverted_bands\n(monotonicity violated)}. Envelope: mean gaps + mean spread\nacross monotonic rows, tightest_yellow_orange + widest_total_\nspread (canonical indicator_id asc tie-break), by_vertical\nrollup (every ScoringVertical at 0 when absent), flagged_\nindicators[] (any quality flag set; sorted indicator_id asc).\nPlatform-static. Mounted BEFORE `/v1/indicators/thresholds/\neffective` + catch-all `/:indicator_id` so the literal `/band\ngap` segment isn't captured by the param wildcard. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/indicators/thresholds/band-gap",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "LIBRARY DEFAULTS themselves. Per-row: {indicator_id, vertical,"
        },
        {
          "name": "POST /v1/indicators/thresholds/check",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/indicators/thresholds/check",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "indicators",
                "thresholds",
                "check"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/indicators/thresholds/check",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "classify a value into green|yellow|orange|red. */"
        },
        {
          "name": "GET /v1/indicators/thresholds/drift",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/indicators/thresholds/drift",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "indicators",
                "thresholds",
                "drift"
              ]
            },
            "description": "band deltas (default/effective/delta_abs/delta_rel) for\nyellow/orange/red + drift_score (mean |delta_rel|) +\npeak_band_drift + peak_band. Envelope adds total_overrides +\ntotal_with_drift + total_zero_drift + mean_drift_score +\nmost_drifted_indicator. Sorted by drift_score desc. Mounted\nBEFORE catch-all `/:indicator_id` so the literal segment wins. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/indicators/thresholds/drift",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "drift score over the M4.4 override store. Per-row: band-by"
        },
        {
          "name": "GET /v1/indicators/thresholds/effective",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/indicators/thresholds/effective",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "indicators",
                "thresholds",
                "effective"
              ]
            },
            "description": "caller's tenant, with the resolution chain (library_default vs\ntenant_override) showing which level wins per indicator + the\nlibrary_default kept visible side-by-side. `audit:read` RBAC.\nMounted BEFORE the catch-all `/:indicator_id` so the literal\n`/effective` segment isn't captured. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/indicators/thresholds/effective",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "(T6 M4.9) — every platform indicator's effective threshold for the"
        },
        {
          "name": "GET /v1/indicators/thresholds/overrides",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/indicators/thresholds/overrides",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "indicators",
                "thresholds",
                "overrides"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/indicators/thresholds/overrides",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "overrides (T6 M4.4). Declared BEFORE /:indicator_id. */"
        },
        {
          "name": "GET /v1/indicators/thresholds/shift-analysis",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/indicators/thresholds/shift-analysis",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "indicators",
                "thresholds",
                "shift-analysis"
              ]
            },
            "description": "overrides. For each override + each band classifies the shift\nvs platform default as raised | lowered | unchanged. Per row:\nyellow_shift + orange_shift + red_shift (each with signed delta\n+ direction + magnitude) + overall_direction (all_raised /\nall_lowered / mixed / unchanged) + total_magnitude + net_signed_\ndelta. Envelope: by_band_direction (per-band raised/lowered/\nunchanged counts) + by_overall_direction + most_aggressive_\ntightening (largest net-negative delta) + most_aggressive_\nloosening (largest net-positive delta) + most_recalibrated_\nindicator (highest total_magnitude regardless of direction).\nDistinct from M4.12 (drift score — magnitude-only, no direction).\nMounted BEFORE `/v1/indicators/thresholds/effective` + catch\nall `/:indicator_id` so the literal segment wins. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/indicators/thresholds/shift-analysis",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DIRECTION-aware shift view over per-tenant M4.4 threshold"
        }
      ]
    },
    {
      "name": "/v1/indicators/usage",
      "description": "1 route(s) under /v1/indicators/usage",
      "item": [
        {
          "name": "GET /v1/indicators/usage",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/indicators/usage",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "indicators",
                "usage"
              ]
            },
            "description": "(M5.1) reference it. Mirror of M5.14 (template → indicators)\nbut flipped. Includes orphan detection (indicators with zero\nreferences), top-5 most-referenced, per-vertical breakdown,\nand has_threshold flag (from M4.3). Platform-static. Mounted\nBEFORE /v1/indicators/thresholds so the literal /usage segment\nwins. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/indicators/usage",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "for every indicator in the M6.2 catalog: which rule templates"
        }
      ]
    },
    {
      "name": "/v1/indicators/vertical-family-matrix",
      "description": "1 route(s) under /v1/indicators/vertical-family-matrix",
      "item": [
        {
          "name": "GET /v1/indicators/vertical-family-matrix",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/indicators/vertical-family-matrix",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "indicators",
                "vertical-family-matrix"
              ]
            },
            "description": "ScoringVerticals × cols = 9 canonical families (FIN, BEH, TXN,\nCRD on banking; POL, CUS-INS, AGT, CLM, OPS on insurance) = 18\ncells. Per-row {vertical, total, by_family (every family at 0),\nfamilies_without[] canonical}. Per-col {family, total,\nby_vertical (every vertical at 0), verticals_without[]}.\nEnvelope: peak_cell (canonical iteration tie-break) + empty_cells[]\n(canonical row-major) + most_diverse_vertical + most_universal_family\n+ unknown_families[] (defensive — catalog drift detector).\nMirror of M14.28 / M12.14 / M3.14 / M15.14 matrix pattern.\nPlatform-static. Mounted BEFORE /thresholds so the literal segment\nisn't captured. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/indicators/vertical-family-matrix",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "cross-tab elevating M4.13's nested by_family view. Rows = 2"
        }
      ]
    },
    {
      "name": "/v1/indicators/vertical-weight-matrix",
      "description": "1 route(s) under /v1/indicators/vertical-weight-matrix",
      "item": [
        {
          "name": "GET /v1/indicators/vertical-weight-matrix",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/indicators/vertical-weight-matrix",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "indicators",
                "vertical-weight-matrix"
              ]
            },
            "description": "Rows = 2 ScoringVerticals × cols = 5 weight buckets (low /\nlow_medium / medium / high / critical) = 10 cells. Each indicator\nin STUB_CATALOG lives in exactly one cell. Same strict-< upper\nbound semantics as M4.15 (critical inclusive at 1.0). Per-row:\n{vertical, total, by_bucket (every bucket at 0), buckets_without[]\ncanonical, mean_weight (rounded 4 decimals), min/max_weight}.\nPer-col: {bucket, label, min, max, max_inclusive, total,\nby_vertical (every vertical at 0), verticals_without[]}.\nEnvelope: peak_cell (canonical iteration tie-break) +\nempty_cells[] (canonical row-major) + most_diverse_vertical +\nheaviest_vertical (highest critical-bucket count). Mirror of\nM4.16 / M14.28 / M3.14 / M15.14 matrix pattern. Platform-static.\nMounted BEFORE /thresholds so the literal segment isn't captured. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/indicators/vertical-weight-matrix",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "cross-tab combining M4.13 vertical axis × M4.15 weight buckets."
        }
      ]
    },
    {
      "name": "/v1/indicators/weight-histogram",
      "description": "1 route(s) under /v1/indicators/weight-histogram",
      "item": [
        {
          "name": "GET /v1/indicators/weight-histogram",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/indicators/weight-histogram",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "indicators",
                "weight-histogram"
              ]
            },
            "description": "buckets (low < 0.2 / low_medium [0.2, 0.4) / medium [0.4, 0.6) /\nhigh [0.6, 0.8) / critical [0.8, 1.0]) with strict-< upper\nbound semantics (1.0 inclusive on critical only). Per-bucket:\ncount + by_vertical (banking + insurance at 0 when absent) +\nsample_indicator_ids (cap 5 sorted asc). Envelope: peak_bucket\n(canonical iteration tie-break), mean_weight (rounded to 4\ndecimals), min_weight + max_weight, empty_buckets[] canonical.\nMirror of M9.11 / M8.12 histogram pattern. Platform-static.\nMounted BEFORE /v1/indicators/thresholds so the literal segment\nisn't captured. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/indicators/weight-histogram",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "distribution histogram over the M6.2 STUB_CATALOG. 5 canonical"
        }
      ]
    },
    {
      "name": "/v1/rules",
      "description": "1 route(s) under /v1/rules",
      "item": [
        {
          "name": "GET /v1/rules",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/rules",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "rules"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/rules",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/rules — auto-discovered (server.ts:31704)"
        }
      ]
    },
    {
      "name": "/v1/rules/:id",
      "description": "4 route(s) under /v1/rules/:id",
      "item": [
        {
          "name": "GET /v1/rules/{id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/rules/:id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "rules",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/rules/{id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/rules/:id — auto-discovered (server.ts:31737)"
        },
        {
          "name": "POST /v1/rules/{id}/backtest",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/rules/:id/backtest",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "rules",
                ":id",
                "backtest"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/rules/{id}/backtest",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/rules/:id/backtest — auto-discovered (server.ts:31848)"
        },
        {
          "name": "GET /v1/rules/{id}/performance",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/rules/:id/performance",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "rules",
                ":id",
                "performance"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/rules/{id}/performance",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/rules/:id/performance — auto-discovered (server.ts:31860)"
        },
        {
          "name": "POST /v1/rules/{id}/transition",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/rules/:id/transition",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "rules",
                ":id",
                "transition"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/rules/{id}/transition",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/rules/:id/transition — auto-discovered (server.ts:31758)"
        }
      ]
    },
    {
      "name": "/v1/rules/simulate",
      "description": "2 route(s) under /v1/rules/simulate",
      "item": [
        {
          "name": "POST /v1/rules/simulate",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/rules/simulate",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "rules",
                "simulate"
              ]
            },
            "description": "Pure-function fire-rate simulator combining M5.1 templates with\nM16.1 scenarios. Drives the SPA's \"what would this rule do under\nRBI Severely Adverse?\" pre-activation check.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/rules/simulate",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Rule simulation against scenario (T6 M5.3)"
        },
        {
          "name": "POST /v1/rules/simulate/bundle",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/rules/simulate/bundle",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "rules",
                "simulate",
                "bundle"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/rules/simulate/bundle",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/rules/simulate/bundle — auto-discovered (server.ts:31663)"
        }
      ]
    },
    {
      "name": "/v1/rules/templates",
      "description": "23 route(s) under /v1/rules/templates",
      "item": [
        {
          "name": "GET /v1/rules/templates",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/rules/templates",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "rules",
                "templates"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/rules/templates",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/rules/templates — auto-discovered (server.ts:29725)"
        },
        {
          "name": "GET /v1/rules/templates/{id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/rules/templates/:id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "rules",
                "templates",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/rules/templates/{id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/rules/templates/:id — auto-discovered (server.ts:30498)"
        },
        {
          "name": "GET /v1/rules/templates/{template_id}/clones-in-tenant",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/rules/templates/:template_id/clones-in-tenant",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "rules",
                "templates",
                ":template_id",
                "clones-in-tenant"
              ],
              "variable": [
                {
                  "key": "template_id",
                  "value": "<template_id>",
                  "description": "Path parameter: template_id"
                }
              ]
            },
            "description": "custom template in the calling tenant that was cloned from it\n(per the M5.9/M5.10 `cloned_from` audit metadata). Companion\nto M5.7 (per-custom audit history): that asks \"show me the\ntrail for THIS custom template\"; this asks \"show me every\ncustom template traced back to THIS library template\" —\nopposite direction across the same audit data. Mounted\nBEFORE `/:id` so the literal `/clones-in-tenant` segment\nisn't captured. 404 EWS_404_unknown_template when the library\nid doesn't exist. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/rules/templates/{template_id}/clones-in-tenant",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "— back-reference query: for this library template, list every"
        },
        {
          "name": "GET /v1/rules/templates/action-inventory",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/rules/templates/action-inventory",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "rules",
                "templates",
                "action-inventory"
              ]
            },
            "description": "reference it. Per-action: reference_count + templates[] +\nby_category + by_severity. Envelope: total_templates +\nunused_actions[] + most_referenced. Lets ops see operational\ncoverage at a glance (e.g. \"no template uses auto_decline\" =\nmanual review still required for high-confidence fraud).\nPlatform-static. Mounted BEFORE catch-all `/:id` so the\nliteral segment wins. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/rules/templates/action-inventory",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "index from `recommended_action` enum to the templates that"
        },
        {
          "name": "GET /v1/rules/templates/action-severity-matrix",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/rules/templates/action-severity-matrix",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "rules",
                "templates",
                "action-severity-matrix"
              ]
            },
            "description": "(6 keys at 0 when absent), actions_without[]}. Envelope: peak_cell\n(canonical iteration tie-break — actions in canonical order ×\nseverities in canonical critical-first order; carries template_ids\nsorted asc; null on empty), empty_cells[] in canonical row-major\norder, most_versatile_action (highest distinct non-zero severities;\ncanonical action-order tie-break; null on empty), most_distributed_\nseverity (highest distinct non-zero actions; canonical severity\norder tie-break; null on empty), total_contributions (= Σ\ntemplate.recommended_actions[].length). Defensive intra-template\naction dedup. Mirror of M5.17 / M4.16 / M14.28 / M12.14 / M3.14\nmatrix pattern for the rule template surface. Platform-static —\nsame response across tenants since RULE_TEMPLATES is platform\ndata. Drives \"do we ever recommend auto_decline at medium\nseverity?\" governance view. Mounted BEFORE catch-all `/:id`. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/rules/templates/action-severity-matrix",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "absent), severities_without[]}. Per-col {severity, total, by_action"
        },
        {
          "name": "POST /v1/rules/templates/bulk-clone",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/rules/templates/bulk-clone",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "rules",
                "templates",
                "bulk-clone"
              ]
            },
            "description": "body: { template_ids?: string[], category?, vertical?, name_prefix? }\nMaterialises N draft rule shapes from the template selection.\nPure preview — does NOT mutate the rules store. The SPA iterates\nthe result and POSTs each clone to /v1/rules to commit.\nSelection: EITHER template_ids[] OR (category, vertical). 400 on\nboth/neither. > 50 ids → 400. RBAC rules:list (analyst+).\nInserted before /v1/rules/templates/:id so Express matches\n/bulk-clone as a literal path.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/rules/templates/bulk-clone",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/rules/templates/bulk-clone"
        },
        {
          "name": "GET /v1/rules/templates/categories",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/rules/templates/categories",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "rules",
                "templates",
                "categories"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/rules/templates/categories",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/rules/templates/categories — auto-discovered (server.ts:29713)"
        },
        {
          "name": "GET /v1/rules/templates/category-vertical-matrix",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/rules/templates/category-vertical-matrix",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "rules",
                "templates",
                "category-vertical-matrix"
              ]
            },
            "description": "(canonical listCategories() order) × cols = 3 RuleTemplateVertical\n(banking/insurance/both). Per-row {category, total, by_vertical\n(every vertical at 0 when absent), verticals_without[] in\ncanonical order — coverage-gap per category}. Per-col {vertical,\ntotal, by_category (every category at 0), categories_without[]\nin canonical category order}. Envelope: peak_cell (canonical\niteration tie-break + template_ids[] sorted asc; null on empty),\nempty_cells[] in canonical category × vertical row-major order,\nmost_covered_category + most_covered_vertical (highest distinct\nnon-zero span; canonical-order tie-break each; null on empty).\nMirror of M13.15 / M14.28 / M3.11 / M9.13 matrix pattern.\nPlatform-static. Mounted BEFORE catch-all `/:id`. Drives \"do we\nhave insurance underwriting templates? where are the gaps?\". */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/rules/templates/category-vertical-matrix",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "2D cross-tab over RULE_TEMPLATES. Rows = 5 RuleTemplateCategory"
        },
        {
          "name": "GET /v1/rules/templates/custom",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/rules/templates/custom",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "rules",
                "templates",
                "custom"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/rules/templates/custom",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/rules/templates/custom — auto-discovered (server.ts:29855)"
        },
        {
          "name": "POST /v1/rules/templates/custom",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/rules/templates/custom",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "rules",
                "templates",
                "custom"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/rules/templates/custom",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/rules/templates/custom — auto-discovered (server.ts:29867)"
        },
        {
          "name": "DELETE /v1/rules/templates/custom/{template_id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/rules/templates/custom/:template_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "rules",
                "templates",
                "custom",
                ":template_id"
              ],
              "variable": [
                {
                  "key": "template_id",
                  "value": "<template_id>",
                  "description": "Path parameter: template_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/rules/templates/custom/{template_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "T6 M5.8 — writes rule.delete audit event. */"
        },
        {
          "name": "PUT /v1/rules/templates/custom/{template_id}",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/rules/templates/custom/:template_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "rules",
                "templates",
                "custom",
                ":template_id"
              ],
              "variable": [
                {
                  "key": "template_id",
                  "value": "<template_id>",
                  "description": "Path parameter: template_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PUT /v1/rules/templates/custom/{template_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "mutable fields. Writes rule.update audit event with metadata. */"
        },
        {
          "name": "GET /v1/rules/templates/custom/{template_id}/history",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/rules/templates/custom/:template_id/history",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "rules",
                "templates",
                "custom",
                ":template_id",
                "history"
              ],
              "variable": [
                {
                  "key": "template_id",
                  "value": "<template_id>",
                  "description": "Path parameter: template_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/rules/templates/custom/{template_id}/history",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "— slim audit-history view filtered to rule events for this id. */"
        },
        {
          "name": "GET /v1/rules/templates/custom/{template_id}/versions",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/rules/templates/custom/:template_id/versions",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "rules",
                "templates",
                "custom",
                ":template_id",
                "versions"
              ],
              "variable": [
                {
                  "key": "template_id",
                  "value": "<template_id>",
                  "description": "Path parameter: template_id"
                }
              ]
            },
            "description": "templates re-snapshot so the restore itself is auditable. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/rules/templates/custom/{template_id}/versions",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "— version snapshots oldest-first. Cap 20 per template; restored"
        },
        {
          "name": "POST /v1/rules/templates/custom/{template_id}/versions/{version}/restore",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/rules/templates/custom/:template_id/versions/:version/restore",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "rules",
                "templates",
                "custom",
                ":template_id",
                "versions",
                ":version",
                "restore"
              ],
              "variable": [
                {
                  "key": "template_id",
                  "value": "<template_id>",
                  "description": "Path parameter: template_id"
                },
                {
                  "key": "version",
                  "value": "<version>",
                  "description": "Path parameter: version"
                }
              ]
            },
            "description": "Returns {template, restored_from_version}. Pushes a new version\nsnapshot recording the restore. */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/rules/templates/custom/{template_id}/versions/{version}/restore",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "(T6 M5.12) — restore the live template to the captured version."
        },
        {
          "name": "POST /v1/rules/templates/custom/bulk-clone-from-library",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/rules/templates/custom/bulk-clone-from-library",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "rules",
                "templates",
                "custom",
                "bulk-clone-from-library"
              ]
            },
            "description": "over the list. Cap 10 per call. Per-row outcome surfaced as\n{created[], skipped[]}. Writes rule.create audit per successful\nclone with metadata.bulk=true. */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/rules/templates/custom/bulk-clone-from-library",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "body { template_ids[], name_prefix? } — iterates M5.9 single-clone"
        },
        {
          "name": "POST /v1/rules/templates/custom/clone-from-library",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/rules/templates/custom/clone-from-library",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "rules",
                "templates",
                "custom",
                "clone-from-library"
              ]
            },
            "description": "creates an editable custom copy. Writes rule.create audit with\n`cloned_from` metadata. Declared BEFORE /:template_id so the\nliteral `clone-from-library` segment wins. */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/rules/templates/custom/clone-from-library",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "body { source_template_id, name? } — reads a library template and"
        },
        {
          "name": "POST /v1/rules/templates/custom/export-bundle",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/rules/templates/custom/export-bundle",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "rules",
                "templates",
                "custom",
                "export-bundle"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/rules/templates/custom/export-bundle",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "body { template_ids: string[] } → bundle envelope. */"
        },
        {
          "name": "POST /v1/rules/templates/custom/import-bundle",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/rules/templates/custom/import-bundle",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "rules",
                "templates",
                "custom",
                "import-bundle"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/rules/templates/custom/import-bundle",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "body { bundle, name_prefix? } → per-row import outcomes. */"
        },
        {
          "name": "POST /v1/rules/templates/diff",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/rules/templates/diff",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "rules",
                "templates",
                "diff"
              ]
            },
            "description": "literal \"diff\" segment wins. */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/rules/templates/diff",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "comparison of two templates. Declared BEFORE /:id so the"
        },
        {
          "name": "GET /v1/rules/templates/indicator-count-histogram",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/rules/templates/indicator-count-histogram",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "rules",
                "templates",
                "indicator-count-histogram"
              ]
            },
            "description": "array length into 5 canonical buckets: minimal (1) / low (2-3) /\nmedium (4-6) / high (7-10) / comprehensive (>10). Per-bucket\n{count, by_category (5 keys at 0 when absent), by_vertical (3\nkeys at 0 when absent), sample_template_ids cap 3 sorted asc}.\nEnvelope: peak_bucket (canonical iteration tie-break; null on\nempty catalog), mean_indicators (rounded 2 decimals; null on\nempty), min/max_indicators across catalog, empty_buckets[] in\ncanonical order. Mirror of M4.15 / M9.11 / M8.12 histogram pattern\nfor the rule template surface. Platform-static — same response\nacross tenants. Mounted BEFORE catch-all `/:id`. Drives \"is our\ntemplate library too narrow (single-indicator predicates) or too\nbroad (10+ indicators)? where's the coverage gap?\". */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/rules/templates/indicator-count-histogram",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "histogram bucketing every M5.1 template by its supporting_indicators"
        },
        {
          "name": "GET /v1/rules/templates/indicator-coverage",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/rules/templates/indicator-coverage",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "rules",
                "templates",
                "indicator-coverage"
              ]
            },
            "description": "the M6.2 catalog. Per-template: indicators_total / known_count /\nunknown_count / vertical_mismatch_count + status enum\n(fully_resolved | has_unknown | has_mismatch | no_indicators).\nEnvelope counters partition the template set. Lets ops catch\ndrift when an indicator was renamed in the catalog but the\ntemplate wasn't updated. Platform-static (same across tenants).\nMounted BEFORE the catch-all `/:id` so the literal segment wins. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/rules/templates/indicator-coverage",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "reference each rule template's supporting_indicators against"
        },
        {
          "name": "GET /v1/rules/templates/severity-distribution",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/rules/templates/severity-distribution",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "rules",
                "templates",
                "severity-distribution"
              ]
            },
            "description": "medium/low). Per-severity: total_count + by_category + by_vertical\n+ template_ids[]. Envelope: most_common_severity + unused_severities[].\nMirror of M5.15 (action inventory) pivoted by severity instead.\nPlatform-static. Mounted BEFORE catch-all `/:id`. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/rules/templates/severity-distribution",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "templates grouped by recommended_severity (critical/high/"
        }
      ]
    },
    {
      "name": "/v1/rules/variables",
      "description": "1 route(s) under /v1/rules/variables",
      "item": [
        {
          "name": "GET /v1/rules/variables",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/rules/variables",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "rules",
                "variables"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/rules/variables",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/rules/variables — auto-discovered (server.ts:29540)"
        }
      ]
    },
    {
      "name": "/v1/streaming/events",
      "description": "1 route(s) under /v1/streaming/events",
      "item": [
        {
          "name": "GET /v1/streaming/events",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/streaming/events",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "streaming",
                "events"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/streaming/events",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/streaming/events — auto-discovered (server.ts:3746)"
        }
      ]
    },
    {
      "name": "/v1/streaming/indicator-events",
      "description": "1 route(s) under /v1/streaming/indicator-events",
      "item": [
        {
          "name": "POST /v1/streaming/indicator-events",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/streaming/indicator-events",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "streaming",
                "indicator-events"
              ]
            },
            "description": "with latencies computed against `observed_at`. */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/streaming/indicator-events",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "a single event. Per-event record written to the streaming ledger"
        }
      ]
    },
    {
      "name": "/v1/streaming/latency",
      "description": "1 route(s) under /v1/streaming/latency",
      "item": [
        {
          "name": "GET /v1/streaming/latency",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/streaming/latency",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "streaming",
                "latency"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/streaming/latency",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "+ by_indicator rollup over the recent ledger. */"
        }
      ]
    }
  ]
}
