{
  "info": {
    "_postman_id": "71bb12ee-cf4e-4c68-844f-4dbcc7edb2f8",
    "name": "ZorEWS — Dashboard APIs",
    "description": "Dashboard routes from the ZorEWS BFF + auth-svc surface. 26 operations. Auto-generated from `docs/api/swagger.json` by `scripts/gen-postman.js`. Re-import after running `npm run openapi:generate` to refresh.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "auth": {
    "type": "bearer",
    "bearer": [
      {
        "key": "token",
        "value": "{{access_token}}",
        "type": "string"
      }
    ]
  },
  "event": [
    {
      "listen": "prerequest",
      "script": {
        "type": "text/javascript",
        "exec": [
          "// Collection-level pre-request — log the active environment + tenant for visibility",
          "if (!pm.environment.get('access_token') && pm.request.url.toString().indexOf('/auth/login') === -1 && pm.request.url.toString().indexOf('/oauth/token') === -1 && pm.request.url.toString().indexOf('/healthz') === -1 && pm.request.url.toString().indexOf('/.well-known/') === -1) {",
          "  console.warn('access_token is empty — run the Smoke > Login request first to capture a token.');",
          "}"
        ]
      }
    }
  ],
  "variable": [],
  "item": [
    {
      "name": "00 — Smoke tests",
      "description": "Happy-path + negative-test sanity checks. Run this folder first to verify the local stack + auth flow.",
      "item": [
        {
          "name": "✅ Happy path — GET /api/dashboard/summary",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{bff_url}}/api/dashboard/summary",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "api",
                "dashboard",
                "summary"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('2xx or 401 (no auth in env)', () => {",
                  "  pm.expect([200, 201, 401, 403]).to.include(pm.response.code);",
                  "});"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "/api/dashboard",
      "description": "1 route(s) under /api/dashboard",
      "item": [
        {
          "name": "GET /api/dashboard/summary",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{bff_url}}/api/dashboard/summary",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "api",
                "dashboard",
                "summary"
              ]
            },
            "description": "in-memory sources so a local `make up` produces a populated dashboard."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /api/dashboard/summary",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Dashboard KPI summary used by the SPA's home page. Aggregates from"
        }
      ]
    },
    {
      "name": "/v1/dashboards/bil",
      "description": "5 route(s) under /v1/dashboards/bil",
      "item": [
        {
          "name": "GET /v1/dashboards/bil/agent",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dashboards/bil/agent",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dashboards",
                "bil",
                "agent"
              ]
            },
            "description": "ranking (highest portfolio risk first), cancellation clusters."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/dashboards/bil/agent",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "M11.3 — Agent Dashboard. Performance leaderboard, risk-contribution"
        },
        {
          "name": "GET /v1/dashboards/bil/claims",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dashboards/bil/claims",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dashboards",
                "bil",
                "claims"
              ]
            },
            "description": "Five BIL dashboards live under /v1/dashboards/bil/*. Each is tenant\nscoped + RBAC-gated; payloads are deterministic stubs today and swap\nto real queries when the BIL synthetic dataset lands. Module 11 of\nT6 ships them one at a time:\nM11.1 — claims (this endpoint)\nM11.2 — underwriting (future)\nM11.3 — agent (future)\nM11.4 — operational (future)\nThe Executive dashboard already exists for banking via /api/dashboards."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/dashboards/bil/claims",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "BIL dashboards (T6 M11.1+) — DataNetworks-EWS-Ver1.pdf §14"
        },
        {
          "name": "GET /v1/dashboards/bil/executive",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dashboards/bil/executive",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dashboards",
                "bil",
                "executive"
              ]
            },
            "description": "Cross-dashboard rollup — pulls top concerns from each of the 4\nBIL dashboards into a single executive feed. Each item cites its\nsource so the SPA can deep-link back to the owning dashboard."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/dashboards/bil/executive",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "BIL Executive Watchlist (T6 M11.5)"
        },
        {
          "name": "GET /v1/dashboards/bil/operational",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dashboards/bil/operational",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dashboards",
                "bil",
                "operational"
              ]
            },
            "description": "underwriter, login anomalies (last 7 days), override audit trail\n(last 30 days)."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/dashboards/bil/operational",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "M11.4 — Operational Dashboard. UW delay breakdown by branch +"
        },
        {
          "name": "GET /v1/dashboards/bil/underwriting",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dashboards/bil/underwriting",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dashboards",
                "bil",
                "underwriting"
              ]
            },
            "description": "(6 months trailing), lapse predictions sorted by 30-day probability."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/dashboards/bil/underwriting",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "M11.2 — Underwriting Dashboard. High-risk proposals, churn trend"
        }
      ]
    },
    {
      "name": "/v1/dashboards/custom",
      "description": "15 route(s) under /v1/dashboards/custom",
      "item": [
        {
          "name": "GET /v1/dashboards/custom",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dashboards/custom",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dashboards",
                "custom"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/dashboards/custom",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/dashboards/custom — auto-discovered (server.ts:12355)"
        },
        {
          "name": "POST /v1/dashboards/custom",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dashboards/custom",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dashboards",
                "custom"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/dashboards/custom",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/dashboards/custom — auto-discovered (server.ts:12367)"
        },
        {
          "name": "DELETE /v1/dashboards/custom/{dashboard_id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dashboards/custom/:dashboard_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dashboards",
                "custom",
                ":dashboard_id"
              ],
              "variable": [
                {
                  "key": "dashboard_id",
                  "value": "<dashboard_id>",
                  "description": "Path parameter: dashboard_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/dashboards/custom/{dashboard_id}",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([204, 200, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DELETE /v1/dashboards/custom/:dashboard_id — auto-discovered (server.ts:12562)"
        },
        {
          "name": "GET /v1/dashboards/custom/{dashboard_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dashboards/custom/:dashboard_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dashboards",
                "custom",
                ":dashboard_id"
              ],
              "variable": [
                {
                  "key": "dashboard_id",
                  "value": "<dashboard_id>",
                  "description": "Path parameter: dashboard_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/dashboards/custom/{dashboard_id}",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/dashboards/custom/:dashboard_id — auto-discovered (server.ts:12508)"
        },
        {
          "name": "PUT /v1/dashboards/custom/{dashboard_id}",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dashboards/custom/:dashboard_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dashboards",
                "custom",
                ":dashboard_id"
              ],
              "variable": [
                {
                  "key": "dashboard_id",
                  "value": "<dashboard_id>",
                  "description": "Path parameter: dashboard_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PUT /v1/dashboards/custom/{dashboard_id}",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PUT /v1/dashboards/custom/:dashboard_id — auto-discovered (server.ts:12529)"
        },
        {
          "name": "GET /v1/dashboards/custom/{dashboard_id}/lint",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dashboards/custom/:dashboard_id/lint",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dashboards",
                "custom",
                ":dashboard_id",
                "lint"
              ],
              "variable": [
                {
                  "key": "dashboard_id",
                  "value": "<dashboard_id>",
                  "description": "Path parameter: dashboard_id"
                }
              ]
            },
            "description": "errors/warnings/info counts + per-issue details. `passes` flag\nis true iff errors_count===0; the SPA gates a \"deploy\" button\non this. Mounted BEFORE the catch-all `/:dashboard_id` so the\nliteral \"/lint\" segment isn't captured. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/dashboards/custom/{dashboard_id}/lint",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "pure lint pass over a saved layout. Returns LintReport with"
        },
        {
          "name": "POST /v1/dashboards/custom/{dashboard_id}/resolve",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dashboards/custom/:dashboard_id/resolve",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dashboards",
                "custom",
                ":dashboard_id",
                "resolve"
              ],
              "variable": [
                {
                  "key": "dashboard_id",
                  "value": "<dashboard_id>",
                  "description": "Path parameter: dashboard_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/dashboards/custom/{dashboard_id}/resolve",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "resolve every widget on a saved dashboard in one shot. */"
        },
        {
          "name": "GET /v1/dashboards/custom/authorship",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dashboards/custom/authorship",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dashboards",
                "custom",
                "authorship"
              ]
            },
            "description": "dashboard_count, total_widgets (Σ widgets.length), distinct_\nwidget_types, dashboard_ids[] (sorted asc), most_recent_created_at,\nmost_recent_updated_at. Envelope: authors[] sorted dashboard_count\ndesc + created_by asc tie-break, most_prolific_author + most_\nwidgets_author (both with canonical tie-break). Mirror of M15.8\nper-actor + M12.12 per-requester for the dashboards surface.\nMounted BEFORE catch-all `/custom/:dashboard_id` so literal wins. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/dashboards/custom/authorship",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "CREATED_BY rollup over saved dashboards. Per author:"
        },
        {
          "name": "POST /v1/dashboards/custom/export",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dashboards/custom/export",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dashboards",
                "custom",
                "export"
              ]
            },
            "description": "Cap 10/bundle. unknown_dashboard → 404; bad shape → 400. */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/dashboards/custom/export",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "into a versioned JSON envelope. body { dashboard_ids: string[] }."
        },
        {
          "name": "GET /v1/dashboards/custom/fleet-lint",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dashboards/custom/fleet-lint",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dashboards",
                "custom",
                "fleet-lint"
              ]
            },
            "description": "tenant. Per-dashboard summary row + envelope totals +\nby_issue_type (all 5 LintIssueType keys present) +\ndashboards_with_errors[] + worst_dashboard. Companion to\nM11.11 (widget usage). Mounted BEFORE the catch-all\n`/custom/:dashboard_id` so the literal segment wins. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/dashboards/custom/fleet-lint",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "M11.10 lint reports across every saved dashboard in the"
        },
        {
          "name": "GET /v1/dashboards/custom/freshness",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dashboards/custom/freshness",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dashboards",
                "custom",
                "freshness"
              ]
            },
            "description": "{dashboard_id, name, created_by, total_widgets, days_since_updated,\ndays_since_created, freshness ∈ 'recent'|'stable'|'stale'}.\nEnvelope: recent_count + stable_count + stale_count + mean +\noldest_updated + newest_updated + stale_dashboards[] cap 20.\nDefault thresholds fresh_days=30 / stale_days=90; both bounded\nto non-negative integers + stale_days ≥ fresh_days. Mirror of\nM13.11 admin config override-age pattern for the dashboards\nsurface. Drives quarterly stale-content cleanup per\ndocs/bau-runbook.md monthly checklist. Mounted BEFORE catch-all\n`/custom/:dashboard_id` so the literal segment wins. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/dashboards/custom/freshness",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "M11.18) — per-dashboard layout freshness rollup. Each row carries"
        },
        {
          "name": "POST /v1/dashboards/custom/import",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dashboards/custom/import",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dashboards",
                "custom",
                "import"
              ]
            },
            "description": "name_prefix?: string }. Returns per-row outcomes (created/skipped/\nerror). */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/dashboards/custom/import",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "into the caller's tenant. body { bundle: DashboardBundle,"
        },
        {
          "name": "GET /v1/dashboards/custom/starter-packs",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dashboards/custom/starter-packs",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dashboards",
                "custom",
                "starter-packs"
              ]
            },
            "description": "overview\", \"Audit + compliance\") for new tenants to adopt\nwith one click. Platform-static. Mounted BEFORE the\n`/custom/:dashboard_id` catch-all so the literal segment\nwins. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/dashboards/custom/starter-packs",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "curated starter dashboard layouts (\"Daily ops\", \"Executive"
        },
        {
          "name": "GET /v1/dashboards/custom/widget-count-histogram",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dashboards/custom/widget-count-histogram",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dashboards",
                "custom",
                "widget-count-histogram"
              ]
            },
            "description": "Per-bucket {count, sample_dashboards cap 3 sorted widget_count\ndesc + dashboard_id asc tie-break — most-dense first}. Envelope\npeak_bucket (canonical iteration tie-break — empty wins over\nminimal at tied count; null on empty), mean_widgets (rounded 2\ndecimals; null on empty), min/max_widgets, empty_buckets[] in\ncanonical order. Mirror of M5.18 / M4.15 / M9.11 / M8.12 / M7.15\nhistogram pattern for the custom dashboards surface. Distinct\nfrom M11.11 (widget usage analytics per widget_type), M11.14\n(fleet lint), M11.15 (authorship), M11.17 (widget × creator\nmatrix), M11.18 (freshness). Drives BIL governance \"do\noperators build dashboards with right content density? any\nabandoned single-widget dashboards to consolidate or delete?\"\nreview. Mounted BEFORE catch-all `/custom/:dashboard_id` so\nthe literal segment isn't captured by the param wildcard. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/dashboards/custom/widget-count-histogram",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Strict-< upper bounds (4 widgets exactly → medium not small)."
        },
        {
          "name": "GET /v1/dashboards/custom/widget-creator-matrix",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dashboards/custom/widget-creator-matrix",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dashboards",
                "custom",
                "widget-creator-matrix"
              ]
            },
            "description": "Each widget instance contributes to (creator, widget_type) cell.\nPer-row {created_by, total_widgets, total_dashboards,\nby_widget_type (every type at 0), widget_types_without[],\ndistinct_widget_types, most_recent_at}. Per-col {widget_type,\ndisplay_name, total_instances, top_creators[] cap 10,\ndistinct_creators}. Envelope: peak_cell + most_versatile_creator\n(highest distinct_widget_types). Mirror of M1.11 / M14.28 /\nM12.14 / M3.14 / M15.14 matrix pattern. Drives \"which user\ngravitates to which widget?\" templating view. Mounted BEFORE\n/custom/:dashboard_id catch-all so literal segment wins. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/dashboards/custom/widget-creator-matrix",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "2D cross-tab combining 7 WidgetType (closed) × N creators (open)."
        }
      ]
    },
    {
      "name": "/v1/dashboards/widgets",
      "description": "5 route(s) under /v1/dashboards/widgets",
      "item": [
        {
          "name": "GET /v1/dashboards/widgets/catalog",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dashboards/widgets/catalog",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dashboards",
                "widgets",
                "catalog"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/dashboards/widgets/catalog",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/dashboards/widgets/catalog — auto-discovered (server.ts:12096)"
        },
        {
          "name": "GET /v1/dashboards/widgets/config-keys",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dashboards/widgets/config-keys",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dashboards",
                "widgets",
                "config-keys"
              ]
            },
            "description": "distinct config_key, list which widget_types declare it.\nPer-key row {config_key, widget_types[] sorted asc, reference_count,\nwidget_display_names[] sorted asc}. Envelope: config_keys[] sorted\nreference_count desc + config_key asc tie-break, single_use_keys[]\n(reference_count=1; refactor candidates), universal_keys[]\n(reference_count = total_widget_types — well-established\ncross-cutting concepts), most_shared_key (canonical tie-break).\nMirror of M10.13 (template-variable cross-reference) for the\ndashboard widget surface. Platform-static. Drives \"when I add a\n`limit` config, which widget_types will pick it up?\" + \"are\nthere single-use keys that could be inlined?\". */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/dashboards/widgets/config-keys",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "index over the WIDGET_CATALOG.config_keys[] arrays. For each"
        },
        {
          "name": "GET /v1/dashboards/widgets/defaults",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dashboards/widgets/defaults",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dashboards",
                "widgets",
                "defaults"
              ]
            },
            "description": "one entry per widget_type with a sensible default_config so the\nform arrives pre-filled instead of empty. Platform-static. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/dashboards/widgets/defaults",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "default config seed for the SPA's \"Add widget\" wizard. Returns"
        },
        {
          "name": "POST /v1/dashboards/widgets/resolve",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dashboards/widgets/resolve",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dashboards",
                "widgets",
                "resolve"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/dashboards/widgets/resolve",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "single ad-hoc widget. Body { widget_type, position, span, config }. */"
        },
        {
          "name": "GET /v1/dashboards/widgets/usage",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dashboards/widgets/usage",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dashboards",
                "widgets",
                "usage"
              ]
            },
            "description": "(every row always emitted, even at count=0) returns\n{widget_type, display_name, dashboard_count, total_instances,\ndashboards[]: {dashboard_id, name, count}}. Surfaces popular\nwidgets + completely-unused catalog entries (candidates for a\ncleanup or guided tour). Mounted right after `/widgets/catalog`\nso the literal `/widgets/usage` segment isn't captured by any\n`:dashboard_id` wildcard. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/dashboards/widgets/usage",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "tenant's saved dashboards. For each widget_type in WIDGET_CATALOG"
        }
      ]
    }
  ]
}
