{
  "info": {
    "_postman_id": "968da455-1e2e-4898-83d3-64d7d59e4fdc",
    "name": "ZorEWS — Config APIs",
    "description": "Config routes from the ZorEWS BFF + auth-svc surface. 590 operations. Auto-generated from `docs/api/swagger.json` by `scripts/gen-postman.js`. Re-import after running `npm run openapi:generate` to refresh.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "auth": {
    "type": "bearer",
    "bearer": [
      {
        "key": "token",
        "value": "{{access_token}}",
        "type": "string"
      }
    ]
  },
  "event": [
    {
      "listen": "prerequest",
      "script": {
        "type": "text/javascript",
        "exec": [
          "// Collection-level pre-request — log the active environment + tenant for visibility",
          "if (!pm.environment.get('access_token') && pm.request.url.toString().indexOf('/auth/login') === -1 && pm.request.url.toString().indexOf('/oauth/token') === -1 && pm.request.url.toString().indexOf('/healthz') === -1 && pm.request.url.toString().indexOf('/.well-known/') === -1) {",
          "  console.warn('access_token is empty — run the Smoke > Login request first to capture a token.');",
          "}"
        ]
      }
    }
  ],
  "variable": [],
  "item": [
    {
      "name": "00 — Smoke tests",
      "description": "Happy-path + negative-test sanity checks. Run this folder first to verify the local stack + auth flow.",
      "item": [
        {
          "name": "✅ Happy path — GET /v1/admin/config",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/config",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "config"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('2xx or 401 (no auth in env)', () => {",
                  "  pm.expect([200, 201, 401, 403]).to.include(pm.response.code);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "❌ Negative — missing X-Tenant-ID → 400",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{bff_url}}/v1/admin/config",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "config"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('400 when tenant header missing', () => pm.expect(pm.response.code).to.equal(400));",
                  "pm.test('error envelope', () => {",
                  "  const j = pm.response.json();",
                  "  pm.expect(j).to.have.property('error');",
                  "  pm.expect(j.error).to.have.property('code');",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "❌ Negative — unknown role → 403",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "unknown_role",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/config",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "config"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('403 with unknown role', () => pm.expect(pm.response.code).to.equal(403));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "/healthz",
      "description": "1 route(s) under /healthz",
      "item": [
        {
          "name": "GET /healthz",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{bff_url}}/healthz",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "healthz"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /healthz",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /healthz — auto-discovered (server.ts:42)"
        }
      ]
    },
    {
      "name": "/v1/admin/adoption-metrics",
      "description": "1 route(s) under /v1/admin/adoption-metrics",
      "item": [
        {
          "name": "GET /v1/admin/adoption-metrics",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/adoption-metrics",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "adoption-metrics"
              ]
            },
            "description": "engagement, alert funnel (total → acked → with_case → decided)\nwith ratio rates, authorship counts (custom rules / scenarios /\ndashboards / checklists / scoring presets), workflow (onboarding\npct / API keys / webhooks / 2FA), and a composite adoption_score\n(0..100) bucketed into at_risk / warming_up / engaged / power_user.\nDrives the SaaS-admin \"is this tenant going to renew?\" leading\nindicator. Production swap = real telemetry from app_iam.sessions\nfor DAU + app_alerts/app_cases for funnel + app_iam.tenants for\ndays_since_provisioned; resolver interface stays stable. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/admin/adoption-metrics",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "rollup answering \"how engaged is this tenant?\". Covers DAU/WAU/MAU"
        }
      ]
    },
    {
      "name": "/v1/admin/audit-activity",
      "description": "3 route(s) under /v1/admin/audit-activity",
      "item": [
        {
          "name": "GET /v1/admin/audit-activity",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/audit-activity",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "audit-activity"
              ]
            },
            "description": "audit.event_log WORM + app_iam.audit_events auth-svc local +\napp_audit.approvals maker-checker). Distinct from M15.1\n/v1/audit/* — that surface reads the in-memory BIL ledger; this\nreads pg-side cross-chain history. Filter axes: source[],\nactor, action, resource_type, resource_id, correlation_id,\nsince, until. Newest-first. Requires unified.audit_activity\nreader to be wired (BFF_PG_URL set). 501 envelope when in-memory. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/admin/audit-activity",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "audit timeline backed by unified.audit_activity (UNIONs"
        },
        {
          "name": "POST /v1/admin/audit-activity/benchmark",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/audit-activity/benchmark",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "audit-activity",
                "benchmark"
              ]
            },
            "description": "distribution. Runs PRE_WARM_RUNS=3 warm-up queries first\n(results discarded) then N timed runs. Returns mean / p50 /\np95 / p99 / max plus a `promotion_verdict`\n('no_action' / 'monitor' / 'materialize_required') derived from\np95 against spec §6.5 thresholds. Drives the ops decision on\nwhether to flip unified.audit_activity from plain VIEW to\nMATERIALIZED (template at bottom of 035_unified_views.sql §9).\nDefault 50 runs; max 500. Requires audit_activity_benchmark\nwired (BFF_PG_URL set); 501 envelope when in-memory. */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/admin/audit-activity/benchmark",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "the canonical /v1/admin/audit-activity query latency"
        },
        {
          "name": "GET /v1/admin/audit-activity/correlation/{correlation_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/audit-activity/correlation/:correlation_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "audit-activity",
                "correlation",
                ":correlation_id"
              ],
              "variable": [
                {
                  "key": "correlation_id",
                  "value": "<correlation_id>",
                  "description": "Path parameter: correlation_id"
                }
              ]
            },
            "description": "oldest-first. Drives \"show me everything that happened to\ncase X across approvals + WORM + auth\" workflows. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/admin/audit-activity/correlation/{correlation_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "(v1.5 B3) — full cross-source ladder for one workflow,"
        }
      ]
    },
    {
      "name": "/v1/admin/audit-retention",
      "description": "7 route(s) under /v1/admin/audit-retention",
      "item": [
        {
          "name": "GET /v1/admin/audit-retention",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/audit-retention",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "audit-retention"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/admin/audit-retention",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/admin/audit-retention — auto-discovered (server.ts:22054)"
        },
        {
          "name": "POST /v1/admin/audit-retention",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/audit-retention",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "audit-retention"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/admin/audit-retention",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/admin/audit-retention — auto-discovered (server.ts:22134)"
        },
        {
          "name": "DELETE /v1/admin/audit-retention/{policy_id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/audit-retention/:policy_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "audit-retention",
                ":policy_id"
              ],
              "variable": [
                {
                  "key": "policy_id",
                  "value": "<policy_id>",
                  "description": "Path parameter: policy_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/admin/audit-retention/{policy_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DELETE /v1/admin/audit-retention/:policy_id — auto-discovered (server.ts:22252)"
        },
        {
          "name": "GET /v1/admin/audit-retention/{policy_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/audit-retention/:policy_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "audit-retention",
                ":policy_id"
              ],
              "variable": [
                {
                  "key": "policy_id",
                  "value": "<policy_id>",
                  "description": "Path parameter: policy_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/admin/audit-retention/{policy_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/admin/audit-retention/:policy_id — auto-discovered (server.ts:22113)"
        },
        {
          "name": "PATCH /v1/admin/audit-retention/{policy_id}",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/audit-retention/:policy_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "audit-retention",
                ":policy_id"
              ],
              "variable": [
                {
                  "key": "policy_id",
                  "value": "<policy_id>",
                  "description": "Path parameter: policy_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PATCH /v1/admin/audit-retention/{policy_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PATCH /v1/admin/audit-retention/:policy_id — auto-discovered (server.ts:22193)"
        },
        {
          "name": "GET /v1/admin/audit-retention/active/{scope}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/audit-retention/active/:scope",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "audit-retention",
                "active",
                ":scope"
              ],
              "variable": [
                {
                  "key": "scope",
                  "value": "<scope>",
                  "description": "Path parameter: scope"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/admin/audit-retention/active/{scope}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/admin/audit-retention/active/:scope — auto-discovered (server.ts:22080)"
        },
        {
          "name": "GET /v1/admin/audit-retention/strategies",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/audit-retention/strategies",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "audit-retention",
                "strategies"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/admin/audit-retention/strategies",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/admin/audit-retention/strategies — auto-discovered (server.ts:22035)"
        }
      ]
    },
    {
      "name": "/v1/admin/config",
      "description": "33 route(s) under /v1/admin/config",
      "item": [
        {
          "name": "GET /v1/admin/config",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/config",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "config"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/admin/config",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/admin/config — auto-discovered (server.ts:24859)"
        },
        {
          "name": "POST /v1/admin/config/_clone",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/config/_clone",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "config",
                "_clone"
              ]
            },
            "description": "dry_run? }. Returns ImportSummary shape. Admin-only. */",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"value\": 4\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/admin/config/_clone",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "overrides into the caller's tenant. Body { source_tenant_id,"
        },
        {
          "name": "POST /v1/admin/config/_clone/selective",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/config/_clone/selective",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "config",
                "_clone",
                "selective"
              ]
            },
            "description": "tenant. Body { source_tenant_id, keys: string[], dry_run? }.\nReturns SelectiveCloneSummary (ImportSummary + not_in_source +\nrequested_keys). Admin-only.\nM13.6 stays as the full-snapshot variant; this is a strict\nadditive sibling for the operator who wants to migrate a SUBSET\nof tunables (e.g. just the threshold overrides, not the channel\ntoggles). */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/admin/config/_clone/selective",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "listed keys from the source tenant's overrides into the caller's"
        },
        {
          "name": "GET /v1/admin/config/_diff",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/config/_diff",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "config",
                "_diff"
              ]
            },
            "description": "Declared before /:key so the literal _diff segment wins. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/admin/config/_diff",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "per-key comparison of two tenants' override states. Admin-only."
        },
        {
          "name": "GET /v1/admin/config/_export",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/config/_export",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "config",
                "_export"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/admin/config/_export",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/admin/config/_export — auto-discovered (server.ts:24893)"
        },
        {
          "name": "POST /v1/admin/config/_import",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/config/_import",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "config",
                "_import"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"value\": 4\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/admin/config/_import",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/admin/config/_import — auto-discovered (server.ts:24905)"
        },
        {
          "name": "POST /v1/admin/config/_reset-category",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/config/_reset-category",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "config",
                "_reset-category"
              ]
            },
            "description": "Body { category: ConfigCategory, dry_run?: boolean }.\nReturns per-key outcomes: keys with an override get reset (or\npreviewed when dry_run=true); keys already at default get skipped\nwith reason='no_override'. Audit-event writes match the\nsingle-DELETE route (one `config.reset` per actually-reset key\nwith metadata { previous_value, default_value, bulk:true }).\nMounted BEFORE DELETE /:key + GET /:key/history so the literal\n\"_reset-category\" segment isn't captured as a key. */",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"value\": 4\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/admin/config/_reset-category",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "every tenant override in a category back to its platform default."
        },
        {
          "name": "DELETE /v1/admin/config/{key}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/config/:key",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "config",
                ":key"
              ],
              "variable": [
                {
                  "key": "key",
                  "value": "{{config_key}}",
                  "description": "Path parameter: key"
                }
              ]
            },
            "description": "T6 M13.2 — writes a config.reset audit event with metadata\n{ previous_value, default_value }."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/admin/config/{key}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DELETE /v1/admin/config/:key — clear override → revert to default."
        },
        {
          "name": "GET /v1/admin/config/{key}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/config/:key",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "config",
                ":key"
              ],
              "variable": [
                {
                  "key": "key",
                  "value": "{{config_key}}",
                  "description": "Path parameter: key"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/admin/config/{key}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/admin/config/:key — auto-discovered (server.ts:25118)"
        },
        {
          "name": "PUT /v1/admin/config/{key}",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/config/:key",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "config",
                ":key"
              ],
              "variable": [
                {
                  "key": "key",
                  "value": "{{config_key}}",
                  "description": "Path parameter: key"
                }
              ]
            },
            "description": "body: { value }\nSets the override for the supplied key. Validates the value\nagainst the declared type. Returns the resulting entry.\nT6 M13.2 — every successful mutation writes an audit event:\naction=config.update, resource_type=config, resource_id=key,\nmetadata={ previous_value, previous_was_default, new_value }\nThe audit write happens after the mutation succeeds, so failed\nsets don't pollute the trail.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"value\": 4\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PUT /v1/admin/config/{key}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PUT /v1/admin/config/:key"
        },
        {
          "name": "GET /v1/admin/config/{key}/history",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/config/:key/history",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "config",
                ":key",
                "history"
              ],
              "variable": [
                {
                  "key": "key",
                  "value": "{{config_key}}",
                  "description": "Path parameter: key"
                }
              ]
            },
            "description": "Returns audit events for the given config key, newest-first.\nT6 M13.2 — filters the audit trail by resource_type='config' AND\nresource_id=key. Returns 404 EWS_404_unknown_key when the key\nisn't in the schema (so callers don't query phantom history)."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/admin/config/{key}/history",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/admin/config/:key/history?limit=50"
        },
        {
          "name": "POST /v1/admin/config/{key}/rollback",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/config/:key/rollback",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "config",
                ":key",
                "rollback"
              ],
              "variable": [
                {
                  "key": "key",
                  "value": "{{config_key}}",
                  "description": "Path parameter: key"
                }
              ]
            },
            "description": "T6 M13.3 — restore the config key to the value it held immediately\nafter the targeted audit event was applied. Records a NEW\nconfig.update event with `rolled_back_from_event_id` metadata.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/admin/config/{key}/rollback",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/admin/config/:key/rollback body { to_event_id }"
        },
        {
          "name": "GET /v1/admin/config/actor-rollup",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/config/actor-rollup",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "config",
                "actor-rollup"
              ]
            },
            "description": "M13.12 by CATEGORY; M13.16 pivots by `updated_by` (the actor\nthat last touched each key). Per-actor row: {updated_by,\ntotal_overrides, distinct_keys[] sorted asc, distinct_categories[]\nsorted asc, by_category (every ConfigCategory at 0 when absent),\nmost_recent_at}. Envelope: most_active_actor (highest\ntotal_overrides + canonical username asc tie-break; null on\nempty), actors_with_features_overrides[] (subset who have touched\nfeatures.* toggles — security signal; sorted asc). Mirror of\nM2.15 / M15.8 / M9.14 / M11.15 per-actor pattern for the admin\nconfig surface. Drives quarterly access review (\"who has been\nchanging what?\"). Mounted BEFORE /v1/admin/config/:key so the\nliteral /actor-rollup segment isn't captured. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/admin/config/actor-rollup",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "over the M13.1 config store. M13.11 pivots overrides by AGE;"
        },
        {
          "name": "GET /v1/admin/config/audit-summary",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/config/audit-summary",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "config",
                "audit-summary"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/admin/config/audit-summary",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/admin/config/audit-summary — auto-discovered (server.ts:24428)"
        },
        {
          "name": "GET /v1/admin/config/best-practices",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/config/best-practices",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "config",
                "best-practices"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/admin/config/best-practices",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/admin/config/best-practices — auto-discovered (server.ts:24542)"
        },
        {
          "name": "GET /v1/admin/config/catalog",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/config/catalog",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "config",
                "catalog"
              ]
            },
            "description": "description} grouped by category + by_type counts. Lets the SPA\nadmin form render type-appropriate controls (number stepper,\ntoggle, textarea for json) without inferring type from value.\nPlatform-static (same response per tenant). Mounted BEFORE the\ncatch-all GET /v1/admin/config and `/:key` routes so the literal\n`/catalog` segment wins. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/admin/config/catalog",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "config registry: per-key {key, category, type, default_value,"
        },
        {
          "name": "GET /v1/admin/config/categories",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/config/categories",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "config",
                "categories"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/admin/config/categories",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/admin/config/categories — auto-discovered (server.ts:24720)"
        },
        {
          "name": "GET /v1/admin/config/category-actor-matrix",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/config/category-actor-matrix",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "config",
                "category-actor-matrix"
              ]
            },
            "description": "top_actors[] cap 3 with canonical asc tie-break}. Per-col\n{actor_username, total_overrides, by_category (every category at\n0 — stable 5-key grid), categories_without[] canonical,\ndistinct_categories (0..5)}. Envelope: peak_cell (canonical\niteration tie-break: categories canonical × actors asc; null on\nempty), most_versatile_actor (highest distinct_categories +\ncanonical asc tie-break; null on empty), most_active_category\n(highest distinct_actors + canonical category-order tie-break;\nnull on empty), empty_cells[] in canonical category × actor\nrow-major order. Default entries (is_default=true) excluded —\nonly actual overrides contribute. Defensive null updated_by\nskipped. Mirror of M1.11 / M14.28 / M12.14 / M3.14 / M15.14 /\nM15.17 matrix pattern combining CLOSED × OPEN axes. Mounted\nBEFORE catch-all `/v1/admin/config/:key`. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/admin/config/category-actor-matrix",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Per-row {category, total_overrides, by_actor (compact), distinct_actors,"
        },
        {
          "name": "GET /v1/admin/config/change-daily-volume",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/config/change-daily-volume",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "config",
                "change-daily-volume"
              ]
            },
            "description": "config.update / config.reset event to M15.1; this route\naggregates those events into a daily bucket distribution. Per\nUTC day: {total, by_action (2 keys: config.update + config.reset),\nby_category (5 keys per ConfigCategory; resource_id → DEFAULTS\nlookup), distinct_actors, distinct_keys}. Envelope: peak_day\n(earliest-wins tie-break) + mean_per_day + growth_rate\n(second-half mean vs first-half; null when first_half=0 OR\ndays<2) + busiest_category (canonical listCategories tie-break).\nDefault days=30, [1, 365]. Distinct from M13.11 (override AGE\ntracker — snapshot, no time-series), M13.12 (category override\nrate snapshot), M13.16 (per-actor rollup), M13.17 (category ×\nactor matrix). Mirror of M2.16 / M12.13 / M1.9 / M15.11\ndaily-volume pattern. Mounted BEFORE the catch-all /v1/admin/\nconfig/:key so the literal segment wins. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/admin/config/change-daily-volume",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Every successful PUT/DELETE on /v1/admin/config/:key writes a"
        },
        {
          "name": "GET /v1/admin/config/change-frequency",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/config/change-frequency",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "config",
                "change-frequency"
              ]
            },
            "description": "Returns keys[] sorted total_changes desc, most_changed_key,\nstable_keys_count (keys in DEFAULTS never changed). RBAC audit:read.\nMounted BEFORE /:key catch-all so the literal segment isn't captured. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/admin/config/change-frequency",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "audit events with resource_type='config' by config key (resource_id)."
        },
        {
          "name": "GET /v1/admin/config/change-hourly-volume",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/config/change-hourly-volume",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "config",
                "change-hourly-volume"
              ]
            },
            "description": "uses, bucketing every config change by UTC hour-of-day 0..23 across\nthe whole drained set (no trailing window — mirrors M12.18 / M3.12).\nDistinct from M13.18 (LINEAR daily trend) — answers \"WHEN in the day\ndo config changes cluster?\". Per-hour {hour, total, by_action (2\nkeys), by_category (5 keys), distinct_actors, distinct_keys}.\nEnvelope: peak_hour (earliest-hour-wins tie-break; null when zero),\nquiet_hours[], mean_per_hour, busiest_action (canonical tie-break),\nbusiest_category, and the headline AFTER-HOURS security signal —\nafter_hours_changes + after_hours_pct (UTC 22:00-06:00 heuristic):\na spike of config changes at 02:00 UTC is a classic insider-threat\ntell a daily trend can't surface. Mounted BEFORE the catch-all\nGET /v1/admin/config + `/:key` routes so the literal segment wins. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/admin/config/change-hourly-volume",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "intraday view over the same M13.2 config-change audit source M13.18"
        },
        {
          "name": "GET /v1/admin/config/change-patterns",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/config/change-patterns",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "config",
                "change-patterns"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/admin/config/change-patterns",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/admin/config/change-patterns — auto-discovered (server.ts:25102)"
        },
        {
          "name": "GET /v1/admin/config/feature-adoption",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/config/feature-adoption",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "config",
                "feature-adoption"
              ]
            },
            "description": "default_value, enabled/disabled counts, override_count,\nenabled_tenant_ids[] / disabled_tenant_ids[] (sorted asc),\nadoption_rate. Envelope: features[] in canonical schema order,\nmost_adopted_feature + least_adopted_feature (canonical key tie\nbreak), features_with_overrides subset. Async because\ntenantLookup.all() may be async. 501 envelope when lookup\ndoesn't expose .all(). Mounted BEFORE GET /v1/admin/config/:key\nso the literal /feature-adoption segment isn't captured. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/admin/config/feature-adoption",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "pivot over the features.* boolean toggles. Per-feature row:"
        },
        {
          "name": "GET /v1/admin/config/impact-scores",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/config/impact-scores",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "config",
                "impact-scores"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/admin/config/impact-scores",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "impact score per key. Mounted BEFORE /:key catch-all. */"
        },
        {
          "name": "GET /v1/admin/config/override-ages",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/config/override-ages",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "config",
                "override-ages"
              ]
            },
            "description": "age_days + bucket into recent/stable/stale via the configurable\nthresholds (defaults: fresh<30d → recent; >90d → stale; in\nbetween → stable). Envelope has oldest/newest pointers + bucket\ncounts so the SPA can render a \"config that needs review\"\nbanner. Sorted by age_days desc with key asc tie-break. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/admin/config/override-ages",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "— per-override age tracker. For each tenant override, compute"
        },
        {
          "name": "GET /v1/admin/config/override-rate",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/config/override-rate",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "config",
                "override-rate"
              ]
            },
            "description": "override counts + override_rate (0..1). Envelope adds aggregate\ntotal_keys/total_overrides + overall_override_rate +\nmost_customised_category + pristine_categories[]. Companion to\nM13.11 (age axis) — same store, different lens. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/admin/config/override-rate",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "rollup over the config registry showing per-category default-vs"
        },
        {
          "name": "GET /v1/admin/config/rollback-readiness",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/config/rollback-readiness",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "config",
                "rollback-readiness"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/admin/config/rollback-readiness",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "T6 M13.26 — Config rollback readiness (before /:key catch-all)"
        },
        {
          "name": "GET /v1/admin/config/schema-drift",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/config/schema-drift",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "config",
                "schema-drift"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/admin/config/schema-drift",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/admin/config/schema-drift — auto-discovered (server.ts:25094)"
        },
        {
          "name": "GET /v1/admin/config/schema.md",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/config/schema.md",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "config",
                "schema.md"
              ]
            },
            "description": "the static catalogue of keys + types + defaults + descriptions).\nHand-publishable in ops onboarding docs / regulator submissions.\ntext/markdown + Content-Disposition for download. Mounted BEFORE\nGET /v1/admin/config so the literal .md suffix isn't lost in\nthe catch-all. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/admin/config/schema.md",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "export of the M13.1 platform schema (NOT tenant overrides; just"
        },
        {
          "name": "GET /v1/admin/config/summary.txt",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/config/summary.txt",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "config",
                "summary.txt"
              ]
            },
            "description": "metadata. Mirrors M15.4 (audit evidence) + M7.6 (model\nperformance) style. text/plain + Content-Disposition for browser\nprint-to-PDF. Mounted BEFORE GET /v1/admin/config so the literal\n/summary.txt suffix isn't lost in the catch-all. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/admin/config/summary.txt",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "summary of every config key with effective value + override"
        },
        {
          "name": "GET /v1/admin/config/type-matrix",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/config/type-matrix",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "config",
                "type-matrix"
              ]
            },
            "description": "{category, total, by_type (every ConfigType at 0 when absent),\ntypes_without[] in canonical type order — coverage-gap per\ncategory}. Per-col {type, total, by_category (every category at\n0), categories_without[] in canonical category order}. Envelope:\npeak_cell (canonical iteration tie-break; null when zero keys),\nempty_cells[] (count=0 cells in canonical category × type order),\nmost_diverse_category (highest distinct non-zero by_type count;\ncanonical category-order tie-break; null when zero keys). Mirror\nof M14.28 / M3.11 / M9.13 matrix pattern for the admin config\nschema surface. Platform-static. Drives \"which categories are\ndominated by booleans vs numbers?\" + \"are there gaps (no JSON in\nreporting?)\". Mounted BEFORE catch-all `/v1/admin/config/:key`\nso the literal `/type-matrix` isn't captured. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/admin/config/type-matrix",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "the static DEFAULTS schema: rows = category, cols = type. Per-row"
        },
        {
          "name": "GET /v1/admin/config/value-distribution",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/config/value-distribution",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "config",
                "value-distribution"
              ]
            },
            "description": "defaults). Groups by type (number/string/boolean/json). For number:\nmin/max/mean. For boolean: true vs false counts + true_pct. For\nstring: distinct_values + max_length. For json: override_count.\nEnvelope: most_customized_type (type with most overrides). Distinct\nfrom M13.11 (override age; actual overrides only), M13.12 (override\nrate per category). Mounted BEFORE /:key catch-all. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/admin/config/value-distribution",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Per-tenant analytics over effective config values (overrides +"
        },
        {
          "name": "GET /v1/admin/config/value-range-validation",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/config/value-range-validation",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "config",
                "value-range-validation"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/admin/config/value-range-validation",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/admin/config/value-range-validation — auto-discovered (server.ts:24469)"
        }
      ]
    },
    {
      "name": "/v1/admin/data-quality",
      "description": "1 route(s) under /v1/admin/data-quality",
      "item": [
        {
          "name": "GET /v1/admin/data-quality/orphan-references",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/data-quality/orphan-references",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "data-quality",
                "orphan-references"
              ]
            },
            "description": "resolve under the unified.* views' LEFT JOIN resilience. Read-only,\nper-tenant, admin (audit:read). 3 orphan classes scanned:\n1. cases_without_alert — case.alert_id ∉ app_alerts.alerts\n2. alerts_without_customer — alert.customer_id ∉ mart.customer_360\n3. approvals_without_case — approval.correlation_id (case-like\nsubject_type only) ∉ app_cases.cases\nReturns per-class total_scanned + orphan_count + orphan_rate +\nsample_orphans (cap 100 default, max 500). Requires the orphan\nscanner to be wired (BFF_PG_URL set + pg pool); 501 envelope when\nunavailable (in-memory mode). */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/admin/data-quality/orphan-references",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Diagnostic surface — surfaces FK-style references that fail to"
        }
      ]
    },
    {
      "name": "/v1/admin/field-masking",
      "description": "6 route(s) under /v1/admin/field-masking",
      "item": [
        {
          "name": "GET /v1/admin/field-masking",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/field-masking",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "field-masking"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/admin/field-masking",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/admin/field-masking — auto-discovered (server.ts:21809)"
        },
        {
          "name": "POST /v1/admin/field-masking",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/field-masking",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "field-masking"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/admin/field-masking",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/admin/field-masking — auto-discovered (server.ts:21858)"
        },
        {
          "name": "DELETE /v1/admin/field-masking/{policy_id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/field-masking/:policy_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "field-masking",
                ":policy_id"
              ],
              "variable": [
                {
                  "key": "policy_id",
                  "value": "<policy_id>",
                  "description": "Path parameter: policy_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/admin/field-masking/{policy_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DELETE /v1/admin/field-masking/:policy_id — auto-discovered (server.ts:21968)"
        },
        {
          "name": "GET /v1/admin/field-masking/{policy_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/field-masking/:policy_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "field-masking",
                ":policy_id"
              ],
              "variable": [
                {
                  "key": "policy_id",
                  "value": "<policy_id>",
                  "description": "Path parameter: policy_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/admin/field-masking/{policy_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/admin/field-masking/:policy_id — auto-discovered (server.ts:21837)"
        },
        {
          "name": "PATCH /v1/admin/field-masking/{policy_id}",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/field-masking/:policy_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "field-masking",
                ":policy_id"
              ],
              "variable": [
                {
                  "key": "policy_id",
                  "value": "<policy_id>",
                  "description": "Path parameter: policy_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PATCH /v1/admin/field-masking/{policy_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PATCH /v1/admin/field-masking/:policy_id — auto-discovered (server.ts:21917)"
        },
        {
          "name": "GET /v1/admin/field-masking/strategies",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/field-masking/strategies",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "field-masking",
                "strategies"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/admin/field-masking/strategies",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/admin/field-masking/strategies — auto-discovered (server.ts:21796)"
        }
      ]
    },
    {
      "name": "/v1/admin/masters",
      "description": "6 route(s) under /v1/admin/masters",
      "item": [
        {
          "name": "GET /v1/admin/masters",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/masters",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "masters"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/admin/masters",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Drives the SPA's master-menu landing page. */"
        },
        {
          "name": "GET /v1/admin/masters/{entity}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/masters/:entity",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "masters",
                ":entity"
              ],
              "variable": [
                {
                  "key": "entity",
                  "value": "<entity>",
                  "description": "Path parameter: entity"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/admin/masters/{entity}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/admin/masters/:entity — auto-discovered (server.ts:37996)"
        },
        {
          "name": "POST /v1/admin/masters/{entity}",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/masters/:entity",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "masters",
                ":entity"
              ],
              "variable": [
                {
                  "key": "entity",
                  "value": "<entity>",
                  "description": "Path parameter: entity"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/admin/masters/{entity}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "per-entity fields object; tenant_id auto-derived from headers. */"
        },
        {
          "name": "DELETE /v1/admin/masters/{entity}/{id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/masters/:entity/:id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "masters",
                ":entity",
                ":id"
              ],
              "variable": [
                {
                  "key": "entity",
                  "value": "<entity>",
                  "description": "Path parameter: entity"
                },
                {
                  "key": "id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/admin/masters/{entity}/{id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "archive yet — wire to registerRecoveryAdapter in a follow-up). */"
        },
        {
          "name": "GET /v1/admin/masters/{entity}/{id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/masters/:entity/:id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "masters",
                ":entity",
                ":id"
              ],
              "variable": [
                {
                  "key": "entity",
                  "value": "<entity>",
                  "description": "Path parameter: entity"
                },
                {
                  "key": "id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/admin/masters/{entity}/{id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/admin/masters/:entity/:id — auto-discovered (server.ts:38030)"
        },
        {
          "name": "PATCH /v1/admin/masters/{entity}/{id}",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/admin/masters/:entity/:id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "admin",
                "masters",
                ":entity",
                ":id"
              ],
              "variable": [
                {
                  "key": "entity",
                  "value": "<entity>",
                  "description": "Path parameter: entity"
                },
                {
                  "key": "id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PATCH /v1/admin/masters/{entity}/{id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PATCH /v1/admin/masters/:entity/:id — auto-discovered (server.ts:38099)"
        }
      ]
    },
    {
      "name": "/v1/analytics/alert-resolution",
      "description": "1 route(s) under /v1/analytics/alert-resolution",
      "item": [
        {
          "name": "GET /v1/analytics/alert-resolution",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/analytics/alert-resolution",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "analytics",
                "alert-resolution"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/analytics/alert-resolution",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/analytics/alert-resolution — auto-discovered (server.ts:3160)"
        }
      ]
    },
    {
      "name": "/v1/analytics/pd-distribution",
      "description": "1 route(s) under /v1/analytics/pd-distribution",
      "item": [
        {
          "name": "GET /v1/analytics/pd-distribution",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/analytics/pd-distribution",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "analytics",
                "pd-distribution"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/analytics/pd-distribution",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/analytics/pd-distribution — auto-discovered (server.ts:3057)"
        }
      ]
    },
    {
      "name": "/v1/analytics/risk-trend",
      "description": "1 route(s) under /v1/analytics/risk-trend",
      "item": [
        {
          "name": "GET /v1/analytics/risk-trend",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/analytics/risk-trend",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "analytics",
                "risk-trend"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/analytics/risk-trend",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/analytics/risk-trend — auto-discovered (server.ts:3114)"
        }
      ]
    },
    {
      "name": "/v1/analytics/stage-migration",
      "description": "1 route(s) under /v1/analytics/stage-migration",
      "item": [
        {
          "name": "GET /v1/analytics/stage-migration",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/analytics/stage-migration",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "analytics",
                "stage-migration"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/analytics/stage-migration",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/analytics/stage-migration — auto-discovered (server.ts:2997)"
        }
      ]
    },
    {
      "name": "/v1/anomalies",
      "description": "1 route(s) under /v1/anomalies",
      "item": [
        {
          "name": "GET /v1/anomalies",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/anomalies",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "anomalies"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/anomalies",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Module #8 — Anomaly Detection (§2.1.8)"
        }
      ]
    },
    {
      "name": "/v1/anomalies/:anomaly_id",
      "description": "3 route(s) under /v1/anomalies/:anomaly_id",
      "item": [
        {
          "name": "GET /v1/anomalies/{anomaly_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/anomalies/:anomaly_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "anomalies",
                ":anomaly_id"
              ],
              "variable": [
                {
                  "key": "anomaly_id",
                  "value": "<anomaly_id>",
                  "description": "Path parameter: anomaly_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/anomalies/{anomaly_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/anomalies/:anomaly_id — auto-discovered (server.ts:41472)"
        },
        {
          "name": "POST /v1/anomalies/{anomaly_id}/dismiss",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/anomalies/:anomaly_id/dismiss",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "anomalies",
                ":anomaly_id",
                "dismiss"
              ],
              "variable": [
                {
                  "key": "anomaly_id",
                  "value": "<anomaly_id>",
                  "description": "Path parameter: anomaly_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/anomalies/{anomaly_id}/dismiss",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Module 1.5 — Dismiss anomaly as false positive. Reason required."
        },
        {
          "name": "POST /v1/anomalies/{anomaly_id}/investigate",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/anomalies/:anomaly_id/investigate",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "anomalies",
                ":anomaly_id",
                "investigate"
              ],
              "variable": [
                {
                  "key": "anomaly_id",
                  "value": "<anomaly_id>",
                  "description": "Path parameter: anomaly_id"
                }
              ]
            },
            "description": "and cross-links a case_id. Caller may pass an existing case_id or\nomit it (the route mints `case-anom-<anomaly_id>` deterministically\nso an audit trail exists even when the cases-svc isn't reachable).",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/anomalies/{anomaly_id}/investigate",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Module 1.5 — Investigate: transitions an anomaly to `investigating`"
        }
      ]
    },
    {
      "name": "/v1/anomalies/inject-spike",
      "description": "1 route(s) under /v1/anomalies/inject-spike",
      "item": [
        {
          "name": "POST /v1/anomalies/inject-spike",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/anomalies/inject-spike",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "anomalies",
                "inject-spike"
              ]
            },
            "description": "Literal path → must be mounted BEFORE `:anomaly_id` catch-all.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/anomalies/inject-spike",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Module 1.5 — inject a 10× spike for demo / acceptance."
        }
      ]
    },
    {
      "name": "/v1/anomalies/patterns",
      "description": "2 route(s) under /v1/anomalies/patterns",
      "item": [
        {
          "name": "GET /v1/anomalies/patterns/config",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/anomalies/patterns/config",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "anomalies",
                "patterns",
                "config"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/anomalies/patterns/config",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/anomalies/patterns/config — auto-discovered (server.ts:41219)"
        },
        {
          "name": "POST /v1/anomalies/patterns/config",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/anomalies/patterns/config",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "anomalies",
                "patterns",
                "config"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/anomalies/patterns/config",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/anomalies/patterns/config — auto-discovered (server.ts:41230)"
        }
      ]
    },
    {
      "name": "/v1/anomalies/rerun",
      "description": "1 route(s) under /v1/anomalies/rerun",
      "item": [
        {
          "name": "POST /v1/anomalies/rerun",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/anomalies/rerun",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "anomalies",
                "rerun"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/anomalies/rerun",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/anomalies/rerun — auto-discovered (server.ts:41259)"
        }
      ]
    },
    {
      "name": "/v1/audit/action-prefix-distribution",
      "description": "1 route(s) under /v1/audit/action-prefix-distribution",
      "item": [
        {
          "name": "GET /v1/audit/action-prefix-distribution",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/audit/action-prefix-distribution",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "audit",
                "action-prefix-distribution"
              ]
            },
            "description": "`.` in `<prefix>.<verb>` actions). Coarser-grained than M15.6\n(per-verb catalog). Per-prefix row {prefix, total_count,\ndistinct_verbs[] sorted asc, by_outcome (3 keys), distinct_\nresource_types[] sorted asc, distinct_actors[] sorted asc,\nmost_recent_at}. Envelope: prefixes[] sorted total_count desc +\nprefix asc tie-break, most_frequent_prefix (canonical tie-break;\nnull on empty), unprefixed_count (actions without `.` — surfaces\naccidentally-flat verb writes). Mirror of M4.13 indicator family\nextraction for the audit verbs surface. Drives \"by-domain\"\nrollup answering \"how much of our audit is config vs scenario\nvs case?\". */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/audit/action-prefix-distribution",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "the audit chain by ACTION PREFIX (everything before the first"
        }
      ]
    },
    {
      "name": "/v1/audit/action-resource-matrix",
      "description": "1 route(s) under /v1/audit/action-resource-matrix",
      "item": [
        {
          "name": "GET /v1/audit/action-resource-matrix",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/audit/action-resource-matrix",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "audit",
                "action-resource-matrix"
              ]
            },
            "description": "axis is CLOSED (10 canonical AuditResourceTypes). Each event\nlives in exactly one (action, resource_type) cell. Per-row\n{action, total, by_resource_type (10 keys at 0 when absent —\nstable grid), resource_types_without (canonical order),\ndistinct_resource_types}. Per-col {resource_type, total, by_action\n(compact), actions_without (asc), distinct_actions}. Envelope:\npeak_cell (canonical iteration tie-break: actions asc × types\ncanonical; null on empty), most_versatile_action (highest\ndistinct_resource_types + canonical action asc tie-break; null\non empty), most_diverse_resource_type (highest distinct_actions\n+ canonical type-order tie-break; null on empty), empty_cells[]\nin canonical action × resource_type row-major order. Mirror of\nM1.11 / M14.28 / M12.14 / M3.14 matrix pattern but with OPEN\naction axis (N can grow) × CLOSED resource_type axis. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/audit/action-resource-matrix",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "axis is OPEN (any action verb from observed events); resource_type"
        }
      ]
    },
    {
      "name": "/v1/audit/actions",
      "description": "1 route(s) under /v1/audit/actions",
      "item": [
        {
          "name": "GET /v1/audit/actions",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/audit/actions",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "audit",
                "actions"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/audit/actions",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/audit/actions — auto-discovered (server.ts:30167)"
        }
      ]
    },
    {
      "name": "/v1/audit/activity-heatmap",
      "description": "1 route(s) under /v1/audit/activity-heatmap",
      "item": [
        {
          "name": "GET /v1/audit/activity-heatmap",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/audit/activity-heatmap",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "audit",
                "activity-heatmap"
              ]
            },
            "description": "(field-visit heatmap) over the audit chain. ISO Mon=0..Sun=6 ×\nhour 0..23. Default tz=UTC; 13-zone whitelist via isScheduleTz.\nReturns {tz, total_events, by_dow_hour, by_dow, by_hour, peak}.\nUseful for \"when are ops most active on the platform?\". */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/audit/activity-heatmap",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "of-week × hour-of-day heatmap of audit events. Mirror of M14.22"
        }
      ]
    },
    {
      "name": "/v1/audit/actor-heatmap",
      "description": "1 route(s) under /v1/audit/actor-heatmap",
      "item": [
        {
          "name": "GET /v1/audit/actor-heatmap",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/audit/actor-heatmap",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "audit",
                "actor-heatmap"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/audit/actor-heatmap",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "per actor for top 5 actors. Mounted BEFORE /events/:event_id. */"
        }
      ]
    },
    {
      "name": "/v1/audit/actor-outcome-matrix",
      "description": "1 route(s) under /v1/audit/actor-outcome-matrix",
      "item": [
        {
          "name": "GET /v1/audit/actor-outcome-matrix",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/audit/actor-outcome-matrix",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "audit",
                "actor-outcome-matrix"
              ]
            },
            "description": "denied)}. Per-col {outcome, total, by_actor (compact), actors_without\n(asc), distinct_actors}. Envelope: peak_cell (canonical iteration\ntie-break: actors asc × outcomes canonical; null on empty),\nmost_failing_actor (highest failure_count — the segregation-of-duties\n/ suspicious-activity signal + canonical actor asc tie-break; null\nwhen no failures/denials), actors_with_denials[] (actors with ≥ 1\ndenied event; asc), most_common_outcome (highest column total +\ncanonical tie-break success > failure > denied; null on empty),\nmost_versatile_actor (most distinct outcomes + canonical asc\ntie-break; null on empty), empty_cells[] in canonical actor ×\noutcome row-major order. Mirror of M15.19 / M15.15 / M1.14 matrix\npattern. Drives BIL access-review + security governance: \"which\nactor racks up the most failed/denied actions? who is repeatedly\ndenied?\". */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/audit/actor-outcome-matrix",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "(canonical order), distinct_outcomes, failure_count (= failure +"
        }
      ]
    },
    {
      "name": "/v1/audit/actor-resource-matrix",
      "description": "1 route(s) under /v1/audit/actor-resource-matrix",
      "item": [
        {
          "name": "GET /v1/audit/actor-resource-matrix",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/audit/actor-resource-matrix",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "audit",
                "actor-resource-matrix"
              ]
            },
            "description": "by_resource_type (10 keys at 0 when absent — stable grid),\nresource_types_without (canonical order), distinct_resource_types}.\nPer-col {resource_type, total, by_actor (compact), actors_without\n(asc), distinct_actors}. Envelope: peak_cell (canonical iteration\ntie-break: actors asc × types canonical; null on empty),\nmost_versatile_actor (highest distinct_resource_types — the\naccess-review broadest-footprint signal + canonical actor asc\ntie-break; null on empty), most_touched_resource_type (highest\ndistinct_actors + canonical type-order tie-break; null on empty),\nempty_cells[] in canonical actor × resource_type row-major order.\nMirror of M15.17 / M1.14 / M13.17 matrix pattern but with OPEN\nactor axis (N can grow) × CLOSED resource_type axis. Drives BIL\naccess-review: \"which actor touches the most resource types — is\nthat consistent with their assigned scope?\". */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/audit/actor-resource-matrix",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "(per-actor 1D) only nests. Per-row {actor_username, total,"
        }
      ]
    },
    {
      "name": "/v1/audit/actor-sessions",
      "description": "1 route(s) under /v1/audit/actor-sessions",
      "item": [
        {
          "name": "GET /v1/audit/actor-sessions",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/audit/actor-sessions",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "audit",
                "actor-sessions"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/audit/actor-sessions",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/audit/actor-sessions — auto-discovered (server.ts:30893)"
        }
      ]
    },
    {
      "name": "/v1/audit/catalog",
      "description": "1 route(s) under /v1/audit/catalog",
      "item": [
        {
          "name": "GET /v1/audit/catalog",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/audit/catalog",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "audit",
                "catalog"
              ]
            },
            "description": "observed_count, distinct resource_types, union of metadata keys,\nand latest event timestamp + actor. Lets the SPA build a\ndata-driven filter dropdown instead of hardcoding the action list. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/audit/catalog",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "For each distinct action emitted by this tenant, returns the"
        }
      ]
    },
    {
      "name": "/v1/audit/chain-completeness",
      "description": "1 route(s) under /v1/audit/chain-completeness",
      "item": [
        {
          "name": "GET /v1/audit/chain-completeness",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/audit/chain-completeness",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "audit",
                "chain-completeness"
              ]
            },
            "description": "total_events, has_gaps, gap_count, out_of_order_count,\nbroken_hash_links, completeness_score (0-100), issues[],\nis_complete (score >= 95). RBAC audit:read. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/audit/chain-completeness",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "completeness check over the tenant's audit chain. Returns:"
        }
      ]
    },
    {
      "name": "/v1/audit/chain-gap-analysis",
      "description": "1 route(s) under /v1/audit/chain-gap-analysis",
      "item": [
        {
          "name": "GET /v1/audit/chain-gap-analysis",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/audit/chain-gap-analysis",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "audit",
                "chain-gap-analysis"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/audit/chain-gap-analysis",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/audit/chain-gap-analysis — auto-discovered (server.ts:30609)"
        }
      ]
    },
    {
      "name": "/v1/audit/completeness-certification",
      "description": "1 route(s) under /v1/audit/completeness-certification",
      "item": [
        {
          "name": "GET /v1/audit/completeness-certification",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/audit/completeness-certification",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "audit",
                "completeness-certification"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/audit/completeness-certification",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/audit/completeness-certification — auto-discovered (server.ts:30807)"
        }
      ]
    },
    {
      "name": "/v1/audit/correlation-duration-histogram",
      "description": "1 route(s) under /v1/audit/correlation-duration-histogram",
      "item": [
        {
          "name": "GET /v1/audit/correlation-duration-histogram",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/audit/correlation-duration-histogram",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "audit",
                "correlation-duration-histogram"
              ]
            },
            "description": "canonical buckets: instant (<1s) / fast (<1m) / medium (<1h) /\nslow (<1d) / day_plus. Per-bucket: count + has_failure_count +\nsample_correlation_ids (cap 3, longest-first). Envelope: peak_bucket\n(canonical iteration tie-break), mean/median/p95 across full\nduration distribution, longest_correlation (id + duration_ms),\nfailed_correlations[] (has_failure=true subset; sorted duration\ndesc + asc). Mirror of M7.15 / M8.12 / M9.11 histogram pattern\nfor the audit-correlation surface. Drives \"how long do our\nworkflows take? are slow correlations failing more?\" view. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/audit/correlation-duration-histogram",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "duration distribution over the M15.10 correlation rollup. 5"
        }
      ]
    },
    {
      "name": "/v1/audit/correlations",
      "description": "1 route(s) under /v1/audit/correlations",
      "item": [
        {
          "name": "GET /v1/audit/correlations",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/audit/correlations",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "audit",
                "correlations"
              ]
            },
            "description": "and surfaces the workflow it represents. Per-correlation row:\nevent_count, distinct_actors[], distinct_resource_types[],\ndistinct_actions[], first/last_event_at, duration_ms, action_chain[]\noldest-first (ts + action + actor + outcome), has_failure. Envelope:\ntotal_events_with/without_correlation, total_correlations,\nmost_active_correlation, longest_running_correlation,\nfailed_correlations[]. Completes the 4-axis pivot family with\nM15.6 (action) + M15.8 (actor) + M15.9 (severity). Trace\n\"everything that happened during request X\" in one round-trip. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/audit/correlations",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "rollup. Groups every audit event with a non-null correlation_id"
        }
      ]
    },
    {
      "name": "/v1/audit/daily-volume",
      "description": "1 route(s) under /v1/audit/daily-volume",
      "item": [
        {
          "name": "GET /v1/audit/daily-volume",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/audit/daily-volume",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "audit",
                "daily-volume"
              ]
            },
            "description": "(cyclic dow×hour heatmap). Per-day bucket: total + by_severity\n(3 keys) + by_outcome (3 keys). Every day in [window_start,\nwindow_end] emitted even when zero — stable time-series x-axis.\nEnvelope adds peak_day (earliest-day tie-break), mean_per_day,\ngrowth_rate (second-half mean vs first-half mean; null when\nfirst-half is zero / days<2), busiest_severity. Default days=30,\nbounds [1, 365]. 400 EWS_400_invalid_input on out-of-range. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/audit/daily-volume",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "across N consecutive UTC calendar days. Complements M15.7"
        }
      ]
    },
    {
      "name": "/v1/audit/events",
      "description": "3 route(s) under /v1/audit/events",
      "item": [
        {
          "name": "GET /v1/audit/events",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/audit/events",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "audit",
                "events"
              ]
            },
            "description": "severity=&since=&until=&page=&page_size=\nNewest-first paginated query. Multiple actions can be supplied as a\ncomma-separated list (action=auth.login,auth.logout)."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/audit/events",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/audit/events?actor_username=&action=&resource_type=&outcome=&"
        },
        {
          "name": "POST /v1/audit/events",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/audit/events",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "audit",
                "events"
              ]
            },
            "description": "Per-tenant structured audit log with filters tuned for RBI/IRDAI\nevidence dumps. All routes RBAC audit:read (admin-only) — audit\nevents can contain PII (actor names, IP addresses, sensitive\nresource ids).\nPOST /v1/audit/events — record a new audit event.\nbody: AuditEventInput\nReturns the recorded event with assigned event_id + ts.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"actor_username\": \"{{apex_user}}\",\n  \"action\": \"config.update\",\n  \"resource_type\": \"config\",\n  \"resource_id\": \"alerts.red_sla_hours\",\n  \"outcome\": \"success\",\n  \"severity\": \"info\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/audit/events",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "BIL Audit & Compliance trail (T6 M15.1)"
        },
        {
          "name": "GET /v1/audit/events/{event_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/audit/events/:event_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "audit",
                "events",
                ":event_id"
              ],
              "variable": [
                {
                  "key": "event_id",
                  "value": "<event_id>",
                  "description": "Path parameter: event_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/audit/events/{event_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/audit/events/:event_id — auto-discovered (server.ts:31452)"
        }
      ]
    },
    {
      "name": "/v1/audit/evidence",
      "description": "6 route(s) under /v1/audit/evidence",
      "item": [
        {
          "name": "GET /v1/audit/evidence",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/audit/evidence",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "audit",
                "evidence"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/audit/evidence",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "newest-first. */"
        },
        {
          "name": "POST /v1/audit/evidence",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/audit/evidence",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "audit",
                "evidence"
              ]
            },
            "description": "build + retain a new package. 201 created. Records X-APEX-USER\nas `generated_by`.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/audit/evidence",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "action?, resource_type?, resource_id?, outcome?, severity? } —"
        },
        {
          "name": "GET /v1/audit/evidence/{package_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/audit/evidence/:package_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "audit",
                "evidence",
                ":package_id"
              ],
              "variable": [
                {
                  "key": "package_id",
                  "value": "<package_id>",
                  "description": "Path parameter: package_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/audit/evidence/{package_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/audit/evidence/:package_id — auto-discovered (server.ts:31297)"
        },
        {
          "name": "GET /v1/audit/evidence/{package_id}/export.csv",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/audit/evidence/:package_id/export.csv",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "audit",
                "evidence",
                ":package_id",
                "export.csv"
              ],
              "variable": [
                {
                  "key": "package_id",
                  "value": "<package_id>",
                  "description": "Path parameter: package_id"
                }
              ]
            },
            "description": "RFC 4180 format: header row + one row per event.\nColumns: event_id, ts, actor_username, action, resource_type,\nresource_id, outcome, severity, correlation_id.\nMounted BEFORE /:package_id catch-all. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/audit/evidence/{package_id}/export.csv",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "— CSV export of all events in the evidence package."
        },
        {
          "name": "GET /v1/audit/evidence/{package_id}/export.pdf",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/audit/evidence/:package_id/export.pdf",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "audit",
                "evidence",
                ":package_id",
                "export.pdf"
              ],
              "variable": [
                {
                  "key": "package_id",
                  "value": "<package_id>",
                  "description": "Path parameter: package_id"
                }
              ]
            },
            "description": "filename so the browser's \"Save as\" dialog defaults to PDF.\nUses the existing renderEvidenceSummary() function from M15.4.\nMounted BEFORE /:package_id catch-all. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/audit/evidence/{package_id}/export.pdf",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "— Printable plain-text evidence summary, served with a .pdf"
        },
        {
          "name": "GET /v1/audit/evidence/{package_id}/summary.txt",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/audit/evidence/:package_id/summary.txt",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "audit",
                "evidence",
                ":package_id",
                "summary.txt"
              ],
              "variable": [
                {
                  "key": "package_id",
                  "value": "<package_id>",
                  "description": "Path parameter: package_id"
                }
              ]
            },
            "description": "Returns text/plain (NOT the T4.24 envelope) so the SPA can pipe\nit straight to a print preview. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/audit/evidence/{package_id}/summary.txt",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "— printable plain-text summary suitable for browser print-to-PDF."
        }
      ]
    },
    {
      "name": "/v1/audit/integrity",
      "description": "2 route(s) under /v1/audit/integrity",
      "item": [
        {
          "name": "GET /v1/audit/integrity",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/audit/integrity",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "audit",
                "integrity"
              ]
            },
            "description": "any tampering. Returns valid=true + last_hash on a clean chain.\nTenant-scoped; admin only."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/audit/integrity",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/audit/integrity — recompute the chain hash and report"
        },
        {
          "name": "GET /v1/audit/integrity/sample",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/audit/integrity/sample",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "audit",
                "integrity",
                "sample"
              ]
            },
            "description": "dashboard health-pulse polling. Verifies hashes + prev_hash\nlinks within the window AND that the first event in the window\ncorrectly chains to the event before it (or 'GENESIS' when the\nwindow covers the entire chain). Default window=50, max=500. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/audit/integrity/sample",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "the newest N events. Cheaper than M15.2's full-chain walk for"
        }
      ]
    },
    {
      "name": "/v1/audit/metadata-richness",
      "description": "1 route(s) under /v1/audit/metadata-richness",
      "item": [
        {
          "name": "GET /v1/audit/metadata-richness",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/audit/metadata-richness",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "audit",
                "metadata-richness"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/audit/metadata-richness",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "T6 M15.28 — Audit event metadata richness score"
        }
      ]
    },
    {
      "name": "/v1/audit/per-actor-activity",
      "description": "1 route(s) under /v1/audit/per-actor-activity",
      "item": [
        {
          "name": "GET /v1/audit/per-actor-activity",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/audit/per-actor-activity",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "audit",
                "per-actor-activity"
              ]
            },
            "description": "distinct_actions, by_action_top (top 5), by_outcome (every\nAuditOutcome key), by_resource_type (every AuditResourceType\nkey), first/last event ts, primary_role. Envelope adds\nmost_active_actor + failure_only_actors[]. Mirror of M15.6\n(action catalog) pivoted by actor instead. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/audit/per-actor-activity",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "rollup over the audit chain. Per-actor: total_events,"
        }
      ]
    },
    {
      "name": "/v1/audit/resource-hotspots",
      "description": "1 route(s) under /v1/audit/resource-hotspots",
      "item": [
        {
          "name": "GET /v1/audit/resource-hotspots",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/audit/resource-hotspots",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "audit",
                "resource-hotspots"
              ]
            },
            "description": "For each unique resource touched, surfaces total_events +\ndistinct_actors + distinct_actions + actors[] cap 50 sorted asc\n+ actions[] cap 50 sorted asc + first/last_event_at. Envelope:\nhottest_resource (top of sorted list) + by_resource_type\nmarginal (every RT key present) + top_hotspots[] sorted by\ntotal_events desc with resource_type asc + resource_id asc\ntie-break. Default limit=20, bounds [1, 200] via\nAuditResourceHotspotsError → 400 EWS_400_invalid_input. Mirror\nof M15.8 / M9.14 / M11.15 per-entity pivot pattern. Drives\nforensic drill-through: \"which case has the most audit activity?\nwhich config key gets edited most? which user has been touched\nmost for privacy review?\". */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/audit/resource-hotspots",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "(resource_type, resource_id) hot-spot pivot over the audit chain."
        }
      ]
    },
    {
      "name": "/v1/audit/resource-severity-matrix",
      "description": "1 route(s) under /v1/audit/resource-severity-matrix",
      "item": [
        {
          "name": "GET /v1/audit/resource-severity-matrix",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/audit/resource-severity-matrix",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "audit",
                "resource-severity-matrix"
              ]
            },
            "description": "severities_without[] canonical}. Per-col {severity, total,\nby_resource_type (every type at 0 when absent),\nresource_types_without[] canonical}. Envelope {rows[], columns[],\npeak_cell (canonical iteration tie-break — types in canonical\norder × severities in critical-first order; null on empty),\nempty_cells[] in canonical resource_type × severity row-major\norder, most_severity_spread_type + most_resource_spread_severity\n(highest distinct non-zero span; canonical-order tie-break;\nnull on empty), total_critical (Σ critical column), resource_\ntypes_with_critical[] (≥1 critical event; canonical order)}.\nMirror of M14.28 / M12.14 / M3.14 / M8.14 matrix pattern. Drives\n\"which resource types have critical events landing?\" + \"which\nseverity spans the most resource types (system-wide noise vs\none-area spike)?\". Mounted with the M15 distribution family. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/audit/resource-severity-matrix",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "total, by_severity (every severity at 0 when absent),"
        }
      ]
    },
    {
      "name": "/v1/audit/resource-type-distribution",
      "description": "1 route(s) under /v1/audit/resource-type-distribution",
      "item": [
        {
          "name": "GET /v1/audit/resource-type-distribution",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/audit/resource-type-distribution",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "audit",
                "resource-type-distribution"
              ]
            },
            "description": "total_count + by_severity (3 keys present) + by_outcome (3 keys\npresent) + by_action_top (cap 5) + distinct_actors +\nmost_recent_at. Envelope: types[] in canonical order, most_\nactive_type (canonical tie-break), unused_types, last_event_at.\nInverse of M15.9 (which has resource_type INSIDE severity rows).\nDrives \"which resource type sees the most audit traffic?\" view. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/audit/resource-type-distribution",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "the audit chain by resource_type (10-key axis). Per-type row:"
        }
      ]
    },
    {
      "name": "/v1/audit/retention-health",
      "description": "1 route(s) under /v1/audit/retention-health",
      "item": [
        {
          "name": "GET /v1/audit/retention-health",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/audit/retention-health",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "audit",
                "retention-health"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/audit/retention-health",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/audit/retention-health — auto-discovered (server.ts:30674)"
        }
      ]
    },
    {
      "name": "/v1/audit/search",
      "description": "1 route(s) under /v1/audit/search",
      "item": [
        {
          "name": "GET /v1/audit/search",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/audit/search",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "audit",
                "search"
              ]
            },
            "description": "substring match across: actor_username, action, resource_id,\nresource_type, outcome, severity, JSON.stringify(metadata).\nPer-match: event_id + ts + key fields + match_fields[] (which\nfields matched) + snippet (200-char context). Returned newest\nfirst. Query 2–200 chars after trim; limit 1–200, default 50.\nDrains audit chain via page_size=1000 up to 10 pages (10k events).\nAuditSearchError codes: invalid_query → 400, invalid_limit → 400.\nDistinct from M15.1 (exact field filters), M15.6 (action catalog),\nM15.8 (per-actor rollup). RBAC audit:read. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/audit/search",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "across all audit events for the calling tenant. Case-insensitive"
        }
      ]
    },
    {
      "name": "/v1/audit/severity-distribution",
      "description": "1 route(s) under /v1/audit/severity-distribution",
      "item": [
        {
          "name": "GET /v1/audit/severity-distribution",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/audit/severity-distribution",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "audit",
                "severity-distribution"
              ]
            },
            "description": "(10 keys) + by_outcome (3 keys) + by_action_top (top 5) +\nmost_recent_at. Envelope: most_common_severity +\nlast_critical_event_at. Mirror of M5.16 (template severity\ndistribution) for audit events. Pairs with M15.6 (action\ncatalog) + M15.8 (per-actor) for a complete 3-axis pivot. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/audit/severity-distribution",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "pivoted rollup. Per-severity: total_count + by_resource_type"
        }
      ]
    },
    {
      "name": "/v1/audit/severity-outcome-matrix",
      "description": "1 route(s) under /v1/audit/severity-outcome-matrix",
      "item": [
        {
          "name": "GET /v1/audit/severity-outcome-matrix",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/audit/severity-outcome-matrix",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "audit",
                "severity-outcome-matrix"
              ]
            },
            "description": "AuditOutcome (canonical success → failure → denied) = 9 cells.\nEach event lives in exactly one cell (partition invariant).\nPer-row {severity, total, by_outcome (every outcome at 0 when\nabsent), outcomes_without[] canonical}. Per-col {outcome, total,\nby_severity (every severity at 0 when absent), severities_without[]\ncanonical}. Envelope: peak_cell + empty_cells (canonical row-major:\nseverity outer × outcome inner) + most_failing_severity (severity\nwith most failure+denied combined; canonical tie-break: critical\nwins; null on success-only or empty) + most_critical_outcome\n(outcome carrying most critical-severity events; canonical\ntie-break: success wins; null when no critical events). Mirror of\nM14.28 / M12.14 / M3.14 / M15.14 matrix pattern. Drives BIL\ncompliance \"are critical events succeeding or failing? where do\ndenied actions cluster?\". */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/audit/severity-outcome-matrix",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "AuditSeverity (canonical critical → warning → info) × cols = 3"
        }
      ]
    },
    {
      "name": "/v1/audit/summary",
      "description": "1 route(s) under /v1/audit/summary",
      "item": [
        {
          "name": "GET /v1/audit/summary",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/audit/summary",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "audit",
                "summary"
              ]
            },
            "description": "Aggregate counts by outcome / severity / action / resource_type\nover the trailing window. days defaults to 30, clamped to [1, 365]."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/audit/summary",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/audit/summary?days=30"
        }
      ]
    },
    {
      "name": "/v1/audit/volume-anomalies",
      "description": "1 route(s) under /v1/audit/volume-anomalies",
      "item": [
        {
          "name": "GET /v1/audit/volume-anomalies",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/audit/volume-anomalies",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "audit",
                "volume-anomalies"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/audit/volume-anomalies",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/audit/volume-anomalies — auto-discovered (server.ts:30866)"
        }
      ]
    },
    {
      "name": "/v1/audit/volume-forecast",
      "description": "1 route(s) under /v1/audit/volume-forecast",
      "item": [
        {
          "name": "GET /v1/audit/volume-forecast",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/audit/volume-forecast",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "audit",
                "volume-forecast"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/audit/volume-forecast",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/audit/volume-forecast — auto-discovered (server.ts:31121)"
        }
      ]
    },
    {
      "name": "/v1/banking/accounts",
      "description": "6 route(s) under /v1/banking/accounts",
      "item": [
        {
          "name": "POST /v1/banking/accounts/{account_id}/block",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/accounts/:account_id/block",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "accounts",
                ":account_id",
                "block"
              ],
              "variable": [
                {
                  "key": "account_id",
                  "value": "<account_id>",
                  "description": "Path parameter: account_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/banking/accounts/{account_id}/block",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/banking/accounts/:account_id/block — auto-discovered (server.ts:39396)"
        },
        {
          "name": "GET /v1/banking/accounts/{account_id}/patterns",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/accounts/:account_id/patterns",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "accounts",
                ":account_id",
                "patterns"
              ],
              "variable": [
                {
                  "key": "account_id",
                  "value": "<account_id>",
                  "description": "Path parameter: account_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/banking/accounts/{account_id}/patterns",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/banking/accounts/:account_id/patterns — auto-discovered (server.ts:39376)"
        },
        {
          "name": "GET /v1/banking/accounts/{account_id}/transactions",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/accounts/:account_id/transactions",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "accounts",
                ":account_id",
                "transactions"
              ],
              "variable": [
                {
                  "key": "account_id",
                  "value": "<account_id>",
                  "description": "Path parameter: account_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/banking/accounts/{account_id}/transactions",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/banking/accounts/:account_id/transactions — auto-discovered (server.ts:39458)"
        },
        {
          "name": "GET /v1/banking/accounts/signals",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/accounts/signals",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "accounts",
                "signals"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/banking/accounts/signals",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Module #3 — Account Behaviour signals (ZorEWS_Pending_Gap_Analysis §2.1.3)"
        },
        {
          "name": "POST /v1/banking/accounts/signals/{signal_id}/dismiss",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/accounts/signals/:signal_id/dismiss",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "accounts",
                "signals",
                ":signal_id",
                "dismiss"
              ],
              "variable": [
                {
                  "key": "signal_id",
                  "value": "<signal_id>",
                  "description": "Path parameter: signal_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/banking/accounts/signals/{signal_id}/dismiss",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/banking/accounts/signals/:signal_id/dismiss — auto-discovered (server.ts:39565)"
        },
        {
          "name": "POST /v1/banking/accounts/signals/{signal_id}/review",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/accounts/signals/:signal_id/review",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "accounts",
                "signals",
                ":signal_id",
                "review"
              ],
              "variable": [
                {
                  "key": "signal_id",
                  "value": "<signal_id>",
                  "description": "Path parameter: signal_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/banking/accounts/signals/{signal_id}/review",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/banking/accounts/signals/:signal_id/review — auto-discovered (server.ts:39573)"
        }
      ]
    },
    {
      "name": "/v1/banking/borrowers",
      "description": "1 route(s) under /v1/banking/borrowers",
      "item": [
        {
          "name": "GET /v1/banking/borrowers/{customer_id}/timeline",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/borrowers/:customer_id/timeline",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "borrowers",
                ":customer_id",
                "timeline"
              ],
              "variable": [
                {
                  "key": "customer_id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: customer_id"
                }
              ]
            },
            "description": "Distinct from the CMS case timelines (single-case state ladder) — this\nis the borrower's whole risk journey across products."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/banking/borrowers/{customer_id}/timeline",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Module #9 — Borrower Timeline (§2.1.9) — per-borrower risk-event stream."
        }
      ]
    },
    {
      "name": "/v1/banking/branches",
      "description": "3 route(s) under /v1/banking/branches",
      "item": [
        {
          "name": "GET /v1/banking/branches/{branch_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/branches/:branch_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "branches",
                ":branch_id"
              ],
              "variable": [
                {
                  "key": "branch_id",
                  "value": "<branch_id>",
                  "description": "Path parameter: branch_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/banking/branches/{branch_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/banking/branches/:branch_id — auto-discovered (server.ts:40725)"
        },
        {
          "name": "GET /v1/banking/branches/{branch_id}/deep-dive",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/branches/:branch_id/deep-dive",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "branches",
                ":branch_id",
                "deep-dive"
              ],
              "variable": [
                {
                  "key": "branch_id",
                  "value": "<branch_id>",
                  "description": "Path parameter: branch_id"
                }
              ]
            },
            "description": "`/deep-dive` segment isn't captured as a branch id."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/banking/branches/{branch_id}/deep-dive",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Deep-dive registered BEFORE the bare /:branch_id so the literal"
        },
        {
          "name": "GET /v1/banking/branches/heatmap",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/branches/heatmap",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "branches",
                "heatmap"
              ]
            },
            "description": "Geographic / org-unit view; distinct from Sector Watch (industry)."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/banking/branches/heatmap",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Module #8 — Branch / Geography Risk heatmap (§2.1.8)."
        }
      ]
    },
    {
      "name": "/v1/banking/cma",
      "description": "1 route(s) under /v1/banking/cma",
      "item": [
        {
          "name": "POST /v1/banking/cma/pack",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/cma/pack",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "cma",
                "pack"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/banking/cma/pack",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/banking/cma/pack — auto-discovered (server.ts:39300)"
        }
      ]
    },
    {
      "name": "/v1/banking/cohort",
      "description": "1 route(s) under /v1/banking/cohort",
      "item": [
        {
          "name": "POST /v1/banking/cohort/cma-pack",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/cohort/cma-pack",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "cohort",
                "cma-pack"
              ]
            },
            "description": "Arrangement pack metadata blob from a cohort. Caller (SPA) uses\nthe returned pack_id + download_filename to build the actual\nXLSX/PDF client-side.\nReads the SAME borrower list as `/v1/customers` (mode='all' so\nany borrower can be in the cohort, even S3) so the cohort can\ninclude all borrowers visible to the operator. */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/banking/cohort/cma-pack",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "body { cohort_ids: string[] }. Builds a Credit Monitoring"
        }
      ]
    },
    {
      "name": "/v1/banking/collections",
      "description": "5 route(s) under /v1/banking/collections",
      "item": [
        {
          "name": "GET /v1/banking/collections/{account_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/collections/:account_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "collections",
                ":account_id"
              ],
              "variable": [
                {
                  "key": "account_id",
                  "value": "<account_id>",
                  "description": "Path parameter: account_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/banking/collections/{account_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/banking/collections/:account_id — auto-discovered (server.ts:40616)"
        },
        {
          "name": "POST /v1/banking/collections/{account_id}/log-contact",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/collections/:account_id/log-contact",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "collections",
                ":account_id",
                "log-contact"
              ],
              "variable": [
                {
                  "key": "account_id",
                  "value": "<account_id>",
                  "description": "Path parameter: account_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/banking/collections/{account_id}/log-contact",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/banking/collections/:account_id/log-contact — auto-discovered (server.ts:40578)"
        },
        {
          "name": "POST /v1/banking/collections/{account_id}/ptp",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/collections/:account_id/ptp",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "collections",
                ":account_id",
                "ptp"
              ],
              "variable": [
                {
                  "key": "account_id",
                  "value": "<account_id>",
                  "description": "Path parameter: account_id"
                }
              ]
            },
            "description": "literal `/ptp` + `/log-contact` segments aren't captured as an\naccount id by the param route.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/banking/collections/{account_id}/ptp",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST mutations registered BEFORE the bare /:account_id GET so the"
        },
        {
          "name": "GET /v1/banking/collections/queue",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/collections/queue",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "collections",
                "queue"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/banking/collections/queue",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/banking/collections/queue — auto-discovered (server.ts:40505)"
        },
        {
          "name": "GET /v1/banking/collections/summary",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/collections/summary",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "collections",
                "summary"
              ]
            },
            "description": "Distinct from the collection-adapter service (auto case routing) —\nthis is the read/operate surface for the collections work-queue."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/banking/collections/summary",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Module #7 — Collections Risk / Recovery desk (§2.1.7)"
        }
      ]
    },
    {
      "name": "/v1/banking/npa",
      "description": "5 route(s) under /v1/banking/npa",
      "item": [
        {
          "name": "GET /v1/banking/npa/backtest/latest",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/npa/backtest/latest",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "npa",
                "backtest",
                "latest"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/banking/npa/backtest/latest",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/banking/npa/backtest/latest — auto-discovered (server.ts:40817)"
        },
        {
          "name": "GET /v1/banking/npa/high-risk",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/npa/high-risk",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "npa",
                "high-risk"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/banking/npa/high-risk",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Module #5 — NPA Prediction wrap (§2.1.5)"
        },
        {
          "name": "GET /v1/banking/npa/portfolio-drivers",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/npa/portfolio-drivers",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "npa",
                "portfolio-drivers"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/banking/npa/portfolio-drivers",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "M7.19 — Portfolio-level NPA driver aggregation"
        },
        {
          "name": "GET /v1/banking/npa/predictions/{account_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/npa/predictions/:account_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "npa",
                "predictions",
                ":account_id"
              ],
              "variable": [
                {
                  "key": "account_id",
                  "value": "<account_id>",
                  "description": "Path parameter: account_id"
                }
              ]
            },
            "description": "Single-prediction lookup. Re-uses the explainNpaPrediction synth path\nbut projects only the prediction fields (PD across 30/60/90d horizons +\ncurrent band + recommended actions) — the SPA row-expand view. The\nfull feature importance lives on the sibling /why endpoint."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/banking/npa/predictions/{account_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "M2.5 — GET /v1/banking/npa/predictions/:account_id"
        },
        {
          "name": "GET /v1/banking/npa/predictions/{prediction_id}/why",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/npa/predictions/:prediction_id/why",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "npa",
                "predictions",
                ":prediction_id",
                "why"
              ],
              "variable": [
                {
                  "key": "prediction_id",
                  "value": "{{prediction_id}}",
                  "description": "Path parameter: prediction_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/banking/npa/predictions/{prediction_id}/why",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/banking/npa/predictions/:prediction_id/why — auto-discovered (server.ts:40798)"
        }
      ]
    },
    {
      "name": "/v1/banking/ratios",
      "description": "9 route(s) under /v1/banking/ratios",
      "item": [
        {
          "name": "GET /v1/banking/ratios/customer/{customer_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/ratios/customer/:customer_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "ratios",
                "customer",
                ":customer_id"
              ],
              "variable": [
                {
                  "key": "customer_id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: customer_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/banking/ratios/customer/{customer_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/banking/ratios/customer/:customer_id — auto-discovered (server.ts:39131)"
        },
        {
          "name": "GET /v1/banking/ratios/customer/{customer_id}/history",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/ratios/customer/:customer_id/history",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "ratios",
                "customer",
                ":customer_id",
                "history"
              ],
              "variable": [
                {
                  "key": "customer_id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: customer_id"
                }
              ]
            },
            "description": "Mounted BEFORE /v1/banking/ratios/customer/:customer_id so the literal\n/history segment is captured first."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/banking/ratios/customer/{customer_id}/history",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "M2.3 — single-ratio history slice with sector benchmark overlay."
        },
        {
          "name": "GET /v1/banking/ratios/master",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/ratios/master",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "ratios",
                "master"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/banking/ratios/master",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/banking/ratios/master — auto-discovered (server.ts:39045)"
        },
        {
          "name": "GET /v1/banking/ratios/notes",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/ratios/notes",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "ratios",
                "notes"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/banking/ratios/notes",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "M2.3 — ratio notes: GET list (filterable) + POST add."
        },
        {
          "name": "POST /v1/banking/ratios/notes",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/ratios/notes",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "ratios",
                "notes"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/banking/ratios/notes",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/banking/ratios/notes — auto-discovered (server.ts:39224)"
        },
        {
          "name": "GET /v1/banking/ratios/sector-benchmark",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/ratios/sector-benchmark",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "ratios",
                "sector-benchmark"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/banking/ratios/sector-benchmark",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/banking/ratios/sector-benchmark — auto-discovered (server.ts:39276)"
        },
        {
          "name": "GET /v1/banking/ratios/thresholds",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/ratios/thresholds",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "ratios",
                "thresholds"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/banking/ratios/thresholds",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/banking/ratios/thresholds — auto-discovered (server.ts:39056)"
        },
        {
          "name": "DELETE /v1/banking/ratios/thresholds/{code}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/ratios/thresholds/:code",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "ratios",
                "thresholds",
                ":code"
              ],
              "variable": [
                {
                  "key": "code",
                  "value": "<code>",
                  "description": "Path parameter: code"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/banking/ratios/thresholds/{code}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DELETE /v1/banking/ratios/thresholds/:code — auto-discovered (server.ts:39113)"
        },
        {
          "name": "PUT /v1/banking/ratios/thresholds/{code}",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/ratios/thresholds/:code",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "ratios",
                "thresholds",
                ":code"
              ],
              "variable": [
                {
                  "key": "code",
                  "value": "<code>",
                  "description": "Path parameter: code"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PUT /v1/banking/ratios/thresholds/{code}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PUT /v1/banking/ratios/thresholds/:code — auto-discovered (server.ts:39067)"
        }
      ]
    },
    {
      "name": "/v1/banking/sectors",
      "description": "6 route(s) under /v1/banking/sectors",
      "item": [
        {
          "name": "GET /v1/banking/sectors/{sector_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/sectors/:sector_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "sectors",
                ":sector_id"
              ],
              "variable": [
                {
                  "key": "sector_id",
                  "value": "<sector_id>",
                  "description": "Path parameter: sector_id"
                }
              ]
            },
            "description": "MUST be registered AFTER the /watchlist routes — Express matches in\ndeclaration order so /:sector_id would otherwise capture the literal\n`watchlist` segment and 404 it as an unknown sector."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/banking/sectors/{sector_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "M2.7 SW-3 — bare single-sector summary (lighter than deep-dive)."
        },
        {
          "name": "GET /v1/banking/sectors/{sector_id}/deep-dive",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/sectors/:sector_id/deep-dive",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "sectors",
                ":sector_id",
                "deep-dive"
              ],
              "variable": [
                {
                  "key": "sector_id",
                  "value": "<sector_id>",
                  "description": "Path parameter: sector_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/banking/sectors/{sector_id}/deep-dive",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/banking/sectors/:sector_id/deep-dive — auto-discovered (server.ts:39601)"
        },
        {
          "name": "GET /v1/banking/sectors/heatmap",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/sectors/heatmap",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "sectors",
                "heatmap"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/banking/sectors/heatmap",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Module #4 — Sector Watch heatmap (§2.1.4)"
        },
        {
          "name": "GET /v1/banking/sectors/watchlist",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/sectors/watchlist",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "sectors",
                "watchlist"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/banking/sectors/watchlist",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/banking/sectors/watchlist — auto-discovered (server.ts:39630)"
        },
        {
          "name": "POST /v1/banking/sectors/watchlist",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/sectors/watchlist",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "sectors",
                "watchlist"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/banking/sectors/watchlist",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/banking/sectors/watchlist — auto-discovered (server.ts:39642)"
        },
        {
          "name": "DELETE /v1/banking/sectors/watchlist/{sector_id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/sectors/watchlist/:sector_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "sectors",
                "watchlist",
                ":sector_id"
              ],
              "variable": [
                {
                  "key": "sector_id",
                  "value": "<sector_id>",
                  "description": "Path parameter: sector_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/banking/sectors/watchlist/{sector_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DELETE /v1/banking/sectors/watchlist/:sector_id — auto-discovered (server.ts:39690)"
        }
      ]
    },
    {
      "name": "/v1/banking/sma",
      "description": "6 route(s) under /v1/banking/sma",
      "item": [
        {
          "name": "GET /v1/banking/sma/drill",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/sma/drill",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "sma",
                "drill"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/banking/sma/drill",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/banking/sma/drill — auto-discovered (server.ts:38883)"
        },
        {
          "name": "GET /v1/banking/sma/framework",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/sma/framework",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "sma",
                "framework"
              ]
            },
            "description": "Returns the currently-active framework + the full catalog of available\ncountry-specific frameworks (RBI India / RMA Bhutan / CBK Kenya, ...).\nDrives the SPA's framework banner + Master → Regulators picker."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/banking/sma/framework",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "M2.4 — GET /v1/banking/sma/framework?framework="
        },
        {
          "name": "GET /v1/banking/sma/movements",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/sma/movements",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "sma",
                "movements"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/banking/sma/movements",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/banking/sma/movements — auto-discovered (server.ts:38859)"
        },
        {
          "name": "POST /v1/banking/sma/run-classification",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/sma/run-classification",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "sma",
                "run-classification"
              ]
            },
            "description": "M2.4 extends with optional `customer_count` query — caller-controlled\ncohort size for stress-testing the spec acceptance (\"10k accounts <2 min\").\nDefault keeps tenant-scaled deterministic synth.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/banking/sma/run-classification",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/banking/sma/run-classification?framework=&customer_count="
        },
        {
          "name": "GET /v1/banking/sma/sector-view",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/sma/sector-view",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "sma",
                "sector-view"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/banking/sma/sector-view",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/banking/sma/sector-view — auto-discovered (server.ts:38909)"
        },
        {
          "name": "GET /v1/banking/sma/trend",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/banking/sma/trend",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "banking",
                "sma",
                "trend"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/banking/sma/trend",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/banking/sma/trend — auto-discovered (server.ts:38932)"
        }
      ]
    },
    {
      "name": "/v1/config/access-control",
      "description": "4 route(s) under /v1/config/access-control",
      "item": [
        {
          "name": "GET /v1/config/access-control",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/config/access-control",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "config",
                "access-control"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/config/access-control",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/config/access-control — auto-discovered (server.ts:40421)"
        },
        {
          "name": "GET /v1/config/access-control/check",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/config/access-control/check",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "config",
                "access-control",
                "check"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/config/access-control/check",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Runtime \"can this role do X?\" check — fail-closed, same as the HTTP guards."
        },
        {
          "name": "GET /v1/config/access-control/matrix",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/config/access-control/matrix",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "config",
                "access-control",
                "matrix"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/config/access-control/matrix",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/config/access-control/matrix — auto-discovered (server.ts:40432)"
        },
        {
          "name": "GET /v1/config/access-control/roles/{role}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/config/access-control/roles/:role",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "config",
                "access-control",
                "roles",
                ":role"
              ],
              "variable": [
                {
                  "key": "role",
                  "value": "<role>",
                  "description": "Path parameter: role"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/config/access-control/roles/{role}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/config/access-control/roles/:role — auto-discovered (server.ts:40462)"
        }
      ]
    },
    {
      "name": "/v1/config/alert-classification",
      "description": "5 route(s) under /v1/config/alert-classification",
      "item": [
        {
          "name": "GET /v1/config/alert-classification",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/config/alert-classification",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "config",
                "alert-classification"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/config/alert-classification",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/config/alert-classification — auto-discovered (server.ts:40029)"
        },
        {
          "name": "PATCH /v1/config/alert-classification/bands/{band}",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/config/alert-classification/bands/:band",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "config",
                "alert-classification",
                "bands",
                ":band"
              ],
              "variable": [
                {
                  "key": "band",
                  "value": "<band>",
                  "description": "Path parameter: band"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PATCH /v1/config/alert-classification/bands/{band}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PATCH /v1/config/alert-classification/bands/:band — auto-discovered (server.ts:40064)"
        },
        {
          "name": "PUT /v1/config/alert-classification/boundaries",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/config/alert-classification/boundaries",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "config",
                "alert-classification",
                "boundaries"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PUT /v1/config/alert-classification/boundaries",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PUT /v1/config/alert-classification/boundaries — auto-discovered (server.ts:40040)"
        },
        {
          "name": "POST /v1/config/alert-classification/classify",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/config/alert-classification/classify",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "config",
                "alert-classification",
                "classify"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/config/alert-classification/classify",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/config/alert-classification/classify — auto-discovered (server.ts:40088)"
        },
        {
          "name": "POST /v1/config/alert-classification/reset",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/config/alert-classification/reset",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "config",
                "alert-classification",
                "reset"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/config/alert-classification/reset",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/config/alert-classification/reset — auto-discovered (server.ts:40108)"
        }
      ]
    },
    {
      "name": "/v1/config/case-types",
      "description": "6 route(s) under /v1/config/case-types",
      "item": [
        {
          "name": "GET /v1/config/case-types",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/config/case-types",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "config",
                "case-types"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/config/case-types",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/config/case-types — auto-discovered (server.ts:40162)"
        },
        {
          "name": "POST /v1/config/case-types",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/config/case-types",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "config",
                "case-types"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/config/case-types",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/config/case-types — auto-discovered (server.ts:40202)"
        },
        {
          "name": "DELETE /v1/config/case-types/{case_type_id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/config/case-types/:case_type_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "config",
                "case-types",
                ":case_type_id"
              ],
              "variable": [
                {
                  "key": "case_type_id",
                  "value": "<case_type_id>",
                  "description": "Path parameter: case_type_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/config/case-types/{case_type_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DELETE /v1/config/case-types/:case_type_id — auto-discovered (server.ts:40238)"
        },
        {
          "name": "GET /v1/config/case-types/{case_type_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/config/case-types/:case_type_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "config",
                "case-types",
                ":case_type_id"
              ],
              "variable": [
                {
                  "key": "case_type_id",
                  "value": "<case_type_id>",
                  "description": "Path parameter: case_type_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/config/case-types/{case_type_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/config/case-types/:case_type_id — auto-discovered (server.ts:40187)"
        },
        {
          "name": "PATCH /v1/config/case-types/{case_type_id}",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/config/case-types/:case_type_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "config",
                "case-types",
                ":case_type_id"
              ],
              "variable": [
                {
                  "key": "case_type_id",
                  "value": "<case_type_id>",
                  "description": "Path parameter: case_type_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PATCH /v1/config/case-types/{case_type_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PATCH /v1/config/case-types/:case_type_id — auto-discovered (server.ts:40220)"
        },
        {
          "name": "GET /v1/config/case-types/summary",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/config/case-types/summary",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "config",
                "case-types",
                "summary"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/config/case-types/summary",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/config/case-types/summary — auto-discovered (server.ts:40175)"
        }
      ]
    },
    {
      "name": "/v1/config/jobs",
      "description": "7 route(s) under /v1/config/jobs",
      "item": [
        {
          "name": "GET /v1/config/jobs",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/config/jobs",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "config",
                "jobs"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/config/jobs",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/config/jobs — auto-discovered (server.ts:40290)"
        },
        {
          "name": "GET /v1/config/jobs/{job_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/config/jobs/:job_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "config",
                "jobs",
                ":job_id"
              ],
              "variable": [
                {
                  "key": "job_id",
                  "value": "<job_id>",
                  "description": "Path parameter: job_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/config/jobs/{job_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/config/jobs/:job_id — auto-discovered (server.ts:40316)"
        },
        {
          "name": "PATCH /v1/config/jobs/{job_id}",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/config/jobs/:job_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "config",
                "jobs",
                ":job_id"
              ],
              "variable": [
                {
                  "key": "job_id",
                  "value": "<job_id>",
                  "description": "Path parameter: job_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PATCH /v1/config/jobs/{job_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PATCH /v1/config/jobs/:job_id — auto-discovered (server.ts:40331)"
        },
        {
          "name": "POST /v1/config/jobs/{job_id}/run",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/config/jobs/:job_id/run",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "config",
                "jobs",
                ":job_id",
                "run"
              ],
              "variable": [
                {
                  "key": "job_id",
                  "value": "<job_id>",
                  "description": "Path parameter: job_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/config/jobs/{job_id}/run",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/config/jobs/:job_id/run — auto-discovered (server.ts:40358)"
        },
        {
          "name": "GET /v1/config/jobs/{job_id}/run-stats",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/config/jobs/:job_id/run-stats",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "config",
                "jobs",
                ":job_id",
                "run-stats"
              ],
              "variable": [
                {
                  "key": "job_id",
                  "value": "<job_id>",
                  "description": "Path parameter: job_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/config/jobs/{job_id}/run-stats",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/config/jobs/:job_id/run-stats — auto-discovered (server.ts:40394)"
        },
        {
          "name": "GET /v1/config/jobs/{job_id}/runs",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/config/jobs/:job_id/runs",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "config",
                "jobs",
                ":job_id",
                "runs"
              ],
              "variable": [
                {
                  "key": "job_id",
                  "value": "<job_id>",
                  "description": "Path parameter: job_id"
                }
              ]
            },
            "description": "Last_Run_Status column into a drill-down (\"recent runs + success rate\")."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/config/jobs/{job_id}/runs",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Run telemetry — execution log + aggregate stats. Turns the registry's"
        },
        {
          "name": "GET /v1/config/jobs/summary",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/config/jobs/summary",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "config",
                "jobs",
                "summary"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/config/jobs/summary",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/config/jobs/summary — auto-discovered (server.ts:40305)"
        }
      ]
    },
    {
      "name": "/v1/config/risk-score",
      "description": "9 route(s) under /v1/config/risk-score",
      "item": [
        {
          "name": "POST /v1/config/risk-score/evaluate",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/config/risk-score/evaluate",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "config",
                "risk-score",
                "evaluate"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/config/risk-score/evaluate",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/config/risk-score/evaluate — auto-discovered (server.ts:39931)"
        },
        {
          "name": "POST /v1/config/risk-score/evaluate-batch",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/config/risk-score/evaluate-batch",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "config",
                "risk-score",
                "evaluate-batch"
              ]
            },
            "description": "RAG bands, returning per-row bands + a distribution rollup.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/config/risk-score/evaluate-batch",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Batch — score N profiles in one call against the configured weights +"
        },
        {
          "name": "GET /v1/config/risk-score/factors",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/config/risk-score/factors",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "config",
                "risk-score",
                "factors"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/config/risk-score/factors",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/config/risk-score/factors — auto-discovered (server.ts:39805)"
        },
        {
          "name": "POST /v1/config/risk-score/factors",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/config/risk-score/factors",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "config",
                "risk-score",
                "factors"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/config/risk-score/factors",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/config/risk-score/factors — auto-discovered (server.ts:39830)"
        },
        {
          "name": "DELETE /v1/config/risk-score/factors/{factor_id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/config/risk-score/factors/:factor_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "config",
                "risk-score",
                "factors",
                ":factor_id"
              ],
              "variable": [
                {
                  "key": "factor_id",
                  "value": "<factor_id>",
                  "description": "Path parameter: factor_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/config/risk-score/factors/{factor_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DELETE /v1/config/risk-score/factors/:factor_id — auto-discovered (server.ts:39866)"
        },
        {
          "name": "PATCH /v1/config/risk-score/factors/{factor_id}",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/config/risk-score/factors/:factor_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "config",
                "risk-score",
                "factors",
                ":factor_id"
              ],
              "variable": [
                {
                  "key": "factor_id",
                  "value": "<factor_id>",
                  "description": "Path parameter: factor_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PATCH /v1/config/risk-score/factors/{factor_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PATCH /v1/config/risk-score/factors/:factor_id — auto-discovered (server.ts:39848)"
        },
        {
          "name": "POST /v1/config/risk-score/normalize",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/config/risk-score/normalize",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "config",
                "risk-score",
                "normalize"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/config/risk-score/normalize",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/config/risk-score/normalize — auto-discovered (server.ts:39906)"
        },
        {
          "name": "POST /v1/config/risk-score/reorder",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/config/risk-score/reorder",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "config",
                "risk-score",
                "reorder"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/config/risk-score/reorder",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/config/risk-score/reorder — auto-discovered (server.ts:39883)"
        },
        {
          "name": "GET /v1/config/risk-score/summary",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/config/risk-score/summary",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "config",
                "risk-score",
                "summary"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/config/risk-score/summary",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/config/risk-score/summary — auto-discovered (server.ts:39817)"
        }
      ]
    },
    {
      "name": "/v1/dashboard/sla-breach-matrix",
      "description": "2 route(s) under /v1/dashboard/sla-breach-matrix",
      "item": [
        {
          "name": "GET /v1/dashboard/sla-breach-matrix",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dashboard/sla-breach-matrix",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dashboard",
                "sla-breach-matrix"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/dashboard/sla-breach-matrix",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/dashboard/sla-breach-matrix — auto-discovered (server.ts:2848)"
        },
        {
          "name": "POST /v1/dashboard/sla-breach-matrix/preview",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dashboard/sla-breach-matrix/preview",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dashboard",
                "sla-breach-matrix",
                "preview"
              ]
            },
            "description": "a hypothetical sla_config patch BEFORE the admin saves. Returns\nthe current matrix, the patched matrix, and a per-bucket delta.\nUsed by the SlaConfigEditModal to surface \"this change will\nmove N cases\" before commit.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/dashboard/sla-breach-matrix/preview",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/dashboard/sla-breach-matrix/preview — show the impact of"
        }
      ]
    },
    {
      "name": "/v1/dbac/me",
      "description": "1 route(s) under /v1/dbac/me",
      "item": [
        {
          "name": "GET /v1/dbac/me",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dbac/me",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dbac",
                "me"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/dbac/me",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/dbac/me — auto-discovered (server.ts:38295)"
        }
      ]
    },
    {
      "name": "/v1/dq/by-attribute",
      "description": "1 route(s) under /v1/dq/by-attribute",
      "item": [
        {
          "name": "GET /v1/dq/by-attribute",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dq/by-attribute",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dq",
                "by-attribute"
              ]
            },
            "description": "to a single column. Returns 404 on bad source. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/dq/by-attribute",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Per-attribute composite scores. Optional `attribute` filter narrows"
        }
      ]
    },
    {
      "name": "/v1/dq/by-source",
      "description": "1 route(s) under /v1/dq/by-source",
      "item": [
        {
          "name": "GET /v1/dq/by-source/{source_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dq/by-source/:source_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dq",
                "by-source",
                ":source_id"
              ],
              "variable": [
                {
                  "key": "source_id",
                  "value": "<source_id>",
                  "description": "Path parameter: source_id"
                }
              ]
            },
            "description": "Returns 404 EWS_404_unknown_source on bad source. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/dq/by-source/{source_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Composite score for a single source + 30-day (configurable) trend."
        }
      ]
    },
    {
      "name": "/v1/dq/dashboard",
      "description": "1 route(s) under /v1/dq/dashboard",
      "item": [
        {
          "name": "GET /v1/dq/dashboard",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dq/dashboard",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dq",
                "dashboard"
              ]
            },
            "description": "with the 5-dimension composite per source, plus a fleet-wide score.\nWeights are read from M13.1 admin config under the key\n`scoring.dq.dimension_weights`. Backward-compat: existing\nrules_status / by_severity / by_kind shape is unchanged. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/dq/dashboard",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Module 1.7 additive: response now carries a `score_overlay` field"
        }
      ]
    },
    {
      "name": "/v1/dq/executions",
      "description": "2 route(s) under /v1/dq/executions",
      "item": [
        {
          "name": "GET /v1/dq/executions",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dq/executions",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dq",
                "executions"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/dq/executions",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/dq/executions — auto-discovered (server.ts:27977)"
        },
        {
          "name": "GET /v1/dq/executions/{execution_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dq/executions/:execution_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dq",
                "executions",
                ":execution_id"
              ],
              "variable": [
                {
                  "key": "execution_id",
                  "value": "<execution_id>",
                  "description": "Path parameter: execution_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/dq/executions/{execution_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/dq/executions/:execution_id — auto-discovered (server.ts:28014)"
        }
      ]
    },
    {
      "name": "/v1/dq/profile",
      "description": "6 route(s) under /v1/dq/profile",
      "item": [
        {
          "name": "GET /v1/dq/profile/{source_id}/column/{col}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dq/profile/:source_id/column/:col",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dq",
                "profile",
                ":source_id",
                "column",
                ":col"
              ],
              "variable": [
                {
                  "key": "source_id",
                  "value": "<source_id>",
                  "description": "Path parameter: source_id"
                },
                {
                  "key": "col",
                  "value": "<col>",
                  "description": "Path parameter: col"
                }
              ]
            },
            "description": "from `/columns/:column/distribution` (which returns the histogram).\nReturns the same ColumnProfile shape as one row of `/columns`. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/dq/profile/{source_id}/column/{col}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Single-column detail accessor per Module 1.2 spec. Distinct path"
        },
        {
          "name": "GET /v1/dq/profile/{source_id}/columns",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dq/profile/:source_id/columns",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dq",
                "profile",
                ":source_id",
                "columns"
              ],
              "variable": [
                {
                  "key": "source_id",
                  "value": "<source_id>",
                  "description": "Path parameter: source_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/dq/profile/{source_id}/columns",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Module #7 — Data Profiling AI (§2.1.7)"
        },
        {
          "name": "GET /v1/dq/profile/{source_id}/columns/{column}/distribution",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dq/profile/:source_id/columns/:column/distribution",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dq",
                "profile",
                ":source_id",
                "columns",
                ":column",
                "distribution"
              ],
              "variable": [
                {
                  "key": "source_id",
                  "value": "<source_id>",
                  "description": "Path parameter: source_id"
                },
                {
                  "key": "column",
                  "value": "<column>",
                  "description": "Path parameter: column"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/dq/profile/{source_id}/columns/{column}/distribution",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/dq/profile/:source_id/columns/:column/distribution — auto-discovered (server.ts:41040)"
        },
        {
          "name": "POST /v1/dq/profile/{source_id}/rules/{rule_id}/promote",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dq/profile/:source_id/rules/:rule_id/promote",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dq",
                "profile",
                ":source_id",
                "rules",
                ":rule_id",
                "promote"
              ],
              "variable": [
                {
                  "key": "source_id",
                  "value": "<source_id>",
                  "description": "Path parameter: source_id"
                },
                {
                  "key": "rule_id",
                  "value": "{{rule_id}}",
                  "description": "Path parameter: rule_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/dq/profile/{source_id}/rules/{rule_id}/promote",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/dq/profile/:source_id/rules/:rule_id/promote — auto-discovered (server.ts:41125)"
        },
        {
          "name": "POST /v1/dq/profile/{source_id}/suggest-rules",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dq/profile/:source_id/suggest-rules",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dq",
                "profile",
                ":source_id",
                "suggest-rules"
              ],
              "variable": [
                {
                  "key": "source_id",
                  "value": "<source_id>",
                  "description": "Path parameter: source_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/dq/profile/{source_id}/suggest-rules",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/dq/profile/:source_id/suggest-rules — auto-discovered (server.ts:41066)"
        },
        {
          "name": "POST /v1/dq/profile/promote-rule",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dq/profile/promote-rule",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dq",
                "profile",
                "promote-rule"
              ]
            },
            "description": "per-source variant; rule_id is supplied in the body. */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/dq/profile/promote-rule",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Tenant-level alias per the Module 1.2 spec. Same handler as the"
        }
      ]
    },
    {
      "name": "/v1/dq/rule-kinds",
      "description": "1 route(s) under /v1/dq/rule-kinds",
      "item": [
        {
          "name": "GET /v1/dq/rule-kinds",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dq/rule-kinds",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dq",
                "rule-kinds"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/dq/rule-kinds",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/dq/rule-kinds — auto-discovered (server.ts:27655)"
        }
      ]
    },
    {
      "name": "/v1/dq/rules",
      "description": "6 route(s) under /v1/dq/rules",
      "item": [
        {
          "name": "GET /v1/dq/rules",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dq/rules",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dq",
                "rules"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/dq/rules",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/dq/rules — auto-discovered (server.ts:27674)"
        },
        {
          "name": "POST /v1/dq/rules",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dq/rules",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dq",
                "rules"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/dq/rules",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/dq/rules — auto-discovered (server.ts:27740)"
        },
        {
          "name": "DELETE /v1/dq/rules/{rule_id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dq/rules/:rule_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dq",
                "rules",
                ":rule_id"
              ],
              "variable": [
                {
                  "key": "rule_id",
                  "value": "{{rule_id}}",
                  "description": "Path parameter: rule_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/dq/rules/{rule_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DELETE /v1/dq/rules/:rule_id — auto-discovered (server.ts:27849)"
        },
        {
          "name": "GET /v1/dq/rules/{rule_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dq/rules/:rule_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dq",
                "rules",
                ":rule_id"
              ],
              "variable": [
                {
                  "key": "rule_id",
                  "value": "{{rule_id}}",
                  "description": "Path parameter: rule_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/dq/rules/{rule_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/dq/rules/:rule_id — auto-discovered (server.ts:27720)"
        },
        {
          "name": "PATCH /v1/dq/rules/{rule_id}",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dq/rules/:rule_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dq",
                "rules",
                ":rule_id"
              ],
              "variable": [
                {
                  "key": "rule_id",
                  "value": "{{rule_id}}",
                  "description": "Path parameter: rule_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PATCH /v1/dq/rules/{rule_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PATCH /v1/dq/rules/:rule_id — auto-discovered (server.ts:27799)"
        },
        {
          "name": "POST /v1/dq/rules/{rule_id}/run",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dq/rules/:rule_id/run",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dq",
                "rules",
                ":rule_id",
                "run"
              ],
              "variable": [
                {
                  "key": "rule_id",
                  "value": "{{rule_id}}",
                  "description": "Path parameter: rule_id"
                }
              ]
            },
            "description": "Body: { records: any[], id_field?: string }\ntriggered_by is read from X-APEX-USER. */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/dq/rules/{rule_id}/run",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "record set. Returns the recorded DqExecution."
        }
      ]
    },
    {
      "name": "/v1/dq/standardisation",
      "description": "11 route(s) under /v1/dq/standardisation",
      "item": [
        {
          "name": "GET /v1/dq/standardisation/dictionaries",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dq/standardisation/dictionaries",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dq",
                "standardisation",
                "dictionaries"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/dq/standardisation/dictionaries",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/dq/standardisation/dictionaries — auto-discovered (server.ts:41746)"
        },
        {
          "name": "POST /v1/dq/standardisation/dictionaries",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dq/standardisation/dictionaries",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dq",
                "standardisation",
                "dictionaries"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/dq/standardisation/dictionaries",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/dq/standardisation/dictionaries — auto-discovered (server.ts:41757)"
        },
        {
          "name": "GET /v1/dq/standardisation/dictionaries/{dictionary_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dq/standardisation/dictionaries/:dictionary_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dq",
                "standardisation",
                "dictionaries",
                ":dictionary_id"
              ],
              "variable": [
                {
                  "key": "dictionary_id",
                  "value": "<dictionary_id>",
                  "description": "Path parameter: dictionary_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/dq/standardisation/dictionaries/{dictionary_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/dq/standardisation/dictionaries/:dictionary_id — auto-discovered (server.ts:41787)"
        },
        {
          "name": "POST /v1/dq/standardisation/dictionaries/{dictionary_id}/entries",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dq/standardisation/dictionaries/:dictionary_id/entries",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dq",
                "standardisation",
                "dictionaries",
                ":dictionary_id",
                "entries"
              ],
              "variable": [
                {
                  "key": "dictionary_id",
                  "value": "<dictionary_id>",
                  "description": "Path parameter: dictionary_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/dq/standardisation/dictionaries/{dictionary_id}/entries",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/dq/standardisation/dictionaries/:dictionary_id/entries — auto-discovered (server.ts:41801)"
        },
        {
          "name": "DELETE /v1/dq/standardisation/dictionaries/{dictionary_id}/entries/{from}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dq/standardisation/dictionaries/:dictionary_id/entries/:from",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dq",
                "standardisation",
                "dictionaries",
                ":dictionary_id",
                "entries",
                ":from"
              ],
              "variable": [
                {
                  "key": "dictionary_id",
                  "value": "<dictionary_id>",
                  "description": "Path parameter: dictionary_id"
                },
                {
                  "key": "from",
                  "value": "<from>",
                  "description": "Path parameter: from"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/dq/standardisation/dictionaries/{dictionary_id}/entries/{from}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DELETE /v1/dq/standardisation/dictionaries/:dictionary_id/entries/:from — auto-discovered (server.ts"
        },
        {
          "name": "GET /v1/dq/standardisation/pipelines",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dq/standardisation/pipelines",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dq",
                "standardisation",
                "pipelines"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/dq/standardisation/pipelines",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Module #16 — DQ Standardisation pipelines + dictionaries (§2.3)"
        },
        {
          "name": "POST /v1/dq/standardisation/pipelines",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dq/standardisation/pipelines",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dq",
                "standardisation",
                "pipelines"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/dq/standardisation/pipelines",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/dq/standardisation/pipelines — auto-discovered (server.ts:41629)"
        },
        {
          "name": "DELETE /v1/dq/standardisation/pipelines/{pipeline_id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dq/standardisation/pipelines/:pipeline_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dq",
                "standardisation",
                "pipelines",
                ":pipeline_id"
              ],
              "variable": [
                {
                  "key": "pipeline_id",
                  "value": "<pipeline_id>",
                  "description": "Path parameter: pipeline_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/dq/standardisation/pipelines/{pipeline_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DELETE /v1/dq/standardisation/pipelines/:pipeline_id — auto-discovered (server.ts:41705)"
        },
        {
          "name": "GET /v1/dq/standardisation/pipelines/{pipeline_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dq/standardisation/pipelines/:pipeline_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dq",
                "standardisation",
                "pipelines",
                ":pipeline_id"
              ],
              "variable": [
                {
                  "key": "pipeline_id",
                  "value": "<pipeline_id>",
                  "description": "Path parameter: pipeline_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/dq/standardisation/pipelines/{pipeline_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/dq/standardisation/pipelines/:pipeline_id — auto-discovered (server.ts:41663)"
        },
        {
          "name": "PATCH /v1/dq/standardisation/pipelines/{pipeline_id}",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dq/standardisation/pipelines/:pipeline_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dq",
                "standardisation",
                "pipelines",
                ":pipeline_id"
              ],
              "variable": [
                {
                  "key": "pipeline_id",
                  "value": "<pipeline_id>",
                  "description": "Path parameter: pipeline_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PATCH /v1/dq/standardisation/pipelines/{pipeline_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PATCH /v1/dq/standardisation/pipelines/:pipeline_id — auto-discovered (server.ts:41677)"
        },
        {
          "name": "POST /v1/dq/standardisation/pipelines/{pipeline_id}/run",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dq/standardisation/pipelines/:pipeline_id/run",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dq",
                "standardisation",
                "pipelines",
                ":pipeline_id",
                "run"
              ],
              "variable": [
                {
                  "key": "pipeline_id",
                  "value": "<pipeline_id>",
                  "description": "Path parameter: pipeline_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/dq/standardisation/pipelines/{pipeline_id}/run",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/dq/standardisation/pipelines/:pipeline_id/run — auto-discovered (server.ts:41719)"
        }
      ]
    },
    {
      "name": "/v1/dr/game-days",
      "description": "5 route(s) under /v1/dr/game-days",
      "item": [
        {
          "name": "GET /v1/dr/game-days",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dr/game-days",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dr",
                "game-days"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/dr/game-days",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/dr/game-days — auto-discovered (server.ts:22558)"
        },
        {
          "name": "POST /v1/dr/game-days",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dr/game-days",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dr",
                "game-days"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/dr/game-days",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/dr/game-days — auto-discovered (server.ts:22607)"
        },
        {
          "name": "DELETE /v1/dr/game-days/{record_id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dr/game-days/:record_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dr",
                "game-days",
                ":record_id"
              ],
              "variable": [
                {
                  "key": "record_id",
                  "value": "<record_id>",
                  "description": "Path parameter: record_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/dr/game-days/{record_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DELETE /v1/dr/game-days/:record_id — auto-discovered (server.ts:22717)"
        },
        {
          "name": "GET /v1/dr/game-days/{record_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dr/game-days/:record_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dr",
                "game-days",
                ":record_id"
              ],
              "variable": [
                {
                  "key": "record_id",
                  "value": "<record_id>",
                  "description": "Path parameter: record_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/dr/game-days/{record_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/dr/game-days/:record_id — auto-discovered (server.ts:22582)"
        },
        {
          "name": "PATCH /v1/dr/game-days/{record_id}",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dr/game-days/:record_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dr",
                "game-days",
                ":record_id"
              ],
              "variable": [
                {
                  "key": "record_id",
                  "value": "<record_id>",
                  "description": "Path parameter: record_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PATCH /v1/dr/game-days/{record_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PATCH /v1/dr/game-days/:record_id — auto-discovered (server.ts:22666)"
        }
      ]
    },
    {
      "name": "/v1/dr/runbook",
      "description": "1 route(s) under /v1/dr/runbook",
      "item": [
        {
          "name": "GET /v1/dr/runbook",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/dr/runbook",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "dr",
                "runbook"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/dr/runbook",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/dr/runbook — auto-discovered (server.ts:22532)"
        }
      ]
    },
    {
      "name": "/v1/export/pdf",
      "description": "1 route(s) under /v1/export/pdf",
      "item": [
        {
          "name": "POST /v1/export/pdf",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/export/pdf",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "export",
                "pdf"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/export/pdf",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/export/pdf — auto-discovered (server.ts:43431)"
        }
      ]
    },
    {
      "name": "/v1/export/xlsx",
      "description": "1 route(s) under /v1/export/xlsx",
      "item": [
        {
          "name": "POST /v1/export/xlsx",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/export/xlsx",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "export",
                "xlsx"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/export/xlsx",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Module #24 — Generic Excel + PDF export (§2.4)"
        }
      ]
    },
    {
      "name": "/v1/finops/dashboard",
      "description": "1 route(s) under /v1/finops/dashboard",
      "item": [
        {
          "name": "GET /v1/finops/dashboard",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/finops/dashboard",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "finops",
                "dashboard"
              ]
            },
            "description": "tenant. Deterministic per (tenant, month). 10-service breakdown\nin canonical ALL_FINOPS_SERVICES order with trend (up/flat/down\nvs prior month at ±5% bands) + delta_pct. optimisation_candidate\nhighlights highest-cost service with up-trend + |delta_pct| ≥\n5%. Production swap = AWS Cost Explorer query satisfying the\nsame shape; resolver interface stays stable. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/finops/dashboard",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "+ cost-per-alert + cost-per-customer efficiency metrics per"
        }
      ]
    },
    {
      "name": "/v1/fraud/cases",
      "description": "6 route(s) under /v1/fraud/cases",
      "item": [
        {
          "name": "GET /v1/fraud/cases",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/fraud/cases",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "fraud",
                "cases"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/fraud/cases",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Module #18 — Banking Fraud full surface (§2.3)"
        },
        {
          "name": "POST /v1/fraud/cases",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/fraud/cases",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "fraud",
                "cases"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/fraud/cases",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/fraud/cases — auto-discovered (server.ts:41889)"
        },
        {
          "name": "GET /v1/fraud/cases/{case_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/fraud/cases/:case_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "fraud",
                "cases",
                ":case_id"
              ],
              "variable": [
                {
                  "key": "case_id",
                  "value": "{{case_id}}",
                  "description": "Path parameter: case_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/fraud/cases/{case_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/fraud/cases/:case_id — auto-discovered (server.ts:41925)"
        },
        {
          "name": "PATCH /v1/fraud/cases/{case_id}",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/fraud/cases/:case_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "fraud",
                "cases",
                ":case_id"
              ],
              "variable": [
                {
                  "key": "case_id",
                  "value": "{{case_id}}",
                  "description": "Path parameter: case_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PATCH /v1/fraud/cases/{case_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PATCH /v1/fraud/cases/:case_id — auto-discovered (server.ts:41939)"
        },
        {
          "name": "POST /v1/fraud/cases/{case_id}/sar",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/fraud/cases/:case_id/sar",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "fraud",
                "cases",
                ":case_id",
                "sar"
              ],
              "variable": [
                {
                  "key": "case_id",
                  "value": "{{case_id}}",
                  "description": "Path parameter: case_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/fraud/cases/{case_id}/sar",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/fraud/cases/:case_id/sar — auto-discovered (server.ts:42055)"
        },
        {
          "name": "POST /v1/fraud/cases/{case_id}/vigilance",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/fraud/cases/:case_id/vigilance",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "fraud",
                "cases",
                ":case_id",
                "vigilance"
              ],
              "variable": [
                {
                  "key": "case_id",
                  "value": "{{case_id}}",
                  "description": "Path parameter: case_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/fraud/cases/{case_id}/vigilance",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/fraud/cases/:case_id/vigilance — auto-discovered (server.ts:42109)"
        }
      ]
    },
    {
      "name": "/v1/fraud/dashboard",
      "description": "1 route(s) under /v1/fraud/dashboard",
      "item": [
        {
          "name": "GET /v1/fraud/dashboard",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/fraud/dashboard",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "fraud",
                "dashboard"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/fraud/dashboard",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/fraud/dashboard — auto-discovered (server.ts:27270)"
        }
      ]
    },
    {
      "name": "/v1/fraud/rules",
      "description": "4 route(s) under /v1/fraud/rules",
      "item": [
        {
          "name": "GET /v1/fraud/rules",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/fraud/rules",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "fraud",
                "rules"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/fraud/rules",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/fraud/rules — auto-discovered (server.ts:41969)"
        },
        {
          "name": "POST /v1/fraud/rules",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/fraud/rules",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "fraud",
                "rules"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/fraud/rules",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/fraud/rules — auto-discovered (server.ts:41981)"
        },
        {
          "name": "DELETE /v1/fraud/rules/{rule_id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/fraud/rules/:rule_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "fraud",
                "rules",
                ":rule_id"
              ],
              "variable": [
                {
                  "key": "rule_id",
                  "value": "{{rule_id}}",
                  "description": "Path parameter: rule_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/fraud/rules/{rule_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DELETE /v1/fraud/rules/:rule_id — auto-discovered (server.ts:42041)"
        },
        {
          "name": "PATCH /v1/fraud/rules/{rule_id}",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/fraud/rules/:rule_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "fraud",
                "rules",
                ":rule_id"
              ],
              "variable": [
                {
                  "key": "rule_id",
                  "value": "{{rule_id}}",
                  "description": "Path parameter: rule_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PATCH /v1/fraud/rules/{rule_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PATCH /v1/fraud/rules/:rule_id — auto-discovered (server.ts:42015)"
        }
      ]
    },
    {
      "name": "/v1/glossary/categories",
      "description": "1 route(s) under /v1/glossary/categories",
      "item": [
        {
          "name": "GET /v1/glossary/categories",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/glossary/categories",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "glossary",
                "categories"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/glossary/categories",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/glossary/categories — auto-discovered (server.ts:43145)"
        }
      ]
    },
    {
      "name": "/v1/glossary/terms",
      "description": "5 route(s) under /v1/glossary/terms",
      "item": [
        {
          "name": "GET /v1/glossary/terms",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/glossary/terms",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "glossary",
                "terms"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/glossary/terms",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Module #22 — Glossary (§2.4)"
        },
        {
          "name": "POST /v1/glossary/terms",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/glossary/terms",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "glossary",
                "terms"
              ]
            },
            "description": "stays immutable). Every mutation writes to the audit trail per\ncross-cutting #6.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/glossary/terms",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "M6.4 — Glossary admin CRUD (per-tenant overlay; platform seed"
        },
        {
          "name": "DELETE /v1/glossary/terms/{term_id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/glossary/terms/:term_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "glossary",
                "terms",
                ":term_id"
              ],
              "variable": [
                {
                  "key": "term_id",
                  "value": "<term_id>",
                  "description": "Path parameter: term_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/glossary/terms/{term_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DELETE /v1/glossary/terms/:term_id — auto-discovered (server.ts:43236)"
        },
        {
          "name": "GET /v1/glossary/terms/{term_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/glossary/terms/:term_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "glossary",
                "terms",
                ":term_id"
              ],
              "variable": [
                {
                  "key": "term_id",
                  "value": "<term_id>",
                  "description": "Path parameter: term_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/glossary/terms/{term_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/glossary/terms/:term_id — auto-discovered (server.ts:43151)"
        },
        {
          "name": "PUT /v1/glossary/terms/{term_id}",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/glossary/terms/:term_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "glossary",
                "terms",
                ":term_id"
              ],
              "variable": [
                {
                  "key": "term_id",
                  "value": "<term_id>",
                  "description": "Path parameter: term_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PUT /v1/glossary/terms/{term_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PUT /v1/glossary/terms/:term_id — auto-discovered (server.ts:43202)"
        }
      ]
    },
    {
      "name": "/v1/governance/branches",
      "description": "5 route(s) under /v1/governance/branches",
      "item": [
        {
          "name": "GET /v1/governance/branches",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/governance/branches",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "governance",
                "branches"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/governance/branches",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/governance/branches — auto-discovered (server.ts:38349)"
        },
        {
          "name": "POST /v1/governance/branches",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/governance/branches",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "governance",
                "branches"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/governance/branches",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/governance/branches — auto-discovered (server.ts:38378)"
        },
        {
          "name": "DELETE /v1/governance/branches/{branch_id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/governance/branches/:branch_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "governance",
                "branches",
                ":branch_id"
              ],
              "variable": [
                {
                  "key": "branch_id",
                  "value": "<branch_id>",
                  "description": "Path parameter: branch_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/governance/branches/{branch_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DELETE /v1/governance/branches/:branch_id — auto-discovered (server.ts:38421)"
        },
        {
          "name": "GET /v1/governance/branches/{branch_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/governance/branches/:branch_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "governance",
                "branches",
                ":branch_id"
              ],
              "variable": [
                {
                  "key": "branch_id",
                  "value": "<branch_id>",
                  "description": "Path parameter: branch_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/governance/branches/{branch_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/governance/branches/:branch_id — auto-discovered (server.ts:38363)"
        },
        {
          "name": "PATCH /v1/governance/branches/{branch_id}",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/governance/branches/:branch_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "governance",
                "branches",
                ":branch_id"
              ],
              "variable": [
                {
                  "key": "branch_id",
                  "value": "<branch_id>",
                  "description": "Path parameter: branch_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PATCH /v1/governance/branches/{branch_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PATCH /v1/governance/branches/:branch_id — auto-discovered (server.ts:38397)"
        }
      ]
    },
    {
      "name": "/v1/governance/compliance-rules",
      "description": "5 route(s) under /v1/governance/compliance-rules",
      "item": [
        {
          "name": "GET /v1/governance/compliance-rules",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/governance/compliance-rules",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "governance",
                "compliance-rules"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/governance/compliance-rules",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/governance/compliance-rules — auto-discovered (server.ts:38436)"
        },
        {
          "name": "POST /v1/governance/compliance-rules",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/governance/compliance-rules",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "governance",
                "compliance-rules"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/governance/compliance-rules",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/governance/compliance-rules — auto-discovered (server.ts:38476)"
        },
        {
          "name": "DELETE /v1/governance/compliance-rules/{rule_id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/governance/compliance-rules/:rule_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "governance",
                "compliance-rules",
                ":rule_id"
              ],
              "variable": [
                {
                  "key": "rule_id",
                  "value": "{{rule_id}}",
                  "description": "Path parameter: rule_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/governance/compliance-rules/{rule_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DELETE /v1/governance/compliance-rules/:rule_id — auto-discovered (server.ts:38514)"
        },
        {
          "name": "GET /v1/governance/compliance-rules/{rule_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/governance/compliance-rules/:rule_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "governance",
                "compliance-rules",
                ":rule_id"
              ],
              "variable": [
                {
                  "key": "rule_id",
                  "value": "{{rule_id}}",
                  "description": "Path parameter: rule_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/governance/compliance-rules/{rule_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/governance/compliance-rules/:rule_id — auto-discovered (server.ts:38461)"
        },
        {
          "name": "PATCH /v1/governance/compliance-rules/{rule_id}",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/governance/compliance-rules/:rule_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "governance",
                "compliance-rules",
                ":rule_id"
              ],
              "variable": [
                {
                  "key": "rule_id",
                  "value": "{{rule_id}}",
                  "description": "Path parameter: rule_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PATCH /v1/governance/compliance-rules/{rule_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PATCH /v1/governance/compliance-rules/:rule_id — auto-discovered (server.ts:38495)"
        }
      ]
    },
    {
      "name": "/v1/governance/me",
      "description": "1 route(s) under /v1/governance/me",
      "item": [
        {
          "name": "GET /v1/governance/me",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/governance/me",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "governance",
                "me"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/governance/me",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/governance/me — auto-discovered (server.ts:38327)"
        }
      ]
    },
    {
      "name": "/v1/ifrs9/ecl",
      "description": "1 route(s) under /v1/ifrs9/ecl",
      "item": [
        {
          "name": "POST /v1/ifrs9/ecl/compute",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ifrs9/ecl/compute",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ifrs9",
                "ecl",
                "compute"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/ifrs9/ecl/compute",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/ifrs9/ecl/compute — auto-discovered (server.ts:23434)"
        }
      ]
    },
    {
      "name": "/v1/ifrs9/ecl-overrides",
      "description": "6 route(s) under /v1/ifrs9/ecl-overrides",
      "item": [
        {
          "name": "GET /v1/ifrs9/ecl-overrides",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ifrs9/ecl-overrides",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ifrs9",
                "ecl-overrides"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ifrs9/ecl-overrides",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/ifrs9/ecl-overrides — auto-discovered (server.ts:23717)"
        },
        {
          "name": "POST /v1/ifrs9/ecl-overrides",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ifrs9/ecl-overrides",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ifrs9",
                "ecl-overrides"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/ifrs9/ecl-overrides",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/ifrs9/ecl-overrides — auto-discovered (server.ts:23804)"
        },
        {
          "name": "DELETE /v1/ifrs9/ecl-overrides/{override_id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ifrs9/ecl-overrides/:override_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ifrs9",
                "ecl-overrides",
                ":override_id"
              ],
              "variable": [
                {
                  "key": "override_id",
                  "value": "<override_id>",
                  "description": "Path parameter: override_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/ifrs9/ecl-overrides/{override_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DELETE /v1/ifrs9/ecl-overrides/:override_id — auto-discovered (server.ts:23922)"
        },
        {
          "name": "GET /v1/ifrs9/ecl-overrides/{override_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ifrs9/ecl-overrides/:override_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ifrs9",
                "ecl-overrides",
                ":override_id"
              ],
              "variable": [
                {
                  "key": "override_id",
                  "value": "<override_id>",
                  "description": "Path parameter: override_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ifrs9/ecl-overrides/{override_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/ifrs9/ecl-overrides/:override_id — auto-discovered (server.ts:23783)"
        },
        {
          "name": "PATCH /v1/ifrs9/ecl-overrides/{override_id}",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ifrs9/ecl-overrides/:override_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ifrs9",
                "ecl-overrides",
                ":override_id"
              ],
              "variable": [
                {
                  "key": "override_id",
                  "value": "<override_id>",
                  "description": "Path parameter: override_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PATCH /v1/ifrs9/ecl-overrides/{override_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PATCH /v1/ifrs9/ecl-overrides/:override_id — auto-discovered (server.ts:23863)"
        },
        {
          "name": "GET /v1/ifrs9/ecl-overrides/active/{customer_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ifrs9/ecl-overrides/active/:customer_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ifrs9",
                "ecl-overrides",
                "active",
                ":customer_id"
              ],
              "variable": [
                {
                  "key": "customer_id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: customer_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ifrs9/ecl-overrides/active/{customer_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/ifrs9/ecl-overrides/active/:customer_id — auto-discovered (server.ts:23761)"
        }
      ]
    },
    {
      "name": "/v1/ifrs9/enums",
      "description": "1 route(s) under /v1/ifrs9/enums",
      "item": [
        {
          "name": "GET /v1/ifrs9/enums",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ifrs9/enums",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ifrs9",
                "enums"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ifrs9/enums",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/ifrs9/enums — auto-discovered (server.ts:23415)"
        }
      ]
    },
    {
      "name": "/v1/ifrs9/movements",
      "description": "5 route(s) under /v1/ifrs9/movements",
      "item": [
        {
          "name": "GET /v1/ifrs9/movements",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ifrs9/movements",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ifrs9",
                "movements"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ifrs9/movements",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/ifrs9/movements — auto-discovered (server.ts:23500)"
        },
        {
          "name": "POST /v1/ifrs9/movements",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ifrs9/movements",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ifrs9",
                "movements"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/ifrs9/movements",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/ifrs9/movements — auto-discovered (server.ts:23597)"
        },
        {
          "name": "DELETE /v1/ifrs9/movements/{movement_id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ifrs9/movements/:movement_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ifrs9",
                "movements",
                ":movement_id"
              ],
              "variable": [
                {
                  "key": "movement_id",
                  "value": "<movement_id>",
                  "description": "Path parameter: movement_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/ifrs9/movements/{movement_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DELETE /v1/ifrs9/movements/:movement_id — auto-discovered (server.ts:23656)"
        },
        {
          "name": "GET /v1/ifrs9/movements/{movement_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ifrs9/movements/:movement_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ifrs9",
                "movements",
                ":movement_id"
              ],
              "variable": [
                {
                  "key": "movement_id",
                  "value": "<movement_id>",
                  "description": "Path parameter: movement_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ifrs9/movements/{movement_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/ifrs9/movements/:movement_id — auto-discovered (server.ts:23576)"
        },
        {
          "name": "GET /v1/ifrs9/movements/current/{customer_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ifrs9/movements/current/:customer_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ifrs9",
                "movements",
                "current",
                ":customer_id"
              ],
              "variable": [
                {
                  "key": "customer_id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: customer_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ifrs9/movements/current/{customer_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/ifrs9/movements/current/:customer_id — auto-discovered (server.ts:23551)"
        }
      ]
    },
    {
      "name": "/v1/ifrs9/portfolio",
      "description": "1 route(s) under /v1/ifrs9/portfolio",
      "item": [
        {
          "name": "GET /v1/ifrs9/portfolio",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ifrs9/portfolio",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ifrs9",
                "portfolio"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ifrs9/portfolio",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/ifrs9/portfolio — auto-discovered (server.ts:23479)"
        }
      ]
    },
    {
      "name": "/v1/ingestion/adapters",
      "description": "9 route(s) under /v1/ingestion/adapters",
      "item": [
        {
          "name": "DELETE /v1/ingestion/adapters/sla-breaches",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/adapters/sla-breaches",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "adapters",
                "sla-breaches"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/ingestion/adapters/sla-breaches",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "tenant's audit history. Returns { cleared: N }. */"
        },
        {
          "name": "GET /v1/ingestion/adapters/sla-breaches",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/adapters/sla-breaches",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "adapters",
                "sla-breaches"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ingestion/adapters/sla-breaches",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "— list recorded breach events newest-first. */"
        },
        {
          "name": "POST /v1/ingestion/adapters/sla-breaches/{event_id}/acknowledge",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/adapters/sla-breaches/:event_id/acknowledge",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "adapters",
                "sla-breaches",
                ":event_id",
                "acknowledge"
              ],
              "variable": [
                {
                  "key": "event_id",
                  "value": "<event_id>",
                  "description": "Path parameter: event_id"
                }
              ]
            },
            "description": "alerting can dedup repeat pages on the same incident. Body\n`{ note?: string }` (optional 0-500 char free text). Idempotent\n— re-ack on an already-acknowledged event returns 200 with\n`already: true` and leaves the original ack metadata intact so\nthe audit trail stays honest about who acked first. */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/ingestion/adapters/sla-breaches/{event_id}/acknowledge",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "— operator acknowledges a recorded breach event so downstream"
        },
        {
          "name": "GET /v1/ingestion/adapters/sla-breaches/analytics",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/adapters/sla-breaches/analytics",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "adapters",
                "sla-breaches",
                "analytics"
              ]
            },
            "description": "breach event store: sample size, distinct connectors, ack split,\nack_rate, by_reason (every key present), by_day (UTC oldest-first),\ntop_breachers (cap 10) with per-connector breach_count +\nlast_breached_at + recent_reasons. Mounted BEFORE the DELETE on\n/v1/ingestion/adapters/sla-breaches and BEFORE the\n/:event_id/acknowledge wildcard so the literal \"analytics\" segment\nisn't captured. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ingestion/adapters/sla-breaches/analytics",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "(T6 M14.20) — tenant-wide rollup over the M14.13 adapter SLA"
        },
        {
          "name": "GET /v1/ingestion/adapters/sla-dashboard",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/adapters/sla-dashboard",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "adapters",
                "sla-dashboard"
              ]
            },
            "description": "tenant + applies per-adapter SLA gates.\nQuery params (all optional):\nwindow=N                 (default 20, max RUN_ANALYTICS_MAX_WINDOW)\nmin_success_rate=0..1    (default 0.95)\nmax_p95_latency_ms=ms    (default 30000, max 86_400_000)\nConnectors with no finished runs in the window report\nsla_status='unknown' (not 'breached'). */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ingestion/adapters/sla-dashboard",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "SLA dashboard. Runs M3.5 analytics across every connector for the"
        },
        {
          "name": "POST /v1/ingestion/adapters/sla-snapshot",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/adapters/sla-snapshot",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "adapters",
                "sla-snapshot"
              ]
            },
            "description": "audit store. Returns the dashboard plus the freshly-recorded\nevent list so the operator can see exactly what was logged.\nUse case: cron-trigger every 5 minutes to build a breach\nhistory operators can replay later. Manual button in the SPA\nalso fires this on demand.\nSame query params as `/sla-dashboard` (window + per-call target\noverrides apply identically). */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/ingestion/adapters/sla-snapshot",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "the current dashboard + record one event per breached row to the"
        },
        {
          "name": "DELETE /v1/ingestion/adapters/sla-targets",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/adapters/sla-targets",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "adapters",
                "sla-targets"
              ]
            },
            "description": "platform defaults. Returns { reset: bool }. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/ingestion/adapters/sla-targets",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "tenant override; subsequent dashboard calls fall back to the"
        },
        {
          "name": "GET /v1/ingestion/adapters/sla-targets",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/adapters/sla-targets",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "adapters",
                "sla-targets"
              ]
            },
            "description": "returns the platform defaults with `updated_at: null`. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ingestion/adapters/sla-targets",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "caller's tenant SLA targets. When the tenant has no override,"
        },
        {
          "name": "PUT /v1/ingestion/adapters/sla-targets",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/adapters/sla-targets",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "adapters",
                "sla-targets"
              ]
            },
            "description": "Admin-only. */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PUT /v1/ingestion/adapters/sla-targets",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "tenant override. Body { min_success_rate, max_p95_latency_ms }."
        }
      ]
    },
    {
      "name": "/v1/ingestion/connectors",
      "description": "21 route(s) under /v1/ingestion/connectors",
      "item": [
        {
          "name": "GET /v1/ingestion/connectors",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/connectors",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "connectors"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ingestion/connectors",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/ingestion/connectors — auto-discovered (server.ts:31851)"
        },
        {
          "name": "POST /v1/ingestion/connectors",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/connectors",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "connectors"
              ]
            },
            "description": "Writes an audit event on success. 201 happy / 400 invalid_input or\ninvalid_id / 409 id_in_use / 501 when the configured registry doesn't\nimplement create. */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/ingestion/connectors",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Adds a new per-tenant custom source (Source Editor \"Add new source\")."
        },
        {
          "name": "GET /v1/ingestion/connectors/{id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/connectors/:id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "connectors",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ingestion/connectors/{id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/ingestion/connectors/:id — auto-discovered (server.ts:31987)"
        },
        {
          "name": "PATCH /v1/ingestion/connectors/{id}",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/connectors/:id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "connectors",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: id"
                }
              ]
            },
            "description": "schedule / description / default_status / owner_user_id). Seed\nconnectors are edited via a per-tenant overlay; custom connectors\nare edited in-place. Writes an audit event on success. */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PATCH /v1/ingestion/connectors/{id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Edits a connector's editable metadata (name / source_system / type /"
        },
        {
          "name": "POST /v1/ingestion/connectors/{id}/pause",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/connectors/:id/pause",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "connectors",
                ":id",
                "pause"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: id"
                }
              ]
            },
            "description": "Pause / resume a connector. 404 on unknown id.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/ingestion/connectors/{id}/pause",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/ingestion/connectors/:id/{pause,resume}"
        },
        {
          "name": "POST /v1/ingestion/connectors/{id}/resume",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/connectors/:id/resume",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "connectors",
                ":id",
                "resume"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/ingestion/connectors/{id}/resume",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/ingestion/connectors/:id/resume — auto-discovered (server.ts:33091)"
        },
        {
          "name": "POST /v1/ingestion/connectors/{id}/run",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/connectors/:id/run",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "connectors",
                ":id",
                "run"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: id"
                }
              ]
            },
            "description": "Trigger an ad-hoc run. 404 on unknown id, 409 on paused connector.\nRecords the triggering user from X-APEX-USER (default 'admin').",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/ingestion/connectors/{id}/run",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/ingestion/connectors/:id/run"
        },
        {
          "name": "GET /v1/ingestion/connectors/{id}/runs",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/connectors/:id/runs",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "connectors",
                ":id",
                "runs"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ingestion/connectors/{id}/runs",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/ingestion/connectors/:id/runs — auto-discovered (server.ts:32074)"
        },
        {
          "name": "GET /v1/ingestion/connectors/{id}/runs/analytics",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/connectors/:id/runs/analytics",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "connectors",
                ":id",
                "runs",
                "analytics"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: id"
                }
              ]
            },
            "description": "records processed, last failure. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ingestion/connectors/{id}/runs/analytics",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "— aggregate metrics: success rate, mean/p50/p95 latency,"
        },
        {
          "name": "GET /v1/ingestion/connectors/{id}/runs/failure-patterns",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/connectors/:id/runs/failure-patterns",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "connectors",
                ":id",
                "runs",
                "failure-patterns"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: id"
                }
              ]
            },
            "description": "message. Same window semantics as M3.5 (default 20, max 200).\nReturns top 10 clusters with pattern + count + recent_messages\n(3 newest) + last_failed_at + sample_run_id. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ingestion/connectors/{id}/runs/failure-patterns",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "(T6 M3.6) — cluster failed/partial runs by normalized error"
        },
        {
          "name": "GET /v1/ingestion/connectors/{id}/schema",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/connectors/:id/schema",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "connectors",
                ":id",
                "schema"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ingestion/connectors/{id}/schema",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/ingestion/connectors/:id/schema — auto-discovered (server.ts:32808)"
        },
        {
          "name": "POST /v1/ingestion/connectors/{id}/schema/compare",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/connectors/:id/schema/compare",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "connectors",
                ":id",
                "schema",
                "compare"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: id"
                }
              ]
            },
            "description": "what BREAKS for existing publishers and what's ADDITIVE. Mirror\nof infra/schema-registry/scripts/check_compat.py for the M3.2\nconnector schema shape. Mounted BEFORE other /schema/* routes\nso the literal /compare segment wins. */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/ingestion/connectors/{id}/schema/compare",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "forward-looking compat check: given a candidate schema, report"
        },
        {
          "name": "GET /v1/ingestion/connectors/{id}/schema/effective",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/connectors/:id/schema/effective",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "connectors",
                ":id",
                "schema",
                "effective"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ingestion/connectors/{id}/schema/effective",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/ingestion/connectors/:id/schema/effective — auto-discovered (server.ts:32991)"
        },
        {
          "name": "GET /v1/ingestion/connectors/{id}/schema/overrides",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/connectors/:id/schema/overrides",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "connectors",
                ":id",
                "schema",
                "overrides"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ingestion/connectors/{id}/schema/overrides",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/ingestion/connectors/:id/schema/overrides — auto-discovered (server.ts:32880)"
        },
        {
          "name": "POST /v1/ingestion/connectors/{id}/schema/overrides",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/connectors/:id/schema/overrides",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "connectors",
                ":id",
                "schema",
                "overrides"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/ingestion/connectors/{id}/schema/overrides",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/ingestion/connectors/:id/schema/overrides — auto-discovered (server.ts:32901)"
        },
        {
          "name": "DELETE /v1/ingestion/connectors/{id}/schema/overrides/{field_name}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/connectors/:id/schema/overrides/:field_name",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "connectors",
                ":id",
                "schema",
                "overrides",
                ":field_name"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: id"
                },
                {
                  "key": "field_name",
                  "value": "<field_name>",
                  "description": "Path parameter: field_name"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/ingestion/connectors/{id}/schema/overrides/{field_name}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DELETE /v1/ingestion/connectors/:id/schema/overrides/:field_name — auto-discovered (server.ts:32940)"
        },
        {
          "name": "GET /v1/ingestion/connectors/{id}/schema/source-map",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/connectors/:id/schema/source-map",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "connectors",
                ":id",
                "schema",
                "source-map"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: id"
                }
              ]
            },
            "description": "Companion to /schema/effective that lets the SPA badge each\nfield's origin without comparing platform vs effective by hand.\naudit:read RBAC; 404 unknown_connector. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ingestion/connectors/{id}/schema/source-map",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "per-field source attribution (platform vs tenant_addition)."
        },
        {
          "name": "POST /v1/ingestion/connectors/{id}/schema/validate",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/connectors/:id/schema/validate",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "connectors",
                ":id",
                "schema",
                "validate"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: id"
                }
              ]
            },
            "description": "unless the connector id is unknown (404) or the record shape is\nmalformed (400). */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/ingestion/connectors/{id}/schema/validate",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "pure-function validator. Always 200 (valid: true | false in body)"
        },
        {
          "name": "GET /v1/ingestion/connectors/data-quality",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/connectors/data-quality",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "connectors",
                "data-quality"
              ]
            },
            "description": "status (30%), and field completeness from M3.2 schema (20%). Mounted\nBEFORE the /:id catch-all connectors route. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ingestion/connectors/data-quality",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "data quality score (0-100) based on success_rate (50%), connectivity"
        },
        {
          "name": "GET /v1/ingestion/connectors/schema-drift",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/connectors/schema-drift",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "connectors",
                "schema-drift"
              ]
            },
            "description": "compares two specific schemas); this endpoint lists every connector\n+ how many tenant overrides it carries (= drift signal). Drives the\nSPA \"Schema Drift\" card on /admin/ingestion. **Declared BEFORE\ncatch-all `/v1/ingestion/connectors/:id` so the literal segment wins.** */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ingestion/connectors/schema-drift",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Cross-source schema-drift dashboard view. Distinct from M3.9 (which"
        },
        {
          "name": "GET /v1/ingestion/connectors/uptime-stats",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/connectors/uptime-stats",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "connectors",
                "uptime-stats"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ingestion/connectors/uptime-stats",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/ingestion/connectors/uptime-stats — auto-discovered (server.ts:31843)"
        }
      ]
    },
    {
      "name": "/v1/ingestion/data",
      "description": "1 route(s) under /v1/ingestion/data",
      "item": [
        {
          "name": "GET /v1/ingestion/data/freshness-report",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/data/freshness-report",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "data",
                "freshness-report"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ingestion/data/freshness-report",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/ingestion/data/freshness-report — auto-discovered (server.ts:31615)"
        }
      ]
    },
    {
      "name": "/v1/ingestion/freshness-alert",
      "description": "1 route(s) under /v1/ingestion/freshness-alert",
      "item": [
        {
          "name": "GET /v1/ingestion/freshness-alert",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/freshness-alert",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "freshness-alert"
              ]
            },
            "description": "deadline. Per-row {connector_id, name, source_system, schedule,\nconnector_status, expected_interval_minutes (parsed), last_run_at,\nexpected_next_run_at, overdue_minutes (signed), freshness_status\n∈ {on_time, overdue, critical_stale, never_run,\nunparseable_schedule, paused}}. Envelope: by_status counts +\nconnectors[] sorted most-stale-first + worst_offender + total_at_risk\n(overdue + critical_stale) + total_paused. Distinct from M3.1\n(registry), M3.5 (run analytics), M3.12 (hourly volume), M14.26\n(M14 adapter SLA budget). Drives ops \"which connector hasn't run?\nis the CBS feed stale?\" answer in one round-trip. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ingestion/freshness-alert",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "data-freshness detector. Compares `last_run_at` against schedule"
        }
      ]
    },
    {
      "name": "/v1/ingestion/health",
      "description": "1 route(s) under /v1/ingestion/health",
      "item": [
        {
          "name": "GET /v1/ingestion/health",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/health",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "health"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ingestion/health",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/ingestion/health — auto-discovered (server.ts:31573)"
        }
      ]
    },
    {
      "name": "/v1/ingestion/health-scorecard",
      "description": "1 route(s) under /v1/ingestion/health-scorecard",
      "item": [
        {
          "name": "GET /v1/ingestion/health-scorecard",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/health-scorecard",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "health-scorecard"
              ]
            },
            "description": "data freshness (20%), schema violations (10%). Sorted worst-first.\nMirror of M14.26 (adapter SLA budget) for the ingestion surface.\nMounted BEFORE /v1/ingestion/health to capture the longer literal. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ingestion/health-scorecard",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "score per connector based on success rate (40%), p95 latency (30%),"
        }
      ]
    },
    {
      "name": "/v1/ingestion/pipeline",
      "description": "1 route(s) under /v1/ingestion/pipeline",
      "item": [
        {
          "name": "GET /v1/ingestion/pipeline/timing",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/pipeline/timing",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "pipeline",
                "timing"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ingestion/pipeline/timing",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "T6 M3.28 — Data pipeline execution time analysis"
        }
      ]
    },
    {
      "name": "/v1/ingestion/retry-policies",
      "description": "1 route(s) under /v1/ingestion/retry-policies",
      "item": [
        {
          "name": "GET /v1/ingestion/retry-policies",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/retry-policies",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "retry-policies"
              ]
            },
            "description": "retryable error codes, dead-letter flag). Policy keyed by\nconnector TYPE — kafka_stream connectors share one curve,\nrest_api share another, etc. Lets the SPA render a \"retry\ntransparency\" panel next to M3.6 failure pattern report. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ingestion/retry-policies",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "hand-calibrated retry strategy (max_retries, backoff curve,"
        }
      ]
    },
    {
      "name": "/v1/ingestion/run-errors",
      "description": "1 route(s) under /v1/ingestion/run-errors",
      "item": [
        {
          "name": "GET /v1/ingestion/run-errors/taxonomy",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/run-errors/taxonomy",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "run-errors",
                "taxonomy"
              ]
            },
            "description": "connectors. Categories: timeout / connection / schema / auth /\nrate_limit / data / unknown. Per-category: count, pct, sample_messages\ncap 3, affected_connectors[] sorted asc. Envelope: most_common_\nerror_category, connectors_with_most_errors cap 5. Tenant-scoped.\nDistinct from M3.6 (per-connector regex clustering). */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ingestion/run-errors/taxonomy",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "error message taxonomy over recent failed/partial runs across all"
        }
      ]
    },
    {
      "name": "/v1/ingestion/run-latency-histogram",
      "description": "1 route(s) under /v1/ingestion/run-latency-histogram",
      "item": [
        {
          "name": "GET /v1/ingestion/run-latency-histogram",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/run-latency-histogram",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "run-latency-histogram"
              ]
            },
            "description": "buckets: instant (<1s) / fast (<1m) / medium (<10m) / slow (<1h)\n/ very_slow (>=1h) / still_running (finished_at=null). Per-bucket\n{count, by_status (4 keys), distinct_connectors, samples cap 3\nlongest-first (oldest-started-first for still_running)}.\nEnvelope: peak_bucket (canonical iteration tie-break), mean / p50\n/ p95 over FINISHED runs, slowest_run (id + connector + duration).\nMirror of M7.15 / M8.12 / M15.16 histogram pattern. Drives \"how\nlong do ingestion runs typically take? are any connectors\nexcessively slow?\" answers. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ingestion/run-latency-histogram",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "duration distribution over ConnectorRun entries. 6 canonical"
        }
      ]
    },
    {
      "name": "/v1/ingestion/run-volume",
      "description": "2 route(s) under /v1/ingestion/run-volume",
      "item": [
        {
          "name": "GET /v1/ingestion/run-volume/daily",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/run-volume/daily",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "run-volume",
                "daily"
              ]
            },
            "description": "bounds [1, 365]). Per UTC calendar day: total + by_status\n(success/failure/partial/running with every key emitted at 0) +\ndistinct_connectors (per-day Set-dedup). Envelope: peak_day\n(earliest-day-wins tie-break; null on empty), mean_per_day\n(rounded Σ/days), growth_rate (second-half mean vs first-half;\nnull when first-half=0 OR days<2), busiest_status (canonical\ntie-break: success > failure > partial > running at tied; null\non empty), total_runs_in_window + total_runs_observed (latter\nincludes runs OUTSIDE the window — surfaces \"runs falling outside\nthe window?\" gap). Distinct from M3.12 (hourly cyclic — intraday\npattern) by being the LINEAR trend view. Mirror of M1.9 / M8.15\n/ M10.15 / M12.13 / M15.11 daily-volume pattern. 400 EWS_400_invalid_input\non out-of-range days. Pulls up to 200 runs per connector. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ingestion/run-volume/daily",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "wide TREND view across the trailing-N-day window (default 30,"
        },
        {
          "name": "GET /v1/ingestion/run-volume/hourly",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/run-volume/hourly",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "run-volume",
                "hourly"
              ]
            },
            "description": "Per bucket: total_runs + by_status (every RunStatus key at 0\nwhen absent: success / failure / partial / running). Envelope:\ntotal_runs, active_connectors, total_connectors, by_hour[24]\nalways present in 0..23 order, peak_hour (asc tie-break), peak_count,\nquiet_hours[], mean_runs_per_hour. Drives the ops dashboard\nFleet Activity timeline + maintenance-window planning. Mirror\nof M14.22 / M15.7 heatmap pattern but as a 1D timeline. Pulls\nup to 200 runs per connector via listRuns. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ingestion/run-volume/hourly",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "histogram of connector runs bucketed by UTC hour-of-day 0..23."
        }
      ]
    },
    {
      "name": "/v1/ingestion/schema",
      "description": "10 route(s) under /v1/ingestion/schema",
      "item": [
        {
          "name": "GET /v1/ingestion/schema/coverage-gaps",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/schema/coverage-gaps",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "schema",
                "coverage-gaps"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ingestion/schema/coverage-gaps",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/ingestion/schema/coverage-gaps — auto-discovered (server.ts:32769)"
        },
        {
          "name": "GET /v1/ingestion/schema/evolution",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/schema/evolution",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "schema",
                "evolution"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ingestion/schema/evolution",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/ingestion/schema/evolution — auto-discovered (server.ts:31645)"
        },
        {
          "name": "GET /v1/ingestion/schema/field-count-histogram",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/schema/field-count-histogram",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "schema",
                "field-count-histogram"
              ]
            },
            "description": "(minimal 1-3 / small 4-6 / medium 7-10 / large 11-15 / x_large\n16+). Per-bucket: count + by_record_format (every key at 0) +\nsample_connector_ids cap 3 sorted asc. Envelope: peak_bucket +\nmean_fields + min_fields + max_fields + empty_buckets[].\nPlatform-static. Mirror of M4.15 / M5.18 / M9.11 / M8.12 / M7.15\n/ M3.16 bucketing pattern for the connector schema surface. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ingestion/schema/field-count-histogram",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "bucket field-count histogram over the M3.2 schema catalog"
        },
        {
          "name": "GET /v1/ingestion/schema/field-frequency",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/schema/field-frequency",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "schema",
                "field-frequency"
              ]
            },
            "description": "connectors declare it, observed types, and type_drift flag.\nDistinct from M3.8 (field cross-index), M3.11 (type-matrix),\nM3.14 (type × required matrix). Platform-static.\nMounted BEFORE catch-all /:id/schema routes. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ingestion/schema/field-frequency",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "field-name frequency index. For each field name, surfaces how many"
        },
        {
          "name": "GET /v1/ingestion/schema/field-index",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/schema/field-index",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "schema",
                "field-index"
              ]
            },
            "description": "the platform schema catalogue, returns the connector_ids that\ncarry it + the distinct observed types (multi-entry signals\ntype drift across connectors). Useful for the dataops integrity\naudit dashboard (\"which connectors share a `customer_id` field\nand do they all agree it's a string?\"). Mounted BEFORE\n`/v1/ingestion/connectors/:id/schema` so the literal\n`/schema/field-index` segment isn't captured. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ingestion/schema/field-index",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "over every connector's fields. For each unique field_name across"
        },
        {
          "name": "GET /v1/ingestion/schema/field-usage-stats",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/schema/field-usage-stats",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "schema",
                "field-usage-stats"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ingestion/schema/field-usage-stats",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/ingestion/schema/field-usage-stats — auto-discovered (server.ts:31653)"
        },
        {
          "name": "GET /v1/ingestion/schema/record-format-distribution",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/schema/record-format-distribution",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "schema",
                "record-format-distribution"
              ]
            },
            "description": "4 RecordFormat values (kafka_json / csv / sftp_csv / rest_json).\nPer row: count + connector_ids[] sorted asc + total_fields +\ntotal_required_fields + total_optional_fields + mean_field_count\n(rounded) + sample_connectors (cap 3, sorted field_count desc +\nconnector_id asc). Envelope: formats[] in canonical order,\nmost_common_format (canonical tie-break: kafka_json wins over\ncsv at same count), unused_formats[]. Platform-static — same\nresponse across tenants. Distinct from M3.13 (connector TYPE) +\nM3.14 (field type × required matrix) — record_format is the WIRE\nFORMAT axis, independent of transport. Drives \"schema wire-format\naudit\" panel: \"is anyone still on csv (no header schema)?\". */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ingestion/schema/record-format-distribution",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PIVOT-BY-RECORD_FORMAT over the M3.2 connector schema catalog."
        },
        {
          "name": "GET /v1/ingestion/schema/required-matrix",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/schema/required-matrix",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "schema",
                "required-matrix"
              ]
            },
            "description": "Cells count FIELDS across the catalog (not unique connectors).\nPer-row {type, total, required_count, optional_count,\nrequired_ratio (required / total; 0 when total=0)}. Per-col\n{required_column, total, by_type (every type at 0 when absent)}.\nEnvelope: peak_cell (with samples cap 5 sorted connector_id +\nfield_name asc; canonical iteration tie-break — types in\nALL_FIELD_TYPES order, required before optional; null when\nempty), empty_cells[] in canonical row-major order, strictest_type\n(highest required_ratio among types with total>0; canonical\ntie-break) + loosest_type (lowest required_ratio; canonical\ntie-break). Mirror of M3.11 / M8.14 / M14.28 matrix pattern.\nPlatform-static. Mounted BEFORE catch-all\n`/v1/ingestion/connectors/:id/schema` so the literal `/schema/\nrequired-matrix` segment isn't captured. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ingestion/schema/required-matrix",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "ALL_FIELD_TYPES order) × cols = 2 (required, optional) = 14 cells."
        },
        {
          "name": "GET /v1/ingestion/schema/type-matrix",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/schema/type-matrix",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "schema",
                "type-matrix"
              ]
            },
            "description": "split + dominant_type per row + by_type marginal totals +\nconnectors_without_type[t] inversion. Useful for SPA \"schema\naudit\" stacked-bar charts. Mounted BEFORE catch-all\n`/v1/ingestion/connectors/:id/schema` so the literal segment\nwins. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ingestion/schema/type-matrix",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "per-connector × per-FieldType counts plus required/optional"
        },
        {
          "name": "GET /v1/ingestion/schema/version-drift",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/schema/version-drift",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "schema",
                "version-drift"
              ]
            },
            "description": "divergence. Returns by_source_system[] sorted by drift_magnitude\ndesc, plus drifting_systems[] and stable_systems[]. Platform-static.\nRBAC audit:read. Mounted BEFORE catch-alls. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ingestion/schema/version-drift",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "counts across connectors of the same source_system type to surface"
        }
      ]
    },
    {
      "name": "/v1/ingestion/type-distribution",
      "description": "1 route(s) under /v1/ingestion/type-distribution",
      "item": [
        {
          "name": "GET /v1/ingestion/type-distribution",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/ingestion/type-distribution",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "ingestion",
                "type-distribution"
              ]
            },
            "description": "batch_csv / rest_api / soap_api / sftp_drop). Per row: count +\nby_status (every key present) + by_source_system (compact map) +\ndistinct_source_systems + sample_connectors (cap 3, sorted by\nconnector_id asc). Envelope: types[] in canonical order,\nmost_common_type (canonical tie-break: kafka_stream wins),\nunused_types[]. Tenant-aware (status may be tenant-overridden\nvia M3.1's per-tenant paused state). Mirror of M14.27 method\ndistribution / M5.16 / M11.11 pivot pattern. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/ingestion/type-distribution",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "rollup over the M3.1 registry. 5 ConnectorTypes (kafka_stream /"
        }
      ]
    },
    {
      "name": "/v1/insurance/channel-risk",
      "description": "3 route(s) under /v1/insurance/channel-risk",
      "item": [
        {
          "name": "POST /v1/insurance/channel-risk/analyze",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/insurance/channel-risk/analyze",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "insurance",
                "channel-risk",
                "analyze"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/insurance/channel-risk/analyze",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/insurance/channel-risk/analyze — auto-discovered (server.ts:14606)"
        },
        {
          "name": "GET /v1/insurance/channel-risk/dashboard",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/insurance/channel-risk/dashboard",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "insurance",
                "channel-risk",
                "dashboard"
              ]
            },
            "description": "Distribution-force risk scoring. Composite blend of persistency / fraud\n/ complaint / mis-selling sub-scores per agent + per channel. Routes:\nGET  /v1/insurance/channel-risk/dashboard\nPOST /v1/insurance/channel-risk/analyze\nGET  /v1/insurance/channel-risk/high-risk?channel=&band=&limit="
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/insurance/channel-risk/dashboard",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Insurance EWS · Module 7 — Channel Risk"
        },
        {
          "name": "GET /v1/insurance/channel-risk/high-risk",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/insurance/channel-risk/high-risk",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "insurance",
                "channel-risk",
                "high-risk"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/insurance/channel-risk/high-risk",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/insurance/channel-risk/high-risk — auto-discovered (server.ts:14628)"
        }
      ]
    },
    {
      "name": "/v1/insurance/claims-anomaly",
      "description": "3 route(s) under /v1/insurance/claims-anomaly",
      "item": [
        {
          "name": "POST /v1/insurance/claims-anomaly/analyze",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/insurance/claims-anomaly/analyze",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "insurance",
                "claims-anomaly",
                "analyze"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/insurance/claims-anomaly/analyze",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/insurance/claims-anomaly/analyze — auto-discovered (server.ts:14308)"
        },
        {
          "name": "GET /v1/insurance/claims-anomaly/dashboard",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/insurance/claims-anomaly/dashboard",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "insurance",
                "claims-anomaly",
                "dashboard"
              ]
            },
            "description": "Scores claims for suspicion (0–1), auto-queues high scorers to the SIU.\nRoutes:\nGET  /v1/insurance/claims-anomaly/dashboard\nPOST /v1/insurance/claims-anomaly/analyze\nGET  /v1/insurance/claims-anomaly/suspicious?severity=&limit="
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/insurance/claims-anomaly/dashboard",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Insurance EWS · Module 2 — Claims Anomaly"
        },
        {
          "name": "GET /v1/insurance/claims-anomaly/suspicious",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/insurance/claims-anomaly/suspicious",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "insurance",
                "claims-anomaly",
                "suspicious"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/insurance/claims-anomaly/suspicious",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/insurance/claims-anomaly/suspicious — auto-discovered (server.ts:14286)"
        }
      ]
    },
    {
      "name": "/v1/insurance/fraud",
      "description": "3 route(s) under /v1/insurance/fraud",
      "item": [
        {
          "name": "POST /v1/insurance/fraud/analyze",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/insurance/fraud/analyze",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "insurance",
                "fraud",
                "analyze"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/insurance/fraud/analyze",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/insurance/fraud/analyze — auto-discovered (server.ts:14370)"
        },
        {
          "name": "GET /v1/insurance/fraud/dashboard",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/insurance/fraud/dashboard",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "insurance",
                "fraud",
                "dashboard"
              ]
            },
            "description": "Network/ring detection over an entity-relationship graph. Routes:\nGET  /v1/insurance/fraud/dashboard\nPOST /v1/insurance/fraud/analyze\nGET  /v1/insurance/fraud/high-risk?entity_type=&limit="
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/insurance/fraud/dashboard",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Insurance EWS · Module 3 — Fraud Detection"
        },
        {
          "name": "GET /v1/insurance/fraud/high-risk",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/insurance/fraud/high-risk",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "insurance",
                "fraud",
                "high-risk"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/insurance/fraud/high-risk",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/insurance/fraud/high-risk — auto-discovered (server.ts:14348)"
        }
      ]
    },
    {
      "name": "/v1/insurance/heatmap",
      "description": "2 route(s) under /v1/insurance/heatmap",
      "item": [
        {
          "name": "GET /v1/insurance/heatmap",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/insurance/heatmap",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "insurance",
                "heatmap"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/insurance/heatmap",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/insurance/heatmap — auto-discovered (server.ts:14708)"
        },
        {
          "name": "GET /v1/insurance/heatmap/metrics",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/insurance/heatmap/metrics",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "insurance",
                "heatmap",
                "metrics"
              ]
            },
            "description": "Reusable engine — 5 metrics × 3 dimensions from one code path."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/insurance/heatmap/metrics",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Insurance EWS — Module 10: Insurance Heatmaps (§ Phase-6)."
        }
      ]
    },
    {
      "name": "/v1/insurance/persistency",
      "description": "3 route(s) under /v1/insurance/persistency",
      "item": [
        {
          "name": "GET /v1/insurance/persistency/alerts",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/insurance/persistency/alerts",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "insurance",
                "persistency",
                "alerts"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/insurance/persistency/alerts",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/insurance/persistency/alerts — auto-discovered (server.ts:14501)"
        },
        {
          "name": "POST /v1/insurance/persistency/analyze",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/insurance/persistency/analyze",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "insurance",
                "persistency",
                "analyze"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/insurance/persistency/analyze",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/insurance/persistency/analyze — auto-discovered (server.ts:14479)"
        },
        {
          "name": "GET /v1/insurance/persistency/dashboard",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/insurance/persistency/dashboard",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "insurance",
                "persistency",
                "dashboard"
              ]
            },
            "description": "Tracks policy persistency across the 13/25/37/49/61-month milestones,\nsliced by product / channel / region, with AI root-cause. Routes:\nGET  /v1/insurance/persistency/dashboard\nPOST /v1/insurance/persistency/analyze\nGET  /v1/insurance/persistency/alerts?severity=&limit="
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/insurance/persistency/dashboard",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Insurance EWS · Module 5 — Persistency Watch"
        }
      ]
    },
    {
      "name": "/v1/insurance/policies",
      "description": "1 route(s) under /v1/insurance/policies",
      "item": [
        {
          "name": "GET /v1/insurance/policies/{policy_id}/timeline",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/insurance/policies/:policy_id/timeline",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "insurance",
                "policies",
                ":policy_id",
                "timeline"
              ],
              "variable": [
                {
                  "key": "policy_id",
                  "value": "<policy_id>",
                  "description": "Path parameter: policy_id"
                }
              ]
            },
            "description": "Per-policy lifecycle + risk event stream; insurance analog of the\nbanking Borrower Timeline. Total over policies (drill-through)."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/insurance/policies/{policy_id}/timeline",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Insurance EWS — Module 9: Policy Timeline (§ Phase-6)."
        }
      ]
    },
    {
      "name": "/v1/insurance/policy-lapse",
      "description": "3 route(s) under /v1/insurance/policy-lapse",
      "item": [
        {
          "name": "GET /v1/insurance/policy-lapse/dashboard",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/insurance/policy-lapse/dashboard",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "insurance",
                "policy-lapse",
                "dashboard"
              ]
            },
            "description": "Predicts which in-force policies lapse in the next 30/60/90 days +\nsurfaces retention opportunities. Tenant-scoped, RBAC-gated, enveloped.\nDeterministic synthesis today; swaps to app_insurance.* when the\ninsurer's policy + payment feeds land. Routes:\nGET  /v1/insurance/policy-lapse/dashboard\nPOST /v1/insurance/policy-lapse/predict\nGET  /v1/insurance/policy-lapse/high-risk?horizon_days=&band=&limit="
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/insurance/policy-lapse/dashboard",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Insurance EWS · Module 1 — Policy Lapse Risk"
        },
        {
          "name": "GET /v1/insurance/policy-lapse/high-risk",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/insurance/policy-lapse/high-risk",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "insurance",
                "policy-lapse",
                "high-risk"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/insurance/policy-lapse/high-risk",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/insurance/policy-lapse/high-risk — auto-discovered (server.ts:14221)"
        },
        {
          "name": "POST /v1/insurance/policy-lapse/predict",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/insurance/policy-lapse/predict",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "insurance",
                "policy-lapse",
                "predict"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/insurance/policy-lapse/predict",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/insurance/policy-lapse/predict — auto-discovered (server.ts:14244)"
        }
      ]
    },
    {
      "name": "/v1/insurance/siu",
      "description": "9 route(s) under /v1/insurance/siu",
      "item": [
        {
          "name": "GET /v1/insurance/siu/investigations",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/insurance/siu/investigations",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "insurance",
                "siu",
                "investigations"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/insurance/siu/investigations",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/insurance/siu/investigations — auto-discovered (server.ts:14808)"
        },
        {
          "name": "POST /v1/insurance/siu/investigations",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/insurance/siu/investigations",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "insurance",
                "siu",
                "investigations"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/insurance/siu/investigations",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/insurance/siu/investigations — auto-discovered (server.ts:14776)"
        },
        {
          "name": "GET /v1/insurance/siu/investigations/{investigation_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/insurance/siu/investigations/:investigation_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "insurance",
                "siu",
                "investigations",
                ":investigation_id"
              ],
              "variable": [
                {
                  "key": "investigation_id",
                  "value": "<investigation_id>",
                  "description": "Path parameter: investigation_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/insurance/siu/investigations/{investigation_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/insurance/siu/investigations/:investigation_id — auto-discovered (server.ts:14828)"
        },
        {
          "name": "POST /v1/insurance/siu/investigations/{investigation_id}/escalate",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/insurance/siu/investigations/:investigation_id/escalate",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "insurance",
                "siu",
                "investigations",
                ":investigation_id",
                "escalate"
              ],
              "variable": [
                {
                  "key": "investigation_id",
                  "value": "<investigation_id>",
                  "description": "Path parameter: investigation_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/insurance/siu/investigations/{investigation_id}/escalate",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/insurance/siu/investigations/:investigation_id/escalate — auto-discovered (server.ts:14911)"
        },
        {
          "name": "POST /v1/insurance/siu/investigations/{investigation_id}/evidence",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/insurance/siu/investigations/:investigation_id/evidence",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "insurance",
                "siu",
                "investigations",
                ":investigation_id",
                "evidence"
              ],
              "variable": [
                {
                  "key": "investigation_id",
                  "value": "<investigation_id>",
                  "description": "Path parameter: investigation_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/insurance/siu/investigations/{investigation_id}/evidence",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/insurance/siu/investigations/:investigation_id/evidence — auto-discovered (server.ts:14886)"
        },
        {
          "name": "POST /v1/insurance/siu/investigations/{investigation_id}/link-alert",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/insurance/siu/investigations/:investigation_id/link-alert",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "insurance",
                "siu",
                "investigations",
                ":investigation_id",
                "link-alert"
              ],
              "variable": [
                {
                  "key": "investigation_id",
                  "value": "<investigation_id>",
                  "description": "Path parameter: investigation_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/insurance/siu/investigations/{investigation_id}/link-alert",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/insurance/siu/investigations/:investigation_id/link-alert — auto-discovered (server.ts:1492"
        },
        {
          "name": "POST /v1/insurance/siu/investigations/{investigation_id}/notes",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/insurance/siu/investigations/:investigation_id/notes",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "insurance",
                "siu",
                "investigations",
                ":investigation_id",
                "notes"
              ],
              "variable": [
                {
                  "key": "investigation_id",
                  "value": "<investigation_id>",
                  "description": "Path parameter: investigation_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/insurance/siu/investigations/{investigation_id}/notes",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/insurance/siu/investigations/:investigation_id/notes — auto-discovered (server.ts:14867)"
        },
        {
          "name": "PATCH /v1/insurance/siu/investigations/{investigation_id}/status",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/insurance/siu/investigations/:investigation_id/status",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "insurance",
                "siu",
                "investigations",
                ":investigation_id",
                "status"
              ],
              "variable": [
                {
                  "key": "investigation_id",
                  "value": "<investigation_id>",
                  "description": "Path parameter: investigation_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PATCH /v1/insurance/siu/investigations/{investigation_id}/status",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PATCH /v1/insurance/siu/investigations/:investigation_id/status — auto-discovered (server.ts:14841)"
        },
        {
          "name": "GET /v1/insurance/siu/queue",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/insurance/siu/queue",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "insurance",
                "siu",
                "queue"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/insurance/siu/queue",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/insurance/siu/queue — auto-discovered (server.ts:14756)"
        }
      ]
    },
    {
      "name": "/v1/insurance/solvency",
      "description": "3 route(s) under /v1/insurance/solvency",
      "item": [
        {
          "name": "GET /v1/insurance/solvency/compliance",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/insurance/solvency/compliance",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "insurance",
                "solvency",
                "compliance"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/insurance/solvency/compliance",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/insurance/solvency/compliance — auto-discovered (server.ts:14437)"
        },
        {
          "name": "GET /v1/insurance/solvency/dashboard",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/insurance/solvency/dashboard",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "insurance",
                "solvency",
                "dashboard"
              ]
            },
            "description": "Monitors ASM/RSM vs the 1.50 control level, forecasts under claims\nstress, surfaces compliance alerts. Routes:\nGET  /v1/insurance/solvency/dashboard\nPOST /v1/insurance/solvency/forecast\nGET  /v1/insurance/solvency/compliance?severity=&status=&limit=\n/compliance is gated tighter (insurance:compliance:read = admin+\nsupervisor) — the detailed regulatory view; the dashboard's open-alert\nsummary stays available to every read role."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/insurance/solvency/dashboard",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Insurance EWS · Module 4 — Solvency Watch (IRDAI)"
        },
        {
          "name": "POST /v1/insurance/solvency/forecast",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/insurance/solvency/forecast",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "insurance",
                "solvency",
                "forecast"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/insurance/solvency/forecast",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/insurance/solvency/forecast — auto-discovered (server.ts:14415)"
        }
      ]
    },
    {
      "name": "/v1/insurance/underwriting",
      "description": "3 route(s) under /v1/insurance/underwriting",
      "item": [
        {
          "name": "POST /v1/insurance/underwriting/analyze",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/insurance/underwriting/analyze",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "insurance",
                "underwriting",
                "analyze"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/insurance/underwriting/analyze",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/insurance/underwriting/analyze — auto-discovered (server.ts:14542)"
        },
        {
          "name": "GET /v1/insurance/underwriting/dashboard",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/insurance/underwriting/dashboard",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "insurance",
                "underwriting",
                "dashboard"
              ]
            },
            "description": "Policies approved against UW guidelines (premium / medical-waiver /\nsum-assured / rule-violation) + per-underwriter risk. Routes:\nGET  /v1/insurance/underwriting/dashboard\nPOST /v1/insurance/underwriting/analyze\nGET  /v1/insurance/underwriting/deviations?deviation_type=&status=&limit="
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/insurance/underwriting/dashboard",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Insurance EWS · Module 6 — Underwriting Deviation"
        },
        {
          "name": "GET /v1/insurance/underwriting/deviations",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/insurance/underwriting/deviations",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "insurance",
                "underwriting",
                "deviations"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/insurance/underwriting/deviations",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/insurance/underwriting/deviations — auto-discovered (server.ts:14564)"
        }
      ]
    },
    {
      "name": "/v1/integrations/adapters",
      "description": "20 route(s) under /v1/integrations/adapters",
      "item": [
        {
          "name": "GET /v1/integrations/adapters",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/adapters",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "adapters"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/adapters",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/integrations/adapters — auto-discovered (server.ts:21383)"
        },
        {
          "name": "GET /v1/integrations/adapters/circuit-breaker-status",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/adapters/circuit-breaker-status",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "adapters",
                "circuit-breaker-status"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/adapters/circuit-breaker-status",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/integrations/adapters/circuit-breaker-status — auto-discovered (server.ts:21681)"
        },
        {
          "name": "GET /v1/integrations/adapters/data-freshness",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/adapters/data-freshness",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "adapters",
                "data-freshness"
              ]
            },
            "description": "freshness_status: fresh (age<interval) / aging (<2x) / stale.\nReturns adapters[] sorted age_hours desc, stale/aging/fresh counts,\nmost_stale. RBAC audit:read. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/adapters/data-freshness",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "deterministic last_refreshed_at per (tenant, adapter, day)."
        },
        {
          "name": "GET /v1/integrations/adapters/data-volume",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/adapters/data-volume",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "adapters",
                "data-volume"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/adapters/data-volume",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "T6 M14.38 — Adapter data volume analysis"
        },
        {
          "name": "GET /v1/integrations/adapters/dependency-graph",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/adapters/dependency-graph",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "adapters",
                "dependency-graph"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/adapters/dependency-graph",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/integrations/adapters/dependency-graph — auto-discovered (server.ts:21701)"
        },
        {
          "name": "GET /v1/integrations/adapters/error-classification",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/adapters/error-classification",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "adapters",
                "error-classification"
              ]
            },
            "description": "adapter analytics routes. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/adapters/error-classification",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Per-adapter error type classification. Mounted alongside other"
        },
        {
          "name": "GET /v1/integrations/adapters/health",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/adapters/health",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "adapters",
                "health"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/adapters/health",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/integrations/adapters/health — auto-discovered (server.ts:21689)"
        },
        {
          "name": "GET /v1/integrations/adapters/id-catalog",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/adapters/id-catalog",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "adapters",
                "id-catalog"
              ]
            },
            "description": "pattern_template, example, description). Drives client-side\nID-shape validation + \"paste an ID and we'll route to the\nright adapter\" SPA feature. Platform-static. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/adapters/id-catalog",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "adapter entity ID format catalog (entity, id_field,"
        },
        {
          "name": "GET /v1/integrations/adapters/latency-comparison",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/adapters/latency-comparison",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "adapters",
                "latency-comparison"
              ]
            },
            "description": "fleet-health probe results with M14.23 SLA target catalog to grade\neach adapter's observed latency with a letter grade (A=<50% of SLA,\nB=50-80%, C=80-100%, D=100-125%, F=>125% or degraded). Fleet health\nscore = weighted average (A=100,B=80,C=60,D=40,F=0). Async — runs\nthe fleet probe. Distinct from M14.26 (SLA budget verdict). */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/adapters/latency-comparison",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Adapter request latency percentile comparison. Composes M14.9"
        },
        {
          "name": "GET /v1/integrations/adapters/operations",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/adapters/operations",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "adapters",
                "operations"
              ]
            },
            "description": "parameter contracts). Drives the SPA \"API explorer\" panel so\nadmins can see which queries each adapter supports without\nbrowsing TypeScript types. Platform-static hand-curated. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/adapters/operations",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "adapter operation catalog (operation_id, method, path,"
        },
        {
          "name": "GET /v1/integrations/adapters/operations/location-distribution",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/adapters/operations/location-distribution",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "adapters",
                "operations",
                "location-distribution"
              ]
            },
            "description": "by parameter `in` location (path/query/body). Per-location row:\ncount, by_adapter (compact Record), by_method (compact),\nby_type (4-key ParameterType Record at 0 when absent),\nrequired_count + optional_count split, distinct_adapters,\nsample_parameters (cap 5; sorted adapter_id asc + operation_id\nasc + parameter_name asc). Envelope: locations[] in canonical\norder even when zero-count, most_common_location (canonical\niteration tie-break: path wins over query at tied),\nunused_locations[]. Platform-static. Mirror of M14.27 1D\ndistribution pattern. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/adapters/operations/location-distribution",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "(T6 M14.30) — orthogonal pivot of the M14.24 operation catalog"
        },
        {
          "name": "GET /v1/integrations/adapters/operations/matrix",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/adapters/operations/matrix",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "adapters",
                "operations",
                "matrix"
              ]
            },
            "description": "adapters). Per-row {method, total, by_adapter (every AdapterId\nat 0 when absent), adapters_without[] sorted asc — coverage-gap\nper method}. Per-col {adapter_id, label, base_path, total,\nby_method (every method at 0 when absent), methods_without[] in\ncanonical method order — coverage-gap per adapter}. Envelope:\npeak_cell (highest count + canonical iteration tie-break; null\nwhen zero ops), empty_cells[] (canonical method × adapter asc\norder — comprehensive gap list), most_versatile_adapter\n(highest method-coverage span; canonical adapter_id asc tie\nbreak; null on empty). Mirror of M3.11 connector schema type\nmatrix + M9.13 investigation age × status matrix for the\nadapter operation surface. Platform-static. Drives \"which\nadapters expose PATCH endpoints?\" + \"where are the gaps in\nDELETE coverage?\". */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/adapters/operations/matrix",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "method (canonical GET/POST/PATCH/DELETE), cols = adapter (8 BIL"
        },
        {
          "name": "GET /v1/integrations/adapters/operations/method-distribution",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/adapters/operations/method-distribution",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "adapters",
                "operations",
                "method-distribution"
              ]
            },
            "description": "by HTTP method (GET/POST/PATCH/DELETE). Per-method row: count,\nby_adapter Record (only non-zero adapters as keys),\ndistinct_adapters, sample_operations (cap 5; sorted adapter_id\nasc + path asc). Envelope: methods[] in canonical order even\nwhen zero-count, most_common_method (canonical tie-break),\nunused_methods[]. Platform-static. Mirror of M5.16 / M11.11 /\nM3.12 pivot pattern for the adapter operations surface. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/adapters/operations/method-distribution",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "(T6 M14.27) — orthogonal pivot of the M14.24 operation catalog"
        },
        {
          "name": "GET /v1/integrations/adapters/operations/param-name-index",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/adapters/operations/param-name-index",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "adapters",
                "operations",
                "param-name-index"
              ]
            },
            "description": "every parameter by its `name` and surface which operations declare\nit + whether they AGREE on type and location. Per-name {name,\nreference_count, observed_types[] (canonical; multi-entry = TYPE\nDRIFT), observed_locations[] (canonical; multi-entry = LOCATION\nDRIFT), adapters[] asc, occurrences[] (adapter_id asc + operation_id\nasc), has_type_drift, has_location_drift}. Envelope: names[] sorted\nreference_count desc + name asc, shared_names[] (≥ 2 ops),\nsingle_use_names[] (unique / refactor candidates), drifting_names[]\n(contract-consistency warnings), most_shared_name. Mirror of M3.8 /\nM10.13 / M11.16 inverted-index pattern. Platform-static. Drives\nintegration-contract hygiene (\"does every op agree customer_id is a\nstring in path?\"). Mounted with the operations/* analytics group. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/adapters/operations/param-name-index",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "M14.31) — INVERTED index over the M14.24 operation catalog: group"
        },
        {
          "name": "GET /v1/integrations/adapters/operations/param-type-required-matrix",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/adapters/operations/param-type-required-matrix",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "adapters",
                "operations",
                "param-type-required-matrix"
              ]
            },
            "description": "catalog. Rows = 4 ParameterType (canonical string → integer →\ndatetime → enum) × cols = 2 (required, optional) = 8 cells.\nCells count PARAMETERS — a single operation with N params\ncontributes N to the relevant cells. Per-row {type, total,\nrequired_count, optional_count, required_ratio}. Per-col\n{required_column, total, by_type (every ParameterType at 0\nwhen absent)}. Envelope: peak_cell (with samples cap 5 sorted\nadapter_id + operation_id + parameter_name asc; canonical\niteration tie-break; null on empty), empty_cells[] in canonical\nrow-major order, strictest_type + loosest_type (highest +\nlowest required_ratio among types with total>0; canonical-order\ntie-break). Mirror of M3.14 (connector schema field-type ×\nrequired) for the adapter operation catalog. Platform-static.\nMounted next to M14.28 / M14.27 / M14.24 operation routes. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/adapters/operations/param-type-required-matrix",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "(T6 M14.29) — 2D pivot ACROSS every parameter in the M14.24"
        },
        {
          "name": "GET /v1/integrations/adapters/sla-budget",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/adapters/sla-budget",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "adapters",
                "sla-budget"
              ]
            },
            "description": "{expected_p95_ms, observed_latency_ms, status, within_budget,\nheadroom_ms, headroom_pct, verdict}. Verdict enum:\nwithin_budget | over_budget | over_budget_severe (>125% of\nexpected) | degraded (probe failed). Envelope: totals + worst\noffender + most slack. Drives the SPA's green/amber/red SLA\nbadges. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/adapters/sla-budget",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "M14.9 fleet probe + M14.23 SLA catalog. Per-adapter"
        },
        {
          "name": "GET /v1/integrations/adapters/sla-catalog",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/adapters/sla-catalog",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "adapters",
                "sla-catalog"
              ]
            },
            "description": "limit, uptime SLA). Lets the SPA render green/amber/red SLA\nbadges on top of the M14.9 probe latencies. Platform-static\nhand-calibrated metadata. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/adapters/sla-catalog",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "adapter expected SLA targets (latency p95, freshness, rate"
        },
        {
          "name": "GET /v1/integrations/adapters/sla-trend",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/adapters/sla-trend",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "adapters",
                "sla-trend"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/adapters/sla-trend",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/integrations/adapters/sla-trend — auto-discovered (server.ts:21395)"
        },
        {
          "name": "GET /v1/integrations/adapters/throughput-efficiency",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/adapters/throughput-efficiency",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "adapters",
                "throughput-efficiency"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/adapters/throughput-efficiency",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/integrations/adapters/throughput-efficiency — auto-discovered (server.ts:21447)"
        },
        {
          "name": "GET /v1/integrations/adapters/version-compatibility",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/adapters/version-compatibility",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "adapters",
                "version-compatibility"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/adapters/version-compatibility",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/integrations/adapters/version-compatibility — auto-discovered (server.ts:21403)"
        }
      ]
    },
    {
      "name": "/v1/integrations/agent",
      "description": "4 route(s) under /v1/integrations/agent",
      "item": [
        {
          "name": "GET /v1/integrations/agent/agents",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/agent/agents",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "agent",
                "agents"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/agent/agents",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/integrations/agent/agents — auto-discovered (server.ts:20955)"
        },
        {
          "name": "GET /v1/integrations/agent/agents/{agent_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/agent/agents/:agent_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "agent",
                "agents",
                ":agent_id"
              ],
              "variable": [
                {
                  "key": "agent_id",
                  "value": "<agent_id>",
                  "description": "Path parameter: agent_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/agent/agents/{agent_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/integrations/agent/agents/:agent_id — auto-discovered (server.ts:21002)"
        },
        {
          "name": "GET /v1/integrations/agent/agents/{agent_id}/productivity",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/agent/agents/:agent_id/productivity",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "agent",
                "agents",
                ":agent_id",
                "productivity"
              ],
              "variable": [
                {
                  "key": "agent_id",
                  "value": "<agent_id>",
                  "description": "Path parameter: agent_id"
                }
              ]
            },
            "description": "Single-period productivity. Period defaults to YYYY-MM of now.\n400 EWS_400_invalid_period when period malformed."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/agent/agents/{agent_id}/productivity",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/integrations/agent/agents/:agent_id/productivity[?period=YYYY-MM]"
        },
        {
          "name": "GET /v1/integrations/agent/agents/{agent_id}/productivity/history",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/agent/agents/:agent_id/productivity/history",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "agent",
                "agents",
                ":agent_id",
                "productivity",
                "history"
              ],
              "variable": [
                {
                  "key": "agent_id",
                  "value": "<agent_id>",
                  "description": "Path parameter: agent_id"
                }
              ]
            },
            "description": "Productivity for the last N months ending at now, newest-first.\nmonths clamped to [1, 36]."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/agent/agents/{agent_id}/productivity/history",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/integrations/agent/agents/:agent_id/productivity/history?months=12"
        }
      ]
    },
    {
      "name": "/v1/integrations/aml",
      "description": "4 route(s) under /v1/integrations/aml",
      "item": [
        {
          "name": "GET /v1/integrations/aml/matches",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/aml/matches",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "aml",
                "matches"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/aml/matches",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/integrations/aml/matches — auto-discovered (server.ts:20328)"
        },
        {
          "name": "GET /v1/integrations/aml/matches/{match_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/aml/matches/:match_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "aml",
                "matches",
                ":match_id"
              ],
              "variable": [
                {
                  "key": "match_id",
                  "value": "<match_id>",
                  "description": "Path parameter: match_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/aml/matches/{match_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/integrations/aml/matches/:match_id — auto-discovered (server.ts:20362)"
        },
        {
          "name": "PATCH /v1/integrations/aml/matches/{match_id}",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/aml/matches/:match_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "aml",
                "matches",
                ":match_id"
              ],
              "variable": [
                {
                  "key": "match_id",
                  "value": "<match_id>",
                  "description": "Path parameter: match_id"
                }
              ]
            },
            "description": "Update a match's status (cleared / escalated / false_positive).\nRecords the changed_by user from X-APEX-USER (default 'admin').",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PATCH /v1/integrations/aml/matches/{match_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PATCH /v1/integrations/aml/matches/:match_id body: { status }"
        },
        {
          "name": "POST /v1/integrations/aml/screen",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/aml/screen",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "aml",
                "screen"
              ]
            },
            "description": "POST /v1/integrations/aml/screen body: { customer_id }\nScreen a customer against the configured watchlists. Synchronous\nstub — production wires this to the AML hub.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/integrations/aml/screen",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "AML Watchlist adapter (T6 M14.3)"
        }
      ]
    },
    {
      "name": "/v1/integrations/bureau",
      "description": "4 route(s) under /v1/integrations/bureau",
      "item": [
        {
          "name": "POST /v1/integrations/bureau/pull",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/bureau/pull",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "bureau",
                "pull"
              ]
            },
            "description": "Idempotent per (tenant, customer, bureau, day) — same caller,\nsame day = same report. Pulled-by recorded from X-APEX-USER.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/integrations/bureau/pull",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/integrations/bureau/pull body: { customer_id, bureau_type }"
        },
        {
          "name": "GET /v1/integrations/bureau/reports",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/bureau/reports",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "bureau",
                "reports"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/bureau/reports",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/integrations/bureau/reports — auto-discovered (server.ts:20889)"
        },
        {
          "name": "GET /v1/integrations/bureau/reports/{report_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/bureau/reports/:report_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "bureau",
                "reports",
                ":report_id"
              ],
              "variable": [
                {
                  "key": "report_id",
                  "value": "<report_id>",
                  "description": "Path parameter: report_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/bureau/reports/{report_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/integrations/bureau/reports/:report_id — auto-discovered (server.ts:20919)"
        },
        {
          "name": "GET /v1/integrations/bureau/types",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/bureau/types",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "bureau",
                "types"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/bureau/types",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/integrations/bureau/types — auto-discovered (server.ts:20831)"
        }
      ]
    },
    {
      "name": "/v1/integrations/cbs",
      "description": "8 route(s) under /v1/integrations/cbs",
      "item": [
        {
          "name": "GET /v1/integrations/cbs/enums",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/cbs/enums",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "cbs",
                "enums"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/cbs/enums",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/integrations/cbs/enums — auto-discovered (server.ts:23084)"
        },
        {
          "name": "GET /v1/integrations/cbs/summary",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/cbs/summary",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "cbs",
                "summary"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/cbs/summary",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/integrations/cbs/summary — auto-discovered (server.ts:23104)"
        },
        {
          "name": "GET /v1/integrations/cbs/sync-jobs",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/cbs/sync-jobs",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "cbs",
                "sync-jobs"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/cbs/sync-jobs",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/integrations/cbs/sync-jobs — auto-discovered (server.ts:23125)"
        },
        {
          "name": "POST /v1/integrations/cbs/sync-jobs",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/cbs/sync-jobs",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "cbs",
                "sync-jobs"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/integrations/cbs/sync-jobs",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/integrations/cbs/sync-jobs — auto-discovered (server.ts:23230)"
        },
        {
          "name": "DELETE /v1/integrations/cbs/sync-jobs/{job_id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/cbs/sync-jobs/:job_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "cbs",
                "sync-jobs",
                ":job_id"
              ],
              "variable": [
                {
                  "key": "job_id",
                  "value": "<job_id>",
                  "description": "Path parameter: job_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/integrations/cbs/sync-jobs/{job_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DELETE /v1/integrations/cbs/sync-jobs/:job_id — auto-discovered (server.ts:23348)"
        },
        {
          "name": "GET /v1/integrations/cbs/sync-jobs/{job_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/cbs/sync-jobs/:job_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "cbs",
                "sync-jobs",
                ":job_id"
              ],
              "variable": [
                {
                  "key": "job_id",
                  "value": "<job_id>",
                  "description": "Path parameter: job_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/cbs/sync-jobs/{job_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/integrations/cbs/sync-jobs/:job_id — auto-discovered (server.ts:23209)"
        },
        {
          "name": "POST /v1/integrations/cbs/sync-jobs/{job_id}/transition",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/cbs/sync-jobs/:job_id/transition",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "cbs",
                "sync-jobs",
                ":job_id",
                "transition"
              ],
              "variable": [
                {
                  "key": "job_id",
                  "value": "<job_id>",
                  "description": "Path parameter: job_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/integrations/cbs/sync-jobs/{job_id}/transition",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/integrations/cbs/sync-jobs/:job_id/transition — auto-discovered (server.ts:23289)"
        },
        {
          "name": "GET /v1/integrations/cbs/sync-jobs/by-key/{idempotency_key}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/cbs/sync-jobs/by-key/:idempotency_key",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "cbs",
                "sync-jobs",
                "by-key",
                ":idempotency_key"
              ],
              "variable": [
                {
                  "key": "idempotency_key",
                  "value": "<idempotency_key>",
                  "description": "Path parameter: idempotency_key"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/cbs/sync-jobs/by-key/{idempotency_key}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/integrations/cbs/sync-jobs/by-key/:idempotency_key — auto-discovered (server.ts:23188)"
        }
      ]
    },
    {
      "name": "/v1/integrations/dms",
      "description": "4 route(s) under /v1/integrations/dms",
      "item": [
        {
          "name": "GET /v1/integrations/dms/document-types",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/dms/document-types",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "dms",
                "document-types"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/dms/document-types",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/integrations/dms/document-types — auto-discovered (server.ts:20666)"
        },
        {
          "name": "GET /v1/integrations/dms/documents",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/dms/documents",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "dms",
                "documents"
              ]
            },
            "description": "Exactly one of customer_id / case_id must be provided. case_id\nscopes to documents linked to that case (across customers if\napplicable); customer_id scopes to all documents for a customer."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/dms/documents",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/integrations/dms/documents?customer_id=&case_id="
        },
        {
          "name": "GET /v1/integrations/dms/documents/{document_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/dms/documents/:document_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "dms",
                "documents",
                ":document_id"
              ],
              "variable": [
                {
                  "key": "document_id",
                  "value": "<document_id>",
                  "description": "Path parameter: document_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/dms/documents/{document_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/integrations/dms/documents/:document_id — auto-discovered (server.ts:20729)"
        },
        {
          "name": "PATCH /v1/integrations/dms/documents/{document_id}/status",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/dms/documents/:document_id/status",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "dms",
                "documents",
                ":document_id",
                "status"
              ],
              "variable": [
                {
                  "key": "document_id",
                  "value": "<document_id>",
                  "description": "Path parameter: document_id"
                }
              ]
            },
            "description": "Update review status (pending_review / verified / rejected / expired).\nRecords changed_by from X-APEX-USER (default 'admin').",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PATCH /v1/integrations/dms/documents/{document_id}/status",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PATCH /v1/integrations/dms/documents/:document_id/status body: { status }"
        }
      ]
    },
    {
      "name": "/v1/integrations/finance",
      "description": "3 route(s) under /v1/integrations/finance",
      "item": [
        {
          "name": "GET /v1/integrations/finance/accounts",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/finance/accounts",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "finance",
                "accounts"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/finance/accounts",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/integrations/finance/accounts — auto-discovered (server.ts:21128)"
        },
        {
          "name": "GET /v1/integrations/finance/accounts/{account_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/finance/accounts/:account_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "finance",
                "accounts",
                ":account_id"
              ],
              "variable": [
                {
                  "key": "account_id",
                  "value": "<account_id>",
                  "description": "Path parameter: account_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/finance/accounts/{account_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/integrations/finance/accounts/:account_id — auto-discovered (server.ts:21162)"
        },
        {
          "name": "GET /v1/integrations/finance/accounts/{account_id}/ledger",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/finance/accounts/:account_id/ledger",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "finance",
                "accounts",
                ":account_id",
                "ledger"
              ],
              "variable": [
                {
                  "key": "account_id",
                  "value": "<account_id>",
                  "description": "Path parameter: account_id"
                }
              ]
            },
            "description": "Paginated ledger entries newest-first. since/until are ISO timestamps;\n400 EWS_400_invalid_since / _invalid_until on malformed values.\n404 EWS_404_unknown_account on miss / cross-tenant lookup."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/finance/accounts/{account_id}/ledger",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/integrations/finance/accounts/:account_id/ledger?since=&until=&page=&page_size="
        }
      ]
    },
    {
      "name": "/v1/integrations/health",
      "description": "1 route(s) under /v1/integrations/health",
      "item": [
        {
          "name": "GET /v1/integrations/health",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/health",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "health"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/health",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/integrations/health — auto-discovered (server.ts:20021)"
        }
      ]
    },
    {
      "name": "/v1/integrations/hr",
      "description": "3 route(s) under /v1/integrations/hr",
      "item": [
        {
          "name": "GET /v1/integrations/hr/employees",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/hr/employees",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "hr",
                "employees"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/hr/employees",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/integrations/hr/employees — auto-discovered (server.ts:21252)"
        },
        {
          "name": "GET /v1/integrations/hr/employees/{employee_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/hr/employees/:employee_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "hr",
                "employees",
                ":employee_id"
              ],
              "variable": [
                {
                  "key": "employee_id",
                  "value": "<employee_id>",
                  "description": "Path parameter: employee_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/hr/employees/{employee_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/integrations/hr/employees/:employee_id — auto-discovered (server.ts:21304)"
        },
        {
          "name": "GET /v1/integrations/hr/employees/{employee_id}/leave-balance",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/hr/employees/:employee_id/leave-balance",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "hr",
                "employees",
                ":employee_id",
                "leave-balance"
              ],
              "variable": [
                {
                  "key": "employee_id",
                  "value": "<employee_id>",
                  "description": "Path parameter: employee_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/hr/employees/{employee_id}/leave-balance",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/integrations/hr/employees/:employee_id/leave-balance — auto-discovered (server.ts:21334)"
        }
      ]
    },
    {
      "name": "/v1/integrations/ifrs9",
      "description": "2 route(s) under /v1/integrations/ifrs9",
      "item": [
        {
          "name": "GET /v1/integrations/ifrs9/stages",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/ifrs9/stages",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "ifrs9",
                "stages"
              ]
            },
            "description": "Paginated list of customers in the IFRS 9 book, optionally\nfiltered by stage. Highest-ECL first within each page."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/ifrs9/stages",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/integrations/ifrs9/stages?stage=2&page=1&page_size=50"
        },
        {
          "name": "GET /v1/integrations/ifrs9/stages/{customer_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/ifrs9/stages/:customer_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "ifrs9",
                "stages",
                ":customer_id"
              ],
              "variable": [
                {
                  "key": "customer_id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: customer_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/ifrs9/stages/{customer_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/integrations/ifrs9/stages/:customer_id — auto-discovered (server.ts:20194)"
        }
      ]
    },
    {
      "name": "/v1/integrations/insurance",
      "description": "4 route(s) under /v1/integrations/insurance",
      "item": [
        {
          "name": "GET /v1/integrations/insurance/claims",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/insurance/claims",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "insurance",
                "claims"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/insurance/claims",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/integrations/insurance/claims — auto-discovered (server.ts:20124)"
        },
        {
          "name": "GET /v1/integrations/insurance/claims/{claim_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/insurance/claims/:claim_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "insurance",
                "claims",
                ":claim_id"
              ],
              "variable": [
                {
                  "key": "claim_id",
                  "value": "<claim_id>",
                  "description": "Path parameter: claim_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/insurance/claims/{claim_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/integrations/insurance/claims/:claim_id — auto-discovered (server.ts:20158)"
        },
        {
          "name": "GET /v1/integrations/insurance/policies",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/insurance/policies",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "insurance",
                "policies"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/insurance/policies",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/integrations/insurance/policies — auto-discovered (server.ts:20056)"
        },
        {
          "name": "GET /v1/integrations/insurance/policies/{policy_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/integrations/insurance/policies/:policy_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "integrations",
                "insurance",
                "policies",
                ":policy_id"
              ],
              "variable": [
                {
                  "key": "policy_id",
                  "value": "<policy_id>",
                  "description": "Path parameter: policy_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/integrations/insurance/policies/{policy_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/integrations/insurance/policies/:policy_id — auto-discovered (server.ts:20090)"
        }
      ]
    },
    {
      "name": "/v1/master/:master_type",
      "description": "6 route(s) under /v1/master/:master_type",
      "item": [
        {
          "name": "GET /v1/master/{master_type}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/:master_type",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                ":master_type"
              ],
              "variable": [
                {
                  "key": "master_type",
                  "value": "<master_type>",
                  "description": "Path parameter: master_type"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/master/{master_type}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Module #19 — Missing master-data screens (12 types) (§2.3)"
        },
        {
          "name": "POST /v1/master/{master_type}",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/:master_type",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                ":master_type"
              ],
              "variable": [
                {
                  "key": "master_type",
                  "value": "<master_type>",
                  "description": "Path parameter: master_type"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/master/{master_type}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/master/:master_type — auto-discovered (server.ts:42436)"
        },
        {
          "name": "DELETE /v1/master/{master_type}/{record_id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/:master_type/:record_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                ":master_type",
                ":record_id"
              ],
              "variable": [
                {
                  "key": "master_type",
                  "value": "<master_type>",
                  "description": "Path parameter: master_type"
                },
                {
                  "key": "record_id",
                  "value": "<record_id>",
                  "description": "Path parameter: record_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/master/{master_type}/{record_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DELETE /v1/master/:master_type/:record_id — auto-discovered (server.ts:42593)"
        },
        {
          "name": "GET /v1/master/{master_type}/{record_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/:master_type/:record_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                ":master_type",
                ":record_id"
              ],
              "variable": [
                {
                  "key": "master_type",
                  "value": "<master_type>",
                  "description": "Path parameter: master_type"
                },
                {
                  "key": "record_id",
                  "value": "<record_id>",
                  "description": "Path parameter: record_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/master/{master_type}/{record_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/master/:master_type/:record_id — auto-discovered (server.ts:42483)"
        },
        {
          "name": "PATCH /v1/master/{master_type}/{record_id}",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/:master_type/:record_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                ":master_type",
                ":record_id"
              ],
              "variable": [
                {
                  "key": "master_type",
                  "value": "<master_type>",
                  "description": "Path parameter: master_type"
                },
                {
                  "key": "record_id",
                  "value": "<record_id>",
                  "description": "Path parameter: record_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PATCH /v1/master/{master_type}/{record_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PATCH /v1/master/:master_type/:record_id — auto-discovered (server.ts:42510)"
        },
        {
          "name": "GET /v1/master/{master_type}/{record_id}/where-used",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/:master_type/:record_id/where-used",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                ":master_type",
                ":record_id",
                "where-used"
              ],
              "variable": [
                {
                  "key": "master_type",
                  "value": "<master_type>",
                  "description": "Path parameter: master_type"
                },
                {
                  "key": "record_id",
                  "value": "<record_id>",
                  "description": "Path parameter: record_id"
                }
              ]
            },
            "description": "delete button to surface the \"in use by N records\" message\nupfront. Returns `{master_type, record_id, code, total_references,\nreferences[]}` with references capped to USAGE_SAMPLE_LIMIT.\nMounted BEFORE `/v1/master/:master_type/:record_id` PATCH/DELETE\nso the literal `where-used` segment doesn't get captured. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/master/{master_type}/{record_id}/where-used",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Spec acceptance support — SPA queries this before showing the"
        }
      ]
    },
    {
      "name": "/v1/master/accounts",
      "description": "6 route(s) under /v1/master/accounts",
      "item": [
        {
          "name": "GET /v1/master/accounts",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/accounts",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                "accounts"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/master/accounts",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/master/accounts — auto-discovered (server.ts:26707)"
        },
        {
          "name": "POST /v1/master/accounts",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/accounts",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                "accounts"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/master/accounts",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/master/accounts — auto-discovered (server.ts:26781)"
        },
        {
          "name": "DELETE /v1/master/accounts/{account_type_id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/accounts/:account_type_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                "accounts",
                ":account_type_id"
              ],
              "variable": [
                {
                  "key": "account_type_id",
                  "value": "<account_type_id>",
                  "description": "Path parameter: account_type_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/master/accounts/{account_type_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DELETE /v1/master/accounts/:account_type_id — auto-discovered (server.ts:26890)"
        },
        {
          "name": "GET /v1/master/accounts/{account_type_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/accounts/:account_type_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                "accounts",
                ":account_type_id"
              ],
              "variable": [
                {
                  "key": "account_type_id",
                  "value": "<account_type_id>",
                  "description": "Path parameter: account_type_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/master/accounts/{account_type_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/master/accounts/:account_type_id — auto-discovered (server.ts:26758)"
        },
        {
          "name": "PATCH /v1/master/accounts/{account_type_id}",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/accounts/:account_type_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                "accounts",
                ":account_type_id"
              ],
              "variable": [
                {
                  "key": "account_type_id",
                  "value": "<account_type_id>",
                  "description": "Path parameter: account_type_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PATCH /v1/master/accounts/{account_type_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PATCH /v1/master/accounts/:account_type_id — auto-discovered (server.ts:26840)"
        },
        {
          "name": "GET /v1/master/accounts/categories",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/accounts/categories",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                "accounts",
                "categories"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/master/accounts/categories",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/master/accounts/categories — auto-discovered (server.ts:26688)"
        }
      ]
    },
    {
      "name": "/v1/master/bureaus",
      "description": "7 route(s) under /v1/master/bureaus",
      "item": [
        {
          "name": "GET /v1/master/bureaus",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/bureaus",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                "bureaus"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/master/bureaus",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/master/bureaus — auto-discovered (server.ts:26435)"
        },
        {
          "name": "POST /v1/master/bureaus",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/bureaus",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                "bureaus"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/master/bureaus",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/master/bureaus — auto-discovered (server.ts:26502)"
        },
        {
          "name": "DELETE /v1/master/bureaus/{bureau_id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/bureaus/:bureau_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                "bureaus",
                ":bureau_id"
              ],
              "variable": [
                {
                  "key": "bureau_id",
                  "value": "<bureau_id>",
                  "description": "Path parameter: bureau_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/master/bureaus/{bureau_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DELETE /v1/master/bureaus/:bureau_id — auto-discovered (server.ts:26612)"
        },
        {
          "name": "GET /v1/master/bureaus/{bureau_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/bureaus/:bureau_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                "bureaus",
                ":bureau_id"
              ],
              "variable": [
                {
                  "key": "bureau_id",
                  "value": "<bureau_id>",
                  "description": "Path parameter: bureau_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/master/bureaus/{bureau_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/master/bureaus/:bureau_id — auto-discovered (server.ts:26473)"
        },
        {
          "name": "PATCH /v1/master/bureaus/{bureau_id}",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/bureaus/:bureau_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                "bureaus",
                ":bureau_id"
              ],
              "variable": [
                {
                  "key": "bureau_id",
                  "value": "<bureau_id>",
                  "description": "Path parameter: bureau_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PATCH /v1/master/bureaus/{bureau_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PATCH /v1/master/bureaus/:bureau_id — auto-discovered (server.ts:26553)"
        },
        {
          "name": "GET /v1/master/bureaus/types",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/bureaus/types",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                "bureaus",
                "types"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/master/bureaus/types",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/master/bureaus/types — auto-discovered (server.ts:26393)"
        },
        {
          "name": "GET /v1/master/bureaus/weight-overlay",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/bureaus/weight-overlay",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                "bureaus",
                "weight-overlay"
              ]
            },
            "description": "contribution per bureau. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/master/bureaus/weight-overlay",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "map for the M6.x scoring overlay; SPA shows the effective"
        }
      ]
    },
    {
      "name": "/v1/master/customers",
      "description": "7 route(s) under /v1/master/customers",
      "item": [
        {
          "name": "GET /v1/master/customers",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/customers",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                "customers"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/master/customers",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/master/customers — auto-discovered (server.ts:26114)"
        },
        {
          "name": "POST /v1/master/customers",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/customers",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                "customers"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/master/customers",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/master/customers — auto-discovered (server.ts:26216)"
        },
        {
          "name": "DELETE /v1/master/customers/{customer_id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/customers/:customer_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                "customers",
                ":customer_id"
              ],
              "variable": [
                {
                  "key": "customer_id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: customer_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/master/customers/{customer_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DELETE /v1/master/customers/:customer_id — auto-discovered (server.ts:26325)"
        },
        {
          "name": "GET /v1/master/customers/{customer_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/customers/:customer_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                "customers",
                ":customer_id"
              ],
              "variable": [
                {
                  "key": "customer_id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: customer_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/master/customers/{customer_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/master/customers/:customer_id — auto-discovered (server.ts:26196)"
        },
        {
          "name": "PATCH /v1/master/customers/{customer_id}",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/customers/:customer_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                "customers",
                ":customer_id"
              ],
              "variable": [
                {
                  "key": "customer_id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: customer_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PATCH /v1/master/customers/{customer_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PATCH /v1/master/customers/:customer_id — auto-discovered (server.ts:26275)"
        },
        {
          "name": "GET /v1/master/customers/kyc-expiring",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/customers/kyc-expiring",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                "customers",
                "kyc-expiring"
              ]
            },
            "description": "already expired. Drives the \"KYC re-verification queue\" SPA tile. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/master/customers/kyc-expiring",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "hot-list: customers whose KYC is within N days of expiry OR"
        },
        {
          "name": "GET /v1/master/customers/types",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/customers/types",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                "customers",
                "types"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/master/customers/types",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/master/customers/types — auto-discovered (server.ts:26050)"
        }
      ]
    },
    {
      "name": "/v1/master/geographies",
      "description": "6 route(s) under /v1/master/geographies",
      "item": [
        {
          "name": "GET /v1/master/geographies",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/geographies",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                "geographies"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/master/geographies",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "— tenant-scoped list with optional filters. */"
        },
        {
          "name": "POST /v1/master/geographies",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/geographies",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                "geographies"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/master/geographies",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/master/geographies — auto-discovered (server.ts:25871)"
        },
        {
          "name": "DELETE /v1/master/geographies/{country_code}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/geographies/:country_code",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                "geographies",
                ":country_code"
              ],
              "variable": [
                {
                  "key": "country_code",
                  "value": "<country_code>",
                  "description": "Path parameter: country_code"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/master/geographies/{country_code}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DELETE /v1/master/geographies/:country_code — auto-discovered (server.ts:25981)"
        },
        {
          "name": "GET /v1/master/geographies/{country_code}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/geographies/:country_code",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                "geographies",
                ":country_code"
              ],
              "variable": [
                {
                  "key": "country_code",
                  "value": "<country_code>",
                  "description": "Path parameter: country_code"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/master/geographies/{country_code}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/master/geographies/:country_code — auto-discovered (server.ts:25850)"
        },
        {
          "name": "PATCH /v1/master/geographies/{country_code}",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/geographies/:country_code",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                "geographies",
                ":country_code"
              ],
              "variable": [
                {
                  "key": "country_code",
                  "value": "<country_code>",
                  "description": "Path parameter: country_code"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PATCH /v1/master/geographies/{country_code}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PATCH /v1/master/geographies/:country_code — auto-discovered (server.ts:25930)"
        },
        {
          "name": "GET /v1/master/geographies/risk-levels",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/geographies/risk-levels",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                "geographies",
                "risk-levels"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/master/geographies/risk-levels",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/master/geographies/risk-levels — auto-discovered (server.ts:25777)"
        }
      ]
    },
    {
      "name": "/v1/master/policies",
      "description": "6 route(s) under /v1/master/policies",
      "item": [
        {
          "name": "GET /v1/master/policies",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/policies",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                "policies"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/master/policies",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/master/policies — auto-discovered (server.ts:26979)"
        },
        {
          "name": "POST /v1/master/policies",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/policies",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                "policies"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/master/policies",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/master/policies — auto-discovered (server.ts:27053)"
        },
        {
          "name": "DELETE /v1/master/policies/{policy_type_id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/policies/:policy_type_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                "policies",
                ":policy_type_id"
              ],
              "variable": [
                {
                  "key": "policy_type_id",
                  "value": "<policy_type_id>",
                  "description": "Path parameter: policy_type_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/master/policies/{policy_type_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DELETE /v1/master/policies/:policy_type_id — auto-discovered (server.ts:27162)"
        },
        {
          "name": "GET /v1/master/policies/{policy_type_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/policies/:policy_type_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                "policies",
                ":policy_type_id"
              ],
              "variable": [
                {
                  "key": "policy_type_id",
                  "value": "<policy_type_id>",
                  "description": "Path parameter: policy_type_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/master/policies/{policy_type_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/master/policies/:policy_type_id — auto-discovered (server.ts:27030)"
        },
        {
          "name": "PATCH /v1/master/policies/{policy_type_id}",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/policies/:policy_type_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                "policies",
                ":policy_type_id"
              ],
              "variable": [
                {
                  "key": "policy_type_id",
                  "value": "<policy_type_id>",
                  "description": "Path parameter: policy_type_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PATCH /v1/master/policies/{policy_type_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PATCH /v1/master/policies/:policy_type_id — auto-discovered (server.ts:27112)"
        },
        {
          "name": "GET /v1/master/policies/categories",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/policies/categories",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                "policies",
                "categories"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/master/policies/categories",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/master/policies/categories — auto-discovered (server.ts:26959)"
        }
      ]
    },
    {
      "name": "/v1/master/sectors",
      "description": "6 route(s) under /v1/master/sectors",
      "item": [
        {
          "name": "GET /v1/master/sectors",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/sectors",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                "sectors"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/master/sectors",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/master/sectors — auto-discovered (server.ts:25547)"
        },
        {
          "name": "POST /v1/master/sectors",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/sectors",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                "sectors"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/master/sectors",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/master/sectors — auto-discovered (server.ts:25592)"
        },
        {
          "name": "DELETE /v1/master/sectors/{sector_id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/sectors/:sector_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                "sectors",
                ":sector_id"
              ],
              "variable": [
                {
                  "key": "sector_id",
                  "value": "<sector_id>",
                  "description": "Path parameter: sector_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/master/sectors/{sector_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Recovery Center. 204 on success; 404 unknown. */"
        },
        {
          "name": "GET /v1/master/sectors/{sector_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/sectors/:sector_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                "sectors",
                ":sector_id"
              ],
              "variable": [
                {
                  "key": "sector_id",
                  "value": "<sector_id>",
                  "description": "Path parameter: sector_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/master/sectors/{sector_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/master/sectors/:sector_id — auto-discovered (server.ts:25571)"
        },
        {
          "name": "PATCH /v1/master/sectors/{sector_id}",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/sectors/:sector_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                "sectors",
                ":sector_id"
              ],
              "variable": [
                {
                  "key": "sector_id",
                  "value": "<sector_id>",
                  "description": "Path parameter: sector_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PATCH /v1/master/sectors/{sector_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PATCH /v1/master/sectors/:sector_id — auto-discovered (server.ts:25651)"
        },
        {
          "name": "GET /v1/master/sectors/categories",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/master/sectors/categories",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "master",
                "sectors",
                "categories"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/master/sectors/categories",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/master/sectors/categories — auto-discovered (server.ts:25534)"
        }
      ]
    },
    {
      "name": "/v1/metadata/lineage",
      "description": "5 route(s) under /v1/metadata/lineage",
      "item": [
        {
          "name": "GET /v1/metadata/lineage/catalog",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/metadata/lineage/catalog",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "metadata",
                "lineage",
                "catalog"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/metadata/lineage/catalog",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/metadata/lineage/catalog — auto-discovered (server.ts:22321)"
        },
        {
          "name": "GET /v1/metadata/lineage/datasets/{dataset_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/metadata/lineage/datasets/:dataset_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "metadata",
                "lineage",
                "datasets",
                ":dataset_id"
              ],
              "variable": [
                {
                  "key": "dataset_id",
                  "value": "<dataset_id>",
                  "description": "Path parameter: dataset_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/metadata/lineage/datasets/{dataset_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/metadata/lineage/datasets/:dataset_id — auto-discovered (server.ts:22345)"
        },
        {
          "name": "GET /v1/metadata/lineage/datasets/{dataset_id}/downstream",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/metadata/lineage/datasets/:dataset_id/downstream",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "metadata",
                "lineage",
                "datasets",
                ":dataset_id",
                "downstream"
              ],
              "variable": [
                {
                  "key": "dataset_id",
                  "value": "<dataset_id>",
                  "description": "Path parameter: dataset_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/metadata/lineage/datasets/{dataset_id}/downstream",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/metadata/lineage/datasets/:dataset_id/downstream — auto-discovered (server.ts:22434)"
        },
        {
          "name": "GET /v1/metadata/lineage/datasets/{dataset_id}/impact",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/metadata/lineage/datasets/:dataset_id/impact",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "metadata",
                "lineage",
                "datasets",
                ":dataset_id",
                "impact"
              ],
              "variable": [
                {
                  "key": "dataset_id",
                  "value": "<dataset_id>",
                  "description": "Path parameter: dataset_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/metadata/lineage/datasets/{dataset_id}/impact",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/metadata/lineage/datasets/:dataset_id/impact — auto-discovered (server.ts:22496)"
        },
        {
          "name": "GET /v1/metadata/lineage/datasets/{dataset_id}/upstream",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/metadata/lineage/datasets/:dataset_id/upstream",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "metadata",
                "lineage",
                "datasets",
                ":dataset_id",
                "upstream"
              ],
              "variable": [
                {
                  "key": "dataset_id",
                  "value": "<dataset_id>",
                  "description": "Path parameter: dataset_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/metadata/lineage/datasets/{dataset_id}/upstream",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/metadata/lineage/datasets/:dataset_id/upstream — auto-discovered (server.ts:22370)"
        }
      ]
    },
    {
      "name": "/v1/notices/issue",
      "description": "1 route(s) under /v1/notices/issue",
      "item": [
        {
          "name": "POST /v1/notices/issue",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notices/issue",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notices",
                "issue"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/notices/issue",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/notices/issue — auto-discovered (server.ts:41566)"
        }
      ]
    },
    {
      "name": "/v1/notices/issued",
      "description": "1 route(s) under /v1/notices/issued",
      "item": [
        {
          "name": "GET /v1/notices/issued",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notices/issued",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notices",
                "issued"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/notices/issued",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/notices/issued — auto-discovered (server.ts:41603)"
        }
      ]
    },
    {
      "name": "/v1/notices/preview",
      "description": "1 route(s) under /v1/notices/preview",
      "item": [
        {
          "name": "POST /v1/notices/preview",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notices/preview",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notices",
                "preview"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/notices/preview",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/notices/preview — auto-discovered (server.ts:41539)"
        }
      ]
    },
    {
      "name": "/v1/notices/templates",
      "description": "2 route(s) under /v1/notices/templates",
      "item": [
        {
          "name": "GET /v1/notices/templates",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notices/templates",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notices",
                "templates"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/notices/templates",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Module #17 — Notice generation (§2.3)"
        },
        {
          "name": "GET /v1/notices/templates/{template_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notices/templates/:template_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notices",
                "templates",
                ":template_id"
              ],
              "variable": [
                {
                  "key": "template_id",
                  "value": "<template_id>",
                  "description": "Path parameter: template_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/notices/templates/{template_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/notices/templates/:template_id — auto-discovered (server.ts:41517)"
        }
      ]
    },
    {
      "name": "/v1/notifications/channel-cost-estimate",
      "description": "1 route(s) under /v1/notifications/channel-cost-estimate",
      "item": [
        {
          "name": "GET /v1/notifications/channel-cost-estimate",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/channel-cost-estimate",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "channel-cost-estimate"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/notifications/channel-cost-estimate",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/notifications/channel-cost-estimate — auto-discovered (server.ts:10296)"
        }
      ]
    },
    {
      "name": "/v1/notifications/channel-performance",
      "description": "1 route(s) under /v1/notifications/channel-performance",
      "item": [
        {
          "name": "GET /v1/notifications/channel-performance",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/channel-performance",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "channel-performance"
              ]
            },
            "description": "avg_per_day, most_active_template, peak_send_hour, distinct_\nrecipients_30d. Fleet: total_sent_30d, busiest_channel,\nquietest_channel, combined_distinct_recipients. RBAC audit:read. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/notifications/channel-performance",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "channel analytics over the trailing 30-day window: total_sent_30d,"
        }
      ]
    },
    {
      "name": "/v1/notifications/channel-synergy",
      "description": "1 route(s) under /v1/notifications/channel-synergy",
      "item": [
        {
          "name": "GET /v1/notifications/channel-synergy",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/channel-synergy",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "channel-synergy"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/notifications/channel-synergy",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/notifications/channel-synergy — auto-discovered (server.ts:10262)"
        }
      ]
    },
    {
      "name": "/v1/notifications/daily-volume",
      "description": "1 route(s) under /v1/notifications/daily-volume",
      "item": [
        {
          "name": "GET /v1/notifications/daily-volume",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/daily-volume",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "daily-volume"
              ]
            },
            "description": "total + by_channel (3 keys: email/sms/push). Every day in\n[window_start, window_end] emitted even when zero — stable\ntime-series x-axis. Envelope: peak_day (earliest-day tie-break),\nmean_per_day, growth_rate (second-half vs first-half; null when\nfirst-half=0 / days<2), busiest_channel (canonical email > sms >\npush tie-break). Default days=30, bounds [1, 365]. 400\nEWS_400_invalid_input on out-of-range. Mirror of M15.11 audit\ndaily volume shape. Drives \"are we sending more notifications\nthis month than last?\" trend question. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/notifications/daily-volume",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "LINE view across N consecutive UTC calendar days. Per-day bucket:"
        }
      ]
    },
    {
      "name": "/v1/notifications/delivery-failure-analysis",
      "description": "1 route(s) under /v1/notifications/delivery-failure-analysis",
      "item": [
        {
          "name": "GET /v1/notifications/delivery-failure-analysis",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/delivery-failure-analysis",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "delivery-failure-analysis"
              ]
            },
            "description": "Drains recent entries from email/sms/push transports (500 each)\nand computes per-channel estimated failure stats: {total_sent,\nestimated_failures, failure_rate, top_failure_templates cap 3}.\nEnvelope: by_channel {email, sms, push}, overall_failure_rate\n(weighted; null when nothing sent), most_reliable_channel +\nleast_reliable_channel (null when nothing sent). Uses\ndeterministic PRNG estimation since the StubTransport only stores\nsuccessful sends. Mirror of M3.6 failure-pattern clustering for\nthe notifications surface. RBAC audit:read. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/notifications/delivery-failure-analysis",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Notification delivery failure analysis across all 3 channels."
        }
      ]
    },
    {
      "name": "/v1/notifications/delivery-time-analysis",
      "description": "1 route(s) under /v1/notifications/delivery-time-analysis",
      "item": [
        {
          "name": "GET /v1/notifications/delivery-time-analysis",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/delivery-time-analysis",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "delivery-time-analysis"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/notifications/delivery-time-analysis",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/notifications/delivery-time-analysis — auto-discovered (server.ts:10420)"
        }
      ]
    },
    {
      "name": "/v1/notifications/delivery-trend",
      "description": "1 route(s) under /v1/notifications/delivery-trend",
      "item": [
        {
          "name": "GET /v1/notifications/delivery-trend",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/delivery-trend",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "delivery-trend"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/notifications/delivery-trend",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "trend of notification delivery success rates across all 3 channels. */"
        }
      ]
    },
    {
      "name": "/v1/notifications/email",
      "description": "4 route(s) under /v1/notifications/email",
      "item": [
        {
          "name": "GET /v1/notifications/email/log",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/email/log",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "email",
                "log"
              ]
            },
            "description": "Recent ledger entries scoped to the caller's tenant. Admin-only —\nthe ledger contains rendered subject + body which may include PII."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/notifications/email/log",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/notifications/email/log?limit=50"
        },
        {
          "name": "POST /v1/notifications/email/preview",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/email/preview",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "email",
                "preview"
              ]
            },
            "description": "body: { template_id, template_vars }\nRenders a template + vars to (subject, body_text, body_html?) without\nsending. Useful for the SPA preview pane.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/notifications/email/preview",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/notifications/email/preview"
        },
        {
          "name": "POST /v1/notifications/email/send",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/email/send",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "email",
                "send"
              ]
            },
            "description": "body: EmailMessageInput\nValidates + dispatches via the configured transport. Mirrors the\nRBAC of the publish endpoint (audit:read = admin) — sending email\nis a higher-trust op than reading the SSE stream.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/notifications/email/send",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/notifications/email/send"
        },
        {
          "name": "GET /v1/notifications/email/templates",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/email/templates",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "email",
                "templates"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/notifications/email/templates",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/notifications/email/templates — auto-discovered (server.ts:10503)"
        }
      ]
    },
    {
      "name": "/v1/notifications/ledger-analytics",
      "description": "1 route(s) under /v1/notifications/ledger-analytics",
      "item": [
        {
          "name": "GET /v1/notifications/ledger-analytics",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/ledger-analytics",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "ledger-analytics"
              ]
            },
            "description": "ledgers. Per-channel totals + by_template_id (cap 5) + top\nrecipients (cap 10) + most_recent_at. Lets the SPA show a\nunified \"notifications activity\" panel without 3 round-trips. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/notifications/ledger-analytics",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "channel rollup over M10.1 email + M10.2 SMS + M10.3 push send"
        }
      ]
    },
    {
      "name": "/v1/notifications/per-recipient",
      "description": "1 route(s) under /v1/notifications/per-recipient",
      "item": [
        {
          "name": "GET /v1/notifications/per-recipient",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/per-recipient",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "per-recipient"
              ]
            },
            "description": "SMS phones, and push user_ids surface as separate rows\n(recipient_kind disambiguates). Sorted by total_sent desc\nwith recipient_id asc tie-break. Envelope adds top_recipients\n(cap 20) + by_kind distinct counts + most_active_recipient.\nMirror of M15.8 (audit per-actor activity) for notifications. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/notifications/per-recipient",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "per-recipient list across all 3 channels. Email recipients,"
        }
      ]
    },
    {
      "name": "/v1/notifications/preferences",
      "description": "7 route(s) under /v1/notifications/preferences",
      "item": [
        {
          "name": "GET /v1/notifications/preferences/effective",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/preferences/effective",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "preferences",
                "effective"
              ]
            },
            "description": "resolution chain (user_override / tenant_default /\nplatform_default) per channel. `audit:read` RBAC; admin-only\nview of any user. Mounted BEFORE /me + /:username catch-all\nroutes so the literal /effective segment isn't captured. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/notifications/preferences/effective",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "(T6 M10.10) — effective preferences with the full 3-way"
        },
        {
          "name": "GET /v1/notifications/preferences/me",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/preferences/me",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "preferences",
                "me"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/notifications/preferences/me",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Always 200 (defaults to all-enabled for never-touched users). */"
        },
        {
          "name": "PUT /v1/notifications/preferences/me",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/preferences/me",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "preferences",
                "me"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PUT /v1/notifications/preferences/me",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "partial update; at least one channel must be supplied. */"
        },
        {
          "name": "PUT /v1/notifications/preferences/me/quiet-hours",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/preferences/me/quiet-hours",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "preferences",
                "me",
                "quiet-hours"
              ]
            },
            "description": "to set, or `null` to clear. */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PUT /v1/notifications/preferences/me/quiet-hours",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "set or clear the caller's mute window. Body { start_hour, end_hour }"
        },
        {
          "name": "POST /v1/notifications/preferences/me/reset",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/preferences/me/reset",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "preferences",
                "me",
                "reset"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/notifications/preferences/me/reset",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/notifications/preferences/me/reset — auto-discovered (server.ts:11255)"
        },
        {
          "name": "GET /v1/notifications/preferences/tenant-defaults",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/preferences/tenant-defaults",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "preferences",
                "tenant-defaults"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/notifications/preferences/tenant-defaults",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "per-tenant defaults; admin-only. */"
        },
        {
          "name": "PUT /v1/notifications/preferences/tenant-defaults",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/preferences/tenant-defaults",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "preferences",
                "tenant-defaults"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PUT /v1/notifications/preferences/tenant-defaults",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "defaults via partial patch. */"
        }
      ]
    },
    {
      "name": "/v1/notifications/priority-scores",
      "description": "1 route(s) under /v1/notifications/priority-scores",
      "item": [
        {
          "name": "GET /v1/notifications/priority-scores",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/priority-scores",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "priority-scores"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/notifications/priority-scores",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/notifications/priority-scores — auto-discovered (server.ts:10213)"
        }
      ]
    },
    {
      "name": "/v1/notifications/publish",
      "description": "1 route(s) under /v1/notifications/publish",
      "item": [
        {
          "name": "POST /v1/notifications/publish",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/publish",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "publish"
              ]
            },
            "description": "body: { level, title, body?, href? }\nAdmin-only — used by ops dashboards + tests to seed an event.\nThe scenario route publishes via the in-process bus directly, not\nvia this endpoint.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/notifications/publish",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/notifications/publish"
        }
      ]
    },
    {
      "name": "/v1/notifications/push",
      "description": "5 route(s) under /v1/notifications/push",
      "item": [
        {
          "name": "GET /v1/notifications/push/log",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/push/log",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "push",
                "log"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/notifications/push/log",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/notifications/push/log — auto-discovered (server.ts:10885)"
        },
        {
          "name": "GET /v1/notifications/push/platform-distribution",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/push/platform-distribution",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "push",
                "platform-distribution"
              ]
            },
            "description": "apns / web). Per-platform {dispatch_count (Σ per-device entries),\ndistinct_messages, distinct_users, by_status (every PushStatus\nat 0), most_recent_at}. Envelope: most_used_platform (canonical\ntie-break: fcm beats apns), unused_platforms[], overall_by_status.\nMirror of M14.27 / M5.16 / M3.13 1D distribution pattern. Drives\n\"is our push skewed to FCM vs APNS?\" view. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/notifications/push/platform-distribution",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "1D pivot over the M10.3 push ledger by PushPlatform (fcm /"
        },
        {
          "name": "POST /v1/notifications/push/preview",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/push/preview",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "push",
                "preview"
              ]
            },
            "description": "Render a template + vars to (title, body, missing_vars[]) without sending.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/notifications/push/preview",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/notifications/push/preview body: { template_id, template_vars }"
        },
        {
          "name": "POST /v1/notifications/push/send",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/push/send",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "push",
                "send"
              ]
            },
            "description": "Validates + dispatches via the configured transport. Admin-only —\nsending push is higher-trust than reading the bell stream.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/notifications/push/send",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/notifications/push/send body: PushMessageInput"
        },
        {
          "name": "GET /v1/notifications/push/templates",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/push/templates",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "push",
                "templates"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/notifications/push/templates",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/notifications/push/templates — auto-discovered (server.ts:10755)"
        }
      ]
    },
    {
      "name": "/v1/notifications/retry-patterns",
      "description": "1 route(s) under /v1/notifications/retry-patterns",
      "item": [
        {
          "name": "GET /v1/notifications/retry-patterns",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/retry-patterns",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "retry-patterns"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/notifications/retry-patterns",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "T6 M10.26 — Notification retry pattern analysis"
        }
      ]
    },
    {
      "name": "/v1/notifications/sms",
      "description": "4 route(s) under /v1/notifications/sms",
      "item": [
        {
          "name": "GET /v1/notifications/sms/log",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/sms/log",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "sms",
                "log"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/notifications/sms/log",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/notifications/sms/log — auto-discovered (server.ts:10736)"
        },
        {
          "name": "POST /v1/notifications/sms/preview",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/sms/preview",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "sms",
                "preview"
              ]
            },
            "description": "Render a template + vars to a body without sending.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/notifications/sms/preview",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/notifications/sms/preview body: { template_id, template_vars }"
        },
        {
          "name": "POST /v1/notifications/sms/send",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/sms/send",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "sms",
                "send"
              ]
            },
            "description": "Validates + dispatches via the configured transport. Admin-only —\nsending SMS is higher-trust than reading the bell stream.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/notifications/sms/send",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/notifications/sms/send body: SmsMessageInput"
        },
        {
          "name": "GET /v1/notifications/sms/templates",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/sms/templates",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "sms",
                "templates"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/notifications/sms/templates",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/notifications/sms/templates — auto-discovered (server.ts:10634)"
        }
      ]
    },
    {
      "name": "/v1/notifications/stream",
      "description": "1 route(s) under /v1/notifications/stream",
      "item": [
        {
          "name": "GET /v1/notifications/stream",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/stream",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "stream"
              ]
            },
            "description": "Server-Sent Events feed of in-app notifications. The SPA bell connects\nhere and stays open; reconnect is handled by the browser's EventSource.\nAuth: any role with cases:list (i.e. all 5 seed roles) — notifications\nare intentionally low-sensitivity (titles + deep links, no PII).\nDev-mode fallback: the browser's native EventSource cannot send custom\nheaders, so a missing X-Tenant-ID is defaulted to BANK_DEMO for SSE\nconnections only (identified by Accept: text/event-stream or no Accept\nthat looks like a JSON client). The Vite proxy also injects the header\nvia its `headers` option, so in practice the header will be present."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/notifications/stream",
                  "// Streaming route — newman can't capture a status for SSE/WebSocket.",
                  "// Skip status + duration assertions.",
                  "pm.test('streaming route — request issued', () => pm.expect(true).to.equal(true));",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/notifications/stream"
        }
      ]
    },
    {
      "name": "/v1/notifications/template-freshness",
      "description": "1 route(s) under /v1/notifications/template-freshness",
      "item": [
        {
          "name": "GET /v1/notifications/template-freshness",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/template-freshness",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "template-freshness"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/notifications/template-freshness",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/notifications/template-freshness — auto-discovered (server.ts:10303)"
        }
      ]
    },
    {
      "name": "/v1/notifications/template-render-times",
      "description": "1 route(s) under /v1/notifications/template-render-times",
      "item": [
        {
          "name": "GET /v1/notifications/template-render-times",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/template-render-times",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "template-render-times"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/notifications/template-render-times",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/notifications/template-render-times — auto-discovered (server.ts:10254)"
        }
      ]
    },
    {
      "name": "/v1/notifications/template-usage",
      "description": "1 route(s) under /v1/notifications/template-usage",
      "item": [
        {
          "name": "GET /v1/notifications/template-usage",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/template-usage",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "template-usage"
              ]
            },
            "description": "in the M10.11 catalog (4 email + 4 SMS + 4 push = 12 templates),\nemits {total_sent, distinct_recipients, most_recent_sent_at}.\nEnvelope: total_sent_all_templates, total_templates_in_catalog,\ntotal_templates_used, templates[] in canonical order (always\nemits every catalog entry — unused at 0), unused_templates[]\n(total_sent=0 subset; cleanup candidates), by_channel rollup\n(total_sent + used_count + catalog_count per channel),\nmost_used_template (canonical tie-break), stale_templates[]\n(used but most_recent_sent_at > 30 days ago; sorted oldest-first).\nMirror of M10.12 (by-channel) but pivoted by TEMPLATE. Drives\nthe SPA \"template usage\" inventory + deprecation candidates. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/notifications/template-usage",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "channel ledgers (email/sms/push) by template_id. For each template"
        }
      ]
    },
    {
      "name": "/v1/notifications/templates",
      "description": "1 route(s) under /v1/notifications/templates",
      "item": [
        {
          "name": "GET /v1/notifications/templates/catalog",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/templates/catalog",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "templates",
                "catalog"
              ]
            },
            "description": "template_id, description, required_vars}. Envelope adds total +\nby_channel + distinct_required_vars (union for form-builder UX).\nLets the SPA's notification picker enumerate everything in one\nround-trip + one consistent shape (vs 3 channel-specific routes\nwith 3 different shapes). Platform-static. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/notifications/templates/catalog",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "template catalog across email + SMS + push. Per-template {channel,"
        }
      ]
    },
    {
      "name": "/v1/notifications/variables",
      "description": "2 route(s) under /v1/notifications/variables",
      "item": [
        {
          "name": "GET /v1/notifications/variables/channel-matrix",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/variables/channel-matrix",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "variables",
                "channel-matrix"
              ]
            },
            "description": "variables (open axis, sorted asc) × cols = 3 channels (canonical\nemail → sms → push). Cell counts the templates in that channel\nrequiring this variable. Per-row {variable, total, by_channel\n(3 keys at 0 when absent), channels_with[] canonical,\nchannels_without[] canonical, spans_all_channels}. Per-col\n{channel, total, distinct_variables, templates_count,\ntop_variables cap 5 sorted count desc + variable asc}. Envelope:\npeak_cell (canonical iteration tie-break — variables asc ×\nchannels canonical), empty_cells[] canonical row-major,\nmost_universal_variable (most distinct non-zero channels +\ncanonical asc tie-break), cross_channel_variables (spans_all=true),\nsingle_channel_variables (exactly 1 non-zero channel). Mirror of\nM10.13 with 2D matrix shape. Platform-static. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/notifications/variables/channel-matrix",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "cross-tab elevating M10.13's inverted index. Rows = distinct"
        },
        {
          "name": "GET /v1/notifications/variables/index",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/variables/index",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "variables",
                "index"
              ]
            },
            "description": "reference_count + channels[] + spans_all_channels. Envelope:\nsingle_use_variables (refactor candidates), shared_variables\n(cross-channel concepts), most_referenced. Answers \"which\ntemplates require {{X}}?\". Mirror of M5.15 / M4.11 reverse\ncross-reference shape. Platform-static. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/notifications/variables/index",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "index over the M10.11 catalog. Per-variable: templates[] +"
        }
      ]
    },
    {
      "name": "/v1/notifications/webhook",
      "description": "5 route(s) under /v1/notifications/webhook",
      "item": [
        {
          "name": "POST /v1/notifications/webhook/send",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/webhook/send",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "webhook",
                "send"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/notifications/webhook/send",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/notifications/webhook/send — auto-discovered (server.ts:10975)"
        },
        {
          "name": "GET /v1/notifications/webhook/subscriptions",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/webhook/subscriptions",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "webhook",
                "subscriptions"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/notifications/webhook/subscriptions",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/notifications/webhook/subscriptions — auto-discovered (server.ts:10903)"
        },
        {
          "name": "POST /v1/notifications/webhook/subscriptions",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/webhook/subscriptions",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "webhook",
                "subscriptions"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/notifications/webhook/subscriptions",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/notifications/webhook/subscriptions — auto-discovered (server.ts:10915)"
        },
        {
          "name": "DELETE /v1/notifications/webhook/subscriptions/{webhook_id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/webhook/subscriptions/:webhook_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "webhook",
                "subscriptions",
                ":webhook_id"
              ],
              "variable": [
                {
                  "key": "webhook_id",
                  "value": "<webhook_id>",
                  "description": "Path parameter: webhook_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/notifications/webhook/subscriptions/{webhook_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DELETE /v1/notifications/webhook/subscriptions/:webhook_id — auto-discovered (server.ts:10954)"
        },
        {
          "name": "GET /v1/notifications/webhook/subscriptions/{webhook_id}/deliveries",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/notifications/webhook/subscriptions/:webhook_id/deliveries",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "notifications",
                "webhook",
                "subscriptions",
                ":webhook_id",
                "deliveries"
              ],
              "variable": [
                {
                  "key": "webhook_id",
                  "value": "<webhook_id>",
                  "description": "Path parameter: webhook_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/notifications/webhook/subscriptions/{webhook_id}/deliveries",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/notifications/webhook/subscriptions/:webhook_id/deliveries — auto-discovered (server.ts:1100"
        }
      ]
    },
    {
      "name": "/v1/rbac/actions",
      "description": "1 route(s) under /v1/rbac/actions",
      "item": [
        {
          "name": "GET /v1/rbac/actions",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/rbac/actions",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "rbac",
                "actions"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/rbac/actions",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/rbac/actions — auto-discovered (server.ts:38191)"
        }
      ]
    },
    {
      "name": "/v1/rbac/check",
      "description": "1 route(s) under /v1/rbac/check",
      "item": [
        {
          "name": "POST /v1/rbac/check",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/rbac/check",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "rbac",
                "check"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/rbac/check",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Body: `{ role?, module, action }`. role defaults to caller's. */"
        }
      ]
    },
    {
      "name": "/v1/rbac/matrix",
      "description": "3 route(s) under /v1/rbac/matrix",
      "item": [
        {
          "name": "GET /v1/rbac/matrix",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/rbac/matrix",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "rbac",
                "matrix"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/rbac/matrix",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/rbac/matrix — auto-discovered (server.ts:38209)"
        },
        {
          "name": "GET /v1/rbac/matrix/{role}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/rbac/matrix/:role",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "rbac",
                "matrix",
                ":role"
              ],
              "variable": [
                {
                  "key": "role",
                  "value": "<role>",
                  "description": "Path parameter: role"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/rbac/matrix/{role}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/rbac/matrix/:role — auto-discovered (server.ts:38216)"
        },
        {
          "name": "PUT /v1/rbac/matrix/{role}",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/rbac/matrix/:role",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "rbac",
                "matrix",
                ":role"
              ],
              "variable": [
                {
                  "key": "role",
                  "value": "<role>",
                  "description": "Path parameter: role"
                }
              ]
            },
            "description": "Touches only the cells in the payload — omitted cells are left alone.\nconfigure-action on permission_matrix module is the natural future gate;\nfor now we route through audit:read to stay backward-compatible while\nthe SPA editor lands. */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PUT /v1/rbac/matrix/{role}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Body: `{ grants: { [module_id]: { [action]: boolean } } }`."
        }
      ]
    },
    {
      "name": "/v1/rbac/me",
      "description": "1 route(s) under /v1/rbac/me",
      "item": [
        {
          "name": "GET /v1/rbac/me/permissions",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/rbac/me/permissions",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "rbac",
                "me",
                "permissions"
              ]
            },
            "description": "(JWT auth). Drives the SPA's useCan() hook + UI gating. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/rbac/me/permissions",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Reads role from x-apex-role header (test convention) or req.user.role"
        }
      ]
    },
    {
      "name": "/v1/rbac/modules",
      "description": "1 route(s) under /v1/rbac/modules",
      "item": [
        {
          "name": "GET /v1/rbac/modules",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/rbac/modules",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "rbac",
                "modules"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/rbac/modules",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/rbac/modules — auto-discovered (server.ts:38197)"
        }
      ]
    },
    {
      "name": "/v1/rbac/roles",
      "description": "1 route(s) under /v1/rbac/roles",
      "item": [
        {
          "name": "GET /v1/rbac/roles",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/rbac/roles",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "rbac",
                "roles"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/rbac/roles",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/rbac/roles — auto-discovered (server.ts:38203)"
        }
      ]
    },
    {
      "name": "/v1/recon/dashboard",
      "description": "1 route(s) under /v1/recon/dashboard",
      "item": [
        {
          "name": "GET /v1/recon/dashboard",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/recon/dashboard",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "recon",
                "dashboard"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/recon/dashboard",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/recon/dashboard — auto-discovered (server.ts:28607)"
        }
      ]
    },
    {
      "name": "/v1/recon/definitions",
      "description": "7 route(s) under /v1/recon/definitions",
      "item": [
        {
          "name": "GET /v1/recon/definitions",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/recon/definitions",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "recon",
                "definitions"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/recon/definitions",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/recon/definitions — auto-discovered (server.ts:28204)"
        },
        {
          "name": "POST /v1/recon/definitions",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/recon/definitions",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "recon",
                "definitions"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/recon/definitions",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/recon/definitions — auto-discovered (server.ts:28270)"
        },
        {
          "name": "DELETE /v1/recon/definitions/{recon_id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/recon/definitions/:recon_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "recon",
                "definitions",
                ":recon_id"
              ],
              "variable": [
                {
                  "key": "recon_id",
                  "value": "<recon_id>",
                  "description": "Path parameter: recon_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/recon/definitions/{recon_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DELETE /v1/recon/definitions/:recon_id — auto-discovered (server.ts:28379)"
        },
        {
          "name": "GET /v1/recon/definitions/{recon_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/recon/definitions/:recon_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "recon",
                "definitions",
                ":recon_id"
              ],
              "variable": [
                {
                  "key": "recon_id",
                  "value": "<recon_id>",
                  "description": "Path parameter: recon_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/recon/definitions/{recon_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/recon/definitions/:recon_id — auto-discovered (server.ts:28250)"
        },
        {
          "name": "PATCH /v1/recon/definitions/{recon_id}",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/recon/definitions/:recon_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "recon",
                "definitions",
                ":recon_id"
              ],
              "variable": [
                {
                  "key": "recon_id",
                  "value": "<recon_id>",
                  "description": "Path parameter: recon_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PATCH /v1/recon/definitions/{recon_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PATCH /v1/recon/definitions/:recon_id — auto-discovered (server.ts:28329)"
        },
        {
          "name": "POST /v1/recon/definitions/{recon_id}/inject-drop",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/recon/definitions/:recon_id/inject-drop",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "recon",
                "definitions",
                ":recon_id",
                "inject-drop"
              ],
              "variable": [
                {
                  "key": "recon_id",
                  "value": "<recon_id>",
                  "description": "Path parameter: recon_id"
                }
              ]
            },
            "description": "the next /run call (with no records[] payload) synthesises records\nthat omit this key in the staging leg, producing a non-zero gap.\nRequired for the spec acceptance test. */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/recon/definitions/{recon_id}/inject-drop",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "body { row_key, leg? }. Registers a deliberate row-drop for the demo:"
        },
        {
          "name": "POST /v1/recon/definitions/{recon_id}/run",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/recon/definitions/:recon_id/run",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "recon",
                "definitions",
                ":recon_id",
                "run"
              ],
              "variable": [
                {
                  "key": "recon_id",
                  "value": "<recon_id>",
                  "description": "Path parameter: recon_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/recon/definitions/{recon_id}/run",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "{ source_records: any[], target_records: any[] }. */"
        }
      ]
    },
    {
      "name": "/v1/recon/kinds",
      "description": "1 route(s) under /v1/recon/kinds",
      "item": [
        {
          "name": "GET /v1/recon/kinds",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/recon/kinds",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "recon",
                "kinds"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/recon/kinds",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/recon/kinds — auto-discovered (server.ts:28188)"
        }
      ]
    },
    {
      "name": "/v1/recon/runs",
      "description": "3 route(s) under /v1/recon/runs",
      "item": [
        {
          "name": "GET /v1/recon/runs",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/recon/runs",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "recon",
                "runs"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/recon/runs",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/recon/runs — auto-discovered (server.ts:28550)"
        },
        {
          "name": "GET /v1/recon/runs/{run_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/recon/runs/:run_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "recon",
                "runs",
                ":run_id"
              ],
              "variable": [
                {
                  "key": "run_id",
                  "value": "<run_id>",
                  "description": "Path parameter: run_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/recon/runs/{run_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/recon/runs/:run_id — auto-discovered (server.ts:28587)"
        },
        {
          "name": "POST /v1/recon/runs/{run_id}/accept",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/recon/runs/:run_id/accept",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "recon",
                "runs",
                ":run_id",
                "accept"
              ],
              "variable": [
                {
                  "key": "run_id",
                  "value": "<run_id>",
                  "description": "Path parameter: run_id"
                }
              ]
            },
            "description": "Reason required (≤2000 chars). 404 unknown_run, 409 already_accepted. */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/recon/runs/{run_id}/accept",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Marks a recon run as accepted (e.g. \"known gap, signed off by ops\")."
        }
      ]
    },
    {
      "name": "/v1/recovery",
      "description": "1 route(s) under /v1/recovery",
      "item": [
        {
          "name": "GET /v1/recovery",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/recovery",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "recovery"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/recovery",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/recovery — auto-discovered (server.ts:38556)"
        }
      ]
    },
    {
      "name": "/v1/recovery/:recovery_id",
      "description": "3 route(s) under /v1/recovery/:recovery_id",
      "item": [
        {
          "name": "DELETE /v1/recovery/{recovery_id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/recovery/:recovery_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "recovery",
                ":recovery_id"
              ],
              "variable": [
                {
                  "key": "recovery_id",
                  "value": "<recovery_id>",
                  "description": "Path parameter: recovery_id"
                }
              ]
            },
            "description": "restorable. A future scheduled job hard-deletes rows older than\n30 days where purged_at IS NOT NULL. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/recovery/{recovery_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Marks purged_at/_by; the row stays for audit but is no longer"
        },
        {
          "name": "GET /v1/recovery/{recovery_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/recovery/:recovery_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "recovery",
                ":recovery_id"
              ],
              "variable": [
                {
                  "key": "recovery_id",
                  "value": "<recovery_id>",
                  "description": "Path parameter: recovery_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/recovery/{recovery_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/recovery/:recovery_id — auto-discovered (server.ts:38655)"
        },
        {
          "name": "POST /v1/recovery/{recovery_id}/restore",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/recovery/:recovery_id/restore",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "recovery",
                ":recovery_id",
                "restore"
              ],
              "variable": [
                {
                  "key": "recovery_id",
                  "value": "<recovery_id>",
                  "description": "Path parameter: recovery_id"
                }
              ]
            },
            "description": "pg). Returns 404 if recovery_id not found or already restored/purged. */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/recovery/{recovery_id}/restore",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Admin only. Returns 409 if the original_id already exists (cache or"
        }
      ]
    },
    {
      "name": "/v1/recovery/analytics",
      "description": "1 route(s) under /v1/recovery/analytics",
      "item": [
        {
          "name": "GET /v1/recovery/analytics",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/recovery/analytics",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "recovery",
                "analytics"
              ]
            },
            "description": "per-entity_type rollup, restore-vs-purge rates, time-to-restore\npercentiles. Drains the recovery store across all 3 statuses\nthen feeds the pure summarizeRecoveryActivity resolver.\nMounted BEFORE /v1/recovery/:recovery_id so the literal\n`/analytics` segment isn't captured by the param wildcard. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/recovery/analytics",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "daily archive/restore/purge volume timeline, top actors,"
        }
      ]
    },
    {
      "name": "/v1/recovery/purge-expired",
      "description": "1 route(s) under /v1/recovery/purge-expired",
      "item": [
        {
          "name": "POST /v1/recovery/purge-expired",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/recovery/purge-expired",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "recovery",
                "purge-expired"
              ]
            },
            "description": "Idempotent. Scoped to the caller's tenant.\nIntended scheduling pattern (no in-process timer — multi-instance\nsafe): an external cron (k8s CronJob / pg_cron / GitHub Actions\nscheduled job) POSTs this once a day per tenant. Operators can\nalso call it manually from the Recovery Center page.\nReturns { removed: N, cutoff: <ISO timestamp> } so callers can\nlog the daily reclamation. */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/recovery/purge-expired",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "recovery row whose purged_at is older than N days (default 30)."
        }
      ]
    },
    {
      "name": "/v1/recovery/stats",
      "description": "1 route(s) under /v1/recovery/stats",
      "item": [
        {
          "name": "GET /v1/recovery/stats",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/recovery/stats",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "recovery",
                "stats"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/recovery/stats",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/recovery/stats — auto-discovered (server.ts:38581)"
        }
      ]
    },
    {
      "name": "/v1/release/current",
      "description": "1 route(s) under /v1/release/current",
      "item": [
        {
          "name": "GET /v1/release/current/{environment}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/release/current/:environment",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "release",
                "current",
                ":environment"
              ],
              "variable": [
                {
                  "key": "environment",
                  "value": "<environment>",
                  "description": "Path parameter: environment"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/release/current/{environment}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/release/current/:environment — auto-discovered (server.ts:22804)"
        }
      ]
    },
    {
      "name": "/v1/release/history",
      "description": "5 route(s) under /v1/release/history",
      "item": [
        {
          "name": "GET /v1/release/history",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/release/history",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "release",
                "history"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/release/history",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/release/history — auto-discovered (server.ts:22838)"
        },
        {
          "name": "POST /v1/release/history",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/release/history",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "release",
                "history"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/release/history",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/release/history — auto-discovered (server.ts:22907)"
        },
        {
          "name": "DELETE /v1/release/history/{release_id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/release/history/:release_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "release",
                "history",
                ":release_id"
              ],
              "variable": [
                {
                  "key": "release_id",
                  "value": "<release_id>",
                  "description": "Path parameter: release_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/release/history/{release_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DELETE /v1/release/history/:release_id — auto-discovered (server.ts:23017)"
        },
        {
          "name": "GET /v1/release/history/{release_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/release/history/:release_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "release",
                "history",
                ":release_id"
              ],
              "variable": [
                {
                  "key": "release_id",
                  "value": "<release_id>",
                  "description": "Path parameter: release_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/release/history/{release_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/release/history/:release_id — auto-discovered (server.ts:22882)"
        },
        {
          "name": "PATCH /v1/release/history/{release_id}",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/release/history/:release_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "release",
                "history",
                ":release_id"
              ],
              "variable": [
                {
                  "key": "release_id",
                  "value": "<release_id>",
                  "description": "Path parameter: release_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PATCH /v1/release/history/{release_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PATCH /v1/release/history/:release_id — auto-discovered (server.ts:22966)"
        }
      ]
    },
    {
      "name": "/v1/release/info",
      "description": "1 route(s) under /v1/release/info",
      "item": [
        {
          "name": "GET /v1/release/info",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/release/info",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "release",
                "info"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/release/info",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/release/info — auto-discovered (server.ts:22783)"
        }
      ]
    },
    {
      "name": "/v1/system/monitoring",
      "description": "1 route(s) under /v1/system/monitoring",
      "item": [
        {
          "name": "GET /v1/system/monitoring",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/system/monitoring",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "system",
                "monitoring"
              ]
            },
            "description": "PDF §A4. Rolls up the 3 existing probe surfaces — external upstream\npings, M14 adapter fleet, M3.1 ingestion fleet — into a single ops\ndashboard payload. Pure composer; reads from existing probes only."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/system/monitoring",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Phase D.1 — System Monitoring dashboard (composer)"
        }
      ]
    },
    {
      "name": "/v1/tenants",
      "description": "2 route(s) under /v1/tenants",
      "item": [
        {
          "name": "GET /v1/tenants",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/tenants",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "tenants"
              ]
            },
            "description": "501 envelope when the tenant lookup doesn't expose `all()` (some\ntest stubs don't bother)."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/tenants",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/tenants — admin-only listing of every configured tenant."
        },
        {
          "name": "POST /v1/tenants",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/tenants",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "tenants"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/tenants",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/tenants — auto-discovered (server.ts:19772)"
        }
      ]
    },
    {
      "name": "/v1/tenants/:tenant_id",
      "description": "3 route(s) under /v1/tenants/:tenant_id",
      "item": [
        {
          "name": "DELETE /v1/tenants/{tenant_id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/tenants/:tenant_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                ":tenant_id"
              ],
              "variable": [
                {
                  "key": "tenant_id",
                  "value": "{{tenant_id}}",
                  "description": "Path parameter: tenant_id"
                }
              ]
            },
            "description": "204 on success. 404 envelope when missing. 409 envelope when the\ntenant is system-protected (BANK_DEMO is always protected)."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/tenants/{tenant_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DELETE /v1/tenants/:tenant_id — admin removes a tenant."
        },
        {
          "name": "PATCH /v1/tenants/{tenant_id}",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/tenants/:tenant_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                ":tenant_id"
              ],
              "variable": [
                {
                  "key": "tenant_id",
                  "value": "{{tenant_id}}",
                  "description": "Path parameter: tenant_id"
                }
              ]
            },
            "description": "tenant_id is immutable. 404 envelope when missing.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PATCH /v1/tenants/{tenant_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PATCH /v1/tenants/:tenant_id — admin updates name / channels / active."
        },
        {
          "name": "GET /v1/tenants/{tenant_id}/readiness",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/tenants/:tenant_id/readiness",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                ":tenant_id",
                "readiness"
              ],
              "variable": [
                {
                  "key": "tenant_id",
                  "value": "{{tenant_id}}",
                  "description": "Path parameter: tenant_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/tenants/{tenant_id}/readiness",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/tenants/:tenant_id/readiness — auto-discovered (server.ts:18634)"
        }
      ]
    },
    {
      "name": "/v1/tenants/activity-fingerprint",
      "description": "1 route(s) under /v1/tenants/activity-fingerprint",
      "item": [
        {
          "name": "GET /v1/tenants/activity-fingerprint",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/tenants/activity-fingerprint",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                "activity-fingerprint"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/tenants/activity-fingerprint",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/tenants/activity-fingerprint — auto-discovered (server.ts:18572)"
        }
      ]
    },
    {
      "name": "/v1/tenants/bulk-import",
      "description": "5 route(s) under /v1/tenants/bulk-import",
      "item": [
        {
          "name": "POST /v1/tenants/bulk-import",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/tenants/bulk-import",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                "bulk-import"
              ]
            },
            "description": "tenant_id,name,vertical,channels_allowed\nchannels_allowed cells are `;`-separated. */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/tenants/bulk-import",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Body: { csv: string, dry_run?: boolean }. Header row required:"
        },
        {
          "name": "POST /v1/tenants/bulk-import/apply",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/tenants/bulk-import/apply",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                "bulk-import",
                "apply"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/tenants/bulk-import/apply",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "consumes the preview + commits the snapshotted rows. */"
        },
        {
          "name": "POST /v1/tenants/bulk-import/preview",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/tenants/bulk-import/preview",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                "bulk-import",
                "preview"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/tenants/bulk-import/preview",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "with preview_id + dry-run summary. */"
        },
        {
          "name": "DELETE /v1/tenants/bulk-import/preview/{preview_id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/tenants/bulk-import/preview/:preview_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                "bulk-import",
                "preview",
                ":preview_id"
              ],
              "variable": [
                {
                  "key": "preview_id",
                  "value": "<preview_id>",
                  "description": "Path parameter: preview_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/tenants/bulk-import/preview/{preview_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DELETE /v1/tenants/bulk-import/preview/:preview_id — auto-discovered (server.ts:19741)"
        },
        {
          "name": "GET /v1/tenants/bulk-import/previews",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/tenants/bulk-import/previews",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                "bulk-import",
                "previews"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/tenants/bulk-import/previews",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/tenants/bulk-import/previews — auto-discovered (server.ts:19673)"
        }
      ]
    },
    {
      "name": "/v1/tenants/comparison-matrix",
      "description": "1 route(s) under /v1/tenants/comparison-matrix",
      "item": [
        {
          "name": "GET /v1/tenants/comparison-matrix",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/tenants/comparison-matrix",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                "comparison-matrix"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/tenants/comparison-matrix",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/tenants/comparison-matrix — auto-discovered (server.ts:19861)"
        }
      ]
    },
    {
      "name": "/v1/tenants/config-similarity",
      "description": "1 route(s) under /v1/tenants/config-similarity",
      "item": [
        {
          "name": "GET /v1/tenants/config-similarity",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/tenants/config-similarity",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                "config-similarity"
              ]
            },
            "description": "Returns pairs[] (cap 20) sorted by similarity desc, most_similar_pair,\nmost_divergent_pair, avg_similarity. Async (needs tenantLookup.all()).\n501 when lookup lacks all(). RBAC audit:read admin-only. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/tenants/config-similarity",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "tenants, computes Jaccard similarity over config override key sets."
        }
      ]
    },
    {
      "name": "/v1/tenants/feature-comparison",
      "description": "1 route(s) under /v1/tenants/feature-comparison",
      "item": [
        {
          "name": "GET /v1/tenants/feature-comparison",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/tenants/feature-comparison",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                "feature-comparison"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/tenants/feature-comparison",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/tenants/feature-comparison — auto-discovered (server.ts:19845)"
        }
      ]
    },
    {
      "name": "/v1/tenants/health-score",
      "description": "1 route(s) under /v1/tenants/health-score",
      "item": [
        {
          "name": "GET /v1/tenants/health-score",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/tenants/health-score",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                "health-score"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/tenants/health-score",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/tenants/health-score — auto-discovered (server.ts:19853)"
        }
      ]
    },
    {
      "name": "/v1/tenants/me",
      "description": "2 route(s) under /v1/tenants/me",
      "item": [
        {
          "name": "GET /v1/tenants/me",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/tenants/me",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                "me"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/tenants/me",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/tenants/me — auto-discovered (server.ts:18580)"
        },
        {
          "name": "GET /v1/tenants/me/readiness",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/tenants/me/readiness",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                "me",
                "readiness"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/tenants/me/readiness",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/tenants/me/readiness — auto-discovered (server.ts:18607)"
        }
      ]
    },
    {
      "name": "/v1/tenants/production-readiness",
      "description": "1 route(s) under /v1/tenants/production-readiness",
      "item": [
        {
          "name": "GET /v1/tenants/production-readiness",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/tenants/production-readiness",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                "production-readiness"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/tenants/production-readiness",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/tenants/production-readiness — auto-discovered (server.ts:19339)"
        }
      ]
    },
    {
      "name": "/v1/testing/bulk-upload",
      "description": "1 route(s) under /v1/testing/bulk-upload",
      "item": [
        {
          "name": "POST /v1/testing/bulk-upload",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/testing/bulk-upload",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "testing",
                "bulk-upload"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/testing/bulk-upload",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/testing/bulk-upload  — spec alias of /tests/bulk-upload"
        }
      ]
    },
    {
      "name": "/v1/testing/cases",
      "description": "6 route(s) under /v1/testing/cases",
      "item": [
        {
          "name": "GET /v1/testing/cases",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/testing/cases",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "testing",
                "cases"
              ]
            },
            "description": "The spec talks in terms of \"test cases\" + plural \"schedules\" + a\n/run-all endpoint. The existing routes (above) use \"tests\" /\n\"schedule\" — we keep them for backwards-compat with the SPA's\nexisting wiring and add SPEC-COMPLIANT aliases here. Both surfaces\nshare the SAME store functions in testing_hub.ts — no duplication.\nGET /v1/testing/cases  — spec alias for /tests"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/testing/cases",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "M6.3 — Testing Hub spec routes"
        },
        {
          "name": "POST /v1/testing/cases",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/testing/cases",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "testing",
                "cases"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/testing/cases",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/testing/cases  — spec alias for /tests (create)"
        },
        {
          "name": "DELETE /v1/testing/cases/{case_id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/testing/cases/:case_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "testing",
                "cases",
                ":case_id"
              ],
              "variable": [
                {
                  "key": "case_id",
                  "value": "{{case_id}}",
                  "description": "Path parameter: case_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/testing/cases/{case_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DELETE /v1/testing/cases/:id  — spec alias"
        },
        {
          "name": "GET /v1/testing/cases/{case_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/testing/cases/:case_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "testing",
                "cases",
                ":case_id"
              ],
              "variable": [
                {
                  "key": "case_id",
                  "value": "{{case_id}}",
                  "description": "Path parameter: case_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/testing/cases/{case_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/testing/cases/:id  — spec alias"
        },
        {
          "name": "PUT /v1/testing/cases/{case_id}",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/testing/cases/:case_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "testing",
                "cases",
                ":case_id"
              ],
              "variable": [
                {
                  "key": "case_id",
                  "value": "{{case_id}}",
                  "description": "Path parameter: case_id"
                }
              ]
            },
            "description": "uses PATCH; spec asks for PUT. We accept both for the same path.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PUT /v1/testing/cases/{case_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PUT /v1/testing/cases/:id  — spec alias (update). The existing route"
        },
        {
          "name": "POST /v1/testing/cases/{case_id}/run",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/testing/cases/:case_id/run",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "testing",
                "cases",
                ":case_id",
                "run"
              ],
              "variable": [
                {
                  "key": "case_id",
                  "value": "{{case_id}}",
                  "description": "Path parameter: case_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/testing/cases/{case_id}/run",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/testing/cases/:id/run  — spec alias (single-case run with audit)"
        }
      ]
    },
    {
      "name": "/v1/testing/run-all",
      "description": "1 route(s) under /v1/testing/run-all",
      "item": [
        {
          "name": "POST /v1/testing/run-all",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/testing/run-all",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "testing",
                "run-all"
              ]
            },
            "description": "Spec acceptance: \"A scheduled auto-run produces a results report\nand writes per-case events to Audit Trail.\" This endpoint fires\nthe run-all (which the auto-scheduler will eventually call) and\nwrites ONE audit event per case in the report. Triggered=auto for\ncalls from the scheduler; triggered=manual otherwise.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/testing/run-all",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/testing/run-all  — NEW spec route"
        }
      ]
    },
    {
      "name": "/v1/testing/runs",
      "description": "2 route(s) under /v1/testing/runs",
      "item": [
        {
          "name": "GET /v1/testing/runs",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/testing/runs",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "testing",
                "runs"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/testing/runs",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/testing/runs — auto-discovered (server.ts:42784)"
        },
        {
          "name": "GET /v1/testing/runs/{run_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/testing/runs/:run_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "testing",
                "runs",
                ":run_id"
              ],
              "variable": [
                {
                  "key": "run_id",
                  "value": "<run_id>",
                  "description": "Path parameter: run_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/testing/runs/{run_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/testing/runs/:run_id — auto-discovered (server.ts:42798)"
        }
      ]
    },
    {
      "name": "/v1/testing/schedule",
      "description": "2 route(s) under /v1/testing/schedule",
      "item": [
        {
          "name": "GET /v1/testing/schedule",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/testing/schedule",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "testing",
                "schedule"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/testing/schedule",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/testing/schedule — auto-discovered (server.ts:42806)"
        },
        {
          "name": "POST /v1/testing/schedule",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/testing/schedule",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "testing",
                "schedule"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/testing/schedule",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/testing/schedule — auto-discovered (server.ts:42812)"
        }
      ]
    },
    {
      "name": "/v1/testing/schedules",
      "description": "2 route(s) under /v1/testing/schedules",
      "item": [
        {
          "name": "GET /v1/testing/schedules",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/testing/schedules",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "testing",
                "schedules"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/testing/schedules",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/testing/schedules  — spec plural alias"
        },
        {
          "name": "POST /v1/testing/schedules",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/testing/schedules",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "testing",
                "schedules"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/testing/schedules",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/testing/schedules  — spec plural alias for set"
        }
      ]
    },
    {
      "name": "/v1/testing/tests",
      "description": "7 route(s) under /v1/testing/tests",
      "item": [
        {
          "name": "GET /v1/testing/tests",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/testing/tests",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "testing",
                "tests"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/testing/tests",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Module #21 — Testing Hub (§2.4)"
        },
        {
          "name": "POST /v1/testing/tests",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/testing/tests",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "testing",
                "tests"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/testing/tests",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/testing/tests — auto-discovered (server.ts:42665)"
        },
        {
          "name": "DELETE /v1/testing/tests/{test_id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/testing/tests/:test_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "testing",
                "tests",
                ":test_id"
              ],
              "variable": [
                {
                  "key": "test_id",
                  "value": "<test_id>",
                  "description": "Path parameter: test_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/testing/tests/{test_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DELETE /v1/testing/tests/:test_id — auto-discovered (server.ts:42718)"
        },
        {
          "name": "GET /v1/testing/tests/{test_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/testing/tests/:test_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "testing",
                "tests",
                ":test_id"
              ],
              "variable": [
                {
                  "key": "test_id",
                  "value": "<test_id>",
                  "description": "Path parameter: test_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/testing/tests/{test_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/testing/tests/:test_id — auto-discovered (server.ts:42689)"
        },
        {
          "name": "PATCH /v1/testing/tests/{test_id}",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/testing/tests/:test_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "testing",
                "tests",
                ":test_id"
              ],
              "variable": [
                {
                  "key": "test_id",
                  "value": "<test_id>",
                  "description": "Path parameter: test_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PATCH /v1/testing/tests/{test_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PATCH /v1/testing/tests/:test_id — auto-discovered (server.ts:42697)"
        },
        {
          "name": "POST /v1/testing/tests/{test_id}/run",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/testing/tests/:test_id/run",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "testing",
                "tests",
                ":test_id",
                "run"
              ],
              "variable": [
                {
                  "key": "test_id",
                  "value": "<test_id>",
                  "description": "Path parameter: test_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/testing/tests/{test_id}/run",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/testing/tests/:test_id/run — auto-discovered (server.ts:42744)"
        },
        {
          "name": "POST /v1/testing/tests/bulk-upload",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/testing/tests/bulk-upload",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "testing",
                "tests",
                "bulk-upload"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/testing/tests/bulk-upload",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/testing/tests/bulk-upload — auto-discovered (server.ts:42726)"
        }
      ]
    },
    {
      "name": "/v1/webhooks",
      "description": "2 route(s) under /v1/webhooks",
      "item": [
        {
          "name": "GET /v1/webhooks",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/webhooks",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "webhooks"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/webhooks",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/webhooks — auto-discovered (server.ts:33137)"
        },
        {
          "name": "POST /v1/webhooks",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/webhooks",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "webhooks"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"name\": \"Postman test sub\",\n  \"url\": \"http://localhost:9999/postman-test-sink\",\n  \"event_types\": [\n    \"alert.created\"\n  ]\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/webhooks",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/webhooks — auto-discovered (server.ts:33142)"
        }
      ]
    },
    {
      "name": "/v1/webhooks/:id",
      "description": "3 route(s) under /v1/webhooks/:id",
      "item": [
        {
          "name": "DELETE /v1/webhooks/{id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/webhooks/:id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "webhooks",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/webhooks/{id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DELETE /v1/webhooks/:id — auto-discovered (server.ts:33177)"
        },
        {
          "name": "GET /v1/webhooks/{id}/deliveries",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/webhooks/:id/deliveries",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "webhooks",
                ":id",
                "deliveries"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/webhooks/{id}/deliveries",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/webhooks/:id/deliveries — auto-discovered (server.ts:33229)"
        },
        {
          "name": "POST /v1/webhooks/{id}/test",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/webhooks/:id/test",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "webhooks",
                ":id",
                "test"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: id"
                }
              ]
            },
            "description": "Synthesise a `webhook.test` event and dispatch it just to this\nsubscription. Lets admins verify the URL reachable + signature\nverifying without waiting for a real alert. We AWAIT the delivery\nhere so the response can include the outcome.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/webhooks/{id}/test",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/webhooks/:id/test"
        }
      ]
    },
    {
      "name": "/v1/workflows/templates",
      "description": "7 route(s) under /v1/workflows/templates",
      "item": [
        {
          "name": "GET /v1/workflows/templates",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/workflows/templates",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "workflows",
                "templates"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/workflows/templates",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Module #20 — Workflow templates non-case (§2.3)"
        },
        {
          "name": "POST /v1/workflows/templates",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/workflows/templates",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "workflows",
                "templates"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/workflows/templates",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/workflows/templates — auto-discovered (server.ts:42181)"
        },
        {
          "name": "DELETE /v1/workflows/templates/{template_id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/workflows/templates/:template_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "workflows",
                "templates",
                ":template_id"
              ],
              "variable": [
                {
                  "key": "template_id",
                  "value": "<template_id>",
                  "description": "Path parameter: template_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/workflows/templates/{template_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DELETE /v1/workflows/templates/:template_id — auto-discovered (server.ts:42294)"
        },
        {
          "name": "GET /v1/workflows/templates/{template_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/workflows/templates/:template_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "workflows",
                "templates",
                ":template_id"
              ],
              "variable": [
                {
                  "key": "template_id",
                  "value": "<template_id>",
                  "description": "Path parameter: template_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/workflows/templates/{template_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/workflows/templates/:template_id — auto-discovered (server.ts:42230)"
        },
        {
          "name": "PATCH /v1/workflows/templates/{template_id}",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/workflows/templates/:template_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "workflows",
                "templates",
                ":template_id"
              ],
              "variable": [
                {
                  "key": "template_id",
                  "value": "<template_id>",
                  "description": "Path parameter: template_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PATCH /v1/workflows/templates/{template_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PATCH /v1/workflows/templates/:template_id — auto-discovered (server.ts:42244)"
        },
        {
          "name": "POST /v1/workflows/templates/{template_id}/clone",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/workflows/templates/:template_id/clone",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "workflows",
                "templates",
                ":template_id",
                "clone"
              ],
              "variable": [
                {
                  "key": "template_id",
                  "value": "<template_id>",
                  "description": "Path parameter: template_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/workflows/templates/{template_id}/clone",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/workflows/templates/:template_id/clone — auto-discovered (server.ts:42326)"
        },
        {
          "name": "GET /v1/workflows/templates/{template_id}/routing",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/workflows/templates/:template_id/routing",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "workflows",
                "templates",
                ":template_id",
                "routing"
              ],
              "variable": [
                {
                  "key": "template_id",
                  "value": "<template_id>",
                  "description": "Path parameter: template_id"
                }
              ]
            },
            "description": "surface: \"a 4-eyes stage automatically routes to the configured\nrole pool\"). Pure projection — no mutation, no audit."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/workflows/templates/{template_id}/routing",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "M5.4 — Workflows: stage-routing derivation view (spec acceptance"
        }
      ]
    }
  ]
}
