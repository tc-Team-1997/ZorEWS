{
  "info": {
    "_postman_id": "5fa6c149-afc6-4c5a-88b6-a601cbff928a",
    "name": "ZorEWS — Reports APIs",
    "description": "Reports routes from the ZorEWS BFF + auth-svc surface. 86 operations. Auto-generated from `docs/api/swagger.json` by `scripts/gen-postman.js`. Re-import after running `npm run openapi:generate` to refresh.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "auth": {
    "type": "bearer",
    "bearer": [
      {
        "key": "token",
        "value": "{{access_token}}",
        "type": "string"
      }
    ]
  },
  "event": [
    {
      "listen": "prerequest",
      "script": {
        "type": "text/javascript",
        "exec": [
          "// Collection-level pre-request — log the active environment + tenant for visibility",
          "if (!pm.environment.get('access_token') && pm.request.url.toString().indexOf('/auth/login') === -1 && pm.request.url.toString().indexOf('/oauth/token') === -1 && pm.request.url.toString().indexOf('/healthz') === -1 && pm.request.url.toString().indexOf('/.well-known/') === -1) {",
          "  console.warn('access_token is empty — run the Smoke > Login request first to capture a token.');",
          "}"
        ]
      }
    }
  ],
  "variable": [],
  "item": [
    {
      "name": "00 — Smoke tests",
      "description": "Happy-path + negative-test sanity checks. Run this folder first to verify the local stack + auth flow.",
      "item": [
        {
          "name": "✅ Happy path — GET /v1/reports/catalog",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/catalog",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "catalog"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('2xx or 401 (no auth in env)', () => {",
                  "  pm.expect([200, 201, 401, 403]).to.include(pm.response.code);",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "❌ Negative — missing X-Tenant-ID → 400",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{bff_url}}/v1/reports/catalog",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "catalog"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('400 when tenant header missing', () => pm.expect(pm.response.code).to.equal(400));",
                  "pm.test('error envelope', () => {",
                  "  const j = pm.response.json();",
                  "  pm.expect(j).to.have.property('error');",
                  "  pm.expect(j.error).to.have.property('code');",
                  "});"
                ]
              }
            }
          ]
        },
        {
          "name": "❌ Negative — unknown role → 403",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "unknown_role",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/catalog",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "catalog"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('403 with unknown role', () => pm.expect(pm.response.code).to.equal(403));"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "/v1/reports/:type",
      "description": "1 route(s) under /v1/reports/:type",
      "item": [
        {
          "name": "GET /v1/reports/{type}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/:type",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                ":type"
              ],
              "variable": [
                {
                  "key": "type",
                  "value": "<type>",
                  "description": "Path parameter: type"
                }
              ]
            },
            "description": "type ∈ {snapshot, alerts, cases, rbi}\nReturns a typed Report payload (JSON) or a flat CSV download. The\nsynthetic history is regenerated deterministically per call so the\nsame (type, period, now) tuple always produces the same body."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/reports/{type}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/reports/:type?period={week|month|quarter}&format={json|csv}"
        }
      ]
    },
    {
      "name": "/v1/reports/builder",
      "description": "11 route(s) under /v1/reports/builder",
      "item": [
        {
          "name": "POST /v1/reports/builder/export.csv",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/builder/export.csv",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "builder",
                "export.csv"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/reports/builder/export.csv",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "as RFC 4180 CSV with attachment Content-Disposition. */"
        },
        {
          "name": "POST /v1/reports/builder/preview",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/builder/preview",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "builder",
                "preview"
              ]
            },
            "description": "parameterised SQL preview. Returns sql + params + projection +\nis_aggregate + param_count. Does NOT execute against the mart —\nT4.6.4 ships the execution engine. Validation errors routed to\n400 with code-specific EWS_400_<code> envelopes; unknown_source\n→ 404 EWS_404_unknown_source. Tenant-id always injected from\nthe JWT/header context — body.tenant_id ignored if supplied. */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/reports/builder/preview",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "definition (filters + group_by + metrics + sort) into a safe"
        },
        {
          "name": "POST /v1/reports/builder/run",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/builder/run",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "builder",
                "run"
              ]
            },
            "description": "duration_ms. Admin response includes compiled sql + params;\nnon-admin gets the same envelope minus those two fields. */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/reports/builder/run",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "ReportDefinition. Returns rows + projection + aggregates +"
        },
        {
          "name": "GET /v1/reports/builder/saved",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/builder/saved",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "builder",
                "saved"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/reports/builder/saved",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "for the caller. Filters: ?visibility=&source_id=&created_by=&tag=. */"
        },
        {
          "name": "POST /v1/reports/builder/saved",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/builder/saved",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "builder",
                "saved"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/reports/builder/saved",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "visibility='role' additionally requires reports:share scope. */"
        },
        {
          "name": "DELETE /v1/reports/builder/saved/{report_id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/builder/saved/:report_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "builder",
                "saved",
                ":report_id"
              ],
              "variable": [
                {
                  "key": "report_id",
                  "value": "<report_id>",
                  "description": "Path parameter: report_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/reports/builder/saved/{report_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "404 on miss or cross-tenant. */"
        },
        {
          "name": "GET /v1/reports/builder/saved/{report_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/builder/saved/:report_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "builder",
                "saved",
                ":report_id"
              ],
              "variable": [
                {
                  "key": "report_id",
                  "value": "<report_id>",
                  "description": "Path parameter: report_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/reports/builder/saved/{report_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "404 if not visible OR cross-tenant. */"
        },
        {
          "name": "PATCH /v1/reports/builder/saved/{report_id}",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/builder/saved/:report_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "builder",
                "saved",
                ":report_id"
              ],
              "variable": [
                {
                  "key": "report_id",
                  "value": "<report_id>",
                  "description": "Path parameter: report_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PATCH /v1/reports/builder/saved/{report_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "fields. visibility transitions to 'role' require reports:share. */"
        },
        {
          "name": "POST /v1/reports/builder/saved/{report_id}/run",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/builder/saved/:report_id/run",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "builder",
                "saved",
                ":report_id",
                "run"
              ],
              "variable": [
                {
                  "key": "report_id",
                  "value": "<report_id>",
                  "description": "Path parameter: report_id"
                }
              ]
            },
            "description": "not-visible-to-caller → 404). */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/reports/builder/saved/{report_id}/run",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "saved report. Tenant + visibility checked (cross-tenant or"
        },
        {
          "name": "GET /v1/reports/builder/sources",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/builder/sources",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "builder",
                "sources"
              ]
            },
            "description": "schema + drill-targets + tenant_scoped + required_role. Drives\nthe SPA report builder source picker. Platform-static. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/reports/builder/sources",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "catalog. Returns every queryable mart + app_* surface with field"
        },
        {
          "name": "GET /v1/reports/builder/sources/{source_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/builder/sources/:source_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "builder",
                "sources",
                ":source_id"
              ],
              "variable": [
                {
                  "key": "source_id",
                  "value": "<source_id>",
                  "description": "Path parameter: source_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/reports/builder/sources/{source_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "source lookup. 404 EWS_404_unknown_source on miss. */"
        }
      ]
    },
    {
      "name": "/v1/reports/catalog",
      "description": "2 route(s) under /v1/reports/catalog",
      "item": [
        {
          "name": "GET /v1/reports/catalog",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/catalog",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "catalog"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/reports/catalog",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/reports/catalog — auto-discovered (server.ts:33852)"
        },
        {
          "name": "GET /v1/reports/catalog/{id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/catalog/:id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "catalog",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/reports/catalog/{id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/reports/catalog/:id — auto-discovered (server.ts:33885)"
        }
      ]
    },
    {
      "name": "/v1/reports/jobs",
      "description": "17 route(s) under /v1/reports/jobs",
      "item": [
        {
          "name": "GET /v1/reports/jobs",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/jobs",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "jobs"
              ]
            },
            "description": "Paginated newest-first listing for the caller's tenant."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/reports/jobs",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/reports/jobs?status=&report_id=&requested_by=&page=&page_size="
        },
        {
          "name": "POST /v1/reports/jobs",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/jobs",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "jobs"
              ]
            },
            "description": "Submit a report-export job. Stub completes synchronously and returns\na download_url; production wires this to a queue-backed worker.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/reports/jobs",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/reports/jobs body: ReportJobInput"
        },
        {
          "name": "GET /v1/reports/jobs/{job_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/jobs/:job_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "jobs",
                ":job_id"
              ],
              "variable": [
                {
                  "key": "job_id",
                  "value": "<job_id>",
                  "description": "Path parameter: job_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/reports/jobs/{job_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/reports/jobs/:job_id — auto-discovered (server.ts:34388)"
        },
        {
          "name": "GET /v1/reports/jobs/analytics",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/jobs/analytics",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "jobs",
                "analytics"
              ]
            },
            "description": "per-report counts + success rate + mean processing time, top\nrequesters (cap 10), processing latency percentiles, last failure.\nOptional ?status= / ?report_id= filters reuse the M12.1 list shape.\nMUST be registered before /v1/reports/jobs/:job_id so the literal\nsegment doesn't get captured as a job_id. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/reports/jobs/analytics",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "over the M12.1 reports-job ledger: status mix, format mix,"
        },
        {
          "name": "GET /v1/reports/jobs/consumer-behavior",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/jobs/consumer-behavior",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "jobs",
                "consumer-behavior"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/reports/jobs/consumer-behavior",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/reports/jobs/consumer-behavior — auto-discovered (server.ts:34113)"
        },
        {
          "name": "GET /v1/reports/jobs/daily-volume",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/jobs/daily-volume",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "jobs",
                "daily-volume"
              ]
            },
            "description": "days: {date, total, by_status (4-key), by_format (4-key)}.\nEnvelope: total_jobs_in_window, total_jobs_observed (whole store\ndrained — surfaces \"are jobs falling outside the window?\"),\npeak_day (earliest-wins tie-break), peak_count, mean_per_day,\ngrowth_rate (second-half mean vs first-half; null when\nfirst_half=0 or days<2), busiest_format (canonical\njson>csv>pdf>xlsx tie-break). Default days=30, bounds [1, 365].\nMirror of M15.11 audit daily + M10.15 notification daily for the\nreports surface. MUST be registered before `/v1/reports/jobs/:job_id`\nso the literal segment isn't captured. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/reports/jobs/daily-volume",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "timeline over the M12.1 ReportJobStore. Per UTC day across N"
        },
        {
          "name": "GET /v1/reports/jobs/error-patterns",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/jobs/error-patterns",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "jobs",
                "error-patterns"
              ]
            },
            "description": "normalised error_message template (re-uses `normaliseError` from\nM3.6 connector failure patterns to keep cluster shapes consistent\nacross surfaces). Per cluster: pattern, count, last_failed_at,\nsample_job_id (newest in cluster), recent_messages (cap 3\nnewest-first), report_ids[] sorted asc (distinct reports\ncontributing). Envelope: sample_size (all jobs), failure_count\n(failed + non-empty error_message), distinct_patterns, clusters[]\ncapped at 10, sorted count desc then last_failed_at desc.\nSuccessful jobs + empty error_message excluded. Mirror of M3.6\nconnector failure cluster pattern for the reports surface. Drives\n\"what failure templates dominate my report jobs this week?\".\nMUST be registered before `/v1/reports/jobs/:job_id` so the literal\nsegment isn't captured by the param wildcard. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/reports/jobs/error-patterns",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "FORENSICS view over the M12.1 job store. Clusters failed jobs by"
        },
        {
          "name": "GET /v1/reports/jobs/format-distribution",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/jobs/format-distribution",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "jobs",
                "format-distribution"
              ]
            },
            "description": "total_count, by_status (every JobStatus key present), success_rate\n(completed / completed+failed), mean_processing_ms over completed,\nmost_recent_at, by_report_id_top top-5, distinct_reports.\nEnvelope: every format in canonical order even when zero,\nmost_common_format (canonical-order tie-break), unused_formats[].\nMirror of M5.16 / M11.11 / M7.11 pivot pattern. Reads ALL jobs\n(failed + queued still represent a format choice). MUST be\nregistered before `/v1/reports/jobs/:job_id` so the literal\nsegment isn't captured by the param wildcard. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/reports/jobs/format-distribution",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "ReportJobs by format (json / csv / pdf / xlsx). Per-format row:"
        },
        {
          "name": "GET /v1/reports/jobs/format-size",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/jobs/format-size",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "jobs",
                "format-size"
              ]
            },
            "description": "jobs. Groups by format, computes deterministic estimated output\nsizes (json=50–500KB, csv=100KB–2MB, pdf=500KB–5MB, xlsx=200KB–3MB).\nReturns formats[] sorted total_size_kb desc with avg/median/total\nsize stats + largest_job per format. Envelope: largest_format +\ntotal_estimated_storage_kb. Distinct from M12.11 (format counts,\nnot sizes) + M12.14 (format × status matrix). Mounted BEFORE\n/:job_id so the literal `/format-size` segment wins. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/reports/jobs/format-size",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Per-tenant output format size distribution over completed report"
        },
        {
          "name": "GET /v1/reports/jobs/format-status-matrix",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/jobs/format-status-matrix",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "jobs",
                "format-status-matrix"
              ]
            },
            "description": "— coverage-gap per format}. Per-col {status, total, by_format\n(every format at 0 when absent), formats_without[] canonical\n— coverage-gap per status}. Envelope {rows[], columns[],\npeak_cell (canonical iteration tie-break; null on empty),\nempty_cells[] in canonical format × status row-major order,\nmost_active_format (highest distinct non-zero by_status entries;\ncanonical-order tie-break; null on empty), most_distributed_\nstatus (highest distinct non-zero by_format entries; canonical\norder tie-break; null on empty)}. Mirror of M14.28 / M8.14 /\nM3.14 / M5.17 / M13.15 / M7.14 matrix pattern. Drives \"are PDF\njobs failing more than CSV?\" + \"where are jobs piling up\n(queued cells × format)?\". Mounted BEFORE catch-all\n`/v1/reports/jobs/:job_id` so the literal `/format-status-matrix`\nsegment isn't captured by the param wildcard. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/reports/jobs/format-status-matrix",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "(every status at 0 when absent), statuses_without[] canonical"
        },
        {
          "name": "GET /v1/reports/jobs/hourly-volume",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/jobs/hourly-volume",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "jobs",
                "hourly-volume"
              ]
            },
            "description": "hour-of-day 0..23. Distinct from M12.13 (linear daily trend over\nN days) — orthogonal scheduler-ergonomics view answering \"do\nall our reports fire at 6am? — should we stagger?\". Per-bucket:\nhour + total + by_status (every JobStatus at 0 when absent) +\nby_format (every ReportFormat at 0 when absent) +\ndistinct_requesters (Set-deduped). Envelope: peak_hour\n(earliest-wins tie-break) + mean_per_hour + quiet_hours[] +\nbusiest_format (canonical json>csv>pdf>xlsx tie-break). Mirror\nof M3.12 connector hourly volume + M14.22/M15.7 dow×hour\nheatmap pattern (this is the 1D variant). Mounted BEFORE the\n/:job_id wildcard so the literal segment wins. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/reports/jobs/hourly-volume",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "distribution: every job in the tenant's history bucketed by UTC"
        },
        {
          "name": "GET /v1/reports/jobs/output-size-distribution",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/jobs/output-size-distribution",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "jobs",
                "output-size-distribution"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/reports/jobs/output-size-distribution",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Report output size distribution. Mounted BEFORE /:job_id catch-all. */"
        },
        {
          "name": "GET /v1/reports/jobs/peak-hours",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/jobs/peak-hours",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "jobs",
                "peak-hours"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/reports/jobs/peak-hours",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/reports/jobs/peak-hours — auto-discovered (server.ts:34241)"
        },
        {
          "name": "GET /v1/reports/jobs/per-requester",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/jobs/per-requester",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "jobs",
                "per-requester"
              ]
            },
            "description": "by_status (every JobStatus key), by_format (every ReportFormat key),\nby_report_id_top (cap 5), distinct_reports, most_recent_at,\nhas_failure. Envelope: requesters[] sorted total_jobs desc +\nrequested_by asc tie-break, most_active_requester,\nrequesters_with_failures[] (failed_count desc + requested_by asc).\nMirror of M15.8 (audit per-actor) for the reports surface.\nMUST be registered before `/v1/reports/jobs/:job_id` so the literal\nsegment isn't captured by the param wildcard. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/reports/jobs/per-requester",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "rollup over the M12.1 job store. Per requester: total_jobs,"
        },
        {
          "name": "GET /v1/reports/jobs/processing-time-histogram",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/jobs/processing-time-histogram",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "jobs",
                "processing-time-histogram"
              ]
            },
            "description": "sorts requested_at desc (newest first); in_flight sorts requested_at\nasc (oldest first)}. Envelope: peak_bucket (canonical iteration\ntie-break — under_1s wins over 1_to_5s at tied; null on empty),\nmean/median/p95_duration_ms over COMPLETED only via M3.5\nlinearPercentile (null when no completed), slowest_job (max\nduration), newest_failed (most recent failure), oldest_in_flight\n(longest waiting), empty_buckets[] canonical. Distinct from M12.10\n(per-report trend over time), M12.11 (format distribution), M12.13\n(daily volume timeline), M12.14 (format × status matrix), M12.18\n(hourly volume cycle). Mirror of M9.19 / M8.12 / M7.15 histogram\npattern for the report job surface. Drives \"what's our typical\nreport processing time? any formats hitting > 5min consistently?\nhow many jobs failed today?\" governance. Mounted BEFORE catch\nall /v1/reports/jobs/:job_id so the literal segment wins. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/reports/jobs/processing-time-histogram",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "— completion buckets sort duration desc (slowest first); failed"
        },
        {
          "name": "GET /v1/reports/jobs/quality-metrics",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/jobs/quality-metrics",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "jobs",
                "quality-metrics"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/reports/jobs/quality-metrics",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/reports/jobs/quality-metrics — auto-discovered (server.ts:34380)"
        },
        {
          "name": "GET /v1/reports/jobs/runtime-trend",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/jobs/runtime-trend",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "jobs",
                "runtime-trend"
              ]
            },
            "description": "sample_size, first/last_processing_ms, abs_change_ms,\nslope_ms_per_day. Envelope: biggest_regression_report_id +\nbiggest_improvement_report_id. Mirror of M7.8 (model performance\ntrend) for the reports surface. MUST be registered before\n`/v1/reports/jobs/:job_id` so the literal segment isn't captured. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/reports/jobs/runtime-trend",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "least-squares slope of processing_ms over time. Per-row:"
        }
      ]
    },
    {
      "name": "/v1/reports/schedules",
      "description": "18 route(s) under /v1/reports/schedules",
      "item": [
        {
          "name": "GET /v1/reports/schedules",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/schedules",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "schedules"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/reports/schedules",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/reports/schedules — auto-discovered (server.ts:34460)"
        },
        {
          "name": "POST /v1/reports/schedules",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/schedules",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "schedules"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/reports/schedules",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/reports/schedules — auto-discovered (server.ts:34416)"
        },
        {
          "name": "DELETE /v1/reports/schedules/{schedule_id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/schedules/:schedule_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "schedules",
                ":schedule_id"
              ],
              "variable": [
                {
                  "key": "schedule_id",
                  "value": "<schedule_id>",
                  "description": "Path parameter: schedule_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/reports/schedules/{schedule_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DELETE /v1/reports/schedules/:schedule_id — auto-discovered (server.ts:34841)"
        },
        {
          "name": "GET /v1/reports/schedules/{schedule_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/schedules/:schedule_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "schedules",
                ":schedule_id"
              ],
              "variable": [
                {
                  "key": "schedule_id",
                  "value": "<schedule_id>",
                  "description": "Path parameter: schedule_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/reports/schedules/{schedule_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/reports/schedules/:schedule_id — auto-discovered (server.ts:34778)"
        },
        {
          "name": "PATCH /v1/reports/schedules/{schedule_id}",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/schedules/:schedule_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "schedules",
                ":schedule_id"
              ],
              "variable": [
                {
                  "key": "schedule_id",
                  "value": "<schedule_id>",
                  "description": "Path parameter: schedule_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PATCH /v1/reports/schedules/{schedule_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PATCH /v1/reports/schedules/:schedule_id — auto-discovered (server.ts:34799)"
        },
        {
          "name": "POST /v1/reports/schedules/{schedule_id}/mark-run",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/schedules/:schedule_id/mark-run",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "schedules",
                ":schedule_id",
                "mark-run"
              ],
              "variable": [
                {
                  "key": "schedule_id",
                  "value": "<schedule_id>",
                  "description": "Path parameter: schedule_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/reports/schedules/{schedule_id}/mark-run",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/reports/schedules/:schedule_id/mark-run — auto-discovered (server.ts:34862)"
        },
        {
          "name": "GET /v1/reports/schedules/{schedule_id}/preview",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/schedules/:schedule_id/preview",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "schedules",
                ":schedule_id",
                "preview"
              ],
              "variable": [
                {
                  "key": "schedule_id",
                  "value": "<schedule_id>",
                  "description": "Path parameter: schedule_id"
                }
              ]
            },
            "description": "iterating M12.2 computeNextRun. n bounded [1, 50], default 10.\nfrom defaults to now(). audit:read RBAC. Mounted BEFORE the\ncatch-all `/:schedule_id` so the literal /preview segment isn't\ncaptured as a schedule_id. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/reports/schedules/{schedule_id}/preview",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "(T6 M12.6) — project the next N firings of a saved schedule by"
        },
        {
          "name": "GET /v1/reports/schedules/adherence",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/schedules/adherence",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "schedules",
                "adherence"
              ]
            },
            "description": "actual (proxied from last_run_at). Returns schedules[] sorted\nadherence_rate asc, worst_adherence, fleet_adherence_rate.\nRBAC audit:read. Mounted BEFORE /:schedule_id catch-all. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/reports/schedules/adherence",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "ENABLED schedule, computes expected_runs_30d (by cadence) vs"
        },
        {
          "name": "GET /v1/reports/schedules/cadence-format-matrix",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/schedules/cadence-format-matrix",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "schedules",
                "cadence-format-matrix"
              ]
            },
            "description": "in exactly one (cadence, format) cell. Per-row {cadence, total,\nby_format (4 keys at 0 — stable grid), formats_without[] canonical,\nschedule_ids[] sorted asc, enabled_count, disabled_count}. Per-col\n{format, total, by_cadence (5 keys at 0), cadences_without[]\ncanonical, schedule_ids[] sorted asc, enabled_count, disabled_count}.\nEnvelope: peak_cell (canonical iteration tie-break: cadences in\nVALID_CADENCES order × formats in ALL_REPORT_FORMATS order; null\non empty), most_versatile_cadence (most distinct formats; canonical\ntie-break; null on empty), most_universal_format (most distinct\ncadences; canonical tie-break; null on empty), empty_cells[] in\ncanonical cadence × format row-major order. Mirror of M12.14 /\nM3.14 / M15.14 / M14.28 / M5.17 / M13.15 matrix pattern for the\nrecurring schedule surface. Drives BIL ops \"do we have daily PDF\nschedules? are quarterly schedules always xlsx?\" coverage view. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/reports/schedules/cadence-format-matrix",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "ScheduleCadence × 4 ReportFormat = 20 cells. Each schedule lives"
        },
        {
          "name": "GET /v1/reports/schedules/cadence-stats",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/schedules/cadence-stats",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "schedules",
                "cadence-stats"
              ]
            },
            "description": "total_count, enabled/disabled split, next_run_within_24h_count,\nnext_run_within_7d_count, earliest_next_run_at. Envelope adds\naggregate totals + most_common_cadence + unused_cadences[].\nMounted BEFORE /:schedule_id so the literal /cadence-stats\nsegment wins. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/reports/schedules/cadence-stats",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "pivoted rollup over the M12.2 schedule store. Per-cadence:"
        },
        {
          "name": "GET /v1/reports/schedules/conflicts",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/schedules/conflicts",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "schedules",
                "conflicts"
              ]
            },
            "description": "falls within `window` minutes of each other. Useful for ops to\nspot resource contention (two heavy reports firing the same\nminute and slamming the database). Same-schedule self-pairs are\nfiltered out. Mounted BEFORE /:schedule_id so the literal\n/conflicts wins. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/reports/schedules/conflicts",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "(T6 M12.8) — finds pairs of DIFFERENT schedules whose fire_at"
        },
        {
          "name": "GET /v1/reports/schedules/cost-forecast",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/schedules/cost-forecast",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "schedules",
                "cost-forecast"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/reports/schedules/cost-forecast",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/reports/schedules/cost-forecast — auto-discovered (server.ts:33999)"
        },
        {
          "name": "GET /v1/reports/schedules/due",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/schedules/due",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "schedules",
                "due"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/reports/schedules/due",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Declared BEFORE /:schedule_id so the literal \"due\" wins. */"
        },
        {
          "name": "GET /v1/reports/schedules/optimization",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/schedules/optimization",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "schedules",
                "optimization"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/reports/schedules/optimization",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/reports/schedules/optimization — auto-discovered (server.ts:34545)"
        },
        {
          "name": "GET /v1/reports/schedules/recipient-distribution",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/schedules/recipient-distribution",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "schedules",
                "recipient-distribution"
              ]
            },
            "description": "{recipient, total_schedules, enabled_schedules, disabled_schedules,\nby_cadence (every ScheduleCadence at 0 when absent), by_format\n(every ReportFormat at 0), report_ids[] sorted asc,\nearliest_next_run_at (across enabled schedules; null when none),\nschedule_names[] sorted asc cap 20}. Envelope:\nmost_subscribed_recipient (highest total_schedules + canonical\nemail asc tie-break; null on empty), flooded_recipients[]\n(total_schedules >= 5; surfaces over-emailed addresses).\nMirror of M14.27 / M5.16 / M11.11 / M3.13 1D distribution\npattern for the report-schedule recipient axis. Drives quarterly\nrecipient hygiene + \"who's getting flooded?\" view. Mounted\nBEFORE /:schedule_id so the literal /recipient-distribution\nsegment isn't captured. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/reports/schedules/recipient-distribution",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "per-recipient pivot over the M12.2 schedule store. Per email:"
        },
        {
          "name": "GET /v1/reports/schedules/subscription-health",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/schedules/subscription-health",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "schedules",
                "subscription-health"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/reports/schedules/subscription-health",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "T6 M12.26 — Report subscription health (before /:schedule_id catch-all)"
        },
        {
          "name": "POST /v1/reports/schedules/tick",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/schedules/tick",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "schedules",
                "tick"
              ]
            },
            "description": "max_retries?: number, backoff_minutes?: number,\ndry_run?: boolean, simulate_failures?: string[] }\n- simulate_failures is a test hook: report_ids in this list\nare recorded as failures without actually calling submit.\nUseful for E2E retry/park tests without breaking the real\njob path. Empty in production callers.\nReturns: { tenant_id, generated_at, as_of, tolerance_minutes,\nmax_retries, backoff_minutes, dry_run, total_considered,\nfired[], retried_later[], skipped[], errors[], parked[] }\nRBAC: audit:read (admin) — scheduler tick is an ops-tier surface.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/reports/schedules/tick",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Body: { as_of?: ISO, tolerance_minutes?: number,"
        },
        {
          "name": "GET /v1/reports/schedules/upcoming",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/reports/schedules/upcoming",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "reports",
                "schedules",
                "upcoming"
              ]
            },
            "description": "the tenant, generates each schedule's next-N firings via\npreviewScheduleEntryRuns, merges them sorted by fire_at asc,\nand trims to the top n overall. Useful for a SPA calendar\n(\"what's firing in the next hour / day / week across all\nmy schedules?\"). Mounted BEFORE /:schedule_id so the literal\n/upcoming wins. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/reports/schedules/upcoming",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "— fleet-wide calendar view: walks every ENABLED schedule in"
        }
      ]
    },
    {
      "name": "/v1/scenario/run",
      "description": "1 route(s) under /v1/scenario/run",
      "item": [
        {
          "name": "POST /v1/scenario/run",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/scenario/run",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "scenario",
                "run"
              ]
            },
            "description": "body: { gdp: number, rate: number, fx: number }\nRuns a portfolio-wide stress test and returns the baseline vs. stressed\nPD distribution, ECL delta, segment heatmap rows, and top-affected\ncustomers. Pure compute — no upstream calls — so the response time is\nO(portfolio_size) and a 240-account run lands well under 50ms.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/scenario/run",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/scenario/run — T4.24 envelope + tenant."
        }
      ]
    },
    {
      "name": "/v1/scenarios",
      "description": "2 route(s) under /v1/scenarios",
      "item": [
        {
          "name": "GET /v1/scenarios",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/scenarios",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "scenarios"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/scenarios",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/scenarios — auto-discovered (server.ts:8544)"
        },
        {
          "name": "POST /v1/scenarios",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/scenarios",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "scenarios"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/scenarios",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/scenarios — auto-discovered (server.ts:9929)"
        }
      ]
    },
    {
      "name": "/v1/scenarios/:id",
      "description": "2 route(s) under /v1/scenarios/:id",
      "item": [
        {
          "name": "DELETE /v1/scenarios/{id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/scenarios/:id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "scenarios",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/scenarios/{id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DELETE /v1/scenarios/:id — auto-discovered (server.ts:9985)"
        },
        {
          "name": "GET /v1/scenarios/{id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/scenarios/:id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "scenarios",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: id"
                }
              ]
            },
            "description": "Per-tenant CRUD for user-authored scenario presets. Same shape\nas M16.1 ScenarioPreset so M16.2 bulk-run + M16.3 diff work\nunchanged when a custom id is passed in."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/scenarios/{id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Custom user-defined scenario presets (T6 M16.4)"
        }
      ]
    },
    {
      "name": "/v1/scenarios/bulk-run",
      "description": "1 route(s) under /v1/scenarios/bulk-run",
      "item": [
        {
          "name": "POST /v1/scenarios/bulk-run",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/scenarios/bulk-run",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "scenarios",
                "bulk-run"
              ]
            },
            "description": "Fires multiple presets against the portfolio and returns a ranked\ncomparison table. Caller supplies EITHER preset_ids[] OR category;\nexactly one. Re-uses the existing runScenario engine + portfolio.",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"preset_ids\": [\n    \"rbi_baseline\",\n    \"rbi_adverse\"\n  ]\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/scenarios/bulk-run",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "BIL scenario bulk-run + comparison (T6 M16.2)"
        }
      ]
    },
    {
      "name": "/v1/scenarios/diff",
      "description": "1 route(s) under /v1/scenarios/diff",
      "item": [
        {
          "name": "POST /v1/scenarios/diff",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/scenarios/diff",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "scenarios",
                "diff"
              ]
            },
            "description": "Pure-function field-by-field diff between two M16.1 library\npresets. Drives the SPA's side-by-side compare panel.\nRBAC matches the rest of /v1/scenarios — analyst-level read.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/scenarios/diff",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Scenario diff (T6 M16.3)"
        }
      ]
    },
    {
      "name": "/v1/scenarios/ecl-impact-comparison",
      "description": "1 route(s) under /v1/scenarios/ecl-impact-comparison",
      "item": [
        {
          "name": "POST /v1/scenarios/ecl-impact-comparison",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/scenarios/ecl-impact-comparison",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "scenarios",
                "ecl-impact-comparison"
              ]
            },
            "description": "against the standard portfolio via runScenario (T4.2) and returns\nper-preset: ecl_delta_kes, ecl_pct_change, stage_migrations_total\n(off-diagonal count), worst_segment (highest stressed PD),\nrank (1=most stressed). Envelope: results[] sorted ecl_delta\ndesc + max/min/spread_kes + most_stressed_preset +\nmost_resilient_preset. Distinct from M16.2 (bulk-run which uses\npreset_ids OR category + simpler row shape) by accepting only\npreset_ids and returning richer per-preset ECL diagnostics.\nScenarioEclComparisonError: invalid_input → 400,\nunknown_preset → 404. RBAC customers:read_risk_profile.\nMounted BEFORE `/:id` catch-all. */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/scenarios/ecl-impact-comparison",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "preset-to-preset ECL comparison. Runs preset_ids[] (1-15 items)"
        }
      ]
    },
    {
      "name": "/v1/scenarios/library",
      "description": "29 route(s) under /v1/scenarios/library",
      "item": [
        {
          "name": "GET /v1/scenarios/library",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/scenarios/library",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "scenarios",
                "library"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/scenarios/library",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/scenarios/library — auto-discovered (server.ts:8578)"
        },
        {
          "name": "GET /v1/scenarios/library/{id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/scenarios/library/:id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "scenarios",
                "library",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/scenarios/library/{id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/scenarios/library/:id — auto-discovered (server.ts:9782)"
        },
        {
          "name": "GET /v1/scenarios/library/{preset_id}/clones-in-tenant",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/scenarios/library/:preset_id/clones-in-tenant",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "scenarios",
                "library",
                ":preset_id",
                "clones-in-tenant"
              ],
              "variable": [
                {
                  "key": "preset_id",
                  "value": "{{scenario_preset_id}}",
                  "description": "Path parameter: preset_id"
                }
              ]
            },
            "description": "every custom preset in the calling tenant that was cloned from\nit (per the M16.8/M16.9 `cloned_from` audit metadata). Mirror\nof M5.13 (rule template clone history). Mounted BEFORE `/:id`\nso the literal `/clones-in-tenant` segment isn't captured.\n404 EWS_404_unknown_preset when the library id doesn't exist. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/scenarios/library/{preset_id}/clones-in-tenant",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "— back-reference query: for this library scenario preset, list"
        },
        {
          "name": "GET /v1/scenarios/library/axis-severity-matrix",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/scenarios/library/axis-severity-matrix",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "scenarios",
                "library",
                "axis-severity-matrix"
              ]
            },
            "description": "severity. Rows = 3 ShockAxis (canonical gdp → rate → fx); cols =\n3 ScenarioSeverity (canonical mild → moderate → severe). A cell\ncounts presets that EXERCISE that axis (raw shock ≠ 0) at that\nseverity tier — so a multi-axis preset counts in multiple ROWS\nbut only one column (its single severity). col.preset_count gives\nthe clean per-severity denominator. Envelope: peak_cell (canonical\niteration tie-break; null on empty), most_exercised_axis (highest\nrow total + canonical tie-break), severity_with_widest_axis_coverage\n(most distinct non-zero by_axis entries + canonical tie-break),\nempty_cells[] canonical row-major. Platform-static. Mirror of the\nM8.14 / M14.30 counts-in-multiple-rows cross-tab pattern. Drives\nstress-test coverage analysis: \"at the SEVERE tier, which shock\ndimensions do we exercise? where are the axis × severity gaps?\".\nMounted BEFORE `/:preset_id` so the literal segment wins. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/scenarios/library/axis-severity-matrix",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "cross-tab over the scenario library combining shock-axis ×"
        },
        {
          "name": "GET /v1/scenarios/library/categories",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/scenarios/library/categories",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "scenarios",
                "library",
                "categories"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/scenarios/library/categories",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/scenarios/library/categories — auto-discovered (server.ts:8566)"
        },
        {
          "name": "GET /v1/scenarios/library/coverage-matrix",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/scenarios/library/coverage-matrix",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "scenarios",
                "library",
                "coverage-matrix"
              ]
            },
            "description": "regulator) coverage counts. Cells with `count < expected_min`\nsurface as `is_gap=true` (e.g. regulatory × IRDAI must have\n≥ 1 preset). Envelope: cells[] (always 12), gaps[] sorted by\nshortfall desc, most_populated_cell, by_category, by_regulator.\nLets ops spot stress-test coverage gaps. Mounted BEFORE\n`/:preset_id` so the literal segment wins. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/scenarios/library/coverage-matrix",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "rollup over the M16.1 library showing per-(category,"
        },
        {
          "name": "GET /v1/scenarios/library/custom",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/scenarios/library/custom",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "scenarios",
                "library",
                "custom"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/scenarios/library/custom",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/scenarios/library/custom — auto-discovered (server.ts:8624)"
        },
        {
          "name": "POST /v1/scenarios/library/custom",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/scenarios/library/custom",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "scenarios",
                "library",
                "custom"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/scenarios/library/custom",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/scenarios/library/custom — auto-discovered (server.ts:8636)"
        },
        {
          "name": "DELETE /v1/scenarios/library/custom/{preset_id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/scenarios/library/custom/:preset_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "scenarios",
                "library",
                "custom",
                ":preset_id"
              ],
              "variable": [
                {
                  "key": "preset_id",
                  "value": "{{scenario_preset_id}}",
                  "description": "Path parameter: preset_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/scenarios/library/custom/{preset_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([204, 200, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DELETE /v1/scenarios/library/custom/:preset_id — auto-discovered (server.ts:9357)"
        },
        {
          "name": "PUT /v1/scenarios/library/custom/{preset_id}",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/scenarios/library/custom/:preset_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "scenarios",
                "library",
                "custom",
                ":preset_id"
              ],
              "variable": [
                {
                  "key": "preset_id",
                  "value": "{{scenario_preset_id}}",
                  "description": "Path parameter: preset_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PUT /v1/scenarios/library/custom/{preset_id}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "mutable fields. Writes scenario.update audit event with metadata. */"
        },
        {
          "name": "GET /v1/scenarios/library/custom/{preset_id}/history",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/scenarios/library/custom/:preset_id/history",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "scenarios",
                "library",
                "custom",
                ":preset_id",
                "history"
              ],
              "variable": [
                {
                  "key": "preset_id",
                  "value": "{{scenario_preset_id}}",
                  "description": "Path parameter: preset_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/scenarios/library/custom/{preset_id}/history",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "— slim audit-history view filtered to scenario events for this id. */"
        },
        {
          "name": "POST /v1/scenarios/library/custom/{preset_id}/restore/{version}",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/scenarios/library/custom/:preset_id/restore/:version",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "scenarios",
                "library",
                "custom",
                ":preset_id",
                "restore",
                ":version"
              ],
              "variable": [
                {
                  "key": "preset_id",
                  "value": "{{scenario_preset_id}}",
                  "description": "Path parameter: preset_id"
                },
                {
                  "key": "version",
                  "value": "<version>",
                  "description": "Path parameter: version"
                }
              ]
            },
            "description": "scenario.update audit event with `restored_from_version`. */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/scenarios/library/custom/{preset_id}/restore/{version}",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "— apply a prior snapshot as the live state. Records a new"
        },
        {
          "name": "GET /v1/scenarios/library/custom/{preset_id}/versions",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/scenarios/library/custom/:preset_id/versions",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "scenarios",
                "library",
                "custom",
                ":preset_id",
                "versions"
              ],
              "variable": [
                {
                  "key": "preset_id",
                  "value": "{{scenario_preset_id}}",
                  "description": "Path parameter: preset_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/scenarios/library/custom/{preset_id}/versions",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "— version snapshots in oldest-first order. Cap 20 per preset. */"
        },
        {
          "name": "GET /v1/scenarios/library/custom/{preset_id}/versions/diff",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/scenarios/library/custom/:preset_id/versions/diff",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "scenarios",
                "library",
                "custom",
                ":preset_id",
                "versions",
                "diff"
              ],
              "variable": [
                {
                  "key": "preset_id",
                  "value": "{{scenario_preset_id}}",
                  "description": "Path parameter: preset_id"
                }
              ]
            },
            "description": "RP-1's rule version diff shape so the SPA can reuse the diff\nviewer. Returns 404 when either version has been evicted by the\nM16.10 FIFO cap (default 20). */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/scenarios/library/custom/{preset_id}/versions/diff",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "(T6 M16.11) — field-by-field diff between two snapshots. Mirrors"
        },
        {
          "name": "POST /v1/scenarios/library/custom/bulk-clone-from-library",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/scenarios/library/custom/bulk-clone-from-library",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "scenarios",
                "library",
                "custom",
                "bulk-clone-from-library"
              ]
            },
            "description": "over the list. Cap 10 per call. Writes one scenario.create audit\nevent per successful clone (cloned_from metadata). Per-row\noutcome surfaced as {created[], skipped[]}. */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/scenarios/library/custom/bulk-clone-from-library",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "body { preset_ids[], name_prefix? } — iterates M16.8 single-clone"
        },
        {
          "name": "POST /v1/scenarios/library/custom/bulk-delete",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/scenarios/library/custom/bulk-delete",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "scenarios",
                "library",
                "custom",
                "bulk-delete"
              ]
            },
            "description": "success (some ids unknown / cross-tenant) surfaces each row's\nresult. Writes a `scenario.delete` audit event per successful delete.\nMUST be declared BEFORE `/:preset_id` so the literal segment isn't\ncaptured as a preset_id. */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/scenarios/library/custom/bulk-delete",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "up to 10 custom presets in one call. Per-row outcomes so a partial"
        },
        {
          "name": "POST /v1/scenarios/library/custom/clone-from-library",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/scenarios/library/custom/clone-from-library",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "scenarios",
                "library",
                "custom",
                "clone-from-library"
              ]
            },
            "description": "creates an editable custom copy. Writes scenario.create audit\nwith `cloned_from` metadata. Declared BEFORE /:preset_id so the\nliteral `clone-from-library` segment wins. */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/scenarios/library/custom/clone-from-library",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "body { source_preset_id, name? } — reads a library preset and"
        },
        {
          "name": "POST /v1/scenarios/library/custom/export-bundle",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/scenarios/library/custom/export-bundle",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "scenarios",
                "library",
                "custom",
                "export-bundle"
              ]
            },
            "description": "between tenants. Body { preset_ids: string[] } (cap 30). Returns\nthe bundle with deep-copied items stripped of live identity\n(re-minted on import). Mirrors M5.11 / M11.9 shape. */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/scenarios/library/custom/export-bundle",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "versioned JSON envelope for migrating N custom scenario presets"
        },
        {
          "name": "POST /v1/scenarios/library/custom/import-bundle",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/scenarios/library/custom/import-bundle",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "scenarios",
                "library",
                "custom",
                "import-bundle"
              ]
            },
            "description": "{ bundle, name_prefix? }. Per-row outcomes (created / skipped\nalready_exists / error). */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/scenarios/library/custom/import-bundle",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409, 422, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "replay an export-bundle into the caller's tenant. Body"
        },
        {
          "name": "GET /v1/scenarios/library/inventory",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/scenarios/library/inventory",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "scenarios",
                "library",
                "inventory"
              ]
            },
            "description": "4 categories × 3 severities = 12 cells in canonical row-major\norder (category major, severity minor). Per cell: library_count,\ncustom_count, total_count, library_preset_ids[] / custom_preset_ids[]\nsorted asc. Envelope: by_category + by_severity marginals (every key\npresent), most_populated_cell + most_customised_category (canonical\ntie-breaks), uncovered_cells[] (library_count=0). Mirror of M6.15\npattern. Distinct from M16.17 (category × REGULATOR) — orthogonal\npivot answers \"do we have severe-stress for every category?\".\nMounted BEFORE catch-all /:preset_id so the literal segment wins. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/scenarios/library/inventory",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "pivot of M16.1 library + M16.4 custom by category × severity."
        },
        {
          "name": "GET /v1/scenarios/library/magnitude",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/scenarios/library/magnitude",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "scenarios",
                "library",
                "magnitude"
              ]
            },
            "description": "absolute shock values vs library max). Companion to M16.15\n(radar normalisation) — M16.18 emits ONE comparable score.\nPer-preset row + per-category rollup + most_aggressive\npointers. Mounted BEFORE `/:preset_id` catch-all. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/scenarios/library/magnitude",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "severity score per preset (mean of per-axis normalised"
        },
        {
          "name": "GET /v1/scenarios/library/narratives",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/scenarios/library/narratives",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "scenarios",
                "library",
                "narratives"
              ]
            },
            "description": "three shock numbers into a single label like \"GDP −4.0%,\nrates +200bps, INR +8.5%\". Saves the SPA from rendering 3\nseparate chips with their own format specs. Mounted BEFORE\n`/:preset_id` so the literal segment wins. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/scenarios/library/narratives",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "human-readable one-liner per scenario preset. Combines the"
        },
        {
          "name": "GET /v1/scenarios/library/portfolio-ranking",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/scenarios/library/portfolio-ranking",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "scenarios",
                "library",
                "portfolio-ranking"
              ]
            },
            "description": "|gdp|/7*0.4 + |rate|/400*0.35 + |fx|/15*0.25 (RBI severely\nadverse maxima as reference). Per-row: rank, preset metadata,\nimpact_index [0-1], portfolio_tier (catastrophic/severe/\nmoderate/mild). Envelope: most_impactful, average_impact_index,\ncatastrophic_count, zero_impact_count. RBAC customers:read_risk_\nprofile. Platform-static. Mounted BEFORE /:preset_id. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/scenarios/library/portfolio-ranking",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Ranks every library preset by a weighted impact_index:"
        },
        {
          "name": "GET /v1/scenarios/library/regulator-severity-matrix",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/scenarios/library/regulator-severity-matrix",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "scenarios",
                "library",
                "regulator-severity-matrix"
              ]
            },
            "description": "INTERNAL) × 3 ScenarioSeverity cols (mild / moderate / severe).\n9 cells, each preset in exactly one cell. Per-row + per-col +\npeak_cell (canonical iteration tie-break) + empty_cells[] +\nmost_severe_regulator (highest severe count) + most_diverse +\nmost_universal. Orthogonal to M16.17 (category × regulator) —\nanswers \"do we have a severe-level RBI scenario? where are the\ngaps in stress-test severity per regulator?\". Mirror of M14.28\n/ M12.14 / M15.14 matrix pattern. Platform-static. Mounted\nBEFORE `/:preset_id` so the literal segment wins. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/scenarios/library/regulator-severity-matrix",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "— 2D pivot combining 3 ScenarioRegulator rows (RBI / IRDAI /"
        },
        {
          "name": "GET /v1/scenarios/library/sensitivity-ranking",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/scenarios/library/sensitivity-ranking",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "scenarios",
                "library",
                "sensitivity-ranking"
              ]
            },
            "description": "fx axis. Returns by_factor.{gdp,rate,fx}[] sorted by |shock| desc\nwith rank, most_*_sensitive leaderboards, and balanced_presets[]\n(all 3 factors non-zero). Mounted BEFORE /:preset_id. RBAC\ncustomers:read_risk_profile. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/scenarios/library/sensitivity-ranking",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "the M16.1 library presets by absolute shock magnitude per GDP/rate/"
        },
        {
          "name": "GET /v1/scenarios/library/shock-axis-histogram",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/scenarios/library/shock-axis-histogram",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "scenarios",
                "library",
                "shock-axis-histogram"
              ]
            },
            "description": "axes (canonical gdp → rate → fx), bucket the 10 presets'\n|shock| values into 4 magnitude ranges (zero / mild / moderate\n/ severe) with axis-aware thresholds (GDP pp / rate bps / fx %).\nPer-row {axis, thresholds, by_bucket, total_presets, min_abs,\nmax_abs, mean_abs, positive/negative/zero counts, severe_examples\ncap 3 sorted asc}. Envelope: most_severe_axis (highest severe\ncount; canonical-axis-order tie-break; null when no severe\nentries), highest_mean_axis + lowest_mean_axis (canonical-order\ntie-break; null on empty library), by_bucket_totals (marginal\ncounts across 3 axes). Companion to M16.18 (per-preset\nmagnitude — single number) — M16.20 is the per-axis bucket\ndistribution. Mirror of M6.16 multiplier histogram shape.\nPlatform-static. Mounted next to other /scenarios/library/* routes. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/scenarios/library/shock-axis-histogram",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "AXIS-pivoted view over SCENARIO_PRESETS. For each of 3 shock"
        },
        {
          "name": "GET /v1/scenarios/library/shock-directions",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/scenarios/library/shock-directions",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "scenarios",
                "library",
                "shock-directions"
              ]
            },
            "description": "from M16.18 (single magnitude per preset) + M16.20 (per-axis\nmagnitude histogram, sign-agnostic). Surfaces direction balance:\n\"are all GDP scenarios down-shocks? does the library lean\nupward on rates?\". Per-row {axis, positive_count, negative_count,\nzero_count, by_category (4-key partition), positive_examples\ncap 3 sorted raw desc, negative_examples cap 3 sorted raw asc}.\nEnvelope: most_positive_axis + most_negative_axis +\nmost_neutral_axis (all canonical ALL_SHOCK_AXES tie-break);\nby_direction_totals (Σ across 3 axes = 3 × total_presets).\nPlatform-static. Mounted BEFORE `/:preset_id` so the literal\nsegment wins. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/scenarios/library/shock-directions",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "pivot by SIGN of the shock (positive / negative / zero). Distinct"
        },
        {
          "name": "GET /v1/scenarios/library/shock-vectors",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/scenarios/library/shock-vectors",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "scenarios",
                "library",
                "shock-vectors"
              ]
            },
            "description": "library-wide MIN/MAX per axis. Returns raw + normalized per\npreset plus the per-axis range envelope. Lets the SPA render\na unified radar chart comparing all presets without each axis\nfighting on its own scale. Mounted BEFORE `/:preset_id` so the\nliteral segment wins. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/scenarios/library/shock-vectors",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "every library preset's GDP/rate/FX shocks onto [-1, 1] using the"
        },
        {
          "name": "GET /v1/scenarios/library/stress-contribution",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/scenarios/library/stress-contribution",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "scenarios",
                "library",
                "stress-contribution"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/scenarios/library/stress-contribution",
                  "pm.test('status is documented', () => {",
                  "  pm.expect([200, 204, 400, 401, 403, 404, 409, 501, 503]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "// Envelope shape only for application/json responses.",
                  "// Skip on text/plain (.txt summaries), text/markdown (.md schema),",
                  "// text/event-stream (SSE), and any non-JSON response.",
                  "const ct = (pm.response.headers.get('content-type') || '').toLowerCase();",
                  "const isJson = ct.includes('application/json');",
                  "if (isJson && pm.response.code >= 200 && pm.response.code < 300 && pm.response.code !== 204) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'SUCCESS');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (isJson && pm.response.code >= 400 && pm.response.code !== 429 && pm.response.code !== 501) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'FAILURE');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "preset stress factor decomposition. Mounted BEFORE /:id catch-all. */"
        }
      ]
    }
  ]
}
