{
  "info": {
    "_postman_id": "a6cf06b0-c73e-4f6d-8a3d-c461bf9fb6ab",
    "name": "ZorEWS — Workflow APIs",
    "description": "Workflow routes from the ZorEWS BFF + auth-svc surface. 92 operations. Auto-generated from `docs/api/swagger.json` by `scripts/gen-postman.js`. Re-import after running `npm run openapi:generate` to refresh.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "auth": {
    "type": "bearer",
    "bearer": [
      {
        "key": "token",
        "value": "{{access_token}}",
        "type": "string"
      }
    ]
  },
  "event": [
    {
      "listen": "prerequest",
      "script": {
        "type": "text/javascript",
        "exec": [
          "// Collection-level pre-request — log the active environment + tenant for visibility",
          "if (!pm.environment.get('access_token') && pm.request.url.toString().indexOf('/auth/login') === -1 && pm.request.url.toString().indexOf('/oauth/token') === -1 && pm.request.url.toString().indexOf('/healthz') === -1 && pm.request.url.toString().indexOf('/.well-known/') === -1) {",
          "  console.warn('access_token is empty — run the Smoke > Login request first to capture a token.');",
          "}"
        ]
      }
    }
  ],
  "variable": [],
  "item": [
    {
      "name": "00 — Smoke tests",
      "description": "Happy-path + negative-test sanity checks. Run this folder first to verify the local stack + auth flow.",
      "item": [
        {
          "name": "✅ Happy path — GET /api/cases",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{bff_url}}/api/cases",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "api",
                "cases"
              ]
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('2xx or 401 (no auth in env)', () => {",
                  "  pm.expect([200, 201, 401, 403]).to.include(pm.response.code);",
                  "});"
                ]
              }
            }
          ]
        }
      ]
    },
    {
      "name": "/api/cases",
      "description": "2 route(s) under /api/cases",
      "item": [
        {
          "name": "GET /api/cases",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{bff_url}}/api/cases",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "api",
                "cases"
              ]
            },
            "description": "v1.5 B4: when unified.cases reader is wired, read directly from pg\n(skips HTTP hop to cases-svc + gains has_blocking_caps + customer\nrisk overlay for free). Else fall through to the cases-svc proxy."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /api/cases",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "/api/cases — list of cases for the SPA Cases page."
        },
        {
          "name": "GET /api/cases/{id}",
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{bff_url}}/api/cases/:id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "api",
                "cases",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: id"
                }
              ]
            },
            "description": "v1.5 B4: when unified.cases reader is wired, read pg directly\n(no HTTP hop, has_blocking_caps + customer_risk_level overlays\nsurfaced for free). Else fall through to cases-svc proxy."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /api/cases/{id}",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "/api/cases/:id — single case detail"
        }
      ]
    },
    {
      "name": "/v1/action",
      "description": "1 route(s) under /v1/action",
      "item": [
        {
          "name": "POST /v1/action",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/action",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "action"
              ]
            },
            "description": "body: { case_id, kind, officer_id, outcome_note?, gps? } (or wrapped).\nProxies to regulatory-svc/cases POST /cases/:id/actions.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/action",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/action — T4.24 envelope + tenant."
        }
      ]
    },
    {
      "name": "/v1/cases/:case_id",
      "description": "2 route(s) under /v1/cases/:case_id",
      "item": [
        {
          "name": "GET /v1/cases/{case_id}/events",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/cases/:case_id/events",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "cases",
                ":case_id",
                "events"
              ],
              "variable": [
                {
                  "key": "case_id",
                  "value": "{{case_id}}",
                  "description": "Path parameter: case_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/cases/{case_id}/events",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/cases/:case_id/events — auto-discovered (server.ts:11464)"
        },
        {
          "name": "GET /v1/cases/{case_id}/timeline",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/cases/:case_id/timeline",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "cases",
                ":case_id",
                "timeline"
              ],
              "variable": [
                {
                  "key": "case_id",
                  "value": "{{case_id}}",
                  "description": "Path parameter: case_id"
                }
              ]
            },
            "description": "CaseTimeline with `transitions[]` (per-state durations), action\ncounts, current_state + age, total_age_hours. Pure-function over\nthe existing forCase store API; unknown case returns an empty\ntimeline (not 404) because the event log is total. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/cases/{case_id}/timeline",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "full state-transition ladder from the M9.4 event journal. Returns"
        }
      ]
    },
    {
      "name": "/v1/cases/events",
      "description": "5 route(s) under /v1/cases/events",
      "item": [
        {
          "name": "GET /v1/cases/events",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/cases/events",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "cases",
                "events"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/cases/events",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/cases/events — auto-discovered (server.ts:11331)"
        },
        {
          "name": "POST /v1/cases/events",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/cases/events",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "cases",
                "events"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/cases/events",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "body { case_id, action, actor, payload? }. */"
        },
        {
          "name": "GET /v1/cases/events/{event_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/cases/events/:event_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "cases",
                "events",
                ":event_id"
              ],
              "variable": [
                {
                  "key": "event_id",
                  "value": "<event_id>",
                  "description": "Path parameter: event_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/cases/events/{event_id}",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/cases/events/:event_id — auto-discovered (server.ts:11443)"
        },
        {
          "name": "GET /v1/cases/events/action-distribution",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/cases/events/action-distribution",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "cases",
                "events",
                "action-distribution"
              ]
            },
            "description": "actions (opened / state_change / closed / escalated /\noverride_requested / override_approved / override_rejected /\nnote_added / checklist_updated). Per-row {action, count,\ndistinct_cases, distinct_actors, most_recent_at (null when\ncount=0), sample_actors (cap 3, sorted newest-first by their\nmost-recent event of this action then actor asc tie-break)}.\nEnvelope: most_common_action (highest count + canonical-order\ntie-break via iteration; null on empty), unused_actions[]\n(canonical order; zero-count subset), most_active_actor\n(fleet-wide highest count regardless of action + canonical\nusername asc tie-break; null on empty). Mirror of M14.27 / M7.13\n/ M3.13 1D distribution pattern. MUST be registered BEFORE\n`/v1/cases/events/:event_id` so the literal `/action-distribution`\nsegment isn't captured by the param wildcard. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/cases/events/action-distribution",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "ACTION 1D rollup over the M9.4 case event journal. 9 canonical"
        },
        {
          "name": "GET /v1/cases/events/transition-matrix",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/cases/events/transition-matrix",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "cases",
                "events",
                "transition-matrix"
              ]
            },
            "description": "matrix of from-state × to-state. Per-cell: count + distinct_cases\n(Set-deduped at the (case, to-state) granularity for rows /\ncase-only for columns). States are AUTO-DISCOVERED from observed\npayload values — works across CmsCaseState + InvestigationStatus\nwithout branching. Envelope: peak_cell (canonical iteration\ntie-break: state names asc; null on empty), most_common_destination\n+ most_common_source (canonical tie-break by state name asc),\ndead_ends[] (states seen as destinations but never as sources —\nterminal-only; sorted asc), origins[] (states seen as sources but\nnever as destinations — entry-only), self_transition_count\n(sanity check; should be near-zero with proper state machines).\nDistinct from M9.6 (per-case chronological ladder) by being\nfleet-wide cross-tab. MUST be registered BEFORE `/:event_id`\nwildcard. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/cases/events/transition-matrix",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "state_change event with payload {from, to}, pivot into a 2D"
        }
      ]
    },
    {
      "name": "/v1/cases/maker-checker",
      "description": "6 route(s) under /v1/cases/maker-checker",
      "item": [
        {
          "name": "GET /v1/cases/maker-checker",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/cases/maker-checker",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "cases",
                "maker-checker"
              ]
            },
            "description": "page=&page_size="
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/cases/maker-checker",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/cases/maker-checker?status=&action_type=&case_id=&maker_username=&"
        },
        {
          "name": "POST /v1/cases/maker-checker",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/cases/maker-checker",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "cases",
                "maker-checker"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/cases/maker-checker",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/cases/maker-checker — auto-discovered (server.ts:11088)"
        },
        {
          "name": "GET /v1/cases/maker-checker/{action_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/cases/maker-checker/:action_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "cases",
                "maker-checker",
                ":action_id"
              ],
              "variable": [
                {
                  "key": "action_id",
                  "value": "<action_id>",
                  "description": "Path parameter: action_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/cases/maker-checker/{action_id}",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/cases/maker-checker/:action_id — auto-discovered (server.ts:11207)"
        },
        {
          "name": "POST /v1/cases/maker-checker/{action_id}/approve",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/cases/maker-checker/:action_id/approve",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "cases",
                "maker-checker",
                ":action_id",
                "approve"
              ],
              "variable": [
                {
                  "key": "action_id",
                  "value": "<action_id>",
                  "description": "Path parameter: action_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/cases/maker-checker/{action_id}/approve",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/cases/maker-checker/:action_id/approve — auto-discovered (server.ts:11228)"
        },
        {
          "name": "POST /v1/cases/maker-checker/{action_id}/reject",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/cases/maker-checker/:action_id/reject",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "cases",
                "maker-checker",
                ":action_id",
                "reject"
              ],
              "variable": [
                {
                  "key": "action_id",
                  "value": "<action_id>",
                  "description": "Path parameter: action_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/cases/maker-checker/{action_id}/reject",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/cases/maker-checker/:action_id/reject — auto-discovered (server.ts:11261)"
        },
        {
          "name": "GET /v1/cases/maker-checker/reviewer-rollup",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/cases/maker-checker/reviewer-rollup",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "cases",
                "maker-checker",
                "reviewer-rollup"
              ]
            },
            "description": "decided actions (approved + rejected). Per checker:\n{checker_username, total_decisions, approved_count, rejected_count,\napproval_rate (null when 0), distinct_cases + case_ids[] sorted\nasc cap 50, by_action_type (every SensitiveActionType at 0 when\nabsent: case.close / case.escalate / case.override_decision),\nmost_recent_at}. Envelope: most_active_reviewer (top by\ntotal_decisions + canonical username asc tie-break; null on\nempty), rubber_stamp_reviewers[] (approval_rate=1.0 AND >=3\ndecisions — surfaces checkers who haven't rejected any sensitive\ncase action). Mirror of M7.16 (AI promotion reviewer rollup) +\nM2.15 / M13.16 / M15.8 / M9.14 / M11.15 per-actor pattern for the\ncase maker-checker surface. Drives BIL compliance segregation\nof-duties quarterly review. Mounted BEFORE /:action_id so the\nliteral /reviewer-rollup segment isn't captured. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/cases/maker-checker/reviewer-rollup",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "CHECKER pivot over M9.3 sensitive case actions. Counts only"
        }
      ]
    },
    {
      "name": "/v1/cases/sla-breaches",
      "description": "1 route(s) under /v1/cases/sla-breaches",
      "item": [
        {
          "name": "GET /v1/cases/sla-breaches",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/cases/sla-breaches",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "cases",
                "sla-breaches"
              ]
            },
            "description": "against the per-state SLA, and surface cases past their window.\nWorst-first list capped at 50 entries. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/cases/sla-breaches",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "state timeline from the M9.4 event journal, compare time-in-state"
        }
      ]
    },
    {
      "name": "/v1/cases/sla-summary",
      "description": "1 route(s) under /v1/cases/sla-summary",
      "item": [
        {
          "name": "GET /v1/cases/sla-summary",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/cases/sla-summary",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "cases",
                "sla-summary"
              ]
            },
            "description": "GET /v1/cases/sla-summary\nReturns SLA classification across the synthetic case fleet:\n- by_severity counts (on_track / approaching / breached / closed)\n- portfolio totals\n- the breached_cases list (most-overdue first)\nVisible to anyone with cases:list."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/cases/sla-summary",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "End CMS-3 routes"
        }
      ]
    },
    {
      "name": "/v1/cases/states",
      "description": "1 route(s) under /v1/cases/states",
      "item": [
        {
          "name": "GET /v1/cases/states/graph",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/cases/states/graph",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "cases",
                "states",
                "graph"
              ]
            },
            "description": "allowed_next_states[]}. Lets the SPA build a data-driven \"Move\ncase to...\" dropdown + \"Status legend\" tooltip instead of\nhardcoding the state graph in two places. Same shape for every\ntenant (the state machine is platform-static). audit:read RBAC. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/cases/states/graph",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "catalog. Per state: {state, sla_hours_default, terminal,"
        }
      ]
    },
    {
      "name": "/v1/cms/automation",
      "description": "4 route(s) under /v1/cms/automation",
      "item": [
        {
          "name": "POST /v1/cms/automation/auto-create-from-alert",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/cms/automation/auto-create-from-alert",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "cms",
                "automation",
                "auto-create-from-alert"
              ]
            },
            "description": "Idempotent on alert_id. Returns {case, created, matched_case_id?}. */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/cms/automation/auto-create-from-alert",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "body { alert_id, alert_severity, customer_id?, rule_id?, rule_name?, context? }"
        },
        {
          "name": "GET /v1/cms/automation/inactive-cases",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/cms/automation/inactive-cases",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "cms",
                "automation",
                "inactive-cases"
              ]
            },
            "description": "sorted longest-inactive first. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/cms/automation/inactive-cases",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Cases with status != CLOSED + updated_at older than threshold,"
        },
        {
          "name": "GET /v1/cms/automation/pool",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/cms/automation/pool",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "cms",
                "automation",
                "pool"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/cms/automation/pool",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/cms/automation/pool — auto-discovered (server.ts:10620)"
        },
        {
          "name": "PUT /v1/cms/automation/pool",
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/cms/automation/pool",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "cms",
                "automation",
                "pool"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PUT /v1/cms/automation/pool",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PUT /v1/cms/automation/pool — auto-discovered (server.ts:10632)"
        }
      ]
    },
    {
      "name": "/v1/cms/cases",
      "description": "21 route(s) under /v1/cms/cases",
      "item": [
        {
          "name": "GET /v1/cms/cases",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/cms/cases",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "cms",
                "cases"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/cms/cases",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/cms/cases — auto-discovered (server.ts:9839)"
        },
        {
          "name": "POST /v1/cms/cases",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/cms/cases",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "cms",
                "cases"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/cms/cases",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/cms/cases — auto-discovered (server.ts:9919)"
        },
        {
          "name": "GET /v1/cms/cases/{case_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/cms/cases/:case_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "cms",
                "cases",
                ":case_id"
              ],
              "variable": [
                {
                  "key": "case_id",
                  "value": "{{case_id}}",
                  "description": "Path parameter: case_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/cms/cases/{case_id}",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/cms/cases/:case_id — auto-discovered (server.ts:9946)"
        },
        {
          "name": "PATCH /v1/cms/cases/{case_id}",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/cms/cases/:case_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "cms",
                "cases",
                ":case_id"
              ],
              "variable": [
                {
                  "key": "case_id",
                  "value": "{{case_id}}",
                  "description": "Path parameter: case_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PATCH /v1/cms/cases/{case_id}",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PATCH /v1/cms/cases/:case_id — auto-discovered (server.ts:9989)"
        },
        {
          "name": "POST /v1/cms/cases/{case_id}/assign",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/cms/cases/:case_id/assign",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "cms",
                "cases",
                ":case_id",
                "assign"
              ],
              "variable": [
                {
                  "key": "case_id",
                  "value": "{{case_id}}",
                  "description": "Path parameter: case_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/cms/cases/{case_id}/assign",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/cms/cases/:case_id/assign — auto-discovered (server.ts:10106)"
        },
        {
          "name": "POST /v1/cms/cases/{case_id}/assign-from-pool",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/cms/cases/:case_id/assign-from-pool",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "cms",
                "cases",
                ":case_id",
                "assign-from-pool"
              ],
              "variable": [
                {
                  "key": "case_id",
                  "value": "{{case_id}}",
                  "description": "Path parameter: case_id"
                }
              ]
            },
            "description": "assignment + advances rotation deterministically. */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/cms/cases/{case_id}/assign-from-pool",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Round-robin from the tenant's assignee pool. Updates last"
        },
        {
          "name": "GET /v1/cms/cases/{case_id}/attachments",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/cms/cases/:case_id/attachments",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "cms",
                "cases",
                ":case_id",
                "attachments"
              ],
              "variable": [
                {
                  "key": "case_id",
                  "value": "{{case_id}}",
                  "description": "Path parameter: case_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/cms/cases/{case_id}/attachments",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/cms/cases/:case_id/attachments — auto-discovered (server.ts:10343)"
        },
        {
          "name": "POST /v1/cms/cases/{case_id}/attachments",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/cms/cases/:case_id/attachments",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "cms",
                "cases",
                ":case_id",
                "attachments"
              ],
              "variable": [
                {
                  "key": "case_id",
                  "value": "{{case_id}}",
                  "description": "Path parameter: case_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/cms/cases/{case_id}/attachments",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "{ file_name, file_size, mime_type } — registers metadata. */"
        },
        {
          "name": "DELETE /v1/cms/cases/{case_id}/attachments/{attachment_id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/cms/cases/:case_id/attachments/:attachment_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "cms",
                "cases",
                ":case_id",
                "attachments",
                ":attachment_id"
              ],
              "variable": [
                {
                  "key": "case_id",
                  "value": "{{case_id}}",
                  "description": "Path parameter: case_id"
                },
                {
                  "key": "attachment_id",
                  "value": "<attachment_id>",
                  "description": "Path parameter: attachment_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/cms/cases/{case_id}/attachments/{attachment_id}",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([204, 200, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DELETE /v1/cms/cases/:case_id/attachments/:attachment_id — auto-discovered (server.ts:10393)"
        },
        {
          "name": "GET /v1/cms/cases/{case_id}/attachments/{attachment_id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/cms/cases/:case_id/attachments/:attachment_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "cms",
                "cases",
                ":case_id",
                "attachments",
                ":attachment_id"
              ],
              "variable": [
                {
                  "key": "case_id",
                  "value": "{{case_id}}",
                  "description": "Path parameter: case_id"
                },
                {
                  "key": "attachment_id",
                  "value": "<attachment_id>",
                  "description": "Path parameter: attachment_id"
                }
              ]
            },
            "description": "+ a `cms://` placeholder URL since blobs aren't persisted). */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/cms/cases/{case_id}/attachments/{attachment_id}",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "attachment metadata (the prototype's \"download\" surfaces metadata"
        },
        {
          "name": "PATCH /v1/cms/cases/{case_id}/category",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/cms/cases/:case_id/category",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "cms",
                "cases",
                ":case_id",
                "category"
              ],
              "variable": [
                {
                  "key": "case_id",
                  "value": "{{case_id}}",
                  "description": "Path parameter: case_id"
                }
              ]
            },
            "description": "fell through to NULL can be re-categorised by an admin so the\ndashboard SLA Breach Matrix resolver picks up the right config. */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PATCH /v1/cms/cases/{case_id}/category",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Closes the loop on migration 019's heuristic backfill: rows that"
        },
        {
          "name": "POST /v1/cms/cases/{case_id}/close",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/cms/cases/:case_id/close",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "cms",
                "cases",
                ":case_id",
                "close"
              ],
              "variable": [
                {
                  "key": "case_id",
                  "value": "{{case_id}}",
                  "description": "Path parameter: case_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/cms/cases/{case_id}/close",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/cms/cases/:case_id/close — auto-discovered (server.ts:10192)"
        },
        {
          "name": "POST /v1/cms/cases/{case_id}/escalate",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/cms/cases/:case_id/escalate",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "cms",
                "cases",
                ":case_id",
                "escalate"
              ],
              "variable": [
                {
                  "key": "case_id",
                  "value": "{{case_id}}",
                  "description": "Path parameter: case_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/cms/cases/{case_id}/escalate",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/cms/cases/:case_id/escalate — auto-discovered (server.ts:10163)"
        },
        {
          "name": "GET /v1/cms/cases/{case_id}/history",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/cms/cases/:case_id/history",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "cms",
                "cases",
                ":case_id",
                "history"
              ],
              "variable": [
                {
                  "key": "case_id",
                  "value": "{{case_id}}",
                  "description": "Path parameter: case_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/cms/cases/{case_id}/history",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/cms/cases/:case_id/history — auto-discovered (server.ts:10482)"
        },
        {
          "name": "GET /v1/cms/cases/{case_id}/notes",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/cms/cases/:case_id/notes",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "cms",
                "cases",
                ":case_id",
                "notes"
              ],
              "variable": [
                {
                  "key": "case_id",
                  "value": "{{case_id}}",
                  "description": "Path parameter: case_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/cms/cases/{case_id}/notes",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/cms/cases/:case_id/notes — auto-discovered (server.ts:10278)"
        },
        {
          "name": "POST /v1/cms/cases/{case_id}/notes",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/cms/cases/:case_id/notes",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "cms",
                "cases",
                ":case_id",
                "notes"
              ],
              "variable": [
                {
                  "key": "case_id",
                  "value": "{{case_id}}",
                  "description": "Path parameter: case_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/cms/cases/{case_id}/notes",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/cms/cases/:case_id/notes — auto-discovered (server.ts:10247)"
        },
        {
          "name": "GET /v1/cms/cases/{case_id}/tracking",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/cms/cases/:case_id/tracking",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "cms",
                "cases",
                ":case_id",
                "tracking"
              ],
              "variable": [
                {
                  "key": "case_id",
                  "value": "{{case_id}}",
                  "description": "Path parameter: case_id"
                }
              ]
            },
            "description": "type discrimination + payload context so the SPA's Timeline tab can\nrender each card as a clickable drill-down with appropriate\nper-type behaviour. Stub events for cross-service sources\n(CAS / CAP / approval) only appear when ?include_stubs=true. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/cms/cases/{case_id}/tracking",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "Per-case tracking timeline — wraps the existing history rows with"
        },
        {
          "name": "POST /v1/cms/cases/{case_id}/transition",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/cms/cases/:case_id/transition",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "cms",
                "cases",
                ":case_id",
                "transition"
              ],
              "variable": [
                {
                  "key": "case_id",
                  "value": "{{case_id}}",
                  "description": "Path parameter: case_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/cms/cases/{case_id}/transition",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/cms/cases/:case_id/transition — auto-discovered (server.ts:10069)"
        },
        {
          "name": "POST /v1/cms/cases/bulk-assign",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/cms/cases/bulk-assign",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "cms",
                "cases",
                "bulk-assign"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/cms/cases/bulk-assign",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/cms/cases/bulk-assign — auto-discovered (server.ts:9781)"
        },
        {
          "name": "GET /v1/cms/cases/sla-breaches",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/cms/cases/sla-breaches",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "cms",
                "cases",
                "sla-breaches"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/cms/cases/sla-breaches",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "sorted by overshoot (most-overdue first). */"
        },
        {
          "name": "GET /v1/cms/cases/stats",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/cms/cases/stats",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "cms",
                "cases",
                "stats"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/cms/cases/stats",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/cms/cases/stats — auto-discovered (server.ts:9693)"
        }
      ]
    },
    {
      "name": "/v1/field/officers",
      "description": "1 route(s) under /v1/field/officers",
      "item": [
        {
          "name": "GET /v1/field/officers/{officer_id}/today",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/field/officers/:officer_id/today",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "field",
                "officers",
                ":officer_id",
                "today"
              ],
              "variable": [
                {
                  "key": "officer_id",
                  "value": "<officer_id>",
                  "description": "Path parameter: officer_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/field/officers/{officer_id}/today",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "— visits logged \"today\" in the requested zone. */"
        }
      ]
    },
    {
      "name": "/v1/field/operations",
      "description": "1 route(s) under /v1/field/operations",
      "item": [
        {
          "name": "GET /v1/field/operations/analytics",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/field/operations/analytics",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "field",
                "operations",
                "analytics"
              ]
            },
            "description": "officers + customers, per-officer breakdown (visit count, success\nrate, last visit), mean visits per officer.\nFilters mirror /v1/field/visits: ?officer_id= ?customer_id=\n?outcome= ?since= ?until=. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/field/operations/analytics",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "rollup over the M14.10 visit ledger: outcome mix, distinct"
        }
      ]
    },
    {
      "name": "/v1/field/visits",
      "description": "4 route(s) under /v1/field/visits",
      "item": [
        {
          "name": "GET /v1/field/visits",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/field/visits",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "field",
                "visits"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/field/visits",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "?customer_id=&officer_id=&outcome=&since=ISO&until=ISO. */"
        },
        {
          "name": "POST /v1/field/visits",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/field/visits",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "field",
                "visits"
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/field/visits",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "{officer_id, customer_id, visit_at, outcome, note, location?}. */"
        },
        {
          "name": "GET /v1/field/visits/dow-hour-heatmap",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/field/visits/dow-hour-heatmap",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "field",
                "visits",
                "dow-hour-heatmap"
              ]
            },
            "description": "..Sun=6 ordering. ?tz= shifts the wall-clock used for bucketing\n(13-zone whitelist from M12.4 / report_schedules). Filters mirror\n/v1/field/visits: ?officer_id= ?customer_id= ?outcome= ?since=\n?until=. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/field/visits/dow-hour-heatmap",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "week × hour-of-day matrix over the M14.10 visit ledger. ISO Mon=0"
        },
        {
          "name": "GET /v1/field/visits/geo-clusters",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/field/visits/geo-clusters",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "field",
                "visits",
                "geo-clusters"
              ]
            },
            "description": "Visits without GPS are skipped + counted in total_without_gps.\nDefault radius 1 km, max 500 km. Clusters sorted by visit_count\ndesc with latest_visit_at tie-break. audit:read RBAC. Mounted\nBEFORE any `:visit_id` wildcard would conflict (none currently). */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/field/visits/geo-clusters",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "— greedy Haversine clustering of field visits with GPS pins."
        }
      ]
    },
    {
      "name": "/v1/investigations",
      "description": "2 route(s) under /v1/investigations",
      "item": [
        {
          "name": "GET /v1/investigations",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/investigations",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "investigations"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/investigations",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/investigations — auto-discovered (server.ts:11518)"
        },
        {
          "name": "POST /v1/investigations",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/investigations",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "investigations"
              ]
            },
            "description": "Open a fresh investigation for a case. 409 when the case already\nhas an open one (must close first). 400 on missing fields.\nWhen `checklist_template_id` is supplied, the template's steps\nseed the investigation instead of the default 8-step BIL §17 set.\n404 when the template id is unknown / cross-tenant.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/investigations",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/investigations body: { case_id, customer_id, checklist_template_id? }"
        }
      ]
    },
    {
      "name": "/v1/investigations/:id",
      "description": "6 route(s) under /v1/investigations/:id",
      "item": [
        {
          "name": "GET /v1/investigations/{id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/investigations/:id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "investigations",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/investigations/{id}",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/investigations/:id — auto-discovered (server.ts:11855)"
        },
        {
          "name": "GET /v1/investigations/{id}/notes",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/investigations/:id/notes",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "investigations",
                ":id",
                "notes"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/investigations/{id}/notes",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/investigations/:id/notes — auto-discovered (server.ts:11991)"
        },
        {
          "name": "POST /v1/investigations/{id}/notes",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/investigations/:id/notes",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "investigations",
                ":id",
                "notes"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: id"
                }
              ]
            },
            "description": "Append a note to the investigation thread. Returns the note +\nthe updated investigation (with bumped notes_count).",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/investigations/{id}/notes",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/investigations/:id/notes body: { body }"
        },
        {
          "name": "PATCH /v1/investigations/{id}/status",
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/investigations/:id/status",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "investigations",
                ":id",
                "status"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: id"
                }
              ]
            },
            "description": "Transition through the workflow. The state machine enforces legal\ntransitions; closing from `decision` requires a non-null decision.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for PATCH /v1/investigations/{id}/status",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PATCH /v1/investigations/:id/status body: { status, decision? }"
        },
        {
          "name": "GET /v1/investigations/{id}/step-progress",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/investigations/:id/step-progress",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "investigations",
                ":id",
                "step-progress"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: id"
                }
              ]
            },
            "description": "newest-first recent completions (cap 5). Mounted BEFORE the\ncatch-all /:id GET so the literal /step-progress segment wins. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/investigations/{id}/step-progress",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "step progress card: counts + completion rate + oldest pending +"
        },
        {
          "name": "POST /v1/investigations/{id}/steps/{step_id}/complete",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/investigations/:id/steps/:step_id/complete",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "investigations",
                ":id",
                "steps",
                ":step_id",
                "complete"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: id"
                },
                {
                  "key": "step_id",
                  "value": "<step_id>",
                  "description": "Path parameter: step_id"
                }
              ]
            },
            "description": "Mark a checklist step as completed.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/investigations/{id}/steps/{step_id}/complete",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/investigations/:id/steps/:step_id/complete body: { evidence_link? }"
        }
      ]
    },
    {
      "name": "/v1/investigations/age-distribution",
      "description": "1 route(s) under /v1/investigations/age-distribution",
      "item": [
        {
          "name": "GET /v1/investigations/age-distribution",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/investigations/age-distribution",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "investigations",
                "age-distribution"
              ]
            },
            "description": "3-7d / 7-30d / 30d+). Per-bucket includes up to 3 oldest as\ndrill-through samples. Mounted BEFORE /:id catch-all so the\nliteral /age-distribution segment wins. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/investigations/age-distribution",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "of open + closed investigations by age bucket (< 24h / 1-3d /"
        }
      ]
    },
    {
      "name": "/v1/investigations/age-status-matrix",
      "description": "1 route(s) under /v1/investigations/age-status-matrix",
      "item": [
        {
          "name": "GET /v1/investigations/age-status-matrix",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/investigations/age-status-matrix",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "investigations",
                "age-status-matrix"
              ]
            },
            "description": "with every (status, bucket) pair emitted at 0 when absent.\nPer-row totals (Σ buckets per status) + per-column totals\n(Σ statuses per bucket). Envelope: peak_cell (highest count;\ncanonical row × col iteration tie-break), oldest_open_status\n(status with most cases in 7_to_30d + 30d_plus combined; canonical\nstatus order tie-break). Drives \"which status has the most stuck\ncases?\" triage view. Mirror of M3.11 type-matrix pattern.\nMounted BEFORE /:id catch-all so the literal segment wins. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/investigations/age-status-matrix",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "combining M9.11 age buckets × M9.8 cohort status. Per-cell count"
        }
      ]
    },
    {
      "name": "/v1/investigations/checklists",
      "description": "4 route(s) under /v1/investigations/checklists",
      "item": [
        {
          "name": "GET /v1/investigations/checklists",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/investigations/checklists",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "investigations",
                "checklists"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/investigations/checklists",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/investigations/checklists — auto-discovered (server.ts:10938)"
        },
        {
          "name": "POST /v1/investigations/checklists",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/investigations/checklists",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "investigations",
                "checklists"
              ]
            },
            "description": "Create a custom template for the caller's tenant.",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/investigations/checklists",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/investigations/checklists body: CreateTemplateInput"
        },
        {
          "name": "DELETE /v1/investigations/checklists/{id}",
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/investigations/checklists/:id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "investigations",
                "checklists",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: id"
                }
              ]
            },
            "description": "409 EWS_409_cannot_delete_builtin when targeting the platform default.\n404 EWS_404_unknown_template when id is unknown / cross-tenant."
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for DELETE /v1/investigations/checklists/{id}",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([204, 200, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "DELETE /v1/investigations/checklists/:id — delete a custom template."
        },
        {
          "name": "GET /v1/investigations/checklists/{id}",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/investigations/checklists/:id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "investigations",
                "checklists",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "{{customer_id}}",
                  "description": "Path parameter: id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/investigations/checklists/{id}",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/investigations/checklists/:id — auto-discovered (server.ts:10961)"
        }
      ]
    },
    {
      "name": "/v1/investigations/duration-histogram",
      "description": "1 route(s) under /v1/investigations/duration-histogram",
      "item": [
        {
          "name": "GET /v1/investigations/duration-histogram",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/investigations/duration-histogram",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "investigations",
                "duration-histogram"
              ]
            },
            "description": "Per-bucket {count, samples cap 3 — closed buckets sort\nduration desc (longest first); still_open sorts opened_at asc\n(oldest pending first)}. Envelope: peak_bucket (canonical\niteration tie-break — under_1h beats 1_to_24h at tied; null\nwhen zero), mean/median/p95_duration_ms over closed only via\nM3.5 linear-interpolation percentile (null when no closed),\nlongest_closed + oldest_open samples + empty_buckets[]\ncanonical. Distinct from M9.11 (case age — time SINCE opened),\nM9.13 (age × status matrix), M9.18 (note volume timeline).\nMirror of M8.12 alert ack-time + M7.15 promotion latency\nhistogram for the investigation surface. Drives BIL ops\n\"what's our typical fraud-investigation turnaround? any\ninvestigations dragging past 30 days?\" review. Mounted BEFORE\n/:id catch-all so the literal segment wins. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/investigations/duration-histogram",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "30d → 30d_plus; matches M8.12 / M9.11 boundary convention)."
        }
      ]
    },
    {
      "name": "/v1/investigations/note-authorship",
      "description": "1 route(s) under /v1/investigations/note-authorship",
      "item": [
        {
          "name": "GET /v1/investigations/note-authorship",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/investigations/note-authorship",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "investigations",
                "note-authorship"
              ]
            },
            "description": "total_notes, distinct_investigations, first_note_at, last_note_at,\nnotes_per_investigation_status (every InvestigationStatus key at 0)}.\nEnvelope: authors[] sorted total_notes desc + author_username asc,\nmost_active_author (null on empty). Mirror of M15.8 (audit per\nactor activity) for the investigation-notes surface. Companion to\nM9.10 (cross-investigation note SEARCH) — that surface is BY-TEXT;\nthis is BY-AUTHOR. Mounted BEFORE /:id catch-all so the literal\nsegment isn't captured by the param wildcard. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/investigations/note-authorship",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "rollup over investigation notes. Per-author row {author_username,"
        }
      ]
    },
    {
      "name": "/v1/investigations/notes",
      "description": "2 route(s) under /v1/investigations/notes",
      "item": [
        {
          "name": "GET /v1/investigations/notes/daily-volume",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/investigations/notes/daily-volume",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "investigations",
                "notes",
                "daily-volume"
              ]
            },
            "description": "the tenant. Companion to M9.14 (per-actor authorship pivot).\nPer-bucket {date, total, distinct_investigations, distinct_authors};\nenvelope: peak_day (earliest-day-wins tie-break) + mean_per_day\n+ growth_rate (second-half vs first-half; null when first-half=0\nOR days<2) + busiest_author (canonical username asc tie-break).\nDefault days=30, bounds [1, 365]. Mirror of M15.11 / M10.15 /\nM12.13 / M1.9 / M3.17 / M8.15 daily-volume pattern. Mounted\nBEFORE /:id catch-all so the literal segment wins. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/investigations/notes/daily-volume",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "per-UTC-day count of notes added across all investigations in"
        },
        {
          "name": "GET /v1/investigations/notes/search",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/investigations/notes/search",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "investigations",
                "notes",
                "search"
              ]
            },
            "description": "Returns per-match {note_id, investigation_id, case_id, ts,\nauthor, snippet, match_count_in_note}. Newest-first; cap 200.\nq must be 2..200 chars (after trim). Mounted BEFORE /:id\ncatch-all so the literal /notes/search wins. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/investigations/notes/search",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "cross-investigation substring search over the notes thread."
        }
      ]
    },
    {
      "name": "/v1/investigations/outcome-by-template",
      "description": "1 route(s) under /v1/investigations/outcome-by-template",
      "item": [
        {
          "name": "GET /v1/investigations/outcome-by-template",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/investigations/outcome-by-template",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "investigations",
                "outcome-by-template"
              ]
            },
            "description": "\"which checklist gives the highest fraud-confirmation rate?\".\nPer-template per-decision split + confirmation_rate\n(fraud_confirmed + partial_fraud / closed-with-decision).\nMounted BEFORE /:id catch-all so the literal\n/outcome-by-template segment wins. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/investigations/outcome-by-template",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "groups investigations by `checklist_template_id` to answer"
        }
      ]
    },
    {
      "name": "/v1/investigations/step-backlog",
      "description": "1 route(s) under /v1/investigations/step-backlog",
      "item": [
        {
          "name": "GET /v1/investigations/step-backlog",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/investigations/step-backlog",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "investigations",
                "step-backlog"
              ]
            },
            "description": "emits {step_id, name, pending_count, completed_count,\ncases_with_step, open_pending_count}. Sorted by\nopen_pending_count desc — biggest bottleneck first. Lets ops\nspot \"most cases get stuck at the interview_claimant step\".\nMounted BEFORE /:id catch-all so the literal segment wins. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/investigations/step-backlog",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "per-step backlog. For each step_id seen across the cohort"
        }
      ]
    },
    {
      "name": "/v1/investigations/summary",
      "description": "1 route(s) under /v1/investigations/summary",
      "item": [
        {
          "name": "GET /v1/investigations/summary",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/investigations/summary",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "investigations",
                "summary"
              ]
            },
            "description": "counts (every state key emitted), per-decision counts for closed\ncases (4 named buckets + 'null' bucket for closed-without\ndecision), open/closed split, mean age of opens, mean time-to\nclose, oldest open + newest closed pointers. Mirrors the M14.19\n/ M3.5 analytics rollup shape but for case investigations. Mounted\nBEFORE `/:id` so the literal `/summary` segment wins. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/investigations/summary",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "rollup over ALL investigations in the tenant. Returns per-status"
        }
      ]
    },
    {
      "name": "/v1/tenants/:tenant_id",
      "description": "5 route(s) under /v1/tenants/:tenant_id",
      "item": [
        {
          "name": "GET /v1/tenants/{tenant_id}/onboarding",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/tenants/:tenant_id/onboarding",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                ":tenant_id",
                "onboarding"
              ],
              "variable": [
                {
                  "key": "tenant_id",
                  "value": "{{tenant_id}}",
                  "description": "Path parameter: tenant_id"
                }
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/tenants/{tenant_id}/onboarding",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/tenants/:tenant_id/onboarding — auto-discovered (server.ts:15526)"
        },
        {
          "name": "GET /v1/tenants/{tenant_id}/onboarding/readiness",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/tenants/:tenant_id/onboarding/readiness",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                ":tenant_id",
                "onboarding",
                "readiness"
              ],
              "variable": [
                {
                  "key": "tenant_id",
                  "value": "{{tenant_id}}",
                  "description": "Path parameter: tenant_id"
                }
              ]
            },
            "description": "`/:tenant_id/onboarding` so the literal /readiness segment isn't\ncaptured as a sub-path of the state route. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/tenants/{tenant_id}/onboarding/readiness",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "lookup of any tenant's readiness. Mounted BEFORE the catch-all"
        },
        {
          "name": "POST /v1/tenants/{tenant_id}/onboarding/reset",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/tenants/:tenant_id/onboarding/reset",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                ":tenant_id",
                "onboarding",
                "reset"
              ],
              "variable": [
                {
                  "key": "tenant_id",
                  "value": "{{tenant_id}}",
                  "description": "Path parameter: tenant_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/tenants/{tenant_id}/onboarding/reset",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/tenants/:tenant_id/onboarding/reset — auto-discovered (server.ts:15661)"
        },
        {
          "name": "POST /v1/tenants/{tenant_id}/onboarding/steps/{step_id}",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/tenants/:tenant_id/onboarding/steps/:step_id",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                ":tenant_id",
                "onboarding",
                "steps",
                ":step_id"
              ],
              "variable": [
                {
                  "key": "tenant_id",
                  "value": "{{tenant_id}}",
                  "description": "Path parameter: tenant_id"
                },
                {
                  "key": "step_id",
                  "value": "<step_id>",
                  "description": "Path parameter: step_id"
                }
              ]
            },
            "description": "",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/tenants/{tenant_id}/onboarding/steps/{step_id}",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "POST /v1/tenants/:tenant_id/onboarding/steps/:step_id — auto-discovered (server.ts:15608)"
        },
        {
          "name": "POST /v1/tenants/{tenant_id}/onboarding/steps/{step_id}/skip",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              },
              {
                "key": "Content-Type",
                "value": "application/json",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/tenants/:tenant_id/onboarding/steps/:step_id/skip",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                ":tenant_id",
                "onboarding",
                "steps",
                ":step_id",
                "skip"
              ],
              "variable": [
                {
                  "key": "tenant_id",
                  "value": "{{tenant_id}}",
                  "description": "Path parameter: tenant_id"
                },
                {
                  "key": "step_id",
                  "value": "<step_id>",
                  "description": "Path parameter: step_id"
                }
              ]
            },
            "description": "compliance reason on the StepProgress for audit + reviewer trail.\nValidates reason length [5..500] after whitespace collapse.\nMUST be declared BEFORE the catch-all .../steps/:step_id so the\nliteral \"/skip\" segment isn't swallowed as a step_id. */",
            "body": {
              "mode": "raw",
              "raw": "{}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            }
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for POST /v1/tenants/{tenant_id}/onboarding/steps/{step_id}/skip",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 201, 400, 401, 403, 404, 409]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "body { reason }. Forces status=skipped and captures the regulatory/"
        }
      ]
    },
    {
      "name": "/v1/tenants/me",
      "description": "7 route(s) under /v1/tenants/me",
      "item": [
        {
          "name": "GET /v1/tenants/me/onboarding",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/tenants/me/onboarding",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                "me",
                "onboarding"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/tenants/me/onboarding",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/tenants/me/onboarding — auto-discovered (server.ts:15383)"
        },
        {
          "name": "GET /v1/tenants/me/onboarding/actors",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/tenants/me/onboarding/actors",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                "me",
                "onboarding",
                "actors"
              ]
            },
            "description": "skipped step lists. Lets the BIL ops lead see \"who did what\" when\ndistributed teams share onboarding work. Mounted alongside M2.9\noverview, BEFORE the catch-all readiness wildcard. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/tenants/me/onboarding/actors",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "view: each user who touched a step grouped with their completed +"
        },
        {
          "name": "GET /v1/tenants/me/onboarding/eta",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/tenants/me/onboarding/eta",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                "me",
                "onboarding",
                "eta"
              ]
            },
            "description": "projected_completion_at. Lets the SPA show \"this tenant has\n~85 minutes of setup work left\". Mounted alongside M2.6/M2.7\nonboarding sub-routes. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/tenants/me/onboarding/eta",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "projection: per-step minute estimates → remaining_minutes +"
        },
        {
          "name": "GET /v1/tenants/me/onboarding/milestone",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/tenants/me/onboarding/milestone",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                "me",
                "onboarding",
                "milestone"
              ]
            },
            "description": "→ final_review → complete) derived from the M2.6\ncompleteness_score, with progress_within_stage [0..1] and\nremaining_required_blockers. Enriches the numeric score with a\nSPA-friendly higher-level progress bar. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/tenants/me/onboarding/milestone",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "milestone classification (starting → in_progress → near_done"
        },
        {
          "name": "GET /v1/tenants/me/onboarding/overview",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/tenants/me/onboarding/overview",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                "me",
                "onboarding",
                "overview"
              ]
            },
            "description": "ETA in one round-trip. Saves the SPA's onboarding dashboard\n3 separate calls on every refresh. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/tenants/me/onboarding/overview",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "payload combining M2.6 readiness + M2.7 skip-history + M2.8"
        },
        {
          "name": "GET /v1/tenants/me/onboarding/readiness",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/tenants/me/onboarding/readiness",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                "me",
                "onboarding",
                "readiness"
              ]
            },
            "description": "blend (70% required, 30% overall) + structured blockers + next\npending required step. audit:read RBAC. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/tenants/me/onboarding/readiness",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "readiness score derived from the M2.2 onboarding state. Weighted"
        },
        {
          "name": "GET /v1/tenants/me/onboarding/skip-history",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/tenants/me/onboarding/skip-history",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                "me",
                "onboarding",
                "skip-history"
              ]
            },
            "description": "captured reasons. Companion to M2.6 readiness: that gives a\nsingle number + blockers; this is the auditable \"why was each\nstep skipped?\" report. Separates M2.5 explicit-reason skips\nfrom legacy markStep('skipped') skips so compliance can spot\nsteps lacking the new audit trail. Sorted by step.order asc. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/tenants/me/onboarding/skip-history",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "view of just the caller's tenant skipped onboarding steps with"
        }
      ]
    },
    {
      "name": "/v1/tenants/onboarding",
      "description": "9 route(s) under /v1/tenants/onboarding",
      "item": [
        {
          "name": "GET /v1/tenants/onboarding/actor-fleet",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/tenants/onboarding/actor-fleet",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                "onboarding",
                "actor-fleet"
              ]
            },
            "description": "actor summary) by aggregating across ALL tenants in the registry.\nPer-actor row: {actor_username, total_actions (completed +\nskipped), distinct_tenants, tenant_ids[] sorted asc cap 50,\ndistinct_steps, completed_count, skipped_count, most_recent_at}.\nEnvelope: most_prolific_actor (highest total_actions; canonical\nusername asc tie-break; null on empty), most_broad_actor (highest\ndistinct_tenants — surfaces operators touching many tenants for\ncross-tenant access review; canonical username asc tie-break;\nnull on empty). Drives ops recognition + quarterly access review\n(\"alice has touched 8 tenants — does that match her scope?\").\nAsync because tenantLookup.all() may be async. Mounted BEFORE\n/v1/tenants/:tenant_id catch-all so the literal segment wins. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/tenants/onboarding/actor-fleet",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "per-actor onboarding contribution. Distinct from M2.10 (per-tenant"
        },
        {
          "name": "GET /v1/tenants/onboarding/completion-timeline",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/tenants/onboarding/completion-timeline",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                "onboarding",
                "completion-timeline"
              ]
            },
            "description": "completions per UTC calendar day. Per-day {date, total\n(completed + skipped), completed_count, skipped_count,\ndistinct_tenants}. Envelope: peak_day (earliest-day-wins\ntie-break), mean_per_day, growth_rate. Mirror of M1.9 / M8.15 /\nM10.15 / M12.13 / M15.11 daily-volume pattern. Async because\ntenantLookup.all() may be async. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/tenants/onboarding/completion-timeline",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "TREND-LINE view: across all tenants in the registry, count step"
        },
        {
          "name": "GET /v1/tenants/onboarding/fleet",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/tenants/onboarding/fleet",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                "onboarding",
                "fleet"
              ]
            },
            "description": "tenant in one round-trip. Per-row: tenant_id, name, vertical,\nactive, completeness_score, current_stage, is_complete,\ntotal_blockers, remaining_required_blockers, last_updated_at.\nEnvelope: by_stage rollup (every OnboardingStage at 0 when\nabsent), mean_completeness_score, tenants_needing_attention[]\n(completeness < 70 subset; least-progressed first), most_\nadvanced_tenant. Async because tenantLookup.all() may be async.\nMounted BEFORE /v1/tenants/:tenant_id catch-all so the literal\n/onboarding/fleet segment isn't captured. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/tenants/onboarding/fleet",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "admin view returning onboarding posture for EVERY configured"
        },
        {
          "name": "GET /v1/tenants/onboarding/skip-reason-analytics",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/tenants/onboarding/skip-reason-analytics",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                "onboarding",
                "skip-reason-analytics"
              ]
            },
            "description": "Per-step: {total_skips, total_with_reason (M2.5 path),\ntotal_legacy (markStep('skipped') without reason), distinct_\nactors, distinct_tenants, sample_skips (cap 5 newest-first)}.\nEnvelope: most_skipped_step (canonical step.order tie-break),\nmost_skipped_required_step (required-only filter — compliance\nrelevant bottlenecks), total_legacy_skips + total_with_reason_\nskips (partition: Σ = total_skips_observed). Distinct from M2.7\n(single-tenant skip-history), M2.10 (per-tenant actor summary),\nM2.13 (per-step completion ranking — doesn't surface reasons).\nAsync since tenantLookup.all() may be async. 501 envelope when\nthe lookup doesn't expose .all(). Mounted BEFORE the catch-all\n/v1/tenants/:tenant_id so the literal segment isn't captured. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/tenants/onboarding/skip-reason-analytics",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "cross-tenant fleet rollup over the M2.5 skip_reason capture."
        },
        {
          "name": "GET /v1/tenants/onboarding/stage-vertical-matrix",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/tenants/onboarding/stage-vertical-matrix",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                "onboarding",
                "stage-vertical-matrix"
              ]
            },
            "description": "grid), verticals_without[] canonical}. Per-col {vertical, total,\nby_stage (every stage at 0), stages_without[] canonical,\nmean_completeness_score (across tenants in this vertical;\nrounded; null when zero), tenant_ids[] sorted asc}. Envelope:\npeak_cell (canonical iteration tie-break: stages × verticals\ncanonical; null on empty), fastest_vertical (highest\nmean_completeness; canonical-order tie-break; null when zero\ndata in either), slowest_vertical (lowest mean; null on empty),\nempty_cells[] in canonical stage × vertical row-major order.\nDistinct from M2.14 (step × vertical — granular per-step\ncoverage) by aggregating into the higher-level milestone stage.\nAsync since tenantLookup.all() may be async. Drives SaaS admin\n\"where do banking tenants stall vs insurance?\" governance view.\nMounted BEFORE /v1/tenants/:tenant_id catch-all. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/tenants/onboarding/stage-vertical-matrix",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "{stage, label, total, by_vertical (every vertical at 0 — stable"
        },
        {
          "name": "GET /v1/tenants/onboarding/step-completion",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/tenants/onboarding/step-completion",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                "onboarding",
                "step-completion"
              ]
            },
            "description": "(\"where does each tenant stand?\"); M2.13 pivots BY-STEP (\"which\nsteps get stuck for the most tenants?\"). Per-step row {step_id,\nname, order, required, total_tenants, completed_count,\nskipped_count, pending_count, completion_rate, pending_tenant_ids[]\nsorted asc}. Envelope: steps[] in canonical step.order asc,\nbiggest_required_bottleneck (required step with highest\npending_count; canonical step.order tie-break; null when none),\nsteps_needing_attention[] (completion_rate < 0.5, sorted asc),\nfully_adopted_steps[] (completion_rate=1.0). Async since\ntenantLookup.all() may be async. Mounted BEFORE\n/v1/tenants/:tenant_id catch-all so the literal segment isn't\ncaptured. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/tenants/onboarding/step-completion",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "PER-STEP cross-tenant completion rollup. M2.12 pivots BY-TENANT"
        },
        {
          "name": "GET /v1/tenants/onboarding/step-vertical-matrix",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/tenants/onboarding/step-vertical-matrix",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                "onboarding",
                "step-vertical-matrix"
              ]
            },
            "description": "Cells count tenants who have COMPLETED that step. Per-row {step_id,\nname, order, required, total_completed, by_vertical, verticals_\nwithout[]}. Per-col {vertical, total_tenants (in this vertical),\ntotal_completed (Σ across all steps for this vertical), by_step,\nsteps_universally_completed[] (every tenant in vertical has\ncompleted)}. Envelope: peak_cell (canonical iteration tie-break;\nnull when no completion anywhere), empty_cells[] in canonical\nstep.order × vertical row-major order, fastest_vertical +\nslowest_vertical (highest/lowest mean completion_rate across\nsteps; canonical-order tie-break; null on empty fleet). Mirror of\nM3.14 / M14.28 / M14.29 / M8.14 matrix pattern for tenant\nonboarding. Async — tenantLookup.all() may be async. 501 envelope\nwhen lookup doesn't expose .all(). Mounted BEFORE catch-all\n`/v1/tenants/:tenant_id` so the literal segment isn't captured. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/tenants/onboarding/step-vertical-matrix",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "asc) × cols = 2 verticals (banking → insurance) = 16 cells."
        },
        {
          "name": "GET /v1/tenants/onboarding/steps",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/tenants/onboarding/steps",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                "onboarding",
                "steps"
              ]
            },
            "description": ""
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/tenants/onboarding/steps",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "GET /v1/tenants/onboarding/steps — auto-discovered (server.ts:14970)"
        },
        {
          "name": "GET /v1/tenants/onboarding/velocity",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "X-Tenant-ID",
                "value": "{{tenant_id}}",
                "type": "text"
              },
              {
                "key": "X-Channel",
                "value": "{{channel}}",
                "type": "text"
              },
              {
                "key": "X-APEX-USER",
                "value": "{{apex_user}}",
                "type": "text"
              },
              {
                "key": "X-APEX-Role",
                "value": "{{apex_role}}",
                "type": "text"
              }
            ],
            "url": {
              "raw": "{{bff_url}}/v1/tenants/onboarding/velocity",
              "host": [
                "{{bff_url}}"
              ],
              "path": [
                "v1",
                "tenants",
                "onboarding",
                "velocity"
              ]
            },
            "description": "3 — fast/normal/slow sorted duration asc (fastest first); stalled\nsorted desc (worst first); not_started sorted tenant_id asc}.\nEnvelope: peak_bucket (canonical iteration tie-break — fast wins\nover normal at tied), mean/min/max_completion_days (over COMPLETE\ntenants only; null when no complete), fastest_tenant + slowest_\ntenant + most_stalled_tenant samples + per-tenant tenants[] full\nlist. Distinct from M2.6 (snapshot completeness), M2.8 (forward\nETA), M2.11 (milestone stage), M2.12 (fleet snapshot), M2.13\n(per-step ranking), M2.15 (per-actor pivot), M2.16 (cross-tenant\ndaily timeline), M2.17 (stage × vertical matrix), M2.18 (skip\nreasons). Mirror of M9.19 (investigation duration histogram) for\nthe tenant onboarding surface. Async since tenantLookup.all()\nmay be async. Mounted BEFORE /v1/tenants/:tenant_id catch-all\nso the literal segment wins. */"
          },
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Smoke assertion — accepts any documented status for GET /v1/tenants/onboarding/velocity",
                  "pm.test('status is documented (200/201/204 success OR 4xx envelope)', () => {",
                  "  pm.expect([200, 400, 401, 403, 404]).to.include(pm.response.code);",
                  "});",
                  "pm.test('responds in < 5s', () => {",
                  "  pm.expect(pm.response.responseTime).to.be.below(5000);",
                  "});",
                  "if (pm.response.code >= 200 && pm.response.code < 300) {",
                  "  pm.test('success envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j.header).to.have.property('status', 'success');",
                  "    pm.expect(j).to.have.property('body');",
                  "  });",
                  "}",
                  "if (pm.response.code >= 400) {",
                  "  pm.test('error envelope shape', () => {",
                  "    const j = pm.response.json();",
                  "    pm.expect(j).to.have.property('header');",
                  "    pm.expect(j).to.have.property('error');",
                  "    pm.expect(j.error).to.have.property('code');",
                  "    pm.expect(j.error).to.have.property('severity');",
                  "  });",
                  "}"
                ]
              }
            }
          ],
          "response": [],
          "_summary": "upper bound semantics. Per-bucket {count, sample_tenant_ids cap"
        }
      ]
    }
  ]
}
