# EWS.docx — Feature Gap Registry

**Last updated:** 2026-05-20 (T6 M1.15 — API key revocation daily volume timeline shipped — trend-line view over `revoked_at` (vs M1.9's `created_at`). Same window mechanics (default 30, [1, 365]). Per UTC day {date, total_revocations, by_scope (7 keys at 0; multi-scope key contributes to each + Set dedup), distinct_revokers per-day Set-dedup of revoked_by}. Envelope: peak_day (earliest-day-wins tie-break) + mean_per_day + growth_rate (null when first-half=0 OR days<2) + most_revoked_scope (canonical VALID_SCOPES tie-break) + total_revocations_in_window + total_revocations_observed. Only status=revoked AND non-null revoked_at keys count. Distinct from M1.9 (creation timeline) by surface — answers "are we revoking faster than creating?" rotation-hygiene view. Mirror of M1.9 / M3.17 / M7.17 / M8.15 daily-volume pattern. New route `GET /v1/admin/api-keys/revocation-daily-volume?days=N`. Previous: M1.14 — API key creator × status cross-tab matrix shipped — 2D pivot combining OPEN creator axis × CLOSED 2-status axis (active/revoked). Each key in exactly one cell. Per-row {created_by, total_keys, by_status (2 keys), revocation_rate (revoked/total), key_ids[] cap 50 sorted asc}. Per-col {status, total, by_creator (compact), distinct_creators, top_creators[] cap 3}. Envelope: peak_cell + most_revoked_creator (highest absolute count) + highest_revocation_rate_creator (favors RATE over COUNT) + creators_with_zero_revocations[] (clean ops surface) + empty_cells[] canonical. Distinct from M1.11 (creator × full LIFECYCLE 7 stages) by collapsing to simpler 2-state audit view. Mirror of M15.17 / M9.17 OPEN × CLOSED matrix pattern. Drives "which creators have highest revocation rates? which are clean?" audit views. New route `GET /v1/admin/api-keys/creator-status-matrix` (mounted BEFORE `/:key_id`). Previous: M6.19 — Custom weight preset multiplier histogram shipped — mirror of M6.16 (library) but over the M6.4 per-tenant CUSTOM store. Same 4 canonical buckets (strong_dampen < 0.5 / mild_dampen [0.5, 1.0) / mild_boost (1.0, 1.5] / strong_boost > 1.5; exact 1.0 a no-op). Per-row {preset_id, name, mode, vertical, total_multipliers, by_bucket (4 keys), min/mean/max_multiplier, boost_count, dampen_count}. Envelope: most_active_preset (highest total_multipliers + canonical asc tie-break) + most_boosted_preset + most_dampened_preset + highest_multiplier ({preset_id, indicator_id, value}) + lowest_multiplier + by_bucket_totals. Distinct from M6.16 (library-static) by being TENANT-SCOPED — drives "are operators creating AGGRESSIVE customs?" governance view. New route `GET /v1/scoring/presets/custom/multiplier-histogram`. Previous: M4.17 — Indicator threshold override vertical × family cross-tab matrix shipped — 2D pivot over M4.4 override store combining 2 ScoringVertical × 9 IndicatorFamily = 18 cells. Both axes CLOSED. Same axes as M4.16 (catalog matrix) but over the OVERRIDE surface. Per-row {vertical, total_overrides, by_family (9 keys), families_without[], indicator_ids[] sorted asc, distinct_families}. Per-col {family, total_overrides, by_vertical (2 keys), verticals_without[], indicator_ids[] sorted asc, distinct_verticals}. Envelope: peak_cell (canonical iteration tie-break) + most_overridden_family + most_active_vertical + empty_cells[] canonical + unknown_families[] (catalog drift detector). Mirror of M4.16 for override surface. Drives "which (vertical, family) combinations do operators override most?" governance view. New route `GET /v1/indicators/overrides/vertical-family-matrix`. Previous: M12.17 — Report schedule cadence × format cross-tab matrix shipped — 2D pivot over M12.2 schedule store combining 5 ScheduleCadence × 4 ReportFormat = 20 cells. Both axes CLOSED. Per-row {cadence, total, by_format (4 keys), formats_without[], schedule_ids[] sorted asc, enabled/disabled counts}. Per-col {format, total, by_cadence (5 keys), cadences_without[], schedule_ids[] sorted asc, enabled/disabled counts}. Envelope: peak_cell (canonical iteration tie-break) + most_versatile_cadence (most distinct formats + canonical tie-break) + most_universal_format (most distinct cadences + canonical tie-break) + empty_cells[] canonical. Distinct from M12.11/M12.14 (job surface) + M12.9 (cadence 1D) + M12.16 (recipient pivot) by axis combination over the SCHEDULE surface. Mirror of M12.14 / M3.14 / M15.14 matrix pattern. Drives "do we have daily PDF schedules? are quarterly always xlsx?" coverage view. New route `GET /v1/reports/schedules/cadence-format-matrix`. Previous: M8.17 — Alert routing day-of-week × hour-of-day heatmap shipped — CYCLIC intraday heatmap over the M8.6 routing ledger. For each routed alert in the window, bucket created_at UTC into 7-day × 24-hour grid (ISO Mon=0..Sun=6 × hour 0..23 = 168 cells). Per cell {dow, hour, count, by_class (4 keys: red/orange/yellow/green)}. Marginals: by_dow[7] + by_hour[24] each with total + by_class. Envelope: peak_cell (canonical dow asc × hour asc tie-break) + peak_dow (Mon-first tie-break) + peak_hour (earliest-hour-wins tie-break) + most_active_class (canonical BIL_CLASS_ORDER: red wins). Distinct from M8.15 (linear N-day trend) by being cyclic intraday pattern. Mirror of M14.22 / M15.7 dow×hour heatmap pattern. Same window semantics as M8.6 family (default 50, max 200). New route `GET /v1/alerts/dow-hour-heatmap?window=N`. Also removed broken parallel-session M6.17 indicator-index route (deleted source file but server.ts still referenced it, blocking TS compilation). Previous: M2.17 — Onboarding milestone stage × tenant vertical cross-tab matrix shipped — async 2D pivot across tenant fleet combining M2.11 milestone STAGE (5 canonical: starting → in_progress → near_done → final_review → complete) × tenant vertical (banking → insurance). 5×2=10 cells. Per-row {stage, label, total, by_vertical (2 keys), verticals_without[]}. Per-col {vertical, total, by_stage (5 keys), stages_without[], mean_completeness_score, tenant_ids[] sorted asc}. Envelope: peak_cell + fastest_vertical (highest mean) + slowest_vertical (lowest mean) + empty_cells[] canonical. ALL_ONBOARDING_STAGES exported. Distinct from M2.14 (step × vertical — granular) by aggregating to higher-level stage. Mirror of M15.17 / M13.17 / M1.11 matrix pattern. Drives "where do banking tenants stall vs insurance?" SaaS admin view. New route `GET /v1/tenants/onboarding/stage-vertical-matrix`. Previous: M13.17 — Config override category × actor cross-tab matrix shipped — 2D pivot over per-tenant overrides combining CLOSED 5-category axis (alerts/notifications/reporting/scoring/features) × OPEN actor axis (any updated_by). Each override in exactly one cell. Per-row {category, total_overrides, by_actor (compact), distinct_actors, top_actors cap 3}. Per-col {actor_username, total_overrides, by_category (5 keys), categories_without[] canonical, distinct_categories}. Envelope: peak_cell (canonical iteration tie-break) + most_versatile_actor (highest distinct_categories) + most_active_category (highest distinct_actors) + empty_cells[] canonical. Defaults excluded. Mirror of M15.17 / M9.17 / M1.11 CLOSED × OPEN matrix pattern. Drives "which actors span every category vs single-domain specialists?" access-review view. New route `GET /v1/admin/config/category-actor-matrix` (mounted BEFORE `/:key`). Previous: M15.17 — Audit action × resource_type cross-tab matrix shipped — 2D pivot over audit chain combining OPEN action axis × CLOSED 10-AuditResourceType axis (user/session/config/case/alert/report/scenario/rule/integration/system). Each event in exactly one cell. Per-row {action, total, by_resource_type (10 keys), resource_types_without[], distinct_resource_types}. Per-col {resource_type, total, by_action (compact), actions_without[] sorted asc, distinct_actions}. Envelope: peak_cell (canonical iteration tie-break: actions asc × types canonical) + most_versatile_action (highest distinct_resource_types + canonical asc tie-break) + most_diverse_resource_type (highest distinct_actions + canonical type-order tie-break) + empty_cells[] canonical row-major. Distinct from M15.6/M15.12/M15.13/M15.14/M15.15 by axis combination. Mirror of M1.11 / M14.28 / M12.14 matrix pattern combining OPEN × CLOSED axes. New route `GET /v1/audit/action-resource-matrix`. Previous: M9.17 — Case state transition cross-tab matrix shipped — FLEET 2D pivot over M9.4 case event journal. For every state_change event with payload {from, to}, pivots into matrix of from-state × to-state. Per-cell {count, distinct_cases (Set-deduped)}. State labels AUTO-DISCOVERED from observed payload values — works across CmsCaseState + InvestigationStatus without branching. Per-row {from_state, total_outbound, by_to (compact), distinct_cases}. Per-col {to_state, total_inbound, by_from (compact), distinct_cases}. Envelope: peak_cell (canonical iteration tie-break: state names asc) + most_common_destination + most_common_source (state-asc tie-break each) + dead_ends[] (dest never source — terminal) + origins[] (source never dest — entry) + self_transition_count (sanity check). Distinct from M9.6 (per-case chronological ladder) by being FLEET-WIDE cross-tab. New route `GET /v1/cases/events/transition-matrix` (mounted BEFORE `/:event_id`). Previous: M7.17 — Promotion request daily volume timeline shipped — TREND-LINE view across trailing-N-day window over M7.2 promotion engine. Distinct from M7.10 (per-model timeline) + M7.15 (latency histogram) by being fleet DAILY volume trend over requested_at. Per UTC day {date, total, by_status (4 keys: pending/approved/rejected/cancelled), distinct_models per-day Set-dedup, distinct_requesters per-day Set-dedup}. Envelope: peak_day (earliest-day-wins tie-break) + mean_per_day + growth_rate (second-half vs first-half; null when first-half=0 OR days<2) + busiest_status (canonical tie-break: pending > approved > rejected > cancelled) + total_requests_in_window + total_requests_observed. Default 30 days, [1,365] bounds. Drains engine.list(tenant, {page, page_size: 500}) with 200-page cap. Mirror of M1.9 / M8.15 / M10.15 / M12.13 / M15.11 / M3.17 daily-volume pattern. New route `GET /v1/ai/promotions/daily-volume?days=N` (mounted BEFORE `/:request_id`). Previous: M3.17 — Connector run daily volume timeline shipped — TREND-LINE view across trailing-N-day window over M3.1 ingestion registry. Distinct from M3.12 (hourly cyclic — intraday) by being LINEAR trend. Per UTC day {date, total, by_status (4 keys: success/failure/partial/running), distinct_connectors per-day Set-dedup}. Envelope: peak_day (earliest-day-wins tie-break) + mean_per_day + growth_rate (second-half vs first-half; null when first-half=0 OR days<2) + busiest_status (canonical tie-break: success > failure > partial > running) + total_runs_in_window + total_runs_observed. Default 30 days, [1,365] bounds. Drains via registry.list + listRuns(200/connector). Mirror of M1.9 / M8.15 / M10.15 / M12.13 / M15.11 daily-volume pattern. New route `GET /v1/ingestion/run-volume/daily?days=N`. Previous: M1.13 — API key usage recency histogram shipped — pure time-based recency histogram over M1.2 ApiKeyEntry. Orthogonal to M1.10 (rule-based first-match lifecycle stages). 6 canonical buckets: revoked / never_used / used_within_7d / used_within_30d / used_within_90d / stale (90d+) with strict-< upper bound semantics. Per-bucket {count, by_scope (every VALID_SCOPES at 0; multi-scope key contributes to each + intra-key dedup via Set), distinct_creators (Set-deduped), sample_key_ids cap 5 sorted asc}. Envelope: peak_bucket (canonical iteration tie-break: revoked wins at tied) + unused_buckets[] (canonical order) + total_active_used_recently_count (used_within_*) + total_active_stale_or_never_count (never_used + stale). Drives "what fraction of active keys are ACTUALLY being used recently?" governance view. Mirror of M9.11 / M8.12 / M7.15 / M4.15 / M5.18 histogram pattern. New route `GET /v1/admin/api-keys/usage-recency-histogram` (mounted BEFORE `/:key_id`). Previous: M6.18 — Library weight preset × indicator family cross-tab matrix shipped — 2D pivot combining 6 library presets (open-axis preset_id asc) × 9 closed indicator families (FIN/BEH/TXN/CRD banking + POL/CUS-INS/AGT/CLM/OPS insurance). Cells = # indicators in family with EXPLICIT multiplier in preset. Per-row {preset_id, mode, vertical, total_overrides, by_family (9 keys), families_without[], distinct_families}. Per-col {family, total_overrides, by_preset (compact), presets_without[], distinct_presets}. Envelope: peak_cell (canonical iteration tie-break) + most_focused_preset + most_overridden_family + unused_families[]. Balanced presets correctly produce all-zero rows. ALL_INDICATOR_FAMILIES exported. Distinct from M6.17 indicator-index (per-indicator inverted view) — M6.18 is the FAMILY-aggregated matrix. Mirror of M1.11 / M14.28 / M3.14 matrix pattern. Platform-static. New route `GET /v1/scoring/presets/family-matrix` (mounted BEFORE `/:preset_id`). Previous: M5.18 — Rule template supporting_indicators count histogram shipped — 5 canonical buckets (minimal=1 / low=2-3 / medium=4-6 / high=7-10 / comprehensive=>10) over the M5.1 RULE_TEMPLATES library. Per-bucket {count, by_category (5 keys), by_vertical (3 keys), sample_template_ids cap 3 sorted asc}. Envelope: total_templates (bucketed) + templates_with_zero_indicators (config-gap signal) + total_catalog_size, peak_bucket (canonical iteration tie-break), mean/min/max_indicators over non-zero templates, empty_buckets[] canonical. Mirror of M4.15 / M9.11 / M8.12 / M7.15 histogram pattern. Platform-static. New route `GET /v1/rules/templates/indicator-count-histogram` (mounted BEFORE `/:id`). Previous: M1.12 — API key scope × creator cross-tab matrix shipped — 2D pivot combining 7 closed ApiKeyScope × N creators (open). Each key contributes 1 per scope to (scope, creator) cell. Per-row {scope, total_bindings, by_creator (compact), distinct_creators, top_creators cap 3}. Per-col {created_by, total_bindings, total_keys, by_scope (7 keys at 0 when absent), scopes_without[], distinct_scopes}. Envelope: peak_cell (canonical iteration tie-break) + most_versatile_creator (highest distinct_scopes + canonical username asc tie-break) + unused_scopes[]. Defensive intra-key scope dedup via Set + closed-enum filter via VALID_SCOPES. Distinct from M1.11 (creator × LIFECYCLE) by axis — this is SCOPE × creator answering "which user has webhooks:dispatch?" + "who provisioned all the audit:read keys?". Mirror of M1.11 / M14.28 / M12.14 / M3.14 / M15.14 / M8.14 matrix pattern combining closed-axis × open-axis. New route `GET /v1/admin/api-keys/scope-creator-matrix` (mounted BEFORE `/:key_id`). Previous: M11.17 Dashboard widget × creator cross-tab matrix shipped — 2D pivot combining 7 closed WidgetTypes × N creators (open). Each widget on each dashboard contributes 1 to (creator, widget_type) cell. Per-row {created_by, total_widgets, total_dashboards, by_widget_type (7 keys), widget_types_without, distinct_widget_types, most_recent_at}. Per-col {widget_type, display_name, total_instances, top_creators[] cap 10, distinct_creators}. Envelope: peak_cell + most_versatile_creator. Mirror of M1.11 / M14.28 / M12.14 matrix pattern. Drives "which user gravitates to which widget?" templating view. New route `GET /v1/dashboards/custom/widget-creator-matrix`. Previous: M10.17 Push notification platform distribution shipped — 1D pivot over the M10.3 push ledger by PushPlatform (fcm/apns/web). Per-platform {dispatch_count, distinct_messages, distinct_users, by_status (3 keys), most_recent_at}. Envelope: most_used_platform (canonical tie-break: fcm beats apns), unused_platforms[], overall_by_status. Mirror of M14.27 / M5.16 / M3.13 / M7.13 1D distribution pattern. Drives "is our push traffic skewed to FCM vs APNS?" view. New route `GET /v1/notifications/push/platform-distribution`. Previous: M1.11 API key creator × lifecycle stage cross-tab matrix shipped — 2D pivot combining M1.10 lifecycle stages × M1.6 creators. Open-row × closed-col matrix. Per-row {created_by, total_keys, by_stage (7 keys), stages_without[], attention_count (sum of expired + expiring_soon + idle_never_used + dormant)}. Per-col {stage, total, top_creators[] cap 10, distinct_creators}. Envelope: peak_cell + top_attention_creator (most cleanup work). Mirror of M14.28 / M12.14 / M3.14 matrix pattern. New route `GET /v1/admin/api-keys/creator-lifecycle-matrix`. Also cleaned up duplicate M1.10 route block from parallel-commit race. Previous: M1.10 API key lifecycle stage distribution shipped — bucket each key into exactly ONE of 7 canonical lifecycle stages: revoked > expired > expiring_soon > idle_never_used > dormant > fresh > mature_active (priority order, first-match-wins for overlapping flags). Per-stage {count, sample_key_ids cap 5, sample_names}. Envelope: peak_stage + empty_stages + attention_stages[] (subset needing ops action: expired + expiring_soon + idle_never_used + dormant). Drives "how many keys need rotation? how many are dormant?" view. Also restored M2.16 route registration lost in parallel-commit race. Mirror of M9.11 / M4.15 / M7.15 bucketing pattern. New route `GET /v1/admin/api-keys/lifecycle-distribution`. Previous: M2.16 Onboarding step completion daily timeline shipped — trend-line view across all tenants in registry. Per UTC day {date, total (completed + skipped), completed_count, skipped_count, distinct_tenants}. Envelope: peak_day (earliest-day-wins tie-break) + mean_per_day + growth_rate (second-half vs first-half; null when first-half=0 OR days<2). Pending status not counted. Mirror of M1.9 / M8.15 / M10.15 / M12.13 / M15.11 daily-volume pattern for onboarding surface. New route `GET /v1/tenants/onboarding/completion-timeline?days=N`. **🎉 255 sub-phases milestone.** Previous: M4.16 Indicator vertical × family cross-tab matrix shipped — elevates M4.13's nested by_family view to proper 2D pivot. 2 verticals × 9 families = 18 cells. Per-row {total, by_family (9 keys), families_without[]}. Per-col {total, by_vertical (2 keys), verticals_without[]}. Envelope: peak_cell + empty_cells (canonical row-major) + most_diverse_vertical (insurance: 5 families) + most_universal_family (FIN: canonical tie-break) + unknown_families[] (catalog drift detector). Mirror of M14.28 / M12.14 / M3.14 / M15.14 matrix pattern. Platform-static. New route `GET /v1/indicators/vertical-family-matrix`. Previous: M3.16 Connector run latency histogram shipped — fleet-wide duration distribution across every connector + every run. 6 canonical buckets (instant <1s / fast <1m / medium <10m / slow <1h / very_slow >=1h / still_running). Per-bucket {count, by_status (4 keys), distinct_connectors, samples cap 3 longest-first / oldest-waiting first for still_running}. Envelope: peak_bucket (canonical iteration tie-break), mean/median/p95 over finished runs, slowest_run. Mirror of M7.15 / M8.12 / M15.16 histogram pattern. New route `GET /v1/ingestion/run-latency-histogram`. Previous: M8.16 Alert SLA compliance rate by class shipped — per-class compliance rollup over the M8.6 routing ledger. Per class {total, sla_eligible_count, on_time_count, late_count, open_breached_count, total_breach_count, compliance_rate, breach_rate}. Envelope: overall_compliance_rate + worst_class + best_class (canonical worst-first tie-break). monitor_only + sla_hours=null excluded from eligibility. Mirror of M14.26 SLA budget for alerts. Drives "what's compliance per class?" answer. New route `GET /v1/alerts/sla-compliance-by-class?window=N`. Previous: M15.16 Audit correlation duration histogram shipped — duration distribution over the M15.10 correlation rollup. 5 canonical buckets (instant <1s / fast <1m / medium <1h / slow <1d / day_plus) with strict-< upper bound semantics. Per-bucket {count, has_failure_count, sample_correlation_ids cap 3 longest-first}. Envelope: peak_bucket (canonical iteration tie-break), mean/median/p95 across full distribution, longest_correlation, failed_correlations[]. Mirror of M7.15 / M8.12 / M9.11 histogram pattern. New route `GET /v1/audit/correlation-duration-histogram`. Previous: M9.16 Maker-checker by-reviewer rollup shipped — per-checker pivot over the M9.3 sensitive case actions engine. Per checker {total_decisions, approved_count, rejected_count, approval_rate, distinct_cases + case_ids[] sorted asc cap 50, by_action_type (3 keys: case.close / case.escalate / case.override_decision), most_recent_at}. Envelope: most_active_reviewer + rubber_stamp_reviewers[] (approval_rate=1.0 AND >=3 decisions — segregation-of-duties review-quality flag). Mirror of M7.16 + M2.15 / M13.16 / M15.8 / M9.14 / M11.15 per-actor pattern. **🎉 250 sub-phases milestone.** New route `GET /v1/cases/maker-checker/reviewer-rollup` (mounted BEFORE `/:action_id`). Previous: M7.16 AI promotion by-reviewer rollup shipped — per-reviewer pivot over M7.2 promotion engine. Counts decided requests only (approved + rejected). Per reviewer {total_decisions, approved_count, rejected_count, approval_rate, distinct_models[] sorted asc, most_recent_at}. Envelope: most_active_reviewer + rubber_stamp_reviewers[] (approval_rate=1.0 AND >=3 decisions — review-quality flag). Mirror of M2.15 / M13.16 / M15.8 / M9.14 / M11.15 per-actor pattern. Drives quarterly AI governance access-review + rubber-stamping detection. New route `GET /v1/ai/promotions/reviewer-rollup` (mounted BEFORE `/:request_id`). Previous: M12.16 Schedule recipient distribution shipped — per-recipient pivot over the M12.2 recurring schedule store. Per email_address {total_schedules, enabled/disabled split, by_cadence (5 keys), by_format (4 keys), report_ids[] sorted asc, earliest_next_run_at, schedule_names cap 20}. Envelope: most_subscribed_recipient + flooded_recipients[] (>=5 schedules — surfaces over-emailed addresses). Mirror of M14.27 / M5.16 / M11.11 / M3.13 1D distribution pattern. Drives quarterly recipient hygiene. New route `GET /v1/reports/schedules/recipient-distribution` (mounted BEFORE `/:schedule_id`). Previous: M13.16 Config override per-actor rollup shipped — pivots overrides by `updated_by` over the M13.1 config store. Per actor {total_overrides, distinct_keys[] sorted asc, distinct_categories[], by_category (5-key Record), most_recent_at}. Envelope: most_active_actor + actors_with_features_overrides[] security signal (subset who've touched features.* toggles). Mirror of M2.15 / M15.8 / M9.14 / M11.15 per-actor pattern. Drives quarterly access review + security flag for high-impact feature-toggle changes. New route `GET /v1/admin/config/actor-rollup` (mounted BEFORE `/:key`). Previous: M4.15 Indicator catalog weight distribution histogram shipped — 5 canonical buckets (low / low_medium / medium / high / critical) bucketing the M6.2 STUB_CATALOG by weight magnitude with strict-< upper bound semantics (critical inclusive of 1.0). Per-bucket {count, by_vertical (banking/insurance), sample_indicator_ids cap 5 sorted asc}. Envelope: peak_bucket (canonical iteration tie-break) + mean_weight (4-decimals rounded) + min_weight + max_weight + empty_buckets[] canonical. Platform-static. Mirror of M9.11 / M8.12 histogram pattern for indicator catalog surface. New route `GET /v1/indicators/weight-histogram` (mounted BEFORE `/thresholds`). Previous: M2.15 Fleet-wide onboarding contribution by actor shipped — per-actor pivot across ALL tenants in the registry. Per-actor {total_actions, distinct_tenants, tenant_ids[] cap 50, distinct_steps, completed_count, skipped_count, most_recent_at}. Envelope: most_prolific_actor (highest total_actions) + most_broad_actor (highest distinct_tenants — distinct from prolific by independent leaderboard for cross-tenant access reviews). Mirror of M15.8 / M9.14 / M11.15 per-actor pattern. New route `GET /v1/tenants/onboarding/actor-fleet` (mounted BEFORE `/:tenant_id`). Previous: M8.15 Alert routing daily volume timeline shipped — TREND-LINE view over the M8.6 alert routing ledger. Per UTC calendar day across N days (default 30, [1, 365]) bucket {date, total, by_class (every BilAlertClass key at 0 when absent: red/orange/yellow/green), acked_count, open_count, monitor_only_count}. Envelope: peak_day (earliest-day-wins tie-break) + mean_per_day + growth_rate (second-half vs first-half; null when first-half=0 OR days<2) + busiest_class (canonical tie-break red > orange > yellow > green). Drains M8.6 ledger via `routingLedger.list(tenant, 200)`. Mirror of M12.13 / M15.11 / M10.15 / M1.9 daily-volume pattern. New route `GET /v1/alerts/daily-volume?days=N`. Previous: M9.15 Case event journal action distribution shipped — 1D PIVOT-BY-ACTION rollup over the M9.4 case event journal. 9 canonical CaseEventAction values (opened / state_change / closed / escalated / override_requested / override_approved / override_rejected / note_added / checklist_updated). Per row {action, count, distinct_cases, distinct_actors, most_recent_at, sample_actors cap 3 newest-first}. Envelope: most_common_action (canonical tie-break: opened wins) + unused_actions[] (canonical order) + most_active_actor (fleet-wide highest event count + canonical asc tie-break). Mirror of M14.27 / M7.13 / M3.13 / M5.16 1D distribution pattern. Distinct from M9.14 (note authorship — note_added events only, pivoted by author) by being all 9 actions × any actor. New route `GET /v1/cases/events/action-distribution` (mounted BEFORE `/:event_id`). Previous: M15.15 Audit severity × outcome cross-tab matrix — 3 AuditSeverity rows × 3 AuditOutcome cols = 9 cells. Each event in exactly one cell. Per-row {severity, total, by_outcome, outcomes_without[]}. Per-col {outcome, total, by_severity, severities_without[]}. Envelope: peak_cell + empty_cells (canonical row-major) + most_failing_severity (severity with most failure+denied; canonical tie-break: critical wins) + most_critical_outcome (outcome with most critical-severity events; canonical tie-break: success wins). Distinct from M15.9 (severity 1D with nested by_outcome) by being proper MATRIX. Mirror of M14.28 / M12.14 / M3.14 / M15.14 matrix pattern. New route `GET /v1/audit/severity-outcome-matrix`. Previous: M12.15 Report job error pattern clustering — FAILURE FORENSICS view over the M12.1 job store. Clusters failed report jobs by normalised error_message template (re-uses `normaliseError` from M3.6 to keep cluster shapes consistent: UUIDs → `<UUID>`, ISO ts → `<TS>`, quoted strings → `<STR>`, paths → `<PATH>`, long hex → `<HASH>`, numbers → `<N>`). Per cluster {pattern, count, recent_messages cap 3 newest-first, last_failed_at, sample_job_id (newest), report_ids[] sorted asc}. Envelope: sample_size + failure_count + distinct_patterns + clusters[] capped 10, sorted count desc + last_failed_at desc tie-break. Successful + empty-error-message jobs excluded. Mirror of M3.6 connector failure pattern for reports surface. New route `GET /v1/reports/jobs/error-patterns` (mounted BEFORE `/:job_id`). Previous: M7.15 AI promotion request approval-latency histogram (**240 milestone**), M3.15 connector schema record_format distribution, M1.9 API key creation daily volume timeline, M16.20 scenario shock-axis magnitude histogram, M2.14 onboarding step × tenant-vertical matrix, M14.29 adapter op param type × required matrix, M6.16 library preset multiplier histogram, M1.8 API key scope × status matrix, M15.14 audit resource_type × severity matrix. Previous: M13.13 config schema Markdown export, M12.10 report job runtime trend (**190 milestone**), M15.9 audit event severity distribution, M2.11 onboarding milestone tracker, M5.16 rule template severity distribution, M16.18 scenario library shock magnitude rollup, M10.14 notification per-recipient cross-channel rollup, M14.25 adapter ID format catalog, M7.11 model deployment age histogram, M11.14 custom dashboard fleet lint summary, M6.14 score what-if across library presets, M4.12 threshold drift analytics (**180 milestone**), M8.10 routing rule diff vs default, M15.8 audit per-actor activity, M3.11 connector schema type-matrix, M12.9 schedule cadence stats, M13.12 config category override-rate snapshot, M7.10 AI model promotion request timeline, M14.24 adapter operation catalog, M10.13 notification template variable cross-reference, M16.17 scenario library coverage matrix, M5.15 rule template recommended-action inventory, M16.16 scenario library narrative one-liners, M9.12 outcome breakdown by checklist template, M2.10 onboarding actor summary, M2.9 onboarding overview composite (5000 BFF jest tests crossed), M3.10 connector retry policy catalog, M11.13 dashboard starter packs, M9.11 investigation age-bucket distribution, M9.10 cross-investigation note search, M6.13 score sensitivity, M14.23 adapter SLA catalog, M7.9 model retirement candidates, M6.12 bulk-clone weight presets, M11.12 widget defaults, M16.15 scenario shock-vector radar, M2.8 onboarding ETA, M15.7 audit activity heatmap, M10.12 cross-channel ledger analytics, M13.11 admin config override age, M4.11 indicator usage / orphan detection, M9.9 investigation step progress, M12.8 schedule conflict detection, M3.9 schema BACKWARD-compat check, M8.9 routing channel coverage, M5.14 template indicator-coverage check, M9.8 investigation cohort summary, M10.11 unified notification template catalog, M13.10 admin config schema catalog, M8.8 routing matrix snapshot + fingerprint, M4.10 threshold auto-tune suggestion, M16.14 scenario library clone-back-reference, M12.7 fleet-wide schedule upcoming runs, M7.8 model performance metric trend, M2.7 onboarding skip-reason history, M11.11 dashboard widget usage, M3.8 connector schema field cross-index, M9.7 investigation state-machine catalog, M6.11 weight preset clone-from-library, M14.22 field-visit dow×hour heatmap, M5.13 template clone back-reference, M15.6 audit action catalog, M13.9 admin config printable summary, M3.7 connector schema source-map, M6.10 preset effective weights, M4.9 threshold effective view, M7.7 outlier detection, M10.10 prefs resolution chain, M14.21 geo-clustering, M16.13 scenario bundle, M2.6 onboarding readiness, M9.6 case timeline, M8.7 alert routing preview, M12.6 schedule preview, M11.10 dashboard linting, M13.8 admin config bulk-reset, M14.20 adapter SLA breach analytics, M2.5 onboarding skip-with-reason, M3.6 failure-pattern clustering, M7.6 printable model summary, M10.9 quiet-hours mute analytics, M15.5 audit chain spot-check, M4.8 backtest comparison, M16.12 scenario bulk delete, M5.12 rule template versions, M6.9 preset diff, M11.9 dashboard bundle, M12.5 report job analytics, M14.19 field-ops analytics, M9.5 case SLA breach, M8.6 alert routing analytics.)

> Section-by-section walk through `EWS.docx` (the original design source — 11 MB Word doc at repo root, 133 text paragraphs) against what is actually shipped in the prototype. Each row is a feature called out by the doc, mapped to its current implementation status and the canonical task in `TASKS.md` (if any).
>
> **Legend:** ✅ done · ⏳ pending (covered by a TASKS.md task) · 🆕 new gap surfaced by this audit
>
> Pair this with `STATUS.md` for the verification matrix and `TASKS.md` for the canonical backlog. This file is read-only audit output — when an item lands, tick it in `TASKS.md`, not here.

## §3 Core Modules

| § | Feature | Status | TASKS.md | Notes |
|---|---------|--------|----------|-------|
| 3.1 | CBS — loan / DPD / repayment ingest | ⏳ | T3.1 | Synthetic seed scaled 2026-05-03 to 10k customers + 24k loans + 247k repayments in `mart.loan_360`; real CBS loader (T1.4b → `raw.cbs_*` tables) still pending. The empty `cbs_*` tables defined by `002_raw_tables.sql` were dropped 2026-05-03 — see `docs/database-gap-analysis.md` Gap 1 (closed) |
| 3.1 | Account transactions | ✅ | T1.3, T1.4 | `mart.txn_features` materialised; scaled to 10k customers / 290k transactions 2026-05-03 |
| 3.1 | Customer profile | ✅ | T1.3 | `mart.customer_360` (10,000 rows as of 2026-05-03) |
| 3.1 | External data (bureau, market) | ⏳ | T1.4 | Bureau-sync DAG scaffolded; 10k bureau snapshots in seed; live bureau integration not in prototype scope |
| 3.1 | 24-month historical backfill | ⏳ | T2.1 | Feature store + Aurora/S3 backfill pending |
| 3.2 | Indicator Engine — Financial family | ✅ | T1.5, T1.6 | 32-indicator catalog, compute coverage green |
| 3.2 | Indicator Engine — Behavioural family | ✅ | T1.5, T1.6 | |
| 3.2 | Indicator Engine — Transaction family | ✅ | T1.5, T1.6 | |
| 3.2 | Indicator Engine — Credit family | ✅ | T1.5, T1.6 | |
| 3.3 | Rule Engine — DSL + lifecycle | ✅ | T1.7, T1.8, T1.9 | 30 seed rules, simulator FP 0.148 |
| 3.4 | AI Risk Scoring — PD + Risk Level | ✅ | T2.2, T2.3, T2.4 | Synthetic-trained champion AUC 0.8822 |
| 3.5 | Alert Engine — real-time alerts | ⏳ | T2.12 | **Partial 2026-05-08**: BFF→SPA live delivery path shipped — `alert.created` now fans out via the in-process notifications bus on every High-PD evaluation, SSE on `/v1/notifications/stream` carries the typed event, SPA AlertListPage shows a "+N new alerts" live banner that auto-invalidates the query (sub-second propagation). **Still pending**: upstream Kafka streaming branch from indicator-update events to rule-engine evaluation (p95 indicator→alert <60s on the rule-eval side) — that still flows through the DAG-batch path. |
| 3.5 | Alert type — high-risk customer | ✅ | T1.10, T2.7 | |
| 3.5 | Alert type — potential default | ✅ | T1.10, T2.7 | |
| 3.5 | Alert type — fraud suspicion | ✅ | T2.11 | Closed 2026-05-08 — Fraud indicator family added (`FRD-001` sudden-withdrawal-spike, `FRD-002` salary-credit-disappeared, `FRD-003` channel-anomaly, `FRD-004` geo-anomaly) under `services/regulatory-svc/indicators/src/compute/fraud.ts`; seed rules `RULE-031..033` carry `fraud_suspicion` tag. DSL schema regex extended to permit `FRD-` prefix. |
| 3.6 | Case Management — Alert → Case → Assign → Action → Monitor | ✅ | T3.5, T3.6, T3.4 | Full lifecycle wired (FR-CASE-1/3/4) |

## §4 AI Capabilities

| § | Feature | Status | TASKS.md | Notes |
|---|---------|--------|----------|-------|
| 4.1 | PD Prediction Model | ✅ | T2.2 | XGBoost + isotonic, SHAP top-5 |
| 4.2 | Early Risk Detection (before DPD) | ✅ | T2.2, T2.4 | PD score is computed independent of DPD; reason codes via SHAP |
| 4.3 | Scenario Simulation (GDP / interest-rate / FX) | ✅ | T4.2 | Shipped 2026-05-02 — full feature pack: 5 templates (Baseline/Mild/Severe/COVID/RBI), IFRS 9 stage migration matrix, segment×risk heatmap, Portfolio PD + NPA% cards, top-affected drill-down, saved scenarios with side-by-side compare, CSV/PDF/Excel export |
| 4.4 | Smart Alert Prioritisation (Critical/Medium/Low) | ✅ | T2.7, T4.9 | Original SmartQueue (T2.7); v2 prioritization (T4.9) added 2026-05-02 with criticality formula + customer dedup + sort dropdown |
| 4.5 | Continuous Learning Model | ⏳ | T5.1, T2.6 | Drift monitoring shipped (T2.6); auto-promotion gate shipped 2026-05-09 (`services/bff/src/ai_auto_promotion_gate.ts` + `/v1/ai/models/:id/promotion-gate/{evaluate,auto-promote}`). Continuous-learning *retraining pipeline* itself still pending — model registry is ready to consume new versions but the training loop isn't wired. |

## §5 UI Screens

| § | Feature | Status | TASKS.md | Notes |
|---|---------|--------|----------|-------|
| 5.1 | EWS Dashboard — total alerts, high-risk customers, PD distribution, alert trend | ✅ | T1.16, T4.8 | Enhanced 2026-05-02 with clickable KPI deep-links + time-range selector + new CustomerListPage |
| 5.2 | Alert List — Customer ID / Risk Score / Indicator / Severity / Status | ✅ | T1.16, T3.10, T4.9 | BFF maps `apex.regulatory.events.v2` to list-row; criticality scoring + dedup added 2026-05-02 |
| 5.3 | Customer Risk Profile (with SHAP top-5) | ✅ | T1.16, T2.8, T4.11 | 360-view enhancement 2026-05-02: Linked Alerts + Linked Cases panels with click-through |
| 5.4 | Rule Configuration Screen | ✅ | T1.16, T4.10 | UX overhaul 2026-05-02: search + sticky list + severity strip + 5-tab unified detail card |
| 5.5 | Analytics Dashboard | ✅ | T4.1 | Closed 2026-05-08 — all 4 sub-dashboards live at `/analytics` (alert-resolution / risk-trend / pd-distribution / stage-migration). Pure resolvers in `services/bff/src/analytics/*.ts` over `app_alerts.alerts`; new RBAC op `dashboard:analytics:read`. Real PD + IFRS-9 stage columns will swap source queries without changing the public shape |

## §6 Other Features

| § | Feature | Status | TASKS.md | Notes |
|---|---------|--------|----------|-------|
| 6 | Real-time risk alerts | 🆕 | T2.12 (new) | See §3.5 |
| 6 | Case View — customer risk summary | ✅ | T3.6 | |
| 6 | Action — Call / Visit | ✅ | T3.5, T3.6 | Action capture + log |
| 6 | Location — field tracking (GPS) | ⏳ | T4.3 | Web action capture has GPS; mobile RN shell pending in T4.3 |

## §7 Integration

| § | Feature | Status | TASKS.md | Notes |
|---|---------|--------|----------|-------|
| 7 | CBS — loan + repayment | ⏳ | T3.1 | OpenAPI mock at `integrations/cbs/openapi.yaml`; live deepening pending |
| 7 | IFRS 9 — stage movement + ECL | ⏳ | T3.2 | Mock contract only |
| 7 | Collection — auto case creation | ✅ | T3.4 | `services/collection-adapter` — case routing + callback wired |
| 7 | AML — suspicious activity | ⏳ | T3.3 | Bidirectional alert correlation pending |

## §8 Reporting

| § | Feature | Status | TASKS.md | Notes |
|---|---------|--------|----------|-------|
| 8 | Risk trend dashboard | ✅ | T4.1 | Shipped 2026-05-08 (4b) |
| 8 | PD distribution dashboard | ✅ | T4.1 | Shipped 2026-05-08 (4c) |
| 8 | Stage migration dashboard | ✅ | T4.1 | Shipped 2026-05-08 (4d) |
| 8 | Alert resolution dashboard | ✅ | T4.1 (amended) | Shipped 2026-05-08 (4a) — 1st of the four |

## §9 API Framework

| § | Endpoint | Status | TASKS.md | Notes |
|---|----------|--------|----------|-------|
| 9 | `POST /ews/evaluate` | ✅ | T3.7 | Public REST API v1 in `services/bff`. Now also fires `alert.created` webhook on High-risk score (T4.12) |
| 9 | `GET /ews/alerts` | ✅ | T3.7, T3.10 | |
| 9 | `GET /ews/risk-profile` | ✅ | T3.7 | |
| 9 | `POST /ews/action` | ✅ | T3.7 | |
| 9 | `POST /scenario/run` | ✅ | T4.2 | Added 2026-05-02; also fires `scenario.run` webhook (T4.12) |
| 9 | `GET /reports/:type?format=` | ✅ | (auth sweep T4.7) | snapshot / alerts / cases / rbi; PDF/Excel work in real BFF, broken in MSW dev — fix scheduled 2026-05-16 |
| 9 | `GET/POST/DELETE /webhooks` + delivery log + test-fire | ✅ | T4.12 | New 2026-05-02 — admin-managed outbound webhook subscriptions |
| 9+ | `POST /oauth/token` (client_credentials) + `X-Tenant-ID` + `X-Channel` envelope on `/v1/ews/evaluate` | ✅ | T4.24 | Banking API doc §3+§6+§7+§11. Phase 1 reference impl shipped 2026-05-03 — see net-new tasks below |

## Mobile (called out in §2 architecture diagram + §5 header)

| Feature | Status | TASKS.md | Notes |
|---------|--------|----------|-------|
| Mobile RN shell — Alert list, Case view, call/visit log, GPS | ⏳ | T4.3 | Phase 4 |

## Net-new tasks added by this audit

These were not represented anywhere in `TASKS.md` and have just been appended:

- **T2.11** — Fraud-suspicion alert type (new `Fraud` indicator family + ≥3 seed rules + alert tagging). Source: §3.5.
- **T2.12** — Real-time alert streaming path (Kafka → indicator-update → rule eval, p95 <60s). Source: §3.5.
- **T4.1 amendment** — alert-resolution explicitly added to the analytics-dashboard scope. Source: §8.
- **T4.7** — SPA auth + security hardening sweep. _(shipped 2026-04-28; not from EWS.docx — surfaced from the in-flight banking-grade hardening backlog)_
- **T4.8** — Dashboard interactivity (clickable KPIs + time-range selector + CustomerListPage). _(shipped 2026-05-02; closes the §5.1 deep-link gap implied by the spec)_
- **T4.9** — Smart Alert Prioritization v2 (criticality formula + customer dedup). _(shipped 2026-05-02; extends T2.7 SmartQueue with per-alert score)_
- **T4.10** — Rule Config UX overhaul (search + sticky list + 5-tab unified detail card). _(shipped 2026-05-02)_
- **T4.11** — Customer Risk Profile §5.3 360-view enhancement (Linked Alerts + Linked Cases panels). _(shipped 2026-05-02)_
- **T4.12** — Outbound webhook subsystem (admin-managed subscriptions with HMAC + retry + admin SPA). _(shipped 2026-05-02; from spec §9 API Framework — webhooks were not enumerated as endpoints in the original 4-line list)_
- **T4.A** — Database fill-out: 10k-customer scale-up + 5 new app schemas (`app_iam`, `app_cases`, `app_alerts`, `app_bff`, `app_scenario`) populated with ~26k rows of synthetic operator data. Closed Gaps 1 + 5 from `docs/database-gap-analysis.md`. Final database state: 9 schemas / 21 tables / ~731,500 rows. _(shipped 2026-05-03; service-wiring T4.13–T4.18 deferred to next sessions)_
- **T4.13–T4.18** — Per-service Postgres wiring (open). Each service replaces its in-memory store with reads/writes against the new `app_*` schemas. Recommended order: T4.13 bff webhooks → T4.14 auth-svc users/sessions → T4.15 cases → T4.16 audit fan-out → T4.17 alerts → T4.18 scenario. ~10-13 hours total across 5-6 sessions. See `TASKS.md` for sized per-task entries.
- **T4.24** — Multi-tenant API foundation + enterprise envelope (`Banking API Integration` doc §3, §6, §7, §11). _(Phases 1–13 shipped 2026-05-03 → 2026-05-04 — initiative complete; standalone follow-ups in TASKS.md)_
- **T6** — BIL 16-module platform expansion (~365 APIs). _(initiative kicked off 2026-05-04; M1.1, M1.2, M1.3, M2.1, M2.2, M3.1, M3.2, M4.1, M4.2, M5.1, M5.2, M5.3, M6.1, M6.2, M6.3, M7.1, M7.2, M8.1, M8.2, M8.3, M9.1, M9.2, M9.3, M10.1, M10.2, M10.3, M11.1, M11.2, M11.3, M11.4, M11.5, M11.6, M12.1, M12.2, M13.1, M13.2, M13.3, M14.1, M14.2, M14.3, M14.4, M14.5, M14.6, M14.7, M14.8, M14.9, M15.1, M15.2, M15.3, M16.1, M16.2, M16.3 shipped 2026-05-04 → 2026-05-05)_. 16 modules step-by-step, additive only. M1.1 ships TOTP 2FA. M4.1 ships the 25-indicator BIL insurance KRI catalogue. **M11.1-M11.4 ship the full BIL dashboard suite** (Claims / Underwriting / Agent / Operational) under `/v1/dashboards/bil/*` — combined with the existing Executive at `/api/dashboards` that's all 5 DataNetworks dashboards, demoable today with deterministic-stub data per (tenant, day). **M6.1 ships the BIL `Σ(W×V)` risk-scoring engine** at `POST /v1/scoring/risk` — pure-function primitive that any caller can invoke with `(indicator_id, weight, value)` tuples and get back a 0-100 score bucketed Low/Medium/High per the operator-overridable thresholds. **M10.1 ships the BIL email notification channel** at `/v1/notifications/email/{templates,preview,send,log}` — `EmailTransport` interface + `StubEmailTransport` (in-memory ledger, tenant-scoped) + 4 canned templates per BIL §13 (ALERT_RED, ALERT_ORANGE, CASE_ASSIGNED, SLA_BREACH); production swap to SES/SMTP is a drop-in. **M8.1 ships the BIL Red/Orange/Yellow alert classification** at `/v1/alerts/{classification/spec,classify,by-class/:class}` — pure mapping from WireSeverity → BIL 4-colour palette per DataNetworks PDF §11 (CRITICAL→red, HIGH→orange, MEDIUM→yellow, LOW→green) with full action metadata (SLA hours, escalation path, monitor_only flag); the existing `/v1/alerts` shape is unchanged. **M14.1 ships the BIL Core Insurance / Policy Master adapter** at `/v1/integrations/insurance/{policies,claims}[/:id]` — `InsuranceAdapter` interface + `StubInsuranceAdapter` (deterministic synthetic data per (tenant, customer, day), 1-4 policies + 0-3 claims/policy, BIL ids disjoint from BANK_DEMO); production swap to a SOAP/REST gateway is a drop-in. **M14.2 ships the IFRS9 Stage adapter** at `/v1/integrations/ifrs9/stages[/:customer_id]` — `Ifrs9Adapter` interface + `StubIfrs9Adapter` (200-customer book per tenant; ~80/15/5% stage distribution) with full IFRS9 invariants (Stage 1 ⇒ DPD=0, Stage 3 ⇒ DPD ≥ 90, ECL = driver_PD × LGD × EAD); paginated list-by-stage sorted highest-ECL first. **M13.1 ships the Admin Configuration registry** at `/v1/admin/config[/categories|/:key]` — `ConfigStore` interface + `InMemoryConfigStore` + 13 BIL operational defaults across 5 categories (alerts, notifications, reporting, scoring, features); typed entries (number/string/boolean/json) with per-tenant overrides + reset-to-default. **M15.1 ships the BIL Audit & Compliance trail** at `/v1/audit/{events,actions,summary}` — `AuditTrailStore` + `InMemoryAuditTrailStore` (per-tenant capped log) with 7-axis filtering (actor / action / resource_type / outcome / severity / since-until / pagination), aggregate summary, and tenant-scoped 404 on cross-tenant lookup. **M3.1 ships the BIL Data Ingestion connector registry** at `/v1/ingestion/{health,connectors[/:id[/run|pause|resume|runs]]}` — 8 seed connectors covering the BIL upstream list (CBS, Core Insurance, Policy Master, Claims, Agent, AML, Bureau, IFRS9), with status + run-history tracking, deterministic per-day stats, and pause/resume controls. **M12.1 ships the BIL reports catalog + async job tracker** at `/v1/reports/{catalog[/:id],jobs[/:job_id]}` — 9 BIL report definitions across 4 categories (operational/regulatory/audit/business) with both RBI + IRDAI regulators represented; `ReportJobStore` + `InMemoryReportJobStore` for per-tenant submit/list/get; status code routing (201/404/409/400) per error class; backwards-compat regression-tested with the existing T4 `/v1/reports/:type`. **M14.3 ships the AML Watchlist adapter** at `/v1/integrations/aml/{screen,matches[/:id]}` — `AmlAdapter` + `StubAmlAdapter` covering the 4 watchlist categories (sanctions / PEP / adverse_media / internal) with deterministic ~85/10/5% match distribution and 4 review states (open/cleared/escalated/false_positive); PATCH endpoint records audit trail (changed_at + changed_by). **M9.1 ships the BIL Case Investigation tracker** at `/v1/investigations[/:id[/status|/steps/:step_id/complete|/notes]]` — `CaseInvestigationStore` + `InMemoryCaseInvestigationStore` with a 6-state workflow gated by an explicit transitions table, the BIL §17 standard 8-step claim-fraud evidence checklist (verify_identity → final_recommendation), 4 decisions, and a notes thread (4000-char cap). **M7.1 ships the BIL AI/ML model registry** at `/v1/ai/models[/types|/by-type/:type|/:model_id[/metrics|/score]]` — `AiModelRegistry` + `InMemoryAiModelRegistry` seeded with 8 model versions across 6 types (PD/fraud/churn/lapse/anomaly/claim_severity); 5 statuses (experimental/staging/production/shadow/retired); deterministic per-(model, tenant, customer, day) inference with band derivation + SHAP-style top features; retired-model 409 + cross-type metrics handling (AUC for binary, MAE for regression). **M5.1 ships the BIL rule template library** at `/v1/rules/templates[/categories|/:id]` — 12 starter templates across 5 categories (risk_monitoring / fraud_detection / compliance / operational / underwriting) × 3 verticals (banking / insurance / both); inclusive vertical filter (banking returns banking AND both); each template carries condition_pseudocode + recommended_severity + recommended_actions + supporting_indicators referencing the existing catalogue prefixes. **M16.1 ships the BIL named scenario library** at `/v1/scenarios/library[/categories|/:id]` — 10 presets (RBI Baseline/Adverse/Severely-Adverse, IRDAI Solvency, business shocks, pandemic + stagflation black-swans, zero-shock baseline) with multi-axis filtering by category/regulator/severity; schema invariants (regulatory→non-INTERNAL, baseline=zero, black_swan=severe). **M14.4 ships the DMS Document Management adapter** at `/v1/integrations/dms/{document-types,documents[/:id[/status]]}` — 9 document types + 4 review statuses; pivots to case_id/policy_id/claim_id; type-aware status distribution + proof-types-only expiry; PATCH-status workflow with audit trail. **M14.5 ships the Credit Bureau adapter** at `/v1/integrations/bureau/{types,pull,reports[/:id]}` — `BureauAdapter` + `StubBureauAdapter` covering 4 bureaus (CIBIL/CRIF/EXPERIAN/EQUIFAX) with 300-900 score → 4 bands (subprime/near_prime/prime/super_prime), per-day idempotency cache, and calibrated summary stats by band (subprime carries higher DPD + more enquiries). **M10.2 ships the BIL SMS notification channel** at `/v1/notifications/sms/{templates,preview,send,log}` — `SmsTransport` + `StubSmsTransport` with 4 BIL canned templates (ALERT_RED_BRIEF, OTP_LOGIN, KYC_REMINDER, PAYMENT_REMINDER), 160-char cap, E.164 phone validation; mirrors the M10.1 email shape so the `<Channel>Transport` pattern is now proven across 2 channels. **M8.2 ships the BIL alert auto-routing matrix** at `/v1/alerts/routing/{rules[/:class],decide}` — `AlertRoutingEngine` + `InMemoryAlertRoutingEngine` with 4 default rules per BIL §11 (red 4h SLA / sms+email / head_of_risk; orange 24h / supervisor; yellow 72h / analyst; green monitor-only); per-tenant overrides + escalation-before-SLA invariant + monitor_only-implies-no-SLA invariant. Combined with M8.1 + M10.1/M10.2 the platform now wires severity → class → routing decision → notification channel end-to-end. **M14.6 ships the Agent Productivity adapter** at `/v1/integrations/agent/agents[/:id[/productivity[/history]]]` — 50 agents per tenant with branch-head invariants + tier-driven productivity calibration (gold gets 2.5× policies + 82% persistency floor; bronze 1.0× / 65%); status-driven reductions (terminated → zeros, suspended/on_leave → 40% of baseline); 36-month history with cross-year traversal. **M2.1 ships the tenant readiness check** at `/v1/tenants/{me,:tenant_id}/readiness` — pure-function `runReadinessChecks` aggregating 9 cross-module structural checks (tenant_exists/active, channels_configured, vertical_assigned, config_schema_complete, email_channel_enabled+from_address, alert_routing_complete+has_active, audit_trail_active) into a single ready/warnings/blocked verdict with severity-aware aggregation rules. **M11.5 ships the BIL Executive Watchlist** at `/v1/dashboards/bil/executive` — `buildExecutiveWatchlist` consolidates top items from all 4 BIL dashboards (M11.1-4) into a single feed sorted critical→high→medium with per-source + per-severity totals. Deterministic per-(tenant, day); demonstrates platform coherence (one route reads 4 builders). **M15.2 ships the audit-trail hash-chain** at `/v1/audit/integrity` — every recorded `AuditEvent` now carries `hash` (SHA-256 over canonical encoding) + `prev_hash` (linked to the prior event); `verifyChain()` walks the chain oldest-first and reports tampering with index + event_id + expected/actual hash + reason (hash_mismatch | prev_hash_mismatch). Per-tenant chain segmentation. **M9.2 ships custom investigation checklists** at `/v1/investigations/checklists[/:id]` — `ChecklistTemplateStore` + `InMemoryChecklistTemplateStore` with platform built-in (BIL §17 default) always visible + per-tenant custom templates across 5 categories (claim_fraud / kyc_review / sanctions / complaint / other); 1-32 steps with regex-validated step_ids; POST /v1/investigations now accepts `checklist_template_id` to seed an investigation with custom steps. **M14.7 ships the Finance / Treasury adapter** at `/v1/integrations/finance/accounts[/:id[/ledger]]` — 4 account types (savings/current/loan/credit_card) with type-driven balance-sign invariants (loans always negative, savings always positive); ledger walks back from current balance with running-balance-per-entry; ISO since/until filters with code-routed 400s. **M6.2 ships the catalog-lookup scoring convenience layer** at `/v1/scoring/risk/by-indicators` — `IndicatorWeightLookup` + `StubIndicatorWeightLookup` (17-entry mirror of banking + insurance catalogs); fail-fast unknown_indicator → 404; cross-vertical mismatch → 404; resolved[] surfaced for SPA display; backwards-compat with M6.1's explicit-weights route. **M16.2 ships the scenario bulk-run** at `/v1/scenarios/bulk-run` — fires either a list of `preset_ids[]` (≤20) OR every preset in a `category` against the portfolio via the T4.2 engine; ranked by |ECL delta| desc with worst/mean/adverse/benign aggregate totals; cross-module integration with M16.1 library + T4.2 scenario engine. **M14.8 ships the HR adapter** at `/v1/integrations/hr/employees[/:id[/leave-balance]]` — 80 staff per tenant across 8 departments with first-8-as-heads invariant; tier-aware leave-balance entitlements (interns < permanent); 4 statuses + 4 employment types + dept filter; tenant-scoped 404 on cross-tenant lookup. **M7.2 ships the AI/ML model promotion workflow** at `/v1/ai/promotions[/:id[/approve|/reject]]` — `PromotionEngine` + `InMemoryPromotionEngine` with explicit state machine (experimental → staging → shadow → production → retired; production can ONLY retire, no in-place downgrades); request → review → approve/reject lifecycle with RBI-required rationale on every decision; refuses 2nd pending request per model. **M13.2 wires config to audit trail** at `/v1/admin/config/:key/history` — every successful PUT/DELETE writes a config.update / config.reset event to M15.1; failed mutations don't pollute the trail; per-key history endpoint returns slim {ts, actor, action, previous_value, new_value} shape. **M11.6 ships the per-customer 360 drill-through** at `/v1/customers/:customer_id/360` — single endpoint orchestrates 6 adapters (insurance, ifrs9, aml, dms, bureau, finance) + M9 investigations into one view; concurrent fetches with panel-level degradation (failed adapter → that panel nulled + name appended to degraded[]; call still 200s); 8 derived summary fields. Maximum platform-coherence play of T6. **M10.3 ships the BIL push notification channel** at `/v1/notifications/push/{templates,preview,send,log}` — 3 platforms (fcm/apns/web) × 4 canned templates (ALERT_RED_PUSH / OTP_CONFIRM / NEW_CASE_ASSIGNED / REPORT_READY); per-device receipt with multicast (up to 1000 devices); deep-link safety (relative `/` or `https://` only, http rejected); 100/240 char title/body caps. **M4.2 ships the indicator backtest** at `/v1/indicators/backtest` — synthesises trailing-window fire history per (tenant, indicator, day, segment); full confusion matrix + precision/recall/F1; weight-biased synthesis (heavier indicators → higher precision); cross-module integration with M6.2's IndicatorWeightLookup. **M9.3 ships the case maker-checker workflow** at `/v1/cases/maker-checker[/:id[/approve|/reject]]` — 4-eyes approval on sensitive case actions (close / escalate / override_decision) with **RBI-mandated self-approval segregation** (maker ≠ checker enforced via 409 EWS_409_self_approval_forbidden). **M5.2 ships the rule template bulk-clone preview** at `POST /v1/rules/templates/bulk-clone` — pure-function expansion that materialises N draft rule shapes from `template_ids[]` OR `(category, vertical)` filter, with `name_prefix` rename + skipped[] reasons (unknown / duplicate); SPA fans out each clone via T4.7's existing `POST /v1/rules` to commit, keeping bulk-clone decoupled from the rules state machine. **M3.2 ships the connector schema metadata** at `GET /v1/ingestion/connectors/:id/schema` + `POST /v1/ingestion/connectors/:id/schema/validate` — 7 field types (string / integer / number / boolean / date / datetime / enum) with required flags, ISO-8601 regex enforcement, enum + length + range bounds + sample values; 8 schemas (1 per M3.1 seed connector); pure-function validator aggregates ALL errors with codes (required / wrong_type / enum_violation / too_long / out_of_range) + extra-key `unknown_field` warnings; `assertSchemaCoverage()` cross-checks schema↔registry drift. **M15.3 ships audit evidence packaging** at `POST /v1/audit/evidence` (build) + `GET /v1/audit/evidence[/:id]` (list/get) — pure-function `buildEvidencePackage` drains filtered audit events (incl. `resource_id` axis post-filter), runs `verifyChain()` for tamper-evidence attestation, and assembles a frozen package with `package_id`, first/last event hashes, chain_last_hash, size_bytes; `InMemoryEvidencePackageStore` retains the last 100 packages per tenant. Closes RBI/IRDAI evidence-request flow end-to-end. **M8.3 ships the alert acknowledgment workflow** at `POST /v1/alerts/:id/{ack,unack}` + `GET /v1/alerts/:id/ack[/history]` — per-(tenant, alert) state machine `open ↔ acknowledged` with full transition history; ack notes + unack reason both capped at 2000 chars; **reason required on unack** per RBI logging; 409 EWS_409_already_acknowledged + EWS_409_not_acknowledged on conflict. Combined with M8.1 classification + M8.2 routing the BIL alert lifecycle now runs end-to-end (severity → class → route → ack → investigation). **M12.2 ships recurring report schedules** at `/v1/reports/schedules` (CRUD + `/due` poll + `/:id/mark-run` advance) — daily/weekly/monthly cadences with pure-function `computeNextRun` (handles strict-future hour rolls + week jumps + month → next-month + Dec→Jan year roll); `report_id` + `format` cross-checked against M12.1 catalog so bogus refs 400 at create; recipients validated as email shape (1-25 list); cap 50 schedules/tenant with 409 EWS_409_cap_reached. SPA polls `/due`, fans M12.1 jobs, calls `/mark-run` to advance — keeps schedule machinery decoupled from job tracker. **M2.2 ships the tenant onboarding wizard** at `/v1/tenants/{me,:id}/onboarding[/steps/:step_id|/reset]` — 8 platform-defined steps in 1-based order (7 required + `operator_invited` optional); each step status `pending → completed | skipped` with actor, timestamp, notes (≤ 1000 chars); `is_complete=true` iff every required step is `completed` (skipped required → still incomplete, skipped optional → fine); per-tenant capped progress map; total `get()` returns all-pending for never-touched. Pairs with M2.1 readiness — onboarding tracks "did ops do it"; readiness tracks "is the system actually configured". **M1.2 ships service-account API keys** at `/v1/admin/api-keys[/:id[/revoke]]` — machine-identity primitive for BIL compliance integrations + production callers; 7 platform scopes (`alerts:read / cases:read / audit:read / reports:read / notifications:send / webhooks:dispatch / integrations:read`); key format `apex_<prefix>.<secret>` (12-char prefix + 24-byte hex secret) stored as SHA-256 hash + prefix; **full key returned ONCE on create** then redacted forever; constant-time verify via `crypto.timingSafeEqual`; optional ISO-future `expires_at`; per-tenant cap 20 *active* keys (revoked don't count) with 409 EWS_409_cap_reached on overflow. **M1.3 ships the API key auth middleware** — three composable Express middlewares (`optionalApiKeyAuth + requireApiKey + requireScope`) wire `Authorization: Bearer apex_…` into the BFF; bearer regex-anchored fast-fail; valid keys populate `req.apiKey + req.tenant` and best-effort `touch()` `last_used_at`; **X-Tenant-ID is ignored when api-key auth wins** (service-account tenant binding baked into the key, can't be overridden); 401 EWS_401_invalid_api_key on malformed/revoked/expired/tampered, 403 EWS_403_missing_scope on scope mismatch. New `/v1/svc/whoami` (no scope) + `/v1/svc/audit/integrity` (audit:read) demonstrate the surface end-to-end. **M13.3 ships config rollback** at `POST /v1/admin/config/:key/rollback {to_event_id}` — pure-function `rollbackTargetFromMetadata` extracts post-event state from M15.1 audit metadata (`new_value` for config.update, `default_value` for config.reset); validates event is for THIS key + tenant + still resolvable against the schema; rejects no-op (`already_at_value` → 409); applies via configStore.set + records a new `config.update` audit event with `rolled_back_from_event_id` so the trail remains a continuous chain; M13.2 history slim shape extended additively with `event_id` + `rolled_back_from_event_id` fields. **M16.3 ships scenario diff** at `POST /v1/scenarios/diff {left_id, right_id}` — pure-function field-by-field comparison over two M16.1 library presets covering 8 fields (3 enum + 3 numeric shocks + 2 string); numeric entries carry `delta_abs` + `delta_pct` (divide-by-zero guarded for left=0); `changed_entries[]` filtered + sorted with numeric block first by **|delta_abs| desc** (biggest mover wins); flat `shocks_delta` convenience for SPA charting; rejects same_preset (400), unknown_preset (404). Inserted before `/v1/scenarios/:id` so the literal `diff` doesn't get shadowed by the saved-scenario lookup. **M14.9 ships the adapter fleet health roll-up** at `GET /v1/integrations/adapters{,/health}` — `runFleetHealth(tenant, asOf, fleet)` `Promise.all` fan-outs 8 probes in parallel against the M14.1–M14.8 adapter family using their existing list/get methods (no new health-method needed); per-adapter result `{status: up|degraded, latency_ms, sample_count, error?}`; never-throws — non-Error thrown values still surface as degraded entries; aggregate `up_count + degraded_count + total_latency_ms`. Distinct from the older `/v1/integrations/health` (which pings *external* CBS/AML/IFRS9-engine/Collection upstreams via integration-mocks). **M5.3 ships rule simulation against the scenario library** at `POST /v1/rules/simulate {rule_template_id, scenario_preset_id, customer_count?}` — pure-function fire-rate forecast combining M5.1 rule templates × M16.1 scenarios; fire_rate built from `templateBaseRate × (1 + categorySensitivity × scenarioStress)`; per-category sensitivity (risk_monitoring 1.8 → compliance 0.4); deterministic per (template, scenario, day); amplification vs zero-shock baseline (capped at 99) + 60/25/15 by-severity bucketing peaking at recommended_severity. Drives the SPA's "what would this rule fire under RBI Severely Adverse?" pre-activation check. **M6.3 ships scoring weight presets** at `GET /v1/scoring/presets[/:id]` + `POST /v1/scoring/risk/by-preset` — 6 named bundles (`conservative / balanced / aggressive` × `banking / insurance`); sparse `weight_multipliers` map (indicators not listed pass through at 1.0); `PresetScopedLookup` wraps M6.2's catalog lookup applying multipliers + **clamping effective weight to [0,1]** to honour M6.1's range (so conservative `FIN-001` 0.9 × 1.15 = 1.035 → 1.0); per-indicator `effective_weights[]` shows catalog vs multiplier vs effective for SPA transparency. Vertical-mismatch enforced (banking preset can't resolve insurance indicators). See TASKS.md for the full module roadmap. — Phase 1: `app_iam.tenants` + `app_iam.service_clients` migration (`005_tenants.sql`); `tenant_id` FK on `app_iam.users`; BFF tenant middleware (`X-Tenant-ID` + `X-Channel`); standard `{header, body}` request/response envelope + `{error: {code, message, severity}}` error shape; OAuth client-credentials `POST /oauth/token` in auth-svc; `POST /v1/ews/evaluate` migrated as the reference endpoint. Phase 2: envelope rolled out to `/v1/alerts`, `/v1/risk-profile/:customer_id`, `/v1/action`, `/v1/copilot/chat`, `/v1/scenario/run`, `/v1/scenarios*`; tenant gate (no envelope) applied to every other `/v1/*` route (webhooks, rules, reports, sla, notifications, integrations) — every public `/v1/*` endpoint now requires tenant context. `PgServiceClientStore` shipped with cache-on-init + idempotent seed. Still deferred: data-layer tenant filtering (mart + app_*); audit-event `tenant_id`/`channel` columns; envelope migration for ops endpoints; admin SPA tenant + service-client CRUD. Source documents: `Banking Api Integration – EWS Full Technical Documentation (1).pdf` + `DataNetworks-EWS-Ver1.pdf` (BIL pitch deck — multi-tenant is the prerequisite for serving BIL alongside the bank demo).

## Scheduled follow-ups (in-session crons)

These are tracked in the active Claude Code session as one-shot scheduled jobs. Recipes saved to memory at `~/.claude/projects/-Users-taniya-apex-ews/memory/project_pending_followups.md` for cross-session recovery.

- **2026-05-16** — Convert existing `/v1/reports` PDF/Excel downloads to client-side (cron `c8db3fd5`). Mirrors the scenario approach so MSW dev mode produces real binaries.
- **2026-05-23** — Build T4.1 Analytics Dashboard suite (cron `231b8391`). 4 sub-dashboards: risk trend / PD distribution / stage migration / alert resolution. Reuses scenario engine helpers for stage migration; new `analytics:read` RBAC op.
- **2026-05-30** — Wire reserved webhook event types `alert.updated`, `case.assigned`, `case.closed` into BFF case lifecycle transitions (cron `6a1cfb0d`). Depends on Task 8 (case management) maturity.

## Database state (post 2026-05-03 fill-out)

| Schema | Tables | Live rows | Source-of-truth doc |
|---|---|---|---|
| `raw` | 5 | 581,369 | `data/dbt/seeds/_generate_seeds.py` |
| `staging` | 5 (views) | n/a | `data/dbt/models/staging/` |
| `mart` | 4 | 124,000 | `data/dbt/models/marts/` |
| `audit` | 1 | 4 (smoke only — Gap 2 still open per `docs/database-gap-analysis.md`) | `data/schema/003_audit_table.sql` |
| `app_iam` | 4 | 17,055 | `data/schema/004_app_schemas.sql` + `_generate_app_seeds.py` |
| `app_cases` | 2 | 2,096 | same |
| `app_alerts` | 2 | 5,958 | same |
| `app_bff` | 2 | 940 | same |
| `app_scenario` | 1 | 120 | same |
| **Total** | **26** | **~731,500** | column-level reference: [`docs/database-schema.md`](docs/database-schema.md) |

Open backlog: services don't yet read/write the `app_*` tables — that's T4.13–T4.18 in TASKS.md. See [`docs/database-gap-analysis.md`](docs/database-gap-analysis.md) Open Gap A for the sized per-service backlog.

## Out of scope for this prototype (per `project_apex_ews_scope.md`)

- Real AWS deploy (Terraform `apply`) and live bank integrations (CBS/IFRS9/AML/Collection) — only OpenAPI mocks ship.
- DR drill, pen-test, ISO 27001 audit — Phase 5 backlog (T5.2–T5.4).
- Production mobile app build — only the RN shell is in scope (T4.3).
