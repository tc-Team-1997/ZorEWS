{
  "name": "auth-svc",
  "version": "0.1.0",
  "private": true,
  "description": "APEX EWS auth-svc — JWT auth (FR-INT-4).",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "dev": "tsx src/server.ts",
    "test": "node --import tsx --test src/__tests__/*.test.ts",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "argon2": "^0.40.3",
    "fastify": "^4.27.0",
    "jose": "^5.6.3",
    "otpauth": "^9.5.1",
    "pg": "^8.20.0",
    "pino": "^9.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "@types/pg": "^8.20.0",
    "tsx": "^4.16.2",
    "typescript": "^5.5.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
