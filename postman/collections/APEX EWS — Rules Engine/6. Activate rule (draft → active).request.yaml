$kind: http-request
order: 7000
method: POST
url: '{{baseUrl}}/v1/ews/rules/{{ruleId}}/activate'
headers:
  - key: X-Tenant-ID
    value: '{{tenantId}}'
  - key: X-Channel
    value: '{{channel}}'
  - key: X-APEX-USER
    value: '{{apexUser}}'
  - key: x-apex-role
    value: '{{apexRole}}'
